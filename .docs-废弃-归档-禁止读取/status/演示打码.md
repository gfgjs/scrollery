---
id: 2026-09-16-status-演示打码
status: active
type: rolling-status
line: 演示打码
created: 2026-09-16
---

# 演示打码 · 滚动状态

## 范围
- 顶栏单开关，记住开启状态；Canvas 图片绘制时强模糊，真实缩略图加载与缓存不变。
- 文件树名称、路径提示、画廊目录标题与信息浮窗使用自然演示文字，同一实体在本次运行内别名稳定。
- 开启立即收起并暂停悬停卡、视频自动预览与滑动预览；关闭恢复，选择与右键保持可用。
- 独立 DOM 画廊、原图查看器与详情页不在本轮范围。打码期间悬停卡的收藏/评分快捷按钮不可用。

## 状态
- 2026-09-16 Review及裁定：顶栏在打码已开启时保留关闭入口，切换DOM或清掉Canvas支持态后仍可关闭。用户确认“DOM无需打码”，Q2关闭；DOM保持真实内容展示，不新增打码或展示限制。[报告与代码证据](../reviews/2026-09-16-近期改动Review.md)。本线仅余原有真机验收1项。
- 2026-09-16 默认引擎调整：按用户要求，画廊与时间轴的后端 schema 默认值、前端缺省回退均改为 Canvas；新配置、缺键与重置使用新默认，已有显式 DOM 值保留。画廊仍受现有平台/总高度能力条件约束。已有配置需在设置页切换为 Canvas 才能恢复打码入口。
- 本次默认值调整验证：渲染偏好、时间轴控制与画廊虚拟引擎相关 33 项测试通过，改动前端文件 ESLint 通过；未运行 Rust 编译或真实应用性能测试。
- 2026-09-16 回归调查：用户确认设置重构后画廊运行于 DOM；顶栏按钮仍由 `demoPrivacySupported` 控制，DOM 下隐藏。`15ce5de6` 按既定无迁移方案停读旧 localStorage，画廊缺省值 DOM 可使原 Canvas 偏好失效。`9f75e1bb` 关闭打码时跳过 blur/额外裁剪与演示别名生成，保留少量逐格判断；本次相关 4 文件 / 30 测试通过，尚未实测真实图库关闭态的性能差值。
- 2026-09-16：功能与自动验证完成。顶栏在 Canvas 偏好且能力可用时提供开启入口；已开启时保留关闭入口，空结果仍可关闭；详情见 [已完成详注](../completed.md#演示打码2026-09-16)。
- 已验证：165 文件 / 1838 测试通过（含新增 31 项），TypeScript 与改动文件 ESLint 通过；浏览器合成图验证 240px/DPR1、120px/DPR2、400px/DPR1 的模糊、清晰覆盖层及关闭恢复。
- 待验收 1 项：真实 WebView2 应用的完整交互和密集图库滚动。合成 400 格/60px/DPR1 的绘制含同步读回中位数为普通 11.30ms、打码 32.60ms，表明即时模糊有额外成本，不作为真实图库 FPS。
