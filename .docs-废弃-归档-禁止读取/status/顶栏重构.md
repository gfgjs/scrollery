---
id: 2026-07-17-status-顶栏重构
status: active
type: rolling-status
line: 顶栏重构
created: 2026-07-17
---

# 顶栏重构 · 滚动状态

> 状态板 2026-08-24 自 docs/todo.md「顶栏重构 · 自绘标题栏 + 上下文工具栏(Command 化)」整体迁入(D-016 分片);已完成项交付史与 ▸ 详注指针仍归 completed.md。

## 权威文档
> 设计文档:[2026-07-08-顶栏重构-自绘标题栏与上下文工具栏.md](../designs/2026-07-08-顶栏重构-自绘标题栏与上下文工具栏.md)(normative,含六层架构 / 数据契约 / 分阶段计划 / GUI 验收 / OS 集成预留)。**用户三裁决**:①标题栏=自适应单行(2026-07-09 修订:画廊亦并入单行 + L5 溢出,见 Phase G)②Windows=完整 frameless(🔴 Snap Layouts 本期 defer,见 Phase 1)③查看器统一为 shell 内路由 + `activeViewer` 单源。核心=六层解耦骨架(WindowChrome / viewerStore / CommandRegistry / ContextualToolbar / useToolbarOverflow / 自定义预留),OS 集成本期只做骨架预留。立项裁决全文 + 施工前评审六处修正详注见 [completed.md](../completed.md) ▸ L-intro。

## 状态板
| 状态 | 待办 | 阻塞源 / 备注 |
|---|---|---|
| ✅ | **Phase 0 地基**(纯新增零回归):平台工具 `platform.ts`(引 `@tauri-apps/plugin-os`+`os:default` cap)+ `viewerKind.ts`(纯函数+单测)+ `viewerStore.ts`(activeViewer 单源)+ `commands/types.ts`(Command/Context 骨架)+ CSS 变量 `--titlebar-height`/`--titlebar-controls-inset` | **✅ 2026-07-08 交付**:五部件全落地(明细见 ▸ 详注 L-P0);vitest 330(+9)/vue-tsc/eslint/cargo check 全绿(仅本地),无 GUI 面 → **▸ 详注 L-P0** |
| 🔨 | **Phase 1 自绘标题栏 chrome**(L1):tauri.conf mac Overlay+trafficLightPosition / Win `decorations:false` + ~~`tauri-plugin-frame`(Snap Layouts)~~**🔴 Snap Layouts 本期 defer(2026-07-08 用户裁决,审计=单人维护/下载 3236/3 yank/JS 注入冲突六主题;设计文档 §9.1 已回写)——不引第三方、frameless 自绘照常交付、`--titlebar-controls-inset` 由 WindowChrome 自算** + `WindowChrome.vue`(拖拽区+平台分叉三键+~~**双击最大化系 Tauri 内建勿自实现**~~**🔴 2026-07-14 真机推翻→改手动判定,见下「顶栏真机反馈批」行**+红绿灯避让+P1→P3 过渡态=标题栏行暂空、旧 AppToolbar 留第二行)+ capability 按设计文档 §4.6 清单增补(`core:window:allow-start-dragging/minimize/toggle-maximize/close` 等均不在默认集)+ 废弃 Win DWM `set_window_theme` 染色链改 CSS(**mac 补 `setTheme` 明暗同步**) | **🔨 代码就位,GUI 门部分已过(2026-07-08 交付 → 2026-07-14/15 真机迭代)**:frameless 自绘三键(getCurrentWindow)+ AppShell 列向 titlebar 槽 + capability §4.6 增补 + 废 DWM 染色改 CSS setTheme。vitest 330(仅本地)。**✅ 已真机验收**:拖动移窗 + 双击最大化——但**两者均系推翻原设计后重做**(`data-tauri-drag-region`→`useWindowDrag` 阈值委托、双击 Tauri 内建→`isDoubleClick` 手动判定),详见下「顶栏真机反馈批」行 + 设计文档 §3.2 横幅。**⏸GUI 余**:Win10/11 三键/八向 resize/最大化不越屏 + mac 红绿灯·主题 hover + 无 ACL 拒绝。过渡态(覆盖层盖标题栏)由 Phase 4 路由化解决。详注见 [completed.md](../completed.md) ▸ L-P1 |
| ✅ | **Phase 2 CommandRegistry + 网格命令化**(L3 先行):`registry.ts`(注册/查询/when/执行+单测)+ `builtins/global.ts`+`grid.ts`(现有网格操作登记,复用 store action,**新增复制路径**)+ 收敛 MediaGrid/MediaDetailOverlay 两处右键菜单硬编码→注册表 | **✅ 2026-07-08 交付**(registry.ts 8 测/command 两册/两处右键菜单收敛+行为对拍 5 测,明细见 ▸ 详注);三处 scope 调整:网格工具栏命令延 P3 / 复制路径 defer / CommandContext 去 stores 字段;vitest 343(仅本地)→ **▸ 详注 L-P2** |
| 🔨 | **Phase 3 ContextualToolbar + 自适应单行 + 溢出**(L4/L5):`useToolbarOverflow.ts`(ResizeObserver+宽度缓存 Priority+)+ `ContextualToolbar.vue`(navigation 组=主按钮/其余=⋯溢出;a11y=role+roving tabindex+aria-label+tooltip 含快捷键)+ ~~现 AppToolbar 瘦身为 `GridToolbar.vue`(网格第二行)~~**🔴 已作废:Phase G(2026-07-09)supersede——画廊工具栏并入标题栏,不再有第二行,GridToolbar 不再建(设计文档 §3.5/§5 已划线)** + App.vue `#toolbar` 换 ContextualToolbar | **🔨 网格半边交付 ⏸GUI(2026-07-08)**:P3-1 useToolbarOverflow(computeOverflowSplit 6 测,`6227e2d`)+ P3-2 网格命令入标题栏槽(route-gate,三键移出 AppToolbar)。vitest 349。**真实余项 = ContextualToolbar 自身溢出接线**(全库 `useToolbarOverflow` 现仅 AppToolbar 与 SelectionActions 消费,ContextualToolbar 未接;与画廊工具栏 flex 纠缠,见 P5 行)。**已消解**:GridToolbar 瘦身(Phase G 作废,见左)/ roving tabindex(UIUX 线 S3 `8f4f249` 2026-07-12 接入 ContextualToolbar)/ 内容页上下文(P4 路由化)。详注见 [completed.md](../completed.md) ▸ L-P3 |
| 🔨 | **Phase 4 查看器统一为 shell 内路由**(L2 落地,A∪C):新增 `/view/:id`(`?path=` 预留外部文件)+ `ContentViewer.vue` 分发 + **迁 MediaDetailOverlay→ImageViewer/VideoViewer**(保留 useMediaDetail 内核,去 body Teleport,补 defineExpose ViewerApi)+ 各查看器 populate viewerStore + 沉浸模式统一 + 分发入口统一 `router.push('/view/'+id)` + **P4-5 网格保活与滚动恢复**(2026-07-08 评审补:现状覆盖层刻意保活网格,迁路由须网格路由 `KeepAlive`+`onActivated` 与虚拟滚动引擎再同步 或 store 滚动锚点恢复,否则最高频开合路径重挂丢位) | **🔨 P4-a…P4-c 主体全交付 ⏸GUI(2026-07-08,vitest 357)**:P4-a 网格重挂载滚动恢复(scrollCache,顺修跨组件路由返回丢位)+ P4-b 覆盖层→路由(新建 ContentViewer 复用 useMediaDetail 内核,退役 MediaDetailOverlay)+ P4-c 沉浸统一 + Esc 分层;迁移前 characterization 8 测锁内核。**⏸GUI**:图/视/文/音四类 shell 内渲染 + 返回网格保位。「沉浸统一 + 内容页专属命令」并入 P5。详注见 [completed.md](../completed.md) ▸ L-P4 |
| 🔨 | **Phase 5 各媒体专属命令补全 + 默认呈现集**(L3 扩册):~~`viewer-reader/image/audio/video.ts`~~ **实际分册 = `viewer-reader/image/audio.ts` 三册**(🔴 2026-07-15 核实:无 `viewer-video.ts` 且**不是缺口**——ContentViewer 同时承载 image+video,故视频命令并入 `viewer-image.ts`,其 when 谓词即 `k === 'image' || k === 'video'`;原设计 §3.4 的四册清单按 ViewerKind 切分,与实际按「谁承载」切分不符) 全操作登记(接现有函数与 ReaderApi;**看图台新增旋转**)+ 各 ViewerKind 默认 navigation 集(附录 B 候选,真机调优)+ DocumentViewer/AudioPlayer 移除自带顶部工具栏(操作已迁)+ **P5-6 键位同源**(tooltip 快捷键与监听共用 registry `keybinding` 常量;查看器级散点 keydown 收敛注册表分发,防「显示≠行为」漂移,现状全仓 ~9 处散点) | **🔨 图/视/音/阅读器顶栏点亮 + 旋转 + 键位同源全交付 ⏸GUI(2026-07-09)**:viewer-image 7 命令 + rotate(`03b8f47`,旋转态隐人脸框)+ keybinding 单源(`abb97eb`)+ 音频(`c2d7657`)/阅读器(`9a774bb`)顶栏 + 阅读器沉浸并入 viewerStore(`f7f2443`);P5-5 按用户 C 决策=局部工具栏保留(记忆 [[viewer-keep-local-controls]])。vitest 426。**余**:默认呈现集 + 溢出接线(与画廊工具栏 flex 纠缠暂缓)+ 数据命令(需 id)。详注见 [completed.md](../completed.md) ▸ L-P5 |
| 🔨 | **Phase G 画廊工具栏并入标题栏**(折中并入 + L5 溢出,2026-07-09 用户裁决改 §2):画廊工具栏(标题/搜索/筛选/分组排序/行高/布局)搬进 WindowChrome 内容槽、~~撤 AppShell 第二行 header~~**🔴 2026-07-12 再修订(`5776ae4` 用户裁决):改「合并/分离」一键切换,默认合并——第二行不撤,降为用户可选形态**(经过:UIUX 线 S3 `bcf0934` 出于 ownership 分层有意改回分离,推翻 G2;真机反馈#4 报「工具栏又与标题栏分离」→ 用户裁决做成可切换。新增 `useTitlebarMode` 单例 + localStorage,设置 › 界面 开关 live 生效;设计文档 §2 横幅已回写);可折叠控件接现成 `useToolbarOverflow`,尾部溢出进「⋯」;viewer 态仍走 ContextualToolbar | **🔨 G1–G9 全交付,G9 真机验收通过 ⏸GUI(2026-07-09)**:G2 结构迁移(`e036787`)→ G3 响应式溢出 → G4 筛选 chips 逐项折(`10e5ce7`,useToolbarOverflow 首次接线)→ G5 统一单测量消振荡(`2648212`)→ G6/G7/G8 CSS Grid 折叠动画(`989586a`/`bd20c7b`)→ G9 两弹层布局真机通过 + ⑪弹层/溢出健壮性收尾(`2ef79dd`);顺修拖拽回归(`e9a1446`)+ logo 入标题栏(`32e9cc7`);8 路 code-review(`08860a6`)。vitest 377。**余**:G2/G3/G5/G7/G8 折叠观感 ⏸GUI 宽窗真机细调。详注见 [completed.md](../completed.md) ▸ L-PG |
| 🔨 | **2026-07-10 全线深审 + 修复批**(四路 agent 分域深读 + 关键结论亲核 + 门禁亲跑;详录=设计文档 §10):代码 2 HIGH/4 MED/9 LOW + 文档 8 处回写时效;溢出引擎/折叠数学/token 防御/右键收敛**零正确性 bug**,25 引用哈希全真 | **🔨 四提交 ⏸GUI(仅本地 vitest 492)**:C1 allow-set-fullscreen 补权限 + Esc 守卫(`6478be1`)/ C2 命令 id 域化 `<域>[.<子域>].<动作>`(`03021b6`)/ C3 P5-6 组合键补全(`6219aa0`)/ C4 沉浸标题栏 hover 滑入(`a08636a`)。**⏸GUI**:F11 无 ACL 拒绝 / 组合键与 tooltip 一致 / 沉浸 hover 手感 / 全屏+文档沉浸 Esc 分层。遗留跟进(ViewerKind 双分类器收敛/溢出接线/spec 隔离)见设计文档 §10 池;roving tabindex 已由 UIUX 重构线 S3 交付(`8f4f249`,2026-07-12)。详注见 [completed.md](../completed.md) ▸ L-审 |
| ✅ | **2026-07-12→15 顶栏真机反馈批**(2026-07-15 补登) | **9 提交,两条线用户真机确认(2026-07-15)**;经验沉淀见 [experience.md](../experience.md) §14(`93a56dc`);⏸GUI 余:RO 判别与 inline-grid sizer 宽窗观感细调 → **▸ 详注 L-真机** |
| ✅ | **2026-07-16 F11 沉浸顶栏唤出根治 + 相邻 IPC/CSP 安全审查批** | **用户真机验收通过(2026-07-16)**:`777824d` 根治(隐形 drag-resize 子窗口拦输入带)+ 3 项安全修复(`cc43ef1`/`617cc51`/`3b5381f`);遗留 28 个未推提交已推 origin/dev(2026-08-13 核证)→ **▸ 详注 L-F11** |
| ✅ | **2026-07-16 两项遗留裁决落地:分离模式工具栏常驻 + 收藏夹软删 limbo** | `038656f`/`99dce7d`,本地全门绿(非 CI),⏸GUI 待 round11(F11 让位观感/跨路由不跳/非画廊页标题条/重启后「已删除」区恢复闭环)→ **▸ 详注 L-裁决** |
| ⬜ | **Phase 6 预留(本期不做)**:用户自定义工具栏 UI(拖拽/右键加入移出+布局持久化)+ OS shell 集成(文件关联+single-instance+深链+路径直达查看器+默认浏览器注册) | 骨架(命令 id/布局持久化槽/`activeViewer.path` 可寻址)已在 P0–P4 就位;按需拉起 |

## 待裁 / 挂起
> **开放问题(§9,待真机/拍板)**:~~`tauri-plugin-frame` 引入 vs vendor vs 自研 HTMAXBUTTON(默认引入+审计,兜底自研)~~(🔴 2026-07-08 已裁决:Snap Layouts 本期不做,重启时四选一,见设计文档 §9.1;2026-07-10 补划线)/ 各 ViewerKind 默认呈现集手感 / 标题栏高度 40 vs 48 及是否可调 / 上下文专属区分色强度 / ~~网格第二行是否也上溢出~~(🔴 2026-07-09 已定:画廊并入标题栏单行 + L5 溢出,见 Phase G) / **顶栏是否显示标题文字 + 内容页 `setTitle` 同步文件名**(2026-07-08 评审补)/ **筛选 ⋯ 弹层固定宽 252px 的 bilingual(§9.7)+ VIEW_POPOVER 钳制常量 vs min-width 失准(2026-07-10 深审 LOW,同族真机项)**。
