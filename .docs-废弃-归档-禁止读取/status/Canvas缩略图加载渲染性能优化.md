---
id: 2026-08-16-status-Canvas缩略图加载渲染性能优化
status: active
type: rolling-status
line: Canvas缩略图加载渲染性能优化
created: 2026-08-16
---

# Canvas缩略图加载渲染性能优化 · 滚动状态

- 2026-09-12（实施）：A调度/回退/指标、B数据窗口、C1稀疏选档、D局部raw绘制视图已分批提交（`bf7806cd`、`df82ab62`、`ae2b5171`、`8b103163`、`3d12de7b`、`33f54140`）。64并发与512MiB缓存保持不变；预取为可见待加载项保留最多16槽，前后近带保护避免短往返取消；Image回退身份保留到真正完成。
- 本机真实库、模拟3840×2160/DPR1、Canvas3544×2094、约64px/1600–1880格的两轮暖态对比：冷格时间比例0.94%–1.01%→0.06%–0.08%，draw P95 25.46–25.93ms→19.40–19.64ms。当前物理屏2560×1440，不能称物理4K验收；冷进入比例仍67%–68%，未证明明显改善或达成原建议的50%下降目标。初版A+B曾回退，已据数据补齐后向保护，再隔离验证raw绘制后保留，见[实施与真机验证](../reviews/2026-09-12-Canvas密集缩略图优化实施与真机验证.md)。
- 聚焦测试按批运行，全局类型检查、相关ESLint、生产构建与Rust clippy通过；真实悬停卡保持代理、raw绘制读同一底层对象，DOM↔Canvas切换通过。新后端真实首屏/短滚动466个asset响应全200且无页面异常；config哈希、原窗口/缩放/滚动状态已恢复，自有进程关闭。正常浏览懒生成推进176项，未手动改库或全库重生成。

- 2026-09-12（分析阶段，随后已授权实施）：4K/64px/1500+密集缩略图分析确认快滚可能无必要取消前向预取、开闸缺跨帧可见优先、数据窗不保证位图预取覆盖、稀疏档位未逐项回退，并发现Image回退所有权导致遗留槽；当时未改业务代码、未新增真机性能数据。建议按“测量与调度→窗口对齐→选档/按需补小档→trace命中后的供给或绘制降本”交付，见[分析与优化方案](../designs/2026-09-12-Canvas密集缩略图性能分析与优化方案.md)。特别修正旧归因：bitmapLoad不含前端等槽，既有串行/并发差不能证明64槽限制是主因；多档选档已落地不等于存量自动补齐。
- 后续独立1组：继续定位陌生密集区冷加载供给，补物理4K/低性能设备/真实输入链验证。C2只在实际缺少合适小档的场景另行评估（本机已有大量64档）；须先解决后台让路和换根/删除/清理与落盘竞态。现有GC已按cache_key保活，无需为多档迁移DB。原有3项未关闭事项保留。

- 2026-08-16:全线收官(阶段 0-5)。已上线:多档服务选档 + 生成档位×DPR 对齐(`19cad9b`/`a510195`)、队列内有界退避重试(`80aebb9`)。数据裁决:4a no-op(维持反证门控)、4b 扩槽不采纳(维持 64)、4c 选中态批处理否决回退(健康实例逐格绘制贴满 vsync)。实测详情:reviews/2026-08-16-Canvas画廊缩略图加载渲染性能审查.md §9;归档:worklogs/2026-08-16-Canvas缩略图加载渲染性能优化/。
- 2026-08-18:后续稳定化收官。Canvas KeepAlive 生命周期与不可取消解码回包所有权以 `35c4cff` 独立修复；查看器返回的视觉空档以 `3efdee6` 的“路由作地址、覆盖层作呈现”消除，不用等待首帧掩盖问题。Windows WebView2 实测通过，归档:worklogs/2026-08-18-Canvas缩略图渲染重做/。
- 2026-08-25:已修复 Canvas 首帧空白的 rAF 代次竞态（仅“画廊渲染引擎=Canvas”）：`CanvasRafScheduler` 记录待执行帧的 generation，同代次继续合并、跨代次取消旧帧并重排当前帧；回归覆盖 generation 0 排帧→`activate()` 切 generation 1→旧回调晚到，避免必须滚轮才能补画。helper 64/64、全量 Vitest 1579/1579、typecheck、相关 ESLint、生产构建通过；真实 WebView2 手动复测待在可稳定触发场景下补做。诊断归档:worklogs/2026-08-25-画廊偶发空白排查/；修复归档:worklogs/2026-08-25-画廊空白首帧竞态修复/。
- 待立项(承 closeout F-009/F-007 立项部分):① app 空视图死态(reload/HMR 重挂载后「0 个项目」不自愈,布局 width<100 放弃后无重试路径,仅重启可恢复;生产首挂载是否可触发待查);② app 窗口保存状态可损坏至屏外(实测 -21333,-21333/158×26,用户可遇「打开看不到窗口」;Win32 MoveWindow 可修但产品侧缺钳制/自愈)。两项均已登记 docs/todo.md 2026-08-16 增补;(2026-08-24 自 todo.md「Canvas 缩略图线立项候选」核对补全——两项已在档,仅补齐细节,无新增条目。)
