---
status: snapshot
type: working-memory
line: 演示打码
created: 2026-09-16
---

# 演示打码实施计划

## 目标
一键开启 Canvas 画廊演示打码：真实缩略图继续加载，绘制时强模糊；目录、文件名、路径和信息浮窗改为普通演示文字；暂停悬停卡，关闭后恢复。

## 当前阶段
已交付：实现、自动检查、主会话审查与合成视觉验证完成；真实应用验收转滚动状态。

## 阶段
1. 实现统一状态、演示名称与字段、Canvas 绘制与悬停控制、文件树和顶栏接入（complete）。
2. 相关行为测试、类型与局部静态检查（complete）。
3. 主会话审查关键路径及验证证据，修正发现的问题（complete）。
4. 回写滚动状态、归档三件套和交付（complete）。

## 关键决策
| 决策 | 理由 | 候选 ID |
|---|---|---|
| 仅 Canvas 图片绘制强模糊，演示时暂停 DOM 悬停卡 | 用户确认的最小方案，避免悬停露出真实内容 | D-001 |
| 普通演示文字代替原文，同一实体的别名本次运行稳定 | 避免模糊遮罩条的突兀感，保留界面结构 | D-002 |
| 不改真实数据、缓存与请求流程，不支持独立 DOM 画廊、查看器和详情页 | 用户确认范围 | D-003 |

## 错误账
暂无。
