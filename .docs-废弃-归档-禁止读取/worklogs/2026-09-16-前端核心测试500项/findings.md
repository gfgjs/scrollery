---
status: snapshot
type: working-memory
line: 仓库架构与流水线全面梳理
created: 2026-09-16
---

# 发现

- 用户在 cf25549a 的 1946 项套件基础上要求最多 500 项，取代上一轮仅删除明显冗余的保守尺度。
- 原始 1999 项；第一轮精简后 1946 项、178 文件，全部通过。
- 优先级：数据/文件安全、持久化失败恢复、请求代次与取消、核心 AI 错误提示、选择与布局边界、IPC/路径输入、主题可读性。
- 低优先：UI 原语 SSR 结构、默认值透传、格式化/展示细节、辅助工具与演示桩逐项测试，允许整文件删除。

## 最终保留

| 范围 | 精简前 | 保留 | 核心文件数 |
|---|---:|---:|---:|
| composables + stores | 909 | 270 | 15 |
| components + themes | 602 | 110 | 4 |
| utils/commands/perf/harness/i18n/scripts | 435 | 107 | 8 |
| 总计 | 1946 | 487 | 27 |

- 选区 classic/sweep 集中；目录撤销、复制与半完成恢复共用 fixture。
- 组件域收敛为画廊几何、Canvas 管线、文件树、主题四套核心测试。
- 不可信内容渲染、路径、命令分发、命令目录、多语言校验分别集中；IPC 与 harness 保持独立环境。
- 关键代次/取消、扫描与设置持久化、AI 搜索及模型缺失提示、备份/目录恢复保留；普通展示、格式化、日志、性能计数、SSR 原语与次要分支覆盖主动舍弃。
- 主会话要求撤销把多个独立 it 拼成一个的做法；最终按 Vitest 展开计数，不靠 skip、include 改动或新增循环隐藏数量。原本的一条契约测试内枚举同类数据保持原粒度。
- 静态计数不能替代加载验证：命令目录测试迁移后四个导入路径遗漏，在首次 collect/typecheck 中发现并修复。
- 旧测试引用随注释与阅读器规范更新；历史金标数据仍保留，不扩展清理 Rust 生成器。

## 耐久候选
按 D-001 一并收口。
