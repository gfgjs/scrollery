---
status: snapshot
type: working-memory
line: 仓库架构与流水线全面梳理
created: 2026-09-16
---

# 进度

- 基线 cf25549a，1946 用例；工作区有其他任务的 ToastContainer.vue 修改，不属于本任务。
- 三个互斥范围按 270/110/110 项分配，仅改测试；主会话集中验证。
- 用户追加集中测试要求：同域兼容 fixture 合并文件，仍按独立展开用例计数。
- 将 500 项与集中维护约定写入 AGENTS.md，避免后续再次无边界堆积测试；未增加额外 CI 或脚本门禁。
- 不运行构建、Rust 或依赖安装，不引入测试分层框架。
- 三组实际保留 270 / 110 / 107 项，27 文件；1999 → 1946 → 487。
- 首次 collect/typecheck 报 commandCatalog 导入旧相对路径，已修为 ./builtins/；diff 检查的两处末尾空行已清理。
- 最终 collect 成功，487 项符合预算；完整 Vitest 487/487 通过，0 跳过；vue-tsc、保留测试的 ESLint、diff 检查通过。
- 只运行一轮完整核心套件；未做构建、Rust、原生 GUI 或覆盖率/变异测试。不宣称业务未被自动测试的部分已有覆盖。
- 产品行为未改，主会话仅更正五处源文件内已失效的测试引用注释；其他任务的 ToastContainer.vue 未纳入。

## 回顾
- 用户要求硬预算时，应先选最高风险覆盖并分配预算，而不是先逐条证明冗余再小幅删减。
- 合并文件降低准备数据与环境重复；独立场景不能以合并 it 的方式规避额度。
- 集中测试移动路径后必须验证实际加载；静态语法检查不能发现所有模块解析问题。
