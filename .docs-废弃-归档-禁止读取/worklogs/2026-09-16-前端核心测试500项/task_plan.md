---
status: snapshot
type: working-memory
line: 仓库架构与流水线全面梳理
created: 2026-09-16
---

# 前端核心测试500项

## 目标
遵照用户最新硬预算，Vitest 展开后的全部用例不超过 500 项，只保留最重要行为。

## 阶段
1. 按风险分配预算：完成。
2. 三组直接删除次要测试：完成，270/110/107 项。
3. 主会话审核保留清单和实际总数：完成，27 文件 / 487 项。
4. 集中运行完整前端套件与类型检查：完成，487 项全绿，类型/lint/diff 通过。
5. 回写状态、归档、提交：完成。

## 预算
- composables + stores：最多 270 项。
- components + themes：最多 110 项。
- utils + commands + perf + harness + i18n + scripts：最多 110 项。
- 合计 490 项，留 10 项整合余量。

## 关键决策
| 决策 | 理由 | 候选 ID |
|---|---|---|
| 最多500项，直接删除次要测试文件或用例，保留风险最高场景 | 用户明确要求硬预算；不能只做小幅去重 | D-001 |

用户追加：该集中测试的集中。同一功能域、兼容 mock/fixture 的测试集中成少量文件；不同隔离环境保持必要分隔。合并文件可行，不通过合并 it、扩大循环或 skip 隐藏数量；不改产品代码、Vitest include、CI 或依赖。
