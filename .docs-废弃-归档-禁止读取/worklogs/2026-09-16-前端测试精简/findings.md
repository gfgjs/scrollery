---
status: snapshot
type: working-memory
line: 仓库架构与流水线全面梳理
created: 2026-09-16
---

# 发现

- 1999 是整个 Vitest 套件用例数，前次 AI 下载引导仅新增 6 个前端用例。
- 2026-09-15 已删纯源码字符串测试和部分重复门禁，本轮以当前文件为准；不重新引入分层测试框架。
- 当前 Vitest 使用 node 环境，包含 src/**/*.spec.ts 与 scripts/**/*.spec.mjs；SSR 断言不能替代原生 WebView 交互。
- 大文件集中在画廊几何、时间轴、目录树、虚拟滚动；数量大不直接等于可删除。

## 实际基线（Vitest list 展开后）

178 文件、1999 用例；采集只加载测试定义，不执行断言。

| 目录 | 用例 | 主要内容 |
|---|---:|---|
| composables | 635 | 选择策略、滚动、键盘鼠标、异步请求、分析控制器 |
| components | 512 | 画廊/时间轴 290、侧栏目录树 98、UI 原语 65、布局筛选 53、设置 6 |
| utils | 328 | 路由查询、格式转换、缓存、IPC、日志等 |
| stores | 288 | 扫描、媒体、AI、人脸、设置、主题、日志等状态流转 |
| themes | 123 | 色彩生成、配置、对比度、快照与源码契约 |
| commands | 45 | 查看器操作、快捷键、命令分发、右键菜单 |
| scripts | 26 | 打包体积检查与开发 CSP |
| perf | 21 | 性能计数、采样、统计 |
| harness | 17 | UI 演示环境的 IPC 数据桩 |
| i18n | 4 | 中英键一致性、实际文案引用、备份提示 |

## 本轮取舍

- 保留完整中英缺键校验：比较的是引用和定义的实际关系，能发现用户界面显示键名的问题。
- 命令、打包检查、性能统计与演示桩本轮保持不变；优先削减 SSR 细节、同分支样例和派生状态重复。
- 全套测试只在两组删减合并后集中运行一次，不重复跑基线或分组全套。

## 删减结果

| 分组 | 原用例 | 保留 | 删除 |
|---|---:|---:|---:|
| composables | 635 | 622 | 13 |
| components | 512 | 489 | 23 |
| utils | 328 | 322 | 6 |
| stores | 288 | 287 | 1 |
| themes | 123 | 113 | 10 |
| 其余 | 113 | 113 | 0 |
| 总计 | 1999 | 1946 | 53 |

- UI：删除 variant/class 映射、样式透传、重复默认文案检查；保留开/关、disabled、表单按钮默认类型、label 关联、条件插槽。
- 主题：删除旧名称与固定源码形状约束、重复手算色值；保留颜色引用闭环、对比度、真实首帧脚本与缓存验证。
- 组合逻辑和工具：删除简单透传、默认常量、同分支重复输入；保留阈值端点、请求队列取消/重试、扫描/设置持久化等。
- Canvas 色板测试原有 `expect(lightTheme.accent).toBe(lightTheme.accent)` 恒真，改为与调用前快照比较，实际验证不修改输入。
- 未改变 Vitest include、未合并 it、未引入 skip。全文件清单见 inventory.md。没有测量删除前后性能，不宣称运行加速。

## 耐久提升候选
采用 D-001 统一记录本次删减规则，无其他候选。
