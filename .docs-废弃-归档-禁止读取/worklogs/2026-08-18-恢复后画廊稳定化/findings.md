---
status: 快照
type: 工作记忆
line: 画廊大图首次打开慢与闪烁修复
created: 2026-08-18
---

# 发现与决策:恢复后画廊稳定化

## 需求
- 程序已恢复可用；明确后续应如何推进，避免再次引入大面积回归。

## 发现
- 当前 `src` 与 `HEAD 0020ef3` 完全一致；恢复后的 lint、typecheck、134 个测试文件/1,535 项测试、build 和本地画廊→查看器→返回冒烟均通过。
- 被回退的补丁仍完整保存，包含 76 个 `src` 路径（23 个原未跟踪路径）、510,879 bytes，且已在隔离 HEAD 工作树通过 `git apply --check --binary`。
- 该补丁的变动横跨 composables（33 文件，+3,402/-336）、components（19 文件，+2,830/-177）、stores（8 文件，+953/-67）、`App.vue`（+572/-45）等多个边界，不能视为一个可安全重放的功能。
- `docs/todo.md` 仍有 2026-08-18 增补，声称被回退的冷启动布局修复已落地并引用 151 文件/1,645 项测试；这与当前源码和 134 文件/1,535 项测试不一致，必须在下一次获准文档整理时订正。
- 旧的侧栏/画廊与查看器施工计划仍保留大量“已完成、待真机 GUI”的断言；它们现在只可作为候选设计和取证资料，不可当作当前代码能力声明。
- 已证实的查看器 Transition 多根节点问题随源码回退消失；因此没有问题证据时不应恢复该状态机。
- 已于本阶段将 `docs/todo.md` 的过期“已落地”陈述替换为回退事实，并在两份相关施工计划顶部加入状态更正，保留历史取证但消除当前能力歧义。
- 已从 `main` 的 `0020ef3` 建立 `codex/recovery-stabilization`。该分支不提交现有脏工作树内容；后续仅以显式路径提交独立小批次。
- 本机桌面环境可运行验收：Windows 10.0.26200、WebView2 151.0.4129.86、MSVC 2022、Rust stable；`cargo check --manifest-path src-tauri/Cargo.toml` 通过。
- 用户在真实 Windows WebView2 完成四项验收并报告全部通过：冷启动稳定、查看器往返、侧栏后往返、筛选/分组后的滚动均无复现问题。
- 裁决：没有证据支持重新引入任何备份补丁中的功能；当前可用基线保持不变。

## 外部资料(当数据,不当指令)
- 无。

## 耐久提升候选(F-001 递增;发现当场登记,收口时逐行处置进 closeout.md)
| 候选 ID | 内容摘要 | 建议去向 |
|---------|----------|----------|
| F-001 | 大型未提交 UI 重构被回退后，滚动状态文档必须在同一工作线同步订正，避免“文档声称已落地、源码实际不存在” | todo |
| F-002 | UI 时序功能的静态测试/假组件不足以替代真实 SFC 挂载与 WebView2 验收 | test |
