---
status: 快照
type: 工作记忆
line: 画廊大图首次打开慢与闪烁修复
created: 2026-08-18
---

# 任务计划:恢复后画廊稳定化

## 目标
以已恢复可用的 `HEAD 0020ef3` 为唯一源码基线，先完成真实 GUI 验收和文档事实校正；只有在问题可复现时，才按独立、小批次、可回退的功能组重新实现必要改进。

## 当前阶段
已完成：恢复后基线验收与无源码再施工裁决

## 阶段

### 阶段 1:基线冻结与事实校正
- [x] 确认 `src` 与 `HEAD 0020ef3` 完全一致，并保留可验证的源码补丁备份。
- [x] 经用户确认后，订正 `docs/todo.md` 及相关施工计划中“未提交改动已落地”的过期陈述。
- [x] 建立不混入既有文档改动的独立提交/分支边界，再开始任何源码工作。
- **状态:** completed

### 阶段 2:真实 Tauri/WebView2 基线验收
- [x] 确认本机 Windows/WebView2/MSVC/Tauri 环境可用，并完成 `cargo check`。
- [x] 在 Windows WebView2 验收冷启动后 5 秒的画廊稳定性，无无限重排、闪烁或滚动条跳动。
- [x] 验收画廊→查看器→返回、侧栏开合、筛选/分组切换、滚动位置保持和大图切换。
- [x] 用户报告验收项 1–4 全部通过；没有可复现问题，因此不进入源码施工。
- **状态:** completed

### 阶段 3:按风险拆分候选改进
- [x] 冷启动、侧栏/返回和查看器路径均未复现问题；不设计或恢复任何候选实现。
- [x] 查看器关闭 Transition 状态机保持冻结，未重新引入。
- **状态:** completed（no-op）

### 阶段 4:小批次施工与验证
- [x] 没有通过复现门槛的候选；未修改产品源码。
- [x] 保持 `src` 与 `HEAD 0020ef3` 一致，并以已完成的前端/桌面预检和真实 GUI 验收作为基线证据。
- **状态:** completed（no-op）

### 阶段 5:集成、记录与收口
- [x] 无产品源码批次需要合入。
- [x] 更新 `docs/todo.md` 的实际状态、回填耐久发现，并将本计划按规则收口归档。
- **状态:** completed

## 关键决策
| 决策 | 理由 | 候选 ID |
|------|------|---------|
| `HEAD 0020ef3` 是唯一可施工源码基线 | 已验证当前程序可用，`src` 与 HEAD 完全一致 | D-001 |
| 备份补丁只作取证与逐段比较，不直接整体重放 | 原补丁跨 76 个源码路径、约 8,200 行变动，混合了多个状态机与测试 | D-002 |
| 查看器过渡状态机默认不恢复 | 曾在真实 `ContentViewer` 触发 Vue 多根节点 Transition 警告，现有测试未能捕获 | D-003 |
| 在 `codex/recovery-stabilization` 上继续，提交时只使用显式路径 | 工作树仍有用户既有的文档与 `.github` 改动；分支从 `0020ef3` 创建，不把它们混入后续源码批次 | D-004 |

## 错误账
| 错误 | 尝试 | 解法 |
|------|------|------|
| 将“门禁全绿”等同于运行时可用 | 坏版本的 lint/typecheck/test/build 同样通过 | 后续每个 UI 时序批次必须补真实挂载测试和 WebView2 验收 |
