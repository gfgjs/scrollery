---
status: 快照
type: 工作记忆
line: 画廊大图首次打开慢与闪烁修复
created: 2026-08-18
---

# 进度日志:恢复后画廊稳定化

## 会话:2026-08-18
- 做了:核对恢复后的源码、备份补丁、滚动状态文档和两份相关施工计划；建立后续稳定化的分阶段路线。
- 验证:`src` 与 `HEAD 0020ef3` 一致；备份补丁 SHA-256 为 `7EEB2092B474258B46C7D9FBD37F1EF969FA970B17A539B1BA8C0D65ABAFAE7C`；恢复后前端四门和本地查看器往返冒烟已在上一阶段通过。
- 遗留:等待用户决定是否先授权阶段 1 的文档事实校正与独立提交边界，或直接进行阶段 2 的真实 Tauri/WebView2 验收。

## 会话:2026-08-18（阶段 1 执行）
- 做了:将 `docs/todo.md` 中未提交冷启动布局改动的“已落地”断言替换为回退事实；在两份相关施工计划顶部声明其实现内容已不在当前源码，统一指向本计划。
- 做了:从 `main` 的 `0020ef3` 创建 `codex/recovery-stabilization`；不提交既有文档或 `.github` 脏工作树内容。
- 验证:改动前 `docs/todo.md` 的工作树差异仅为该两行增补；修改后 `git diff --check` 通过，`src` 仍与 HEAD 一致。
- 遗留:进入真实 Tauri/WebView2 基线验收；不在缺少复现证据时恢复任何源码功能。

## 会话:2026-08-18（阶段 2 预检）
- 做了:运行 `npm run tauri -- info` 与 `cargo check --manifest-path src-tauri/Cargo.toml`，确认本机桌面运行环境和 Rust 基线可用。
- 验证:WebView2 151.0.4129.86、MSVC 2022、stable Rust 均可用；Cargo check 在 46.64 秒内通过。
- 遗留:真实窗口的冷启动、画廊↔查看器、侧栏/返回及筛选/分组验收需用户观察；收到结果前不恢复任何已回退功能。

## 会话:2026-08-18（真实 GUI 验收与裁决）
- 做了:用户完成冷启动、查看器往返、侧栏后往返、筛选/分组滚动四项真实 Windows WebView2 验收。
- 验证:用户报告 1–4 全部通过；结合恢复后的前端门禁、Cargo check 与本地浏览器冒烟，未发现需要重做的产品问题。
- 结论:不恢复备份补丁中的任何源码；保持 `HEAD 0020ef3` 基线并收口本计划。

## 回顾
- 亮点:先恢复可用基线并保留可复现补丁，再让真实 GUI 验收决定是否施工。
- 教训:不要把多条时序/布局/路由改动混在一个未提交工作树中。
- 意外:滚动状态文档会比源码更快过期，必须把源码核对作为状态声明的前提。
