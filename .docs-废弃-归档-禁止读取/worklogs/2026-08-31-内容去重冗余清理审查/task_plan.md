---
status: 快照
type: 工作记忆
line: 内容去重冗余清理审查
created: 2026-08-31
---

# 任务计划:内容去重冗余清理审查

## 目标
以当前代码和可复现实验为唯一事实源，全流程审查最近施工的精确内容去重链路，删除无收益的抽象、状态、兼容分支与测试，修复审查中确认的问题，并集中验证最小完整闭环。

## 当前阶段
收口完成

## 阶段

### 阶段 1:事实边界与并行拆解
- [x] 按提交和实际依赖图界定去重施工面
- [x] 两路 Luna (Max) 子代理独立审查后端索引/任务、清理/IPC、前端/测试冗余
- **状态:** completed

### 阶段 2:主代理交叉核验与取舍
- [x] 对每项建议用调用点、状态机和测试反证
- [x] 区分必要安全不变量与历史/过度设计
- **状态:** completed

### 阶段 3:集中精简与修复
- [x] 删除或折叠已确认冗余代码与设计
- [x] 修复审查中确认的行为缺陷，避免引入兼容层
- **状态:** completed

### 阶段 4:聚焦回归与集中门禁
- [x] 先跑受影响聚焦测试，失败即修
- [x] 批次完成后一次运行必要静态检查和全量相关测试；全量 Rust 测试在既有并发测试处停滞，已记录而未宣称通过
- **状态:** completed

### 阶段 5:文档裁决与收口
- [x] 同步推翻过时设计与滚动状态
- [x] 同步 todo、记录删除量/剩余边界/未自动化验证并归档工作记忆
- **状态:** completed

## 关键决策
| 决策 | 理由 | 候选 ID |
|------|------|---------|
| 不复用同日未跟踪的只读审查作为本轮结论，只把它当待验证线索 | 该成果未覆盖冗余清理目标，且用户明确要求不要信既有代码和文档 | |
| 子代理先只读分面审查，主代理集中裁决和写入 | 去重链路共享文件多，避免并发编辑互相覆盖和局部最优 | |
| 不保留向前兼容层；只保留有当前调用者或安全不变量证明的结构 | 项目高速迭代，用户明确允许推翻现有设计和实现 | |
| 测试按风险保留，精简后集中运行相关门禁 | 用户要求减少不重要测试并集中测试 | |
| 旧数据库中的 `dedup_cleanup_journal` 不由 V31 自动删除 | 删除历史审计/恢复证据属于不可逆迁移，当前代码也不读取该表 | D-001 |
| 成员查询复用分组默认筛选口径 | 清理复核必须与用户看到的组成员集合一致 | D-002 |
| 全量 Rust 测试停滞只记录为验证缺口 | 停滞测试不在本轮改动面，不能为收口擅自改并发状态测试 | D-003 |

## 错误账
| 错误 | 尝试 | 解法 |
|------|------|------|
| `rg.exe` 的 WinGet 链接无法启动 | 用 `rg --files` 搜索 | 回退到 `git ls-files`、`git grep` 和 PowerShell |
| 首次读取 planning 技能时绝对 Windows 路径被执行层错误转义 | 直接传入反斜杠绝对路径 | 改用仓库相对路径 |
