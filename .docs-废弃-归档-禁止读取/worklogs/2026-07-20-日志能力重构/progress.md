---
status: 快照
type: working-memory
line: 日志能力重构
created: 2026-07-20
---

# 进度日志:日志能力重构

<!-- 验证行怎么填(F-005):门禁末行须在**全部内容落盘后**才跑得出来——顺序是
     先写占位 → 跑门 → 回填真实末行(改动了再复跑)。别倒过来抄一行旧输出充数。 -->

## 前情(接续先读这段,≤10 行;旧会话细节在 progress-archive.md)
- 当前:S6 收尾完成(第 9 波会话段),日志能力重构线 S1..S6 全线收官;唯一遗留=用户批准后 push(全部提交均未 push)
- 未解错误:无
- 待办:无(S6 前置两项裁决——worker 日志汇入策略/span TopN 埋点范围——均已于本波裁定并落实,见下方会话段与 task_plan 决策表 D-308)
- 关键指针:施工唯一入口=方案 §9(护栏/陷阱/定型/分步 DoD;与前文冲突以 §9 为准);三调研报告=attachments/research-{A,B,C}-*.md;S1 新增 src-tauri/src/logging.rs(EnvelopeFormat/session_id/enforce_size_budget/build_env_filter_directive)+benches/logging.rs(criterion);S2 新增 error.rs 的 error_log_dedup_check/reset_error_log_dedup/error_chain_strings;dedup 存疑项已裁决闭环(D-307,详见第 5 波会话段);S3 新增 src/utils/logger.ts+src-tauri/src/ipc/system_commands.rs 的 log_frontend_events;S4 新增 logging.rs 的 LogRingBuffer/RingBufferLayer、ipc/log_commands.rs(open_log_window/list_log_files/read_log_file_page,绝对行号锚点分页)、capabilities/logs.json(独立窄权限)、src/stores/logWindowStore.ts+src/views/LogWindowView.vue+src/components/logwindow/*(@tanstack/vue-virtual 虚拟滚动);S5 新增 error.rs 的 error_dedup_snapshot、logging.rs 的 redact_diagnostics_text(逐行 JSON 值树脱敏,不对序列化字节直接跑正则)、log_commands.rs 的 get_log_diagnostics/compute_log_histogram(+compute_histogram_from_content/strip_tz_suffix 纯函数)/export_diagnostics_package、AppState 新字段 log_dropped_counter、src/utils/logAggregation.ts、src/components/logwindow/LogHistogramChart.vue(手写内联 SVG,不引图表库);S6=纯文档回写+CI 核对,唯一代码改动是修复 benches/logging.rs 的 4 处 rustfmt 漂移
- **环境坑**:`cargo fmt`(即便带 `-- <file>` 限定)在本机会重排整个 crate,不止目标文件——只用 `cargo fmt --check` 核实,不要对本仓执行写入模式(详见第 7 波会话段)

## 回顾(收口时填;置于会话段之前——文件尾留给最新会话段,新段追加到末尾)
- 亮点:
- 教训:
- 意外:

## 会话:2026-07-20
- 做了:建线摸底(scout)→ 3 sonnet researcher 并行调研(用户中途补性能硬要求,SendMessage 追问 A)→ 三报告落 attachments → 综合方案+8 项裁决清单落 designs/ → todo.md 登记「进行中未登记」段 + 开 lines/日志能力重构.md → commit b20e9e6
- 验证:check_docs 本线 8 新文件零违反(status 枚举须中文,英文 draft/active 首版被拒已改);check_docs_index 1 处漂移(README 缺「下一空闲字母」)经 stash 复跑证实 HEAD 既有,非本线引入,未代修(基线红属收编线红线)
- 遗留:等裁 Q1..Q8;open item=worker 子进程日志摸底(阶段定义在 task_plan)

## 会话:2026-07-20(第 2 波:裁决落定 + Sonnet 5 适配)
- 做了:用户裁 Q1..Q8 全采纳推荐+主动建议(D-305);方案增补 §9 施工指引(硬护栏 10 条/陷阱表 9 项/实现定型 A-E/S1-S6 分步 DoD/会话 effort 配置),§3.1 文件层定型自定义 FormatEvent(内置 .json() 注入不了信封字段)、重复压缩定型 error.rs:205 调用点辅助非 Layer(D-306);task_plan 决策表/todo.md/记忆同步
- 验证:check_docs 实跑=本线 planning/designs 文件零命中;todo.md 7 条命中均为既有候选表基线红(候选 D-001/F-003 等,与本轮编辑段无关);全仓 222 处均基线,未代修(收编线红线)
- 遗留:开施工线 S1(Sonnet 5,方案 §9);open item=worker 子进程日志摸底不变

## 会话:2026-07-20(第 3 波:S1 内核施工)
- 做了:按方案 §9.4 S1 分步清单施工——①Cargo.toml 加 tracing-subscriber json feature/tracing-panic/criterion dev-dep/`[[bench]] logging`;②新增 `src-tauri/src/logging.rs`:EnvelopeFormat(§9.3-A JSONL 信封格式化器)、generate_session_id(ring::rand,避免新依赖)、enforce_size_budget(启动期>512MB 按最旧删,§3.5);③lib.rs 日志初始化段重写:删 RealTimeDailyAppender(每条 fsync 的旧实现),换 RollingFileAppender(daily+max_log_files=14)+non_blocking,guard 由 `Box::leak` 改 `app.manage`(§9.2 陷阱表唯一正解),控制台文本层加 `cfg(debug_assertions)` 门控(release 不再白付格式化成本),接 tracing-panic 的 panic hook,默认级别 debug→info(落 §7 Q2 裁决);④settingsMap.ts/zh-CN.ts/en-US.ts 加 `logLevel=off` 选项(EnvFilter 原生识别 "off" 关键字,config_commands.rs 的既有 LOG_RELOAD 热切路径零改动即可用);⑤`benches/logging.rs`:criterion 微基准(off/target 过滤/全链路三态每事件成本)+ 流水线宏基准(真实 `generate_thumbnail` 走 CPU 解码+编码,30 张合成 PNG/批,off/info/debug 三档吞吐对比);⑥logging.rs 加 off 档全链路特征化测试(`tracing::subscriber::with_default` 局部作用域,非 `.init()` 全局,避免陷阱表「第二用例 panic」),自动化验证「off 期间事件从未触达写入器」「切回 info 恢复写入」「guard drop 阻塞至后台线程写完(退出前 flush)」——以此替代需要真机 GUI 的手测清单三项
- 验证(施工首版):cargo test 782 passed、clippy 零新增警告、criterion 首版数字(off=0.18ns/filtered=0.18ns/enabled=1.58µs,流水线 off=105.38ms/info=105.81ms/debug=105.73ms,差值 0.33%~0.41%);前端 eslint/vue-tsc/vitest(1280 passed)均绿
- reviewer 深审回合(方案 §9.5):0 严重、2 警告、4 建议。警告①`lib.rs:478` 显式传 `session_id = %session_id` 字段,与 EnvelopeFormat 无条件挂顶层的信封字段重复(FieldCollector 会把非 message/operation_id 字段一律塞 attributes,顶层+attributes 各一份)——已删该显式字段。警告②`benches/logging.rs` 首版 `init_subscriber` 的 writer 直接用 `std::io::sink`,跳过了生产路径 `tracing_appender::non_blocking` 的 crossbeam 入队开销,低估「序列化+入队」真实成本——已改为 `non_blocking(std::io::sink())`,guard 存活到 `bench_all` 结束。建议采纳 2 条:`enforce_size_budget` 返回 `PurgeSummary{purged_files,freed_bytes}`,init 后若非零补一条 info 汇总(丢弃可见化);新增 3 个边界单测(未超限不删/按最旧删到位且不误删活跃文件/只认 .log 不理会其它文件)。建议不采纳 2 条(reviewer 自评非本期必需):log_level 字段结构化留 S2 批量迁移一并做;`AppHandle::exit()` 兜底路径绕过析构导致 guard 不 flush 的边缘场景,留作已知限制不动代码
- 验证(修复后重跑,终态数字):cargo test 785 passed/0 failed/6 ignored(较施工前 781 净增 4:1 个 off 档特征化测试 + 3 个 enforce_size_budget 边界测试);cargo clippy --all-targets 零新增警告(唯一 1 处 pre-existing manual_inspect@scan_commands.rs:96,与本线无关未代修);criterion 修正后数字已回写方案 §3.2 表:每事件成本 off=0.18ns、target 过滤=0.18ns、全链路=1.60µs(含入队开销后略升,符合预期);流水线宏基准(30 张/批)off=104.74ms、info=105.54ms、debug=105.30ms,off 态相对 info/debug 差值 0.53%~0.76%,仍低于 §3.2 验收门槛(>1% 视为未完成),**S1 验收基准通过**;前端 eslint/vue-tsc/vitest(1280 passed)均绿(未再变动,无需重跑)
- 遗留:S2 schema 治理开工(方案 §9.4 S2 清单);open item=worker 子进程日志摸底不变(S6 前补);真机 GUI 层面的「打开日志窗口看实时流」验收要等 S4 UI MVP 落地后才有实物可测,本轮 off/on/flush 机制已用自动化测试覆盖,不再单列为 S1 待办;已知限制(不阻塞,未来可选加固)=`AppHandle::exit()` 兜底路径绕过 guard 析构、崩溃/强制退出场景可能丢最后几行日志

## 会话:2026-07-20(第 4 波:S2 schema 治理)
- 做了:按方案 §9.4 S2 分步清单施工——①operation_id:`src/utils/ipc.ts` 新增 `generateOperationId()`;`derivationStore.ts` 的 `startVideo()` 生成并经 `IPC.START_DERIVATION` 透传 `operationId`;`start_derivation` command 加 `operation_id: Option<String>`,经 `launch_derivation_pipeline`→`start_derivation_pipeline` 带到 `tokio::spawn` 内的启动/完成/失败/panic 四条任务级汇总日志(范围限流水线相关命令,未强求全量,configStore.ts/scanStore.ts/useDerivationAutoStart.ts 三处自动触发调用点仍省略该参数,`Option` 缺省为 `None` 属预期)。②error.rs 富化+重复压缩:`(code,msg)` 匹配前移;新增 `error_chain_strings()` 沿 `source()` 链收集(而非 `{:?}` 整段撕裂文本);新增 `error_log_dedup_check()`/`ErrorLogDecision` 状态机(静态 `LazyLock<Mutex<HashMap<(target,code),Entry>>>`,30s 窗口:首见放行 `repeat_count=1`,窗口内吞掉计数,窗口切换首条放行 `suppressed=N`);`clear_logs` command 补调 `reset_error_log_dedup()`;`logging.rs` 的 `FieldCollector` 新增字段名特判 `error_chain`(JSON 字符串解析回真数组)/`error_code`。③pipeline target 归一:新增 `build_env_filter_directive()` 单一事实源(lib.rs 启动路径+config_commands.rs 热切路径共用),追加 `scrollery::pipeline=warn,reqwest=warn` 后缀;thumbnail/generator.rs 全部 per-item 调用点(11 处)挂 `target: "scrollery::pipeline::thumb"`;ai/pipeline.rs+ai/face_pipeline.rs 挂 `ai`/`face`;derive/pipeline.rs+derive/image.rs 按实际服务对象挂 `video`/`ai`/不挂。④高敏调用点结构化:lib.rs:396/402(asset scope 授权失败路径不再插值进 message)+generator.rs 全部路径类调用点(与③合并施工)。
- 验证(reviewer 修复前):cargo test 789 passed(净增 4);clippy 零新增;criterion 复测 off=108.25ms/info=109.19ms/debug=108.29ms(30 张/批,较 S1 首测 104.74/105.54/105.30ms 整体偏高,判读为同一台开发机负载噪声——参 dev-machine-hw-instability 记忆,非代码劣化;同批三档相对差值才是验收依据),off 相对 info/debug 差值 0.037%~0.87%,仍 < 1% 门槛,**S2 无回退**;抽 3 行真实 JSONL(临时 `--nocapture` 断言取样后已移除,已回填 task_plan.md)。
- reviewer 深审回合(方案 §9.5):0 严重、4 警告、1 存疑。已修 4 警告——(a) `derive/image.rs` AI 缓存 GPU 回退日志误挂 `video`(实服务 AI/人脸分析缓存),改挂 `ai`;(b)(c) `derive/pipeline.rs` 两处误把「6 种 DerivationKind 共用」的通用生产者/消费者日志统一挂 `video`(doc/audio/ai 派生按 video/ai 过滤会漏看),改为:让步循环那条(无 kind 上下文)去掉 target 用默认、失败日志那条(`task.kind` 在场)按 kind 分三支各挂字面量 target(tracing target 是编译期常量不能塞运行时字符串,只能分支各写一份宏调用);(d) `error.rs` 的 chain/chain_json 原在 dedup 判定前就算好,Swallow 热路径白付序列化开销,改为先判决策、只在两个 Emit 分支现算。另有 2 处注释锚点漂移(写「见 error.rs:205」,该行经本轮编辑已挪到 error_chain_strings 函数体内部)改按函数名引用。**存疑项未擅自改动、原样报告**:dedup 签名 `(target,code)` 因当前唯一调用点 target 恒为 `module_path!()`,事实上退化为仅按 `code` 去重——两个毫不相关但共享泛化 code(如 Io/Db/System/Os/Internal,横跨全仓各功能路径)的错误若 30 秒内相继发生,后者会被整段吞掉且直到窗口翻转前无 `suppressed` 提示。方案 §9.3-C 明文定型 Sig=(target,error code),这是该定型在「单一调用点」现状下的真实副作用而非新缺陷,按护栏(§9.1 #10「不擅自变更定型」)不扩充签名,留待用户表态是否接受或另案裁决。
- 验证(reviewer 修复后复测):cargo test 790 passed/0 failed/6 ignored(新增 1 处 warn/error 档不追加降噪后缀的回归测试);clippy --all-targets 零新增(同 S1 唯一 pre-existing `scan_commands.rs:96`);rustfmt --check 对本阶段 5 个改动文件通过;前端 eslint/vue-tsc/vitest(1280 passed)未再变动;宏基准未因修复轮重跑(修复面在 error.rs/logging.rs/derive/pipeline.rs,未触碰宏基准实测的缩略图生成热路径),此前一次实测结果仍代表数。
- 遗留:S3 前端桥开工(方案 §9.4 S3 清单:logger.ts+log_frontend_events command+onerror 兜底+84 处 console 替换+no-console 上闸);open item=worker 子进程日志摸底不变(S6 前补);**待用户表态**=error 去重签名退化为仅按 code 的副作用是否接受(见上「reviewer 深审」小节),不接受则需另案裁决扩充签名或改变 dedup 粒度(→ 第 5 波已裁 D-307 闭环);batch_request_thumbnails/add_scan_root 两个流水线相关 command 本轮未接入 operation_id(方案允许「不强求全量」,仅 start_derivation 作为示范全链路打通)

## 会话:2026-07-20(第 5 波:dedup 存疑项裁决 D-307)
- 做了:S2 commit(e3617ae)后向用户呈报存疑项三候选取舍——A 接受现状(零改动,但同泛化 code 的不相关错误 30s 内第二条日志证据丢失)/B 扩签名 message 或 chain[0](误吞归零,但风暴场景 message 嵌路径等变量致签名基数爆炸、压缩完全失效反杀主目的,且 key 变 String 热路径分配+HashMap 无界需驱逐)/C Swallow 臂降档 debug 留证(error 档保风暴压缩、debug 档保全量真相,签名与定型不动,改一个 match 臂)。建议 C,用户裁 C 且明示「只改这里,不要开 S3」。落地:error.rs 的 Swallow 臂由空臂改为 `tracing::debug!`(附 `dedup_swallowed=true`+error_code+error_chain,tracing 宏 callsite enabled 检查在字段求值之前,默认 info 档下 chain 序列化不执行,保住 reviewer 修过的热路径优化);新增回归测试 `swallowed_repeat_demoted_to_debug_level`(debug 档=首见 ERROR `repeat_count=1` + 第二条 DEBUG 带标记与 error.chain 数组共 2 行;info 档=仅首见 1 行,降档行被档位滤掉);方案 §9.3-C 正文同步改(裁决改定型正文而非只加脚注)+task_plan 阶段 7 存疑句附裁决结果+决策表加 D-307 行。
- 验证:cargo test 全套 791 passed/0 failed/6 ignored(净增 1);cargo clippy --all-targets 零新增(唯一 pre-existing `scan_commands.rs:96` manual_inspect);rustfmt 对 error.rs 通过(测试辅助函数签名超行宽一处已格式化)。宏基准未重跑:改动仅 error 序列化路径的 Swallow 臂,与宏基准实测的缩略图热路径无交集,且默认 info 档下新增成本=每条被吞错误一次 callsite enabled 检查(~ns 级)。
- 遗留:S3 前端桥待用户指令开工(明示勿自动开);open item=worker 子进程日志摸底不变

## 会话:2026-07-20(第 6 波:S3 前端桥施工,用户明示开工)
- 做了:按方案 §9.4 S3 分步清单施工——①后端 `log_frontend_events` command(system_commands.rs)+`FrontendLogEvent`+`emit_frontend_log_event`(固定 `target: "scrollery::frontend"`,来源经 `source` 字段区分——tracing target 编译期常量不能塞运行时字符串,同 S2 derive/pipeline.rs 教训);registry.rs 登记;logging.rs 的 FieldCollector 新增 `frontend_context` 特判(JSON 字符串解回真对象,落 `attributes.context`,同 error_chain 惯例);新增单测 `emit_frontend_log_event_writes_envelope_and_context`。②`src/utils/logger.ts`(§9.3-E 定型骨架完整实现):模块级队列+2s/50 条阈值 flush+`setLoggerEnabled`(configStore 在 loadConfig/setLogLevel 同步 off 档)+`installGlobalErrorHandlers()`(window error/unhandledrejection,push 后立即 flush),main.ts 挂载;`harness/ipcFixtures.ts` 补 LOG_FRONTEND_EVENTS 分支;新增 `logger.spec.ts` 11 测(node 环境无 DOM,`window` 经 `vi.stubGlobal` 打最小桩;`vi.resetModules()` 逐用例换新模块实例,防 setInterval 状态跨用例污染——踩过一次坑:同一 `flushTimer` 在 fake/real timer 切换间会失效,resetModules 后解决)。以上为 commit 3c996a2。③约 100 处(设计稿估的 84 处,施工间隙代码增长到此数)裸 console.* 机械替换为 logger.*,覆盖 34 个文件(App.vue/commands/2/components 6 个/composables 12 个/stores 8 个/views 2 个/utils 的 epubScriptGate);规则=纯消息保留原文只换函数名,伴随 error 值移入 `{ error: e }` fields;`uiStore.ts` 近 20 处同构 `.catch(console.error)`(SET_APP_CONFIG 保存失败)收敛成 `logConfigSaveError(key)` 辅助函数(避免 20 份近乎相同闭包,亦是本文件此前 `warnWindowOp` 一类既有模式的延伸)。刻意保留 console 三处并写进 eslint no-console 规则的 ignores:`utils/logger.ts` 本体(唯一合法桥接点)、`composables/useGalleryPerfProbe.ts`(localStorage dev 门控性能探针,产物即给开发者当场读的 console 输出)、`harness/ipcFixtures.ts`(仅 UI dev harness 场景,不进生产用户路径)。`epubScriptGate.ts` 的诊断 warn 随迁移,spec 同步把 spy 目标从 `console.warn` 改为 `vi.mock('./logger')` 后的 mock(`vi.hoisted` 规避 mock 工厂提升导致的 TDZ)。此为 commit d92c51b。
- 验证:cargo test 792 passed/0 failed/6 ignored(净增1);clippy --all-targets 零新增(同前唯一 pre-existing `scan_commands.rs:96`);cargo fmt --check 对本轮 3 个改动文件(system_commands.rs/registry.rs/logging.rs)无差异(全仓 --check 另有若干与本线无关的存量漂移文件,未代修);前端 `npx eslint .` 全仓零错误(no-console 规则生效后)、`npx vue-tsc --noEmit` 零错误(中途一次遗漏:configStore.ts 早先只导入了 `setLoggerEnabled` 未导入 `logger`,vue-tsc 报 2 处 `Cannot find name 'logger'`,补导入后清零)、`npx vitest run` 1291 passed(净增 11,均为 logger.spec.ts)。
- reviewer 深审(c6b54a4..d92c51b 全 diff):1 严重 + 1 存疑,其余抽查(约 20 处 console→logger 替换点/深层相对路径导入/`Option<T>` tracing Value 语义/`frontend_context` 解析/队列并发 flush 互斥/console 清扫完整性/IPC 封装合规/uiStore 辅助函数行为等价)均未发现问题。
  - **严重(已修)**:off 档联动实际不生效——`setLoggerEnabled` 只在 `configStore.loadConfig()`(仅设置页/模型库懒路由才调)里同步,`logger.ts` 的 `enabled` 默认 `true`,导致「off 时前端不白付 IPC」在典型会话中从未生效(覆盖范围不是设计注释说的「极短窗口」,而是整个非设置页会话)。修复:仿 R2-4 先例把 `log_level` 并入 `get_startup_config` 单次往返批(`config_commands.rs` 的 `StartupConfig` 25 键)+ `uiStore.ts` 启动 hydration(`useUiStore()` 在 App.vue setup 早期即触发)调用 `setLoggerEnabled((cfg.logLevel ?? 'info') !== 'off')`;`harness/ipcFixtures.ts` 的 mock StartupConfig 补 `logLevel: 'info'`(否则 vue-tsc 报字段缺失)。
  - **存疑(已修)**:`useBucketVirtualScroll.ts`/`useVirtualScroll.ts` 两处 dev-only 调试开关诊断(`console.info` → 误改成了 `logger.debug`,与提交信息「消息原样、机械替换」的说法不符——级别被顺手降了一档,默认 info 档下会静默从 JSONL 消失)。改回 `logger.info`,与原 `console.info` 完全对齐,不夹带未声明的判断。
  - 测试覆盖缺口(未补,已知且可接受):`uiStore.ts` 全仓无既有 spec(该文件在 module 顶层直接用 `window.matchMedia`/`document.documentElement`,本项目 vitest `environment: 'node'` 无 DOM 全局,需要相当篇幅的 stub 才能起步测试,且无先例可循)——本次只补了型别层(`StartupConfig` 缺字段会编译失败)与人工代码走查,未加自动化回归测试锁定这条 hydration 路径。

## 会话:2026-07-21(第 9 波:S6 收尾,线全线收官)
- 做了:①先呈报用户两项 S6 前置裁决(AskUserQuestion)——摸底调研 supervisor.rs/ai-worker/psd-worker(Explore 子代理只读调研,报告见工具记录)后,worker 子进程日志汇入策略三候选(现状已足够/各自开 RollingFileAppender/IPC 汇入主 JSONL)用户裁**现状已足够,不额外施工**;span 耗时 Top N 埋点范围三候选(4 条流水线批次/更大范围含 IPC command 级/继续跳过 defer)用户裁**继续跳过,defer 到独立任务**。两项均落空代码变更结论。②方案文档 `docs/designs/2026-07-20-日志能力重构方案.md` 回写:顶部状态行标注 S1-S6 全部完成;§3.4 open item 段落改写为 D-308 已闭环说明;§6 施工分期表 S6 行补勾选(✅ 2026-07-21);§8 风险与 defer 补两条终态说明(span TopN defer 独立任务/worker 日志 D-308 已闭环);§9.4 S6 分步补完成注记+DoD 实测数字回填。③CI 接线检查:通读 `.github/workflows/ci.yml`,确认 `rust` job(fmt --check/check/clippy -D warnings/test --workspace)与 `frontend` job(eslint/vue-tsc/vitest/build)均按 workspace 根扫描,本线全部新增文件天然被现有门禁覆盖,无需新增 job、无需改 workflow。④CI 核对过程中意外发现 `src-tauri/benches/logging.rs`(S1 commit 49abb20 引入,S1/S2 均未对该文件跑过 `fmt --check`)有 4 处 rustfmt 格式漂移(`registry().with().with().init()` 单行链式调用未按行宽拆分、两处 `handle.modify(...).expect(...)` 同理、`synthetic_rgba(...).save(...).expect(...)` 链式调用同理)——若本线提交推送后触发 CI,`cargo fmt --check` 步骤会失败。判定为本线自身遗留(非其它线基线红,`git log` 确认该文件仅本线 commit 触碰过),逐处手工按 rustfmt 建议对齐修复(未对整仓/整个文件跑 `cargo fmt` 写入模式,规避环境坑)。同批 `cargo fmt -p scrollery -- --check --files-with-diff` 扫出另 5 个漂移文件(backup/core.rs、backup/restore.rs、db/queries/export.rs、export/core.rs、ipc/export_commands.rs),`git log -1 -- <file>` 逐个核实最后触碰提交均不属本线(分属 backup 线/export 早期历史提交),判定为其它线基线红,未代修。
- 验证:`cargo test --workspace --locked` 816 passed/0 failed/6 ignored(scrollery_lib,较 S5 终态无变化——本阶段零功能代码改动);`cargo clippy --workspace --locked -- -D warnings` 唯一 1 处 pre-existing `scan_commands.rs:96` manual_inspect(与本线无关,历次 S1-S5 均如此,未代修);`cargo fmt -p scrollery -- --check --files-with-diff` 复跑后本线全部文件(含修复后的 benches/logging.rs)零漂移;`cargo check --workspace --locked --benches` 通过(30.06s,含 criterion 依赖树),确认手工格式化后 bench 仍可编译;前端 `npx vue-tsc --noEmit` 零错误、`npm run lint` 零错误、`npx vitest run` 1328 passed(108 files,与 S5 终态一致,无回归)。
- 遗留:无技术遗留。S6 收口即整线收口——待用户批准 push(全部 9+1 个 commit)。已知限制(非本线新增,均已在文档中如实标注,不隐藏):uiStore.ts 无 spec 基建(S3 已知缺口)、span 耗时 Top N UI 已实现但无数据源(defer 独立任务)、batch_request_thumbnails/add_scan_root 两个流水线 command 未接 operation_id(方案允许不强求全量)。
- 验证(修复后复测):cargo test 792 passed/0 failed/6 ignored;clippy --all-targets 零新增(同前唯一 pre-existing);前端 eslint/vue-tsc 零错误,vitest 1291 passed(未变动)。
- 遗留:open item=worker 子进程日志摸底不变(S6 前补);uiStore 启动 hydration 无自动化测试覆盖(上述缺口,如未来该文件建测试基建可补);下一步=S4 UI MVP(方案 §9.4 S4:环形缓冲层+独立日志窗口+虚拟滚动+过滤+历史分页)(S6 前补)

## 会话:2026-07-20(第 7 波:S4 独立日志窗口 MVP)
- 做了:开工前先摸底(Explore 子代理并行调研多窗口先例/IPC 三层模式/既有虚拟滚动/设置页 debug 区/tracing Layer 挂法/JSONL 路径/Pinia store 惯例/i18n 结构),确认无 `@tanstack/vue-virtual` 依赖、`AppState` 已有 `log_dir` 可复用、`createWebHashHistory` 路由为按 window label 分流方案铺路。按方案 §9.4 S4 分步清单施工——①UI 环形缓冲层(方案 §9.3-D):`logging.rs` 把原内嵌在 `EnvelopeFormat::format_event` 里的字段收集逻辑抽成独立纯函数 `build_envelope`,`EnvelopeFormat`(JSONL 文件层)与新增 `RingBufferLayer`(UI 环形缓冲层)共用,防止两处手写字段特判(error_chain/frontend_context 等)日后各改各的漂移;`LogRingBuffer`(`AtomicBool` 订阅标志 + `Mutex<VecDeque<serde_json::Value>>`,`RING_BUFFER_CAPACITY=20_000` 安全阀——真正面向用户「5k-20k 可调」的渲染保留行数是纯前端内存裁剪,与此常量是两个独立的量,已在 commit message 里显式记录这个解读取舍);`lib.rs` 把 `RingBufferLayer` 接入既有 `registry().with(filter).with(file_layer)` 链(自动继承当前 logLevel/off 档过滤,不用重复实现级别判断),新增 100ms 周期抽干任务(`tauri::async_runtime::spawn` + `tokio::time::interval`,空批跳过 `emit` 调用,纳入既有 `handles_pool` 而非野生 spawn);`AppState` 新增 `log_ring: Arc<LogRingBuffer>` 字段(构造参数注入,因为 tracing subscriber 建好的时机早于 `AppState::new()`,同 `log_dir`/`app_data_dir` 的既有姿态)。②独立日志窗口:新增 `src-tauri/src/ipc/log_commands.rs` 三命令——`open_log_window`(已存在则 show+focus;否则 `WebviewWindowBuilder` 建窗、`build()` 成功后才 `ring.set_subscribed(true)`、`win.on_window_event` 挂 `WindowEvent::Destroyed` 回调复位订阅,覆盖原生关闭按钮/Alt+F4/程序化关闭全部路径,比指望前端在卸载竞态里补一次 IPC 更稳)、`list_log_files`(按 mtime 降序列出 `.log` 文件)、`read_log_file_page`(见下方 reviewer 修复);`capabilities/logs.json` 新建独立窄权限文件。③前端:`main.ts` 按 `getCurrentWindow().label` 分流出 `LogWindowView` 独立挂载路径(不经 App.vue/router/AppShell);`src/types/logEntry.ts` 定义 `LogEntry`(信封字段刻意保持后端原样 snake_case,不转 camelCase——这些数据来自 `Vec<serde_json::Value>` 不经过任何 `#[serde(rename_all)]` struct,原样对齐 agent 直接 `rg`/`jq` 该文件时看到的键名);`src/stores/logWindowStore.ts`(实时流+过滤+Pause/Resume/Clear+历史累积分页);`src/components/logwindow/{LogRow,LogVirtualList}.vue` + `src/views/LogWindowView.vue`;`settingsMap.ts`/`DynamicSettingControl.vue`/两份 i18n 加入口按钮。
- 验证(施工首版):cargo test 792→801(净增 9);clippy --all-targets 零新增(唯一 pre-existing `scan_commands.rs:96`);前端 `npx eslint .`/`npx vue-tsc --noEmit` 零错误,`npx vitest run` 1291→1304(净增 13,`logWindowStore.spec.ts` 13 测)。
- reviewer 深审(方案 §9.5):0 严重、5 警告,全部已修——(a) `capabilities/default.json` 把 `"logs"` 直接并入 `windows` 数组会让日志窗口继承 shell:allow-open/dialog:allow-open/os:default 等主窗口专属权限(日志窗口实际只用自定义 command,均不受 ACL 门控),改为新建独立 `capabilities/logs.json`(仅 `core:default`,`default.json` 的 `windows` 保持 `["main"]`);(b) `read_log_file_page` 原按「距文件当前末尾的相对偏移」(`offset_from_end`)分页——被浏览的往往是 `list_log_files` 排序里最新、很可能仍在持续写入的当日文件,两次分页调用之间新追加的行会让"当前末尾"整体前移,下一页与上一页产生重叠(前端 `loadOlderHistory` 用 concat 累加不去重,历史视图出现重复行),改为**绝对行号锚点**:响应携带 `oldest_loaded_line`,前端原样带回下一次请求的 `before_line` 参数;Rust 侧抽出 `slice_page_bounds` 纯函数 + 3 单测锁定「同一锚点在文件持续增长前后切出完全相同的绝对范围」+「文件变短时钳制不 panic」;(c) `main.ts` 的 `getCurrentWindow()` 在裸浏览器打开 vite dev server(不带 `?ui-harness=` 参数、非真实 Tauri 环境)时会同步抛错致整段脚本中断白屏——这是既有场景(注释明确写着"DEV browser 场景仍挂载真实 shell"),不能被新引入的窗口分流带崩,补 `isTauri = '__TAURI_INTERNALS__' in window` 兜底,`!isTauri` 时同 `isUiHarness` 一样恒走主窗口路径;(d) `logWindowStore` 的 `pendingEntries`(暂停期间的批次积压)原无长度上限——后端环形缓冲对"暂停"是纯前端概念无感知,持续 ~100ms 一批推送,长时间暂停会无界增长,补与 `appendLive` 同口径的 renderCap FIFO 裁剪(只保留最新一批);(e) `src/types/logEntry.ts` 的 `LOG_LEVELS` 只有 debug/info/warn/error 漏了 trace——`settingsMap.ts` 的 `logLevel` 选项明确提供 trace 档,用户选它后 trace 级别日志在日志窗口里会被过滤规则永久挡住且没有勾选框能加回,补齐 `LOG_LEVELS`(级别过滤 UI 循环这个数组生成勾选框,补了就自动出现)+ `LogRow.vue` 补 `.log-row__level--trace` 样式。
- 验证(修复后复测):cargo test 804 passed/0 failed/6 ignored(净增 12,含 `slice_page_bounds` 3 单测 + pendingEntries 裁剪等场景);clippy --all-targets 零新增;前端 eslint/vue-tsc 零错误,`npx vitest run` 1307 passed(净增 16,含 3 个专门锁定这批修复的回归测试:锚点跨文件增长稳定性、暂停积压裁剪、trace 级别不被过滤)。commit `8fe0555`。
- **施工中的环境坑**(记录避免复踩):`cargo fmt`(即便用 `cargo fmt -- <具体文件路径>` 限定参数)在这台机器上会重排**整个 crate** 的全部文件,不止传入的目标文件——两次误将 `backup/core.rs`、`backup/restore.rs`、`db/queries/export.rs`、`export/core.rs`、`ipc/export_commands.rs`、`benches/logging.rs` 等与本线完全无关的文件带出格式漂移(纯 rustfmt 空白改动,非语义改动,已用 `git checkout -- <文件>` 逐次撤销复原,未代修,基线红属其它线);此后只用 `cargo fmt --check`(全仓只读扫描)核实我方改动文件区没有 diff,不再对本仓执行任何 `cargo fmt` 写入模式。
- 遗留:open item=worker 子进程日志摸底不变(S6 前补);真机 GUI 手测清单(开窗见实时流/过滤生效/10k 行滚动不卡/关窗后订阅标志复位)按方案 §9.4 S4 DoD 标注 not automated,留到 S6 前批量真机验收;下一步=S5 分析(方案 §9.4 S5:P1 error.code 聚合/过滤 preset/丢弃计数展示 → P2 rusqlite 直方图/span TopN/诊断包导出)

## 会话:2026-07-21(第 8 波:S5 分析,P1→P2)
- 开工前置裁决:方案 §5 P2「span 耗时 Top N 表」摸底(`grep info_span!|debug_span!|#\[instrument\]` 全仓零命中)发现该子功能无数据源,方案与现实冲突(方案 §9 前言三停规则之一),停下用 AskUserQuestion 呈报三候选(本轮跳过 defer S6 / 最小埋点 4 条流水线批次 span / 更大范围含 IPC command 级 span),用户选**本轮跳过 defer S6**。
- 做了(P1,方案 §9.4 S5):①丢弃计数——lib.rs 捕获 `non_blocking.error_counter()`,新增 `AppState.log_dropped_counter` 字段(构造参数注入,同 `log_ring` 姿态)。②错误去重快照——`error.rs` 新增 `ErrorDedupSnapshotEntry`/`error_dedup_snapshot()`(只列当前窗口 `swallowed>0` 的签名)。③`get_log_diagnostics` IPC 命令聚合①②(纯内存读取,不 spawn_blocking)。④`logWindowStore.ts` 加正则搜索模式(非法正则降级为不过滤+`textFilterInvalid` 提示)、过滤 preset(localStorage,`saveCurrentAsPreset`/`applyPreset`/`deletePreset`)、`refreshDiagnostics()`。⑤`src/utils/logAggregation.ts` 新增 `aggregateErrorEntries()`(按 `error.code` 分组计数+首末时间+样本)。⑥LogWindowView.vue 新增「分析」第三标签整合上述。
- 做了(P2):①`compute_log_histogram` IPC——`rusqlite::Connection::open_in_memory()`(spawn_blocking 内)+ 临时表 + `GROUP BY strftime`,核心逻辑抽纯函数 `compute_histogram_from_content`(同 S4 `slice_page_bounds` 惯例,可单测不必搭 State);`strip_tz_suffix` 裁掉 RFC3339 时区后缀防 SQLite 日期函数自动转 UTC 致桶边界错位。②`LogHistogramChart.vue`(新增,手写内联 SVG 堆叠柱状图+级别分布图例,不引图表库,同 §9.1 护栏精神)。③`export_diagnostics_package` IPC——system-info(版本/OS/架构/GPU **设置值**/DB 大小/扫描根数)+ 最新日志尾部 2000 行 + `logging.rs::redact_diagnostics_text`(只脱敏 Windows/Unix 用户主目录用户名段)脱敏后打 zip,`.tmp` 同卷 rename。④双窗格上下文(klogg 范式)——`selectEntry`/`contextView`(在未过滤源数组里找 ±15 条,零额外 IPC);`LogRow.vue` 点击 emit + 选中高亮;底部上下文面板。
- 验证(reviewer 修复前):cargo test 813 passed(净增 9);clippy 零新增;前端 eslint/vue-tsc 零错误,vitest 1327 passed(净增 20)。
- reviewer 深审(方案 §9.5):1 严重 + 3 警告 + 2 建议 + 1 存疑,全部处理。**严重(已修)**:`redact_diagnostics_text` 早期实现直接对**序列化后的 JSONL 原始字节**跑正则——JSON 转义把路径分隔符 `\` 编码成 `\\`(双写),正则按单反斜杠匹配对真实落盘数据完全不命中,诊断包(明确的"出机"通道)会把 Windows 用户名明文带出;三条早期单测只喂了未转义裸字符串给出假绿灯。修复为逐行解析 JSON、递归脱敏字符串叶子节点(新增 `redact_json_value`)、重新序列化(解析失败的史前纯文本行走原文正则 fallback),新增两条用真实 `serde_json::to_string` 产出验证的回归测试。**警告(已修)**:①`compute_histogram_from_content` 对 `strftime` 返回 NULL(无法解析的时间戳)读 `String` 列会 `Err`,经 `collect` 短路成整份查询失败(非"该行不进桶"原意),SQL 补 `WHERE strftime(?1, ts_naive) IS NOT NULL` + 新增回归测试;②`aggregateErrorEntries` 的 firstTs/lastTs 依赖"输入按时间正序"假设,但 View 层拼接 `liveEntries.concat(historyEntries)` 在浏览当天仍被写入的文件时会时间倒挂,改按时间戳字符串比较取 min/max + 新增乱序输入回归测试;③诊断包导出区补说明文字(GPU 为设置值+仅脱敏用户名段)。**建议(已修)**:①`LogHistogramChart.vue` 的 `shortLabel` 原按字符串形状猜测天/小时粒度会在小时粒度的 00 时桶误判,改为父组件显式传 `bucket` prop(`store.histogramBucketUsed`,记录数据生成时的实际粒度而非下拉框此刻值);②`total_lines`/`parsed_lines` 覆盖率数据此前从未渲染,补直方图区一行展示。**存疑(已评估,轻量兜底)**:诊断包文件名精确到秒的同秒碰撞——`UiButton` 的 `loading` 已隐式禁用按钮挡住正常单击,补时间戳到毫秒精度(`%.3f`)进一步缩小理论窗口,不加计数器/随机后缀。
- 验证(reviewer 修复后复测):cargo test 816 passed/0 failed/6 ignored(净增 3);clippy --all-targets 零新增(同前唯一 pre-existing `scan_commands.rs:96`);`cargo fmt --check` 对本阶段 6 个改动后端文件核实,logging.rs/log_commands.rs 的行宽格式化点已手工按 rustfmt 偏好对齐(未对整仓执行写入模式);前端 eslint/vue-tsc 零错误,vitest 1328 passed(净增 21)。
- 遗留:open item=worker 子进程日志摸底不变(S6 前补);span 耗时 Top N 埋点范围待 S6 前裁定;真机 GUI 手测清单(分析标签三区块+直方图渲染+诊断包导出实测)留到 S6 前批量真机验收,不单列;下一步=S6 收尾(方案 §9.4 S6:worker 子进程日志现状摸底+小裁决、文档回写本文基准数字/todo.md/三件套、CI 接线检查)
