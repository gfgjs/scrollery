# 重构 Plan 复审报告（2026-06-27，第 5 轮外部专家后的多维度复审）

> 📦 **已归档(2026-07-10 文档治理补标)**:重构 plan 开工前第 5 轮审查快照,使命已完成;结论已消化进各 Part 正文。

> 方法：16-finder 工作流（6 文档外维度 + DAG/不变量 + 9 Part 深读）产出 303 条原始 finding，去重 294 簇；
> verify/critic 相因进程中断未完成，由复审人**亲自回源码补核验 top ~15 条**（下表标可信度）。
> 结论：方案成熟、无方向性错误，但仍有真实开工阻断，集中在 ①工具链事实错误 ②**§8 裁决→§3 正文回写不完整**（最严重）。
> 本文件同时充当**修订 changelog**：每条带「修订状态」。

---

## 一、P0 真实开工阻断（已自验）

| # | 问题 | 证据 | 修订状态 |
|---|---|---|---|
| P0-1 | **Part7:258 tauri.conf `version` 指向 Cargo.toml — Tauri 2 不支持**。`tauri-utils-2.9.2/config.rs:3466` deserializer 仅接受 semver 串或 package.json 路径（serde_json 解析），TOML 必失败。 | 已自验（cargo 缓存源码） | ✅ 已修：改为删除 version 字段、回退 `CARGO_PKG_VERSION` |
| P0-2 | **Part4:434/465 memfd 匿名映射缺跨进程 handle/fd 传递机制**。worker 是 `Command::spawn` 的独立 exec 进程，Win HANDLE 进程局部须 `DuplicateHandle`、Linux memfd fd 不随 exec 继承须 SCM_RIGHTS；全文无设计。 | 已自验（worker.rs:90-107 无继承） | ✅ 已修：§3.7.3 补传递机制 |
| P0-3 | **Part1 `run_migrations` 改 `&mut Connection` 破坏 7 处调用方**，T1 未列举 → 编译失败。 | 已自验（grep 7 处调用方） | ✅ 已修：§3.1 + T1 列出 7 处 |
| P0-4 | **Part5 `flat_ids` 无 IPC 暴露、Part2 未认领建命令** → T4 稳定多选卡墙。 | 已自验（cache.rs:57 内部 / ipc 零暴露） | ✅ 已修：Part2 加 T14.5、Part5 引用 |
| P0-5 | **Part5 `list_likely_matches` 后端命令 Part4 未定义** → T10 人脸审批无查询入口。 | 已自验（Part4 §3.5.1 仅 4 命令 / 全仓无） | ✅ 已修：Part4 §3.5.1 补第 5 命令 + T3 |
| P0-6 | **Part8 激活 token 签发密钥治理冲突**：客户端 verify_token 用单一 purpose=license keyset；增强 token 用 license-* 私钥=违气隙，用独立密钥=客户端验签失败。 | 已自验（license.rs:131） | ✅ 已修：§3.3 补 activation-* 密钥方案 + D4 锚点 |
| P0-7 | **Part7 `tauri-plugin-updater` 作普通 dep**，`#[cfg]` 剔不掉 crate → MS Store 10.2.2「不进二进制」承诺落空、T11/RK3 验收不可达。 | 已自验（Cargo 机制 + 文档） | ✅ 已修：改 optional dep + channel-direct feature 绑定 |
| P0-8 | **Part7 macos-latest CI runner 编译必失败**：`windows` crate 无 target 守卫、`wic_engine` mod 无 `#[cfg(windows)]`；隐含强依赖 Part3 mac 门控先行。 | 已自验（Cargo.toml:53 / gpu/mod.rs:2） | ✅ 已修：§3.1.4 + T4 前置门控/排序 |

---

## 二、头号问题：§8 裁决未回写 §3 正文（回归簇，多 finder 命中，已自验）

直接动摇 Part0 §0.3「正文即权威、无须读 §8」承诺：最新一轮裁决部分传播了（EmbedBatch→cache_keys、错误码已进正文），但关键协议/接口定义没传播。

| # | 正文（旧/错） | §8 裁决（新/对） | 修订状态 |
|---|---|---|---|
| R1 | Part6:151 `SessionInitBody{model_paths, provider, batch_capacity}` | Part6:415 `{model_handle, image_provider, model_profile}` | ✅ 已修 Part6 §3.2.1 |
| R2 | Part6:181 GpuLimiter「双层叠加」 | Part6:417「互斥不双锁」 | ✅ 已修 Part6 §3.5 |
| R3 | Part0:408 §10.3 trait=2 方法 + `impl LicenseSource` + 注入 `dyn LicenseSource` | Part0:325 §9.1 trait=4 方法 + `dyn EntitlementProvider` | ✅ 已修 Part0 §10.3 |
| R4 | Part4:171 §3.1.2 G1 `SessionInit{model_paths, provider, profile}` | 同 R1 | ✅ 已修 Part4 §3.1.2 |
| R5 | Part6:206 §3.8 缺 `ManifestVerifier` trait 定义 | Part6:408 §8.2 C3 要求补入 | ✅ 已修 Part6 §3.8 |
| R6 | Part6:234 §3.9.1「迁入 verify_token」 | Part6:451 §8.7 verify_token 留开源 | ✅ 已修 Part6 §3.9.1 |
| ~~R7~~ | ~~Part2:70 §2.2 SourceChanged「仅 mtime」~~ | — | ❌ **finder 过度声称**：§2.2 是现状描述（正确），修复**已在** §3.3.2(:255)/§3.4(:259)「可疑变更 content_hash 路径」。无需修。 |

**附带元修订**：推翻 Part0 §0.3「无须读 §8」→ 改「§3 正文与 §8 裁决冲突时以 §8 为准」（既然正文回写做不全，承诺与现实背离更危险）。✅ 已修。

---

## 三、其它已验证 P1（择要修订）

| # | 问题 | 修订状态 |
|---|---|---|
| P1-1 | Part0 §9.3 + Part7 §3.6.4 CI 命令缺 `--no-default-features`（被铁律兜底，仅注释级不一致） | ✅ 已修两处 |
| P1-2 | Part6:131 §3.1.1 `RegistryCache::accept` 伪代码签名错（实为 `&mut self` 实例方法 + 必带 keyset） | ✅ 已修 |
| P1-3 | Part6:201 `is_task_runnable` 返回 bool vs §3.7 要 `No(NoKey)`，API 破坏未说明 | ✅ 已修（钉死保留 bool + 内部 BlockReason） |
| P1-4 | Part3:337 T4「break→continue」vs §3.8.1「>=→>」矛盾（continue 会引入 unsafe 越界） | ✅ 已修 |
| P1-5 | Part3:164 Q3「scanner 漏扫 heic」误诊（heic 实已收录，真问题是 WIC 未注册 arena） | ✅ 已修 |
| P1-6 | Part1:378 §6 验收「每个 volume_id 非空」过严（孤儿行回填 NULL 属正常） | ✅ 已修 |
| P1-7 | Part3:219 代码片段 `ImageRsEngine::new()`（单元结构体无 `::new()`） | ✅ 已修 |

---

## 四、复审人证伪/降级的 finder 误判（对抗核验价值）

- ❌→P2 **「VolumeOffline 无人认领」**（finder 报 P0）：Part5:214 §3.4 自己的 AppError 改造**已列 `VolumeOffline` code**、T13 标「← §3.4 VolumeOffline」，Part5 §3.4 即 owner。仅未写成独立"新增 enum 变体"子任务。✅ 已加显式子任务（小修）。
- ❌→P3 **`read_version(conn)?` 不可编译**（finder 报 P0）：1 字符问题、编译器秒报。✅ 已去 `?`。

---

## 五、盲区（critic 未跑完，复审人补判，未修订仅记录）

1. **mac 实现链真实可行性**：objc2 桥全是设计、无 mac 实测；P0-8 暴露连编译门控都没落。
2. **跨进程并发协调**：reserved_core_pool × GpuLimiter 跨进程令牌 × supervisor 的死锁/饥饿无时序分析。
3. **性能数字现实性**：ANN 选型仍悬；百万级坐标平移方案 B 未实测；<10MB 仅弹性目标。
4. **测试断层**：184 测试对新增子系统（worker 协议/AES/激活/EntitlementProvider）0 覆盖。

---

## 六、统计

- 原始 finding 303（P0×18 / P1×100 / P2×148 / P3×37，57 标回归）→ 去重 294 簇。
- 自验 P0：8 真实 + 2 降级；头号回归簇 7 处；finder 误判捕获 2。
- 本轮**未补跑** verify/critic 覆盖剩余 ~280 条 P1/P2（用户指示）。
