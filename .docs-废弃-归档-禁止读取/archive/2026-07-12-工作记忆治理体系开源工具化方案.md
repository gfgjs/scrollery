---
status: archived
type: design
line: 工作记忆治理体系开源化
created: 2026-07-12
---

> 📦 **已迁移(2026-07-13)**:本文正式家已按 R3-12 用户裁决迁至独立私有仓 `D:\photoapp\worklog-kit`(其 `docs/designs/` 同名文件,git 史自 `cd8275a` 起);本副本为迁移时点(v0.3 施工基线)快照,**后续演进以新仓为准,勿在此续改**。该工作线自此不入 Scrollery 治理(不取字母号、不进 todo)。

# 工作记忆治理体系 · 开源工具化方案(v0.3 · 三轮深审 28 项全数裁决已回写 · 施工基线)

> 本文是一个**独立开源产品**的 founding 设计:把 Scrollery 自研的 `/planning` skill(易失工作记忆三件套)+ `docs/*` 治理体系(CI 强制的永久知识库)抽取、通用化、产品化,与 Scrollery 隔离到**新仓**。
>
> 本文 **2026-07-13 已迁入本仓**(独立私有仓,工作名 `worklog-kit`,R3-12 裁决)`docs/designs/` 作为首份设计;Scrollery 侧原过渡家副本已移 `docs/archive/` 留横幅(迁移时点快照,后续演进以本仓为准)。
>
> **本文不是施工令**——建仓(本地私有)已按 R3-12 裁决执行;写码 / 建远程仓 / 发布仍待另行批准。
>
> **🔄 v0.2 修订(2026-07-13)**:纳入自审第一轮 12 项发现 + 用户逐条裁决(详见 §15)。关键反转:①语言从「英文默认」改为**中文首发 MVP**(C-2/M-8);②taxonomy 先**保持可配置**不锁死(M-6);③加入 **MVP-first** 与**采纳梯度**(C-2/C-1);④生成式索引降为**按团队规模可选的最重档**(M-4);⑤仓名暂定 **`worklog-kit`**(M-12);⑥**C-3 已裁:生成物 gitignore + 按需构建**。
>
> **🔄 v0.3 修订(2026-07-13)**:第二轮(§16,16 项)+ 第三轮(§17,12 项)深审经用户**全批裁决**(除三条修正:R2-C7 裁①混合写模型、R2-m2 纯中文、R3-12 建仓迁移,其余全采纳),已按「规范文本回写红线」改写正文;裁决记录与 R2-C7 细则见 §18。要点:C-3 语义全文清剿(drift gate 概念退役,CLI 统一 `index build`+`check`);closeout 契约正文化自包含(§7.1);Q7 裁 `lines/<slug>.md` 线实体;采纳 profile 模型(§7.5);配置裁 JSONC + 结构化 disposition(§7.4);机器面锁 ASCII(§5);多人同任务写模型裁定(§4.3)。**本文即 v0.3 施工基线**。

## §0 决策状态板(Review 基线)

已锁定(2026-07-12 / 07-13 对话裁决,作为 Review 不再回炉的基线):

| # | 决策项 | 取值 | 备注 |
|---|--------|------|------|
| L1 | 打包定位 | **两层平级,一仓双入口** | skill 与治理并列一等公民;一仓两条安装路径(全量 `init` / 仅 skill) |
| L2 | License | **MIT** | 开发者工具最大化采纳 |
| L3 | 初始可见性 | **先私有** | 提取/通用化/去私料/README/测试就绪后手动转公开 |
| L4 | 索引/状态形态 | **每单元一文件;索引形态按团队规模可选**(不变量门 / append-only / 生成式) | 生成式为最重档,非唯一形态(M-4 修订;原 v0.1「生成式为唯一形态」已反转) |
| L5 | 语言 | **中文首发 MVP;i18n-ready(字符串外置),英文/多语 catalog 验证后补** | C-2/M-8 裁决,原 v0.1「英文默认」已反转 |
| L6 | 设计支柱 | 两层记忆架构 / CI 强制纪律 / **multi-person-ready** | 第三支柱是本文相对 Scrollery 单人版的最大 delta |
| L7 | 产品成熟度 | 按规范成熟产品思路(semver / CONTRIBUTING / CI / 测试 / i18n-ready) | 面向专业开发者 |
| L8 | 交付策略 | **MVP-first(最小验证优先)** | 先纯中文 MVP(门禁 + skill + init)+ dogfood 本仓,再据信号扩(C-2) |
| L9 | 采纳梯度 | **门禁 off / warn / enforce 三档 + 核心不变量 / 风格建议两级** | 默认值经显式 **profile** 取(greenfield/brownfield/advisory/strict,§7.5;R2-M1 消解「enforce 默认 vs warn 起步」冲突);梯度实现后置 P2/P4(R3-11) |

待定(标 🔵,进入 Review 逐项收敛,清单汇总于 §13):

- 🔵 仓名:暂定 **`worklog-kit`**(M-12;本地私有仓已按此工作名建于 2026-07-13,正式名定案后 rename)。
- 🔵 分发形态:npm 包 / template repo / 二者兼有(§6)。
- ✅ 配置格式已裁 **JSONC + JSON Schema**(R2-C5,§7.4);可配边界**开发期不冻结、保持可配**(M-6),但 disposition 元模型字段集稳定。
- 🔵 Scrollery 是否回流 dogfood 此开源工具(§8.3)。

## §1 背景与动机

**来源**。Scrollery 在长期开发中自研了一套两层记忆体系,已在本仓落地并经 CI 门禁验证:

- **易失层**:`/planning` skill(Scrollery 仓内 `.claude/skills/planning/SKILL.md`;本文引用的 Scrollery 路径均为**出处标注**而非活链接——本文自包含,关键契约已在 §7.1 正文化)——多阶段长任务在 `docs/planning/<日期>-<任务>/` 建 `task_plan`/`findings`/`progress` 三件套;零 hook、零 token 税、支持跨会话/跨机器续作。
- **永久层**:`docs/*` taxonomy + 两道门禁(`tools/check_docs.mjs` 断链/frontmatter/位置/收口契约,`tools/check_docs_index.mjs` 索引新鲜度)+ skill 分发器(`scripts/install-skills.mjs`)。
- **蒸馏缝**:收口时按 `closeout.md` 契约把易失层的耐久候选提升进永久层,由 CI 静态门机械校验(候选全覆盖/disposition 枚举/target 验存)。

**理论溯源**。两层结构对应 agent 记忆研究中的通识:上下文窗口=易失 RAM,文件系统=持久磁盘(Manus 式「文件即工作记忆」);易失→持久的**蒸馏**对应 CoALA / MemGPT / LangMem 里「短时工作记忆固化为长时语义记忆」的环节。市面工具多只做其一(纯 AI memory 框架无 CI 纪律;纯 docs linter 无 agent 自动喂料),两层咬合 + 静态门是本系统的稀缺点。

**为何开源化 + 隔离新仓**。①这套东西的价值(尤其 CI 强制的文档纪律 + closeout 蒸馏缝)对任何专业团队普适,不绑定 Scrollery 业务;②硬结构(门禁 + 结构)甚至不依赖 AI 也成立;③与 Scrollery 隔离可独立版本化/独立受众/独立治理,且避免把 Scrollery 私料混入公开物。

## §2 产品定位

**一句话**:一个把「工程文档纪律」做成 CI 可强制、并配套 AI working-memory 工作流的开发者工具——让文档从「靠自觉」变成「合并前置条件」。

**受众**:专业开发者与团队(用 AI agent 与否皆可受益)。

**两条消费路径(L1 双入口)**:

1. **全量治理**(治理层为主):`npx <tool> init` 在目标仓 stamp 出 docs 骨架 + 门禁 + CI + 生成式索引;团队获得机械强制的文档纪律,**不依赖任何 AI**。
2. **仅 skill**(易失层为主):`npx <tool> init --skill-only` 或作为 Claude Code plugin 装入 `/planning` working-memory 工作流;适合已有文档习惯、只想要 AI 长任务记忆的用户。**价值边界(R3-7)**:此模式只提供工作流指导,closeout 纪律回退为自觉、无机械强制;要机械强制必须装治理层——文档如实陈述,不暗示两路径等强。

**与既有品类的差异**:

| 既有品类 | 代表 | 缺口 | 本工具补什么 |
|---|---|---|---|
| ADR 工具 | adr-tools、log4brains | 只管 decisions,无长任务工作记忆,无跨类型 CI 门 | 全类型 taxonomy + 收口蒸馏缝 + 生成式索引 |
| Docs linter | markdownlint、Vale | 查文风/语法,不查「移动必修链/归档必横幅/收口必蒸馏」结构纪律 | 结构不变量 + 收口契约静态门 |
| AI memory 框架 | LangMem、MemGPT 类 | 运行时记忆,不落地为可 review 的仓内文件 + 无 CI | 文件化易失记忆 + 静态门 |
| Claude Code skill(裸) | 各类 skill | skill 本身只是行为规范,收口靶子须自备 | 把收口靶子(治理层)一并产品化 |

## §3 核心架构:两层记忆 + 蒸馏缝

沿用 Scrollery 已验证的骨架,**结构不变、承载形态按多人场景重塑**:

```
Layer 1  易失工作记忆(ephemeral)          Layer 2  永久知识库(durable, CI-gated)
─────────────────────────────────         ─────────────────────────────────────
planning/<unit>/                           designs/  reviews/  decisions/
  task_plan · findings · progress          runbooks/ worklogs/ archive/
  (一任务一目录,天然分片)                   + 索引按档:手工+不变量门 / 生成式 build(产物不入库)
        │                                          ▲
        │  closeout.md(蒸馏缝)                    │
        └──────── 逐候选提升 ────────────────────────┘
                  disposition ∈ {experience|decision|design|
                   runbook|completed|todo|code|test|no-promotion}
                  由 closeout 静态门机械校验
```

**保持不变**(已验证、跨场景通用的原语):

- 一任务一目录的易失三件套;无共享运行时锁/pointer/env(天然免协调)。
- closeout 收口契约(候选全覆盖不重不漏 / disposition 枚举 / target 验存或冻结引用 / verified=yes)。
- 「以文件为准,不信记忆」的接续纪律;archive/decisions 冻结不变量。
- 门禁把纪律机械化(团队场景价值更高)。

**按多人场景重塑**(详见 §4/§5):

- 索引与滚动状态从「单文件手工 + 不变量门」→「每单元一文件 + 可选生成式索引」(§4.1)。
- 工作线 ID 从「中心分配的字母」→「`lines/<slug>.md` 线实体文件」(§4.1,Q7 已裁):免中心分配,撞名在合并时成 git add/add 冲突原生暴露;文档另携稳定 `id` 与权威/生命周期不变量(§7.2,R2-C3)。
- 全中文内容 → 中文首发 + i18n-ready(§5)。
- 补齐软肋:任务名全局唯一、多人同任务归属、schema 变更权威、异构 agent(§4.3)。

## §4 多人协作重设计(第三支柱)

这是相对 Scrollery 单人版最大的 delta。Scrollery 版明确「用户基本在环、不做无人值守」,并行只想到「单人跨会话」而非「多人跨分支」。下面逐项把它升级到团队级。

### §4.1 每单元一文件 + 生成式索引(替代单文件手工 + 不变量门)

**问题回放**。Scrollery 版有两个共享单文件热点:`todo.md`(唯一滚动现状源)与 `README` 字母登记表。团队里每条 PR 都动 `todo.md` → 每次合并冲突;字母「下一空闲 = max+1」是**中心化分配**,两人同抢即撞号(索引门只能事后抓,已成瓶颈)。

**为何这里要反转 Phase 3 裁决**。Scrollery Phase 3 曾**拒绝生成式索引**,理由是「立项/权威文档/摘要等列是人工判断,frontmatter 里没有,硬做生成式要发明新字段且把人工蒸馏降级为机器倾倒」。该裁决在**单人**场景成立(单文件手工维护无冲突,生成式纯属多余复杂度)。但在**多人**场景取舍翻转:①单文件是冲突磁铁;②那条「元数据不在 frontmatter」的反对理由,其**解法恰好就是把元数据下沉进各单元 frontmatter**——而这同时把编辑面**分片**了。也就是说,消除反对理由的动作与消除合并冲突的动作是**同一个**。故多人版采生成式。

**设计**:

1. **每单元自带元数据**。每个 design/review/decision/runbook/worklog 的 frontmatter 承载索引所需字段,单元即真源:
   ```yaml
   ---
   id: 2026-07-12-gallery-dense-grid   # 稳定 ID:创建即得、改名不变(R2-C3);建议 <创建日>-<slug>
   status: active            # draft|active|snapshot|superseded|archived(机器面 ASCII,R3-1;状态机见 §7.2)
   type: design              # 见 taxonomy 枚举
   line: gallery-perf        # 引用 lines/<slug>.md 线实体(Q7 已裁,见 item2)
   title: Gallery dense-grid rendering
   authoritative: true       # 是否该线权威文档;唯一性不变量见 §7.2(R2-C3)
   created: 2026-07-12
   summary: One-line synopsis for the generated index.
   # supersedes: <旧文档 id>  # 取代关系成对声明(§7.2)
   ---
   ```
   「立项日/权威文档/摘要」这些**人工判断列**不再挤在一张中心表里,而是各单元在自己文件里各自申明——人工判断保留,但**零中心争用**。

2. **工作线 = `lines/<slug>.md` 线实体文件(Q7 已裁,2026-07-13)**。开线 = 新建 `lines/<slug>.md`(一句话使命 + 可选 owner),无需向任何中心表登记取号 → **消除分配瓶颈**;文档 frontmatter 的 `line` 字段只**引用**该 slug。唯一性语义随之可判定(消解 R2-C2 的自相矛盾):①线的唯一声明 = `lines/` 内文件名,两人撞名 = git **add/add 冲突**,合并时即被原生暴露(早于任何事后门);②门禁校验「文档引用的 line 必须存在对应实体文件」;③同线多文档共用 `line` 值是**正常复用**,不再被误判为碰撞——文档级唯一性由 `id` 承担。字母表连同「下一空闲字母」不变量一并退役。(M-5 的「可检出而非根除」结论保留:实体文件把检出左移到合并时刻。)

3. **滚动状态分片**。`todo.md` 单文件 → `status/<line>.md` 每工作线一文件(或每任务一文件),两人改不同线永不冲突;「现在做到哪」由生成器从各 `status/*` + 任务 frontmatter 聚合成只读的 `STATUS.md`。

4. **生成式索引:`index build` + `check`,产物不入库(C-3 已裁;R2-C1 语义统一)**。`index build` 扫描各单元 frontmatter 产出人可读 `INDEX.md`/`STATUS.md`;产物列入 `.gitignore` **不入库** → 两分支根本不含派生文件,**真零合并冲突**(这是生成式相对不变量门的唯一实质好处,唯 gitignore 才兑现)。**索引是派生物,从不手工编辑、从不提交**;因此本工具**没有 drift gate**——「生成物≠源即红」「忘记重生成」「索引不新鲜」这些概念随产物不入库整体消解,pre-commit hook 亦无物可拦、不做。CI 侧跑 `check`:断言**生成成功 + 源结构一致性**(line 引用存在 / 权威唯一 / worklogs 双向 / frontmatter 齐备,见 §7.2)。
   - **可见性补偿(R3-2)**:仓内/GitHub 看不到索引是 gitignore 的真实代价,补偿分层承诺——本地预览 `npm run docs:index` 是**默认路径**(README 如实陈述);CI 发布的最低承诺 = **Actions artifact**(零新增基建);docs site(如 Pages)属 opt-in 增量,不进 MVP;发布管线细节在 P2 上生成式档前裁定。

**冲突面对照**:

| 编辑对象 | Scrollery 单文件版 | 本多人版 |
|---|---|---|
| 滚动状态 | 人人改 `todo.md` → 每 PR 冲突 | `status/<line>.md` 分片 → 仅同线冲突 |
| 工作线登记 | 中心字母表 + max+1 分配 → 撞号 | `lines/<slug>.md` 线实体 → 撞名 = add/add 冲突合并时原生暴露 + 门禁引用检查兜底 |
| 人可读索引 | 手工维护 + 不变量门 | 派生生成,产物不入库(C-3)→ 真零冲突 |
| 单元元数据 | 集中在中心表 | 各单元 frontmatter 自持 → 仅改同一单元才冲突 |

**诚实代价**:生成式要维护一个生成器(更多代码),且索引仓内不可直接浏览(补偿分层见 item4)。**不采「CI 自动 commit 生成物」**(自动 commit 有其自身危险:污染作者归属、与保护分支/签名冲突)——产物不入库后,此选项连同「忘记重生成」问题一并失去存在前提。

**索引形态按团队规模可选(M-4)**。生成式并非唯一形态,而是**最重的一档**;强加给 2–5 人小团队可能杀鸡用牛刀(并发触碰 docs 的 PR 本就少)。故工具提供**三档梯度**,由 `init` 按团队规模选、或 `.<tool>rc` 配置:

| 档 | 机制 | 适用 | 冲突面 |
|---|---|---|---|
| **不变量门**(Scrollery Phase 3 形态) | 手工维护索引 + 结构不变量静态校验(目录↔登记双向一致等;与已退役的生成物 drift gate 无关) | 单人 / 极小团队 | 索引单文件会冲突,但零生成器负担 |
| **append-only 登记** | 登记表只增不改(新行追加末尾)+ 唯一性检查 | 中小团队 | 冲突减少但非免疫:两分支同时追加文末仍相邻行冲突(R2-m1);高并发可改每登记一文件 |
| **生成式**(本节主设计) | 各单元 frontmatter → `index build` 产出索引(不入库)+ `check` 结构校验 | 中大团队 / 高并发 | 真零冲突(C-3 gitignore) |

MVP 首发仅需**不变量门**档(最简、已在 Scrollery 验证);append-only 与生成式作为 opt-in 增量,待 dogfood 验证需求后再上(呼应 L8 MVP-first)。

**档间切换是数据布局迁移,不是配置开关(R3-3)**:不变量门档(中心登记表 + 单文件滚动状态)与生成式档(元数据下沉各单元 frontmatter + `status/` 分片 + `lines/` 实体)的数据承载不同,升档须**一次性迁移**——工具提供迁移命令,或至少文档化手工步骤;且 skill 的收口靶点(登记行 / todo 回写)随档位变,skill 文本须按档参数化(读配置定收口靶点,§7.3/§8.1)。

### §4.2 语义合并竞态消解

全局不变量在多人下的经典陷阱:PR-A、PR-B 各自绿,合并后不变量破了(git 文本合并干净但语义破)——即 semantic merge conflict。

- **索引类**:本版索引为派生物且不入库,合并后按需重建即恢复,无残留破态。
- **分配类**:线实体文件免中心计数器 → 无分配瓶颈;撞名 = add/add 冲突在**合并时**原生暴露(M-5 的「事后可检出」升级为「合并时可见」)。
- **主干完整性(R2-C6 措辞修正)**:main push 后再跑全部结构门是**事后探测 + 恢复兜底**——它**拦不住**已进主干的破态,正文不得称「拦下」。多人档的**要件**(非建议):required status checks + up-to-date-before-merge,或 merge queue(在 `merge_group` 事件上对**拟合并结果**跑门)——这才是把 semantic merge conflict 挡在主干之外的机制;单人/极小团队档可只留事后探测。

### §4.3 软肋修复(团队下必补)

| 软肋 | 症状 | 修法 |
|---|---|---|
| 任务名跨分支不全局唯一 | 两人同日同名 → 各自 `2026-07-12-foo/`,合并即撞 | 任务目录带作者短标识:`<date>-<slug>-<author?>`;init 时可选注入 `git config user` 缩写 |
| 多人协作同一任务 | 共享 `progress.md` 并发追加文末在 git 文本层**必冲突**;候选 ID 分配竞态 | **已裁①混合写模型**(R2-C7,2026-07-13),见下方「同任务写模型」块 |
| schema 变更权威 | `README`/门禁是「宪法」,谁都能改则失序 | 对治理 schema 文件与门禁脚本挂 **CODEOWNERS**,改动须指定 owner 审;门禁自身有 selftest 防回归 |
| 异构 / 无 AI 贡献者 | 有人用别的 agent 或纯手工,拿不到 skill 自动化 | 两层设计红利:**skill 缺席不影响治理层**;文档明示「skill 可选,门禁必装」;MVP 起 `templates/` 附 trio 空模板 + 手册化收口 runbook,无 AI 也能走通 start→closeout(R2-M2 最小解;`task` CLI 全家桶后置 P2+,避免 MVP 膨胀) |
| onboarding 一致性 | 各人 `~/.codex`/`~/.claude` skill 副本可能漂移 | `install-skills --check`(SHA-256 双证,已有范式)+ CONTRIBUTING 写清一键装;仓内副本为 canonical;个人安装一致性属**本机**检查(`--check`/`doctor`),**不进仓库 CI**(R2-M4) |
| 「谁在做什么」不可见 | `ls planning/` 只见任务不见人 | `owner` frontmatter + 生成器产出 STATUS.md 汇总 owner 列 |
| 烂尾三件套无时效信号 | closeout 门只咬**归档目录**,弃置在 planning/ 的任务永不触发任何门,多人仓积尸 | `doctor`/status 报 **stale trio**(>N 天无 progress 更新)为 warn 级(R3-9) |

**同任务多人写模型(R2-C7,已裁①混合,2026-07-13)**:

- **单人档(默认;Scrollery 验证形态)**:常规三件套,单文件 `progress.md`——零新增成本,MVP 即此档。
- **team 模式(多人同任务)**:任务创建时在 `task_plan` frontmatter 声明 `mode: team` + `owner`/`collaborators`;`progress` 改**每事件一文件** `progress/events/<UTC时间戳>-<author>-<seq>.md`(ISO 8601 紧凑时间戳,文件名字典序即时间序;同秒撞时靠 `<seq>` 去歧义),人可读时间线视图由 `index build` 聚合、不入库。`task_plan`/`findings` 保持共享单文件(低频编辑,罕见冲突值得人工审);**候选 ID 带作者命名空间**(`F-<author>-NNN`/`D-<author>-NNN`),消 ID 分配竞态。
- **模式在任务创建时声明;中途切换 = 一次性迁移**(`progress.md` → `events/`,与 §4.1 档间迁移同款语义),不做隐式混用。
- **closeout 秩序**:仅 `owner` 发起 closeout;closeout PR 开启后候选账**冻结**,迟到候选进下一任务/下一轮,不回头追加。
- 该模型与三档索引梯度**同构**(按团队规模选形态、单人档零成本、升档=显式迁移),共用同一套「档位」心智,不新增第二种配置哲学。

**closeout 适用边界(M-7)**。收口契约(F-NNN/D-NNN 候选账 + 蒸馏 + `closeout.md`)对顺手修 bug 的 drive-by 贡献者太重,不应强加到**每个** merged PR。边界:**closeout 只作用于「长任务 / 工作线级」**——即建了 `planning/` 三件套的任务;PR 级小改(无三件套)豁免。触发判据即「该任务是否存在三件套」(门禁对含三件套的归档目录强制 closeout,普通 PR 不涉及)。「够长要建三件套」的判定沿用 skill 既有阈值(≥5 步 / 跨会话),由发起者 / 维护者定,门禁不猜。

## §5 语言与 i18n:中文首发,i18n-ready

**原则:工具产出语言 ⟂ 使用者项目内容语言**。工具只 i18n **自身** UI/报错,绝不翻译使用者的文档内容;使用者可把自己项目的文档写成任何语言。

**第二刀:机器面 ⟂ 展示面(R3-1)**。第一刀切的是「工具自身 ⟂ 使用者内容」,还须切第二刀:frontmatter **字段名与枚举值**(status/type/disposition)、文件名约定、closeout 表头等**机器可判定面锁 ASCII canonical**(如 `status: active`);**人可读面**(报错文案、生成索引的表头、模板 prose)走 catalog 本地化——中文首发即「ASCII 枚举 + 中文展示」。这消解一处潜在自相矛盾:L5 的「模板中文首发」指模板 **prose** 中文,**不指枚举值中文**;若中文模板配中文枚举值,未来国际化 = schema breaking change(major),catalog 映射层从第一天就拆掉这枚雷。(Scrollery 现役中文枚举是单仓自用的合法选择,不随抽取继承。)

- **MVP 首发纯中文(C-2/M-8)**:门禁报错、`init` 输出、模板骨架、`SKILL.md`、README 首发中文——这是作者母语与首个 dogfood 场景,最快验证。原 v0.1「英文默认」在验证前反转:**先证明它有用,再谈国际化**(呼应 L8 MVP-first)。
- **i18n-ready 架构,但不做多语翻译**:即便首发单语,所有面向用户的字符串**从第一天就外置**(如 `locales/zh.json`),门禁/CLI 文案走 catalog 查表,语言经 `--lang` / `.<tool>rc` / `LANG` 选择。如此加英文/他语只是新增一份 catalog、零改逻辑。**英文 catalog 推迟到有采纳信号后再补**(m-8:专业开发者读单语报错无碍,过早双语是纯维护负担)。**MVP 的 fallback 链即 zh**(唯一 catalog;R2-m2 已裁纯中文)——`requested → en → …` 之类的链待英文 catalog 上线后再定义,不预设不存在的落点。
- **术语一致性**:提供 glossary(working memory / closeout / distillation / disposition / work-line / drift gate 等),中文文案保留英文术语(与 Scrollery 约定一致),避免翻译漂移。
- **转公开前评估英文门面**:面向国际贡献者的最小英文集(README/CONTRIBUTING)在 P5 转公开前按需补(见 §12),非 MVP 阻塞项。

## §6 仓库结构与分发

**布局**(工作名 `<tool>`,待定 §13):

```
<tool>/
  README.md                    项目门面:两层记忆架构 + 快速上手(中文首发)
  LICENSE                      MIT
  CONTRIBUTING.md  CODE_OF_CONDUCT.md  CODEOWNERS
  package.json                 bin 暴露 CLI;semver 发布
  bin/<tool>.mjs               CLI 入口(init / index build / check / doctor / upgrade 子命令)
  src/
    init.mjs                   脚手架:stamp docs 骨架 + 配置 + 出 CI(引擎驻包,见「安装拓扑」)
    check-docs.mjs             通用化门禁(断链/frontmatter/位置/收口)
    build-index.mjs            index build 生成器 + check 结构一致性校验(产物不入库)
    install-skills.mjs         通用化 skill 分发器
    lib/frontmatter.mjs        frontmatter/config 共享解析——单一实现防双引擎漂移(R3-6)
    config.mjs                 读取 .<tool>rc(taxonomy/schema/lang 配置)
    i18n.mjs  locales/zh.json  (locales/en.json 后置)
  skills/planning/SKILL.md     skill 本体(中文首发;触发护栏键到工具配置,R3-5)
  templates/                   空白但合法的 docs 种子(中文首发)
    docs-README.md  status/  experience.md
    task_plan.md  findings.md  progress.md    ← trio 空模板(R3-4:无 AI 用户低摩擦起点)
    worklogs-README.md  closeout-template.md  ci-github.yml
  test/                        单元(门 selftest)+ e2e(temp 仓 init→全门绿)
  docs/                        本仓 dogfood 这套治理(吃自己狗粮)
  .github/workflows/ci.yml
```

**分发形态(🔵 OPEN,§13)**:倾向**兼有**——

- **npm 包 + `npx <tool> init`**:主分发路径,`init`/`index`/`check` 皆为子命令;版本化清晰,升级方便。
- **template repo**:GitHub「Use this template」一键起,内含已 wired 的 CI 与 docs 骨架;适合不想过 npm 的用户。
- 二者共享同一 `src/`,template repo 由 CI 从主包生成,避免双份漂移。

**双入口落地(L1)**:`init`(全量)与 `init --skill-only`(仅装 skill)同一命令两参;Claude Code plugin 作为**可选薄封装**指回本仓(锦上添花,非地基)。

**安装拓扑与升级模型(R2-M3)**。唯一安装拓扑 = **thin runner**:引擎(门禁/生成器/分发器)驻 npm 包内,用户仓只落**配置 + templates 实例 + ci.yml**,不复制引擎脚本——杜绝「一次性 template generator」退化,semver 升级即 `npm update`。stamped 文件与配置携带 `schemaVersion` + 生成时工具版本(manifest);`init` 支持 `--dry-run` 与幂等重跑(已存在文件默认不覆盖,报冲突清单);`doctor` 诊断用户仓与当前工具版本的兼容性(含 stale trio 报告,R3-9);`upgrade --dry-run` 预览迁移,migration registry 按 `schemaVersion` 逐级迁移(含 §4.1 档位升档、§4.3 solo→team 迁移);所有写入原子(tmp→rename)+ 先备份可回滚。break schema/CLI 契约 = major + 附迁移条目,semver 由此对用户仓成为**可升级契约**而非空话。

**押注治理层、skill 当可替换插件(m-11)**。若 Anthropic 日后把 skill/memory 一方标准化(plugin marketplace / 官方 skills),skill 半边可能被蒸掉;故产品**主价值押在不依赖 AI 的治理层**(门禁 + 结构 + closeout 蒸馏缝),skill 视为可被官方或他家替换的一等**插件**而非地基——这也是 L1「两层平级」在风险维度的兜底。

**dogfood**:本仓自身用这套治理管理自己的 `docs/`——最强的活文档 + 回归防线。

## §7 门禁套件重设计(通用化 + i18n-ready + 可配置)

从 Scrollery 三脚本抽取,已勘察耦合面很小,主要工作是**通用化 + 英文化 + 抽配置**:

### §7.1 check-docs(断链 / frontmatter / 位置 / 收口)

- **`walk()` 加存在性守卫**:Scrollery 版对 `src/`/`scripts/` 等根目录不设守卫,新项目缺 `src/` 会 `readdirSync` 抛 ENOENT。加一行 `if (!existsSync(dir)) return` 根治(顺带加固,零行为变化)。
- **1b 代码引用扫描去硬编码**:Scrollery 版写死 `src-tauri/`、`Cargo.toml`、`copy.bara.sky`。改为**探测顶层源码目录 + 可配排除表**——⚠️ 纯自动探测会误扫 `dist/`/`vendor/`/`.venv/` 等大生成树(慢 + 对生成物里的 docs 路径误报),且排除表本身项目相关。故诚实降级为「默认排除常见构建/依赖目录 + `.<tool>rc.sourceRoots` 可覆盖」(M-9;非 v0.1 声称的「纯自动」)。去掉 `plan-docs` 旧前缀兼容(Scrollery 专属历史)。
- **枚举外置**:`STATUS_ENUM`、disposition 集合、type 枚举从代码常量提到**配置文件**(§7.4),门禁读配置,不写死特定 taxonomy。
- **报错走 i18n catalog**:所有 `report(loc, msg)` 文案外置查表,首发中文 catalog(L5;英文后补)。
- **收口契约正文化(R2-C4)**:不再以「原样保留 Scrollery 范式」代替定义——源契约散在 Scrollery 私仓三处(worklogs README + skill 模板 + check_docs 收口节),公开受众永不可达,自包含是硬需求。正式 schema 如下,**本节即唯一真源**:
  - **候选声明源**:三件套两张表(findings「耐久提升候选」表、task_plan「关键决策」表)的「候选 ID」列;叙事提及不算。ID 形态 `F-NNN`/`D-NNN`(team 模式带作者命名空间 `F-<author>-NNN`,§4.3)。
  - **closeout 处置表列**:候选 ID / disposition / target / locator / 去重证据 / N-A 理由 / verified。
  - **全覆盖不变量**:声明候选集合与处置表 ID 集合恰好相等(不重、不漏、无未知 ID)。
  - **cardinality**:一候选提升到多处 = 同 ID 多行,行以(ID, disposition, target)组合唯一;「全覆盖」按 ID 集合判定。
  - **per-disposition 语义**(与 §7.4 结构化配置一一对应):docs 类(experience/decision/design/runbook/completed)target = `repo:<仓根相对路径>`,门禁**验存**;`todo` target 固定为配置声明的滚动状态靶点(不变量门档默认 `repo:docs/todo.md`,档位化见 R3-3);`code`/`test` target = **冻结引用** `repo:<路径>@<commit 短hash>`(只验 grammar 不验存在,永不回改);`no-promotion` target = `—` 且 **N-A 理由必填**。
  - **去重证据列**:`new`(新落点)或 `repo:<路径>`(复用既有知识);no-promotion 填 `—`。
  - **verified 语义**:只接受 `yes`,含义 = 提升动作已在**同一变更集**内完成且落点可见——closeout、目录归档、永久文档写入须同 commit/PR **原子完成**(同变更集新建的 target 在合并时即存在,验存无鸡蛋问题)。
  - **冻结边界**:归档后三件套正文冻结;closeout 语义列(ID/disposition/理由)冻结;仅 docs 侧 target 因目标文件迁移可在**同一移动 commit** 更新(target 消失即门红,机械强迫修链);冻结引用永不回改。
  - **最小样例**:合法 `| F-001 | experience | repo:docs/experience.md | §N 标题 | new | — | yes |`;非法 `| F-002 | code | repo:src/foo.mjs | — | new | — | yes |`(code 缺 `@<commit>` 冻结引用)——selftest 正负例即由此对生成(§10)。

### §7.2 build-index(生成 + 结构校验;取代 check_docs_index 的纯不变量门)

- 形态从「校验手工索引不变量」→「**`index build` 从各单元 frontmatter 生成索引(产物不入库)+ `check` 校验源结构**」(§4.1;R2-C1 清剿后,全文不再有任何 drift 语义)。
- 生成:扫 `designs/reviews/decisions/runbooks/worklogs` 各单元 frontmatter,按 `line`/`type`/`authoritative` 聚合,产出 `INDEX.md` 与 `STATUS.md`(带 generated 护栏)。**稳定排序键 = `created` 日期 + `slug`**(纯数值/ASCII 比较,不依赖 locale collation),规避 CJK/混排排序跨平台产出不同字节的脚枪(M-10)。
- `check`(源结构一致性,生成式档):①frontmatter 齐备合法;②文档 `line` 引用的 `lines/<slug>.md` 实体存在且未归档;③文档 `id` 全局唯一;④权威/生命周期不变量(见下);⑤worklogs 归档↔登记双向一致。环境自免疫:无 collation、无着色、行尾归一(沿用 Scrollery 已验证纪律)。
- **权威与生命周期不变量(R2-C3)**:
  - **状态机**:`draft → active → (snapshot | superseded | archived)`;`superseded` 必须携 `supersededBy`,`supersedes`/`supersededBy` 成对且双向一致(门禁验)。
  - **权威唯一**:每 `(line, authorityScope)` 至多一个 `status: active` 且 `authoritative: true` 的文档;`authorityScope` 缺省 = 整线,可细分。
  - **改名**:文件名可变、`id` 不变(引用与取代关系挂 `id`,历史引用免疫改名);同 commit 修仓内链接(断链门兜底)。
  - **拆线/并线**:`lines/` 实体文件显式新建/归档(归档线实体留横幅指去向),受影响文档的 `line` 字段同 commit 迁移;归档线不得再被 active 文档引用(门禁验)。

### §7.3 install-skills(通用化分发器)

- 去 Scrollery 专属默认(`.claude/skills/planning` 源路径改为相对本工具);保留 `install`/`check`/`dry-run` 三模、冲突默认拒绝(exit 2)、`--force` 备份 + 原子拷贝、SHA-256 双证 `--check`。
- 目标可选 Codex(`~/.codex`)与 Claude Code project-local;**不镜像到 `~/.claude`**(个人影子会盖住项目版——Scrollery 已验证教训)。
- **触发护栏重键(R3-5)**:skill 现文案以「存在 `docs/planning/` 或 docs README 登记的契约」为跨项目误触发护栏——产品化后判据改键到**工具配置**(`.<tool>rc` 声明的 planning 目录);护栏判据本身属提取面,已补入 §8.1。
- **检查边界(R2-M4)**:`--check`(个人 skill 副本一致性,SHA-256 双证)是**本机** onboarding 检查(并入 `doctor`),**不进仓库 CI**——CI 的 fixture HOME 没有个人副本,把个人安装状态提升为仓库一致性属范畴错误;仓库 CI 只跑 `--selftest`(fixture 正负例)。这与被抽取系统(Scrollery)的现役 CI 设计一致,修正 v0.2 §9 的唯一事实不符项。

### §7.4 配置化(可配边界:开发期不冻结)

一个 `.<tool>rc`(或 `<tool>.config.mjs`)声明本项目的 taxonomy 与 schema,让门禁不写死:

```jsonc
{
  "schemaVersion": 1,        // 配置/数据布局版本,upgrade 迁移按此逐级走(R2-M3)
  "lang": "zh",
  "dirs": ["designs","reviews","decisions","runbooks","planning","worklogs","archive"],
  "status": ["draft","active","snapshot","superseded","archived"],   // 机器面 ASCII(R3-1)
  // types 仅描述「受治理的入库文档」;INDEX/STATUS 生成物不入库(C-3),不在治理域,
  // 故无 index/rolling-status 型生成物条目——保留 index 型仅用于不变量门档的手工索引文件(R3-8)
  "types": ["design","review","decision","runbook","canon","working-memory","closeout","index"],
  // disposition 是结构化规则,不是裸名字(R2-C5):元模型字段集固定,规则内容开发期可增改(M-6)
  "dispositions": [
    { "name": "experience",   "targetKind": "docs",       "dedupeEvidence": true },
    { "name": "decision",     "targetKind": "docs",       "dedupeEvidence": true },
    { "name": "design",       "targetKind": "docs",       "dedupeEvidence": true },
    { "name": "runbook",      "targetKind": "docs",       "dedupeEvidence": true },
    { "name": "completed",    "targetKind": "docs",       "dedupeEvidence": true },
    { "name": "todo",         "targetKind": "fixed",      "target": "docs/todo.md" },
    { "name": "code",         "targetKind": "frozen-ref" },
    { "name": "test",         "targetKind": "frozen-ref" },
    { "name": "no-promotion", "targetKind": "none",       "reasonRequired": true }
  ],
  "sourceRoots": "auto"      // 或显式列出代码根,供 check-docs 1b 扫描
}
```

`targetKind` 枚举 = `docs`(仓根相对路径,验存)/ `fixed`(固定靶点,档位化)/ `frozen-ref`(`@commit` 冻结引用,验 grammar)/ `none`(须 N-A 理由)——validator 按 kind 分派校验逻辑,语义与 §7.1 schema 一一对应。

**M-6 裁决:开发期不锁死,保持可配**。自审曾建议把 taxonomy/status/dispositions 锁死以强化「标准纪律」身份(Prettier 式有主见);用户按「**开发期不冻结契约**」原则否决——先放开可配、收集真实使用形态,到**发布**时再据证据决定哪些收敛为不可配的「标准」。**可配的是规则内容(增删改条目),不可配的是元模型**(字段集与 targetKind 语义)——否则「保持可配」退化成 validator 读不懂的任意字符串,可配置性反噬核心不变量(R2-C5)。✅ 配置格式已裁(R2-C5 连带):**JSONC + JSON Schema 校验**,不选可执行 mjs——配置不该图灵完备,静态可校验优先。

### §7.5 采纳梯度(C-1:降低团队采纳阻力)

纪律型工具的头号死因是被贡献者当官僚主义拒收(`--no-verify` 绕过 / fork 掉门禁)。为让它能**渐进采纳**:

- **两级门禁**:①**核心不变量**(断链、归档横幅、收口契约、schema 合法);②**风格建议**(命名、摘要长度、可选字段)。
- **三档开关 + 显式 profile(R2-M1)**:每类门可配 `off | warn | enforce`,但默认值**只经显式 profile 取**——规则级别与「新仓/存量仓」不再各自隐式覆盖(消解 v0.2「核心默认 enforce」与「新库从 warn 起步」的自相矛盾;`init` 选 profile,测试按 profile 断言退出码):

| profile | 核心不变量 | 风格建议 | 适用 |
|---|---|---|---|
| `strict` | enforce | enforce | 纪律成熟团队;本仓 dogfood |
| `greenfield`(新仓默认) | enforce | warn | 新仓从第一天上全量核心纪律 |
| `brownfield`(存量仓默认) | 存量违规入 **baseline 豁免清单**,仅**新增**违规 enforce | warn | 存量仓渐进采纳,免「一上来满屏红」劝退 |
| `advisory` | warn | warn | 试用/评估期 |

- **P1 单 profile(R3-11)**:MVP 只内置 `strict`(dogfood 自用即此档),profile 全家与梯度实现后置 P2/P4(对外分发前)——梯度**语义**在此定死,**实现**不抢 MVP。
- 「能被**渐进**采纳」提为**显式设计目标**,与「机械强制」并列——强制是终态,不是入口。

## §8 从 Scrollery 提取的卫生(no-leak)

开源发布前**逐文件核对**,确保零 Scrollery 私料泄漏:

### §8.1 清私料清单

| 来源 | 可复用(结构/规则) | 必清(Scrollery 私料) |
|---|---|---|
| `docs/README.md` | taxonomy 表结构、frontmatter schema、维护规则、收口契约 | 工作线 A–Q、`refactor_2026/`、Copybara/rename-gate、项目名、具体权威文档链 |
| `check_docs.mjs` | 断链/frontmatter/位置/收口全逻辑 | `src-tauri`/`Cargo.toml`/`copy.bara.sky`/`plan-docs` 硬编码 |
| `check_docs_index.mjs` | 不变量逻辑(并入生成器一致性断言) | 中文小节标题写死(改配置/英文) |
| `SKILL.md` | 三件套生命周期、收口仪式、护栏 | Scrollery 具体路径示例;**触发护栏判据**(现认 `docs/planning/`/docs README 契约,改键到工具配置——R3-5);**收口靶点硬编码**(「worklogs README 登记一行」「回写 todo.md」随索引档位变,按档参数化——R3-3)。文案保持中文(L5 中文首发,v0.1「译英」已反转) |
| `install-skills.mjs` | 分发/校验逻辑 | Scrollery 专属默认路径 |
| `worklogs/README.md` | 收口契约说明 | 已归档任务登记(Scrollery 具体任务) |

### §8.2 什么算「可复用结构」

保留的是**形状与规则**(几类目录、frontmatter 有哪些字段、收口要处置候选、索引要新鲜);清掉的是**Scrollery 的具体内容**(哪些线、哪些文档、哪些机制)。判据:一句话能否脱离 Scrollery 独立成立——能则留,不能则清。

### §8.3 反向 dogfood 回流(🔵 OPEN)

提取稳定后,Scrollery 自身是否切换到「依赖这个开源工具」而非维护本地副本?利:单一真源、零漂移;弊:Scrollery 引入外部依赖、需跟随其版本。倾向**先各自独立、待开源版成熟后再评估回流**,避免早期耦合拖累两边。

## §9 CI 设计

GitHub Actions;`init` 产出的 `ci-github.yml` **按 profile 生成**(R2-M4),不是一份通吃:

- **门禁步**(逐条 bare 跑,分别检查退出码——不用 shell `;` 链吞码):`check-docs`;生成式档加 `index check`(生成成功 + 源结构一致性)。**不含 `install-skills --check`**——个人 skill 安装一致性属本机 `doctor` 检查,CI 的 fixture HOME 里没有个人副本(R2-M4;修正 v0.2 唯一事实不符项——Scrollery 现役 CI 本就只跑 `--selftest`,v0.2 的「同构」声称与被抽取系统自身设计意图相悖)。governance-only profile 不含任何 skill 步。
- **selftest 步**:`check-docs --selftest`、`build-index --selftest`、`install-skills --selftest`(正负 fixture,已有范式)。
- **发布步(生成式档,P2+;R3-2)**:`index build` 产物上传 **Actions artifact**(最低承诺);docs site 发布 opt-in。
- **e2e 步**:在临时目录 `init` 一个假仓 → 跑全部门应绿(见 §10)。
- **主干兜底与要件(R2-C6)**:`main` push 触发同一套门 = **事后探测 + 恢复兜底**,不是拦截;多人档要件 = required status checks + up-to-date-before-merge 或 merge queue——CI 模板对 `merge_group` 事件注册同一套门,在**拟合并结果**上跑。
- **跨平台**:至少 ubuntu + windows(Scrollery 已验证 Windows 行尾/路径坑,门禁须行尾归一)。
- **Node 矩阵**:LTS 双版本。

## §10 测试策略

- **单元**:每道门自带 `--selftest`,临时目录构造微型仓断言正负判定(Scrollery 已有 15 例 closeout + 7 例 index 范式,迁移并扩多人/结构化 disposition 用例;closeout 正负例由 §7.1 最小样例对生成)。
- **e2e**:`init` 一个 temp 仓 → 断言目录/文件齐备、frontmatter 合法、全部门 exit 0;再故意制造违规(漏 closeout / 断链 / 结构不变量违规:line 引用缺失、双权威、supersedes 不成对)→ 断言对应门变红。
- **i18n 测试**:MVP 仅 zh catalog——报错文案走 catalog 查表,缺 key 不崩(fallback 链即 zh 自身;R2-m2);`--lang` 机制保留,英文 catalog 上线后再扩 fallback 链测试。
- **生成器幂等**:连跑两次 `index build` 产物字节一致(防不稳定排序)。
- **fixture 自扫描免疫**:门禁 selftest 的 fixture 路径须变量拼接,勿把 `docs` 前缀与 `.md` 后缀连成字面量被自己的引用扫描误伤(Scrollery experience §12 教训)。

## §11 治理与社区(成熟产品)

- **License**:MIT(L2);根 `LICENSE` + 每源文件可选 SPDX 头。**来源与再许可声明(R3-10)**:引擎逻辑抽取自 Scrollery 公开镜像(Apache-2.0,单一版权人)——版权人有权以 MIT 重新发布,但 CONTRIBUTING/NOTICE 须留一句来源与再许可说明,免外部贡献者生疑。
- **CONTRIBUTING**:一键装、如何跑门、如何加 selftest、schema 变更流程(走 CODEOWNERS 审)。
- **CODE_OF_CONDUCT**:Contributor Covenant。
- **CODEOWNERS**:治理 schema 文件 + 门禁脚本 + i18n catalog 指定 owner。
- **版本策略**:semver;`CHANGELOG.md`(Keep a Changelog);break schema/CLI 契约走 major。
- **issue/PR 模板**:bug/feature/schema-change 三类。
- **文档站**(后置,🔵):README 起步足够;成熟后考虑 docs site(如 VitePress)。
- **安全**:`SECURITY.md`;门禁读外部/不可信内容(网页/API)当数据不当指令的纪律写入 skill(已有)。

## §12 路线图(MVP-first:最小验证优先)

**核心原则(L8)**:先交付能自证价值的最小闭环(纯中文、仅不变量门档、无生成器、**无多语 catalog**、无 template repo——i18n **基础设施**(字符串外置+catalog 查表)恰是 P1 交付物,缺的只是翻译内容;R2-m3),用它 dogfood 本工具自己的 docs 拿真实体感;生成式、多语、template repo 全部降为「验证后增量」,据采纳信号再上——避免零验证下冻结一堆契约(自审 C-2 的核心修正)。

| 阶段 | 交付 | 完成判据(evidence) |
|---|---|---|
| **P0 建仓(私有)** | 新仓 + 目录骨架 + MIT + package.json + 空 CI | 仓可 clone;`npm i` 通过。(本地私有仓 + docs 骨架已建 2026-07-13,R3-12;余项随 P1 待批开工) |
| **P1 MVP(纯中文最小闭环)** | check-docs + install-skills 通用化(walk 守卫 / 1b 探测+可配排除 / 枚举外置)、closeout 契约(§7.1 schema,disposition 语义**内置**等价 Scrollery 现役,可配后置 P2)、skill、`init`、**索引仅不变量门档**、**trio 模板 + 手册化收口 runbook**(R2-M2/R3-4)、字符串外置(zh catalog)、**单 profile `strict`**(R3-11,梯度实现出 P1) | 三门 `--selftest` 全绿;`init` + dogfood 本仓 exit 0;文案走 catalog;无 skill 也能按模板+runbook 走通 start→closeout |
| **P1.5 dogfood 验证** | 用 MVP 治理本仓 docs 一段时间,收集摩擦/缺口 | go/no-go 三指标(R2-M5 收窄):合并冲突数 / closeout 完成率 / 绕过次数,加一份「用起来哪疼」反馈,据此定 P2 取舍;多人证据**不在此阶段强求**(单人单仓结构性产不出,移 P3 试点) |
| **P2 生成式索引 + 契约收敛(opt-in 增量)** | `index build` + `check`(§7.2)、status 分片、`lines/<slug>.md` 线实体 + 引用检查(Q7)、权威/生命周期不变量(R2-C3)、结构化 disposition 可配 + `schemaVersion`(R2-C5)、发布管线(Actions artifact,R3-2)、**档间迁移命令**(R3-3)、profile 全家(R2-M1) | 生成器幂等;结构负例(双权威/引用缺失)selftest 可拦;artifact 发布可复现;升档迁移 e2e 过 |
| **P3 多人软肋** | **同任务 team 写模型(events 档,§4.3 已裁①)**、owner/collaborators、任务名唯一、CODEOWNERS、merge-queue 要件化 CI(`merge_group`)、按 profile 生成 CI(R2-M4)、**多人试点**(存量仓 pilot + 两人并发 PR + 无 skill 全流程,R2-M5 移入) | 软肋逐条有测试或机制落地;试点跑通并出三指标数据 |
| **P4 分发** | `bin` CLI(init / index build / check / **doctor / upgrade**,R2-M3)、migration registry、`init --skill-only`、e2e、template repo 生成 | `npx . init` 在 temp 仓一步起且全门绿;`upgrade --dry-run` 在旧 schemaVersion fixture 上出正确迁移计划 |
| **P5 国际化 + 转公开** | 英文 catalog、README/CONTRIBUTING/CHANGELOG 英文门面、去私料终检、可选 plugin 封装 | no-leak 复核清单全过;`--lang en` 文案齐;手动转公开(需批准) |

> 每阶段落新仓后,按 `/planning` skill 建三件套跟踪(dogfood);跨阶段收口走 closeout 门。push / 转公开 / npm publish 均须显式批准。

## §13 待决问题清单(Review 逐项收敛)

状态:✅=已裁 / 🔵=待裁。

| # | 问题 | 状态 / 结论 |
|---|---|---|
| Q1 | 仓名 | 🔵 暂定 **`worklog-kit`**(M-12);本地私有仓已按工作名建于 2026-07-13(R3-12),正式名定案后 rename |
| Q2 | 生成式索引「人工判断列」承载 | ✅ 下沉进各单元 frontmatter(§4.1) |
| Q3 | 分发形态 | 🔵 倾向 npm 包 + template repo 兼有(§6);安装拓扑已裁 thin runner(R2-M3) |
| Q4 | 可配边界 | ✅ 开发期不冻结、保持可配(M-6)但元模型稳定(R2-C5);✅ 配置格式已裁 **JSONC + JSON Schema**(§7.4) |
| Q5 | Scrollery 是否回流 dogfood | 🔵 先独立,成熟后评估(§8.3) |
| Q6 | 语言 | ✅ 中文首发 MVP + i18n-ready,英文后置(C-2/M-8);机器面锁 ASCII(R3-1,§5) |
| Q7 | 工作线承载 | ✅ **`lines/<slug>.md` 线实体**(2026-07-13,R2-C2):撞名 = add/add 冲突合并时原生暴露;文档级唯一性由 `id` 承担(§4.1 item2) |
| Q8 | 生成物「忘重生成」摩擦 | ✅ 问题随 C-3 gitignore **整体消解**(产物不入库,无「重生成」义务,无 hook;§4.1 item4) |
| Q9 | Claude Code plugin 薄封装 | 🔵 后置(P5 可选) |
| **C-3** | 生成式索引产物 commit vs gitignore | ✅ **gitignore + 按需构建**(2026-07-13;§4.1 item4 / §7.2):真零冲突,可见性靠本地预览 / CI artifact(R3-2) |
| Q10 | 索引三档梯度默认档 | ✅ MVP 仅不变量门档;append-only / 生成式 opt-in(M-4,§4.1);档间切换 = 显式迁移(R3-3) |
| Q11 | 多人同任务写模型 | ✅ **①混合已裁**(2026-07-13,R2-C7):单人档常规三件套 / team 模式 `progress/events/` 每事件一文件 + owner 唯一收口(§4.3) |

## §14 附录

### 术语表(glossary)

| 术语 | 含义 |
|---|---|
| working memory(工作记忆) | 长任务易失过程记录(三件套) |
| trio(三件套) | task_plan + findings + progress |
| closeout(收口) | 任务收官时把耐久候选蒸馏进永久库的仪式 |
| distillation seam(蒸馏缝) | 易失层→永久层的提升接口,由 closeout 契约门守 |
| disposition(处置) | 候选的去向枚举 |
| work-line(工作线) | 一条持续的工作主题(slug 标识) |
| drift gate(漂移门) | **已退役概念**(R2-C1):产物入库方案下「生成物≠源即红」的门;C-3 裁 gitignore 后本工具无此物,由「`index build` 生成成功 + `check` 源结构一致性」替代;词条留档仅为溯源 |
| 机器面 / 展示面 | 机器可判定契约(字段名/枚举值/文件名约定,锁 ASCII)⟂ 人可读文案(走 catalog 本地化)(R3-1) |
| events 模式 | team 模式下 progress 的每事件一文件承载(`progress/events/<时间戳>-<作者>-<seq>.md`;R2-C7 裁①) |
| profile | 门禁默认值的显式档位(strict / greenfield / brownfield / advisory;R2-M1) |
| dogfood | 项目用自身工具治理自身文档 |

### 仓名候选(Q1:暂定 `worklog-kit`)

暂定 **`worklog-kit`**(M-12,待查 npm/GitHub 可用性)。其余抛砖:`closeout`、`docgov`、`papertrail`(疑已占)、`memseam`、`scribe`、`loregate`。

### 参考

以下均为 **Scrollery 私仓内的出处标注**(非活链接——本文已迁出该仓且面向自包含,关键契约已在 §7.1 正文化;R2-C4):

- 融合评估方案 `docs/designs/2026-07-12-工作记忆与文档体系融合评估方案.md`——本系统在 Scrollery 内的三轮 Review 与治理裁决,本文的直接前身。
- 现役引擎:`tools/check_docs.mjs`、`tools/check_docs_index.mjs`、`scripts/install-skills.mjs`、`.claude/skills/planning/SKILL.md`(抽取基线 = Scrollery@44c067a)。

## §15 自审第一轮 · 发现与裁决(2026-07-13)

作者摘下作者帽、以唱反调 reviewer 身份对 v0.1 做的一轮 self-review,共 12 项;用户逐条裁决,已按裁决改写正文(**本表留痕,正文即现行**)。严重度:🔴 动摇方向 / 🟠 重伤 / 🟡 记账。处置:accept=采纳已回写 / modified=改形态采纳 / pending=待补信息。

| ID | 严重 | 发现(一句话) | 用户裁决 | 处置 | 回写位置 |
|---|---|---|---|---|---|
| C-1 | 🔴 | 纪律型工具可能被团队当官僚主义拒收,缺采纳梯度 | 采纳提议 | accept | L9 / §7.5 |
| C-2 | 🔴 | 过度施工无 MVP,且违背自家「不冻结契约」哲学 | 先做纯中文 MVP | modified(MVP 改纯中文) | L5 / L8 / §5 / §12 |
| C-3 | 🔴 | 生成式「消冲突」说过头:commit 生成物照样冲突 | 看例后裁 (b) gitignore | accept(b) | §4.1 item4 / §7.2 / §13 |
| M-4 | 🟠 | 生成式对小团队是杀鸡用牛刀,应给梯度 | 做成可选项,按团队规模选 | accept | L4 / §4.1 三档表 |
| M-5 | 🟠 | slug「竞态根除」过头,碰撞仍可能 | 采纳 | accept | §4.1 item2 / §4.2 |
| M-6 | 🟠 | 可配置稀释「标准」身份,建议锁死 taxonomy | 先可配,「开发期不冻结」原则 | 反向裁决(不锁死) | §7.4 |
| M-7 | 🟠 | closeout 强加每个 PR 太重,劝退 drive-by | 同意 | accept | §4.3 边界段 |
| M-8 | 🟡 | i18n 早做低 ROI,应先单语 | 先做纯中文 | accept | L5 / §5 |
| M-9 | 🟡 | 1b 源码根自动探测比 v0.1 写的脆 | 采纳 | accept | §7.1 |
| M-10 | 🟡 | 生成器 CJK 稳定排序是脚枪 | 采纳 | accept | §7.2 |
| M-11 | 🟡 | 一方(Anthropic)可能标准化 skill/memory | 采纳 | accept | §6 |
| M-12 | 🟡 | 过渡家累积债,应尽早建仓 + 仓名 | 采纳,暂定 `worklog-kit` | accept | §0 🔵 / §13 Q1 |

**C-3 举例说明(已裁 (b) gitignore + 按需构建,2026-07-13)**。设 `generate-index` 扫 frontmatter 产出人可读 `INDEX.md`。两人两分支:Alice 加 `designs/alice-feature.md`、Bob 加 `designs/bob-feature.md`,各自本地跑生成器,都合并进 main。

| | 方案 (a):commit `INDEX.md` | 方案 (b):gitignore `INDEX.md` |
|---|---|---|
| 合并 | 两人都改了**同一个** `INDEX.md`(常在相邻行)→ **该派生文件文本冲突**,尽管源文件 alice/bob 互不冲突 | 两分支根本不含 `INDEX.md` → **零冲突** |
| 解冲突 | 忽略冲突标记、重跑生成器即可(能解,但**每次两 doc-PR 交叠都要手动来一遍**,新手还可能手改错 → `--check` 变红) | 无需解;索引按需 `npm run docs:index` 或 CI 构建后发布(如 docs site) |
| 可见性 | ✅ 仓内/GitHub 直接可浏览索引 | ❌ 仓内看不到索引,须构建或访问站点 |

**关键**:v0.1「生成式消除冲突」**只在 (b) 成立**;(a) 只是把冲突从手工表**搬家**到派生文件。中间路:(a′) 拆 per-dir 多 index 文件 + 稳定排序以**减少**(a) 的冲突;(b′) gitignore + hook/CI 发布到站点/分支恢复可见性。**默认倾向 (b)**(真正兑现生成式相对不变量门的唯一好处),配「本地预览」命令补可见性;若你更看重「GitHub 直接可浏览」,则取 (a′)。**已裁:选 (b)**(2026-07-13)——已回写 §4.1 item4 / §7.2 / §13;可见性代价用「本地预览命令 + CI 发布」补。

---

> **Review 说明(v0.3 更新)**:本文已是 **v0.3 施工基线**——三轮 review 共 28 项已于 2026-07-13 全数裁决并回写正文(§18)。基线 L1–L9 不回炉;仅剩 §13 🔵(仓名定案/分发形态/回流/plugin)属后续定案项,不阻塞 P0/P1。

## §16 第二轮深审 · 方案闭环审查(2026-07-13)

本轮只审查**方案自身的内部一致性与可实施性**,不探测仓名、npm 包名、GitHub 可用性等边缘事项,也不回炉 L1–L9 已锁基线。结论:**产品方向成立,但核心数据契约、CLI 语义、多人一致性与升级模型尚未闭环,当前版本不宜直接作为施工基线**。以下项目在正文回写并裁决前均记为 `pending`。(✅ 2026-07-13 用户全批裁决——除 R2-C7 裁①混合、R2-m2 纯中文外全按建议采纳,状态列已翻转,回写记录见 §18;本节其余文字为 v0.2 时点快照。)

严重度:🔴=阻塞施工 / 🟠=进入施工前应补齐 / 🟡=随正文清理。

### §16.1 阻塞项

| ID | 严重 | 发现 | 影响 | 建议裁决 / 回写位置 | 状态 |
|---|---|---|---|---|---|
| R2-C1 | 🔴 | **C-3 裁决未贯穿全文,`drift gate` 在 gitignore 生成物方案下已不存在。** §4.1 item4/§7.2 已承认 `--check` 只能验证「生成成功 + 源结构一致」,但 §4.1 代价段、三档表、仓库布局、§10、P2、Q8 与 glossary 仍写「忘记重生成 / 索引不新鲜 / 生成物与源不一致即红」。 | CLI 命名、CI 行为、测试负例和阶段验收互相冲突;实现者会写出两套不同的 `--check`。 | 统一为 `index build`(生成临时视图)+ `check/validate`(校验源与全局不变量);CI 发布时把构建结果作为 artifact/docs site 输入。删除「drift gate」「忘记重生成」「索引不新鲜」及对应 hook;P2 验收改为生成成功、幂等、结构错误可拦、发布产物可复现。回写 §4.1/§6/§7.2/§10/§12/§13/§14。 | ✅ 采纳,已回写(2026-07-13,§18) |
| R2-C2 | 🔴 | **`line slug` 的唯一性定义自相矛盾。** frontmatter 让同一工作线下的 design/review/decision/worklog 共用 `line`,门禁又要扫描单元做「slug 唯一性检查」。 | 若按文档值唯一,一条工作线只能有一个文档;若允许重复,当前模型无法区分正常复用与两人误建同名工作线。Q7 未裁,P2 却已承诺 slug 唯一性。 | 施工前先裁 Q7。建议采用显式工作线实体:`lines/<slug>.md` 是唯一声明,其他文档只引用 `line`;另给每个文档稳定唯一 `id/doc_id`。若坚持 tag-only,则 `line` 只能校验格式、不能校验值唯一,唯一性应落到文档 ID。回写 §4.1/§7.2/§12/§13。 | ✅ 采纳,已回写(2026-07-13,§18) |
| R2-C3 | 🔴 | **权威文档与生命周期没有形成可判定的不变量。** 当前只有 `status` 枚举和 `authoritative: true|false`,未规定多权威冲突、权威作用域、superseded 指向、允许的状态迁移、rename/拆线/并线语义。 | 两个 `authoritative: true` 可同时通过;生成器无法知道哪个是真源;历史引用会因改名或拆线失稳。 | 增加稳定 ID、`supersedes/supersededBy`、authority scope,并规定每个 `(line, authorityScope)` 至多一个有效权威文档;补状态转换表以及删除、改名、拆线、并线规则。回写 §3/§4.1/§7.2,并纳入 validator selftest。 | ✅ 采纳,已回写(2026-07-13,§18) |
| R2-C4 | 🔴 | **核心卖点 closeout 仍依赖 Scrollery 既有范式,未在独立产品方案内形成正式 schema。** 本文声称可机械校验候选全覆盖、不重不漏、target 验存和 `verified=yes`,但未定义候选账、F-NNN/D-NNN、target cardinality 与验证语义。 | 无法仅依据本文实现最关键的「蒸馏缝」;不同实现者会对同一 disposition 写出不同规则。 | 正式定义 candidate 与 closeout schema:候选声明位置和稳定 ID、一对多 target 规则、各 disposition 的 target/reason/evidence 要求、同 PR 新建 target 的判定、`verified` 的证据语义、closeout/归档/永久文档是否须原子完成。回写 §3/§4.3/§7.1/§10,附最小合法与非法样例。 | ✅ 采纳,已回写(2026-07-13,§18) |
| R2-C5 | 🔴 | **裸字符串数组式 `dispositions` 无法承载 closeout 验证语义。** `design`、`code`、`test`、`no-promotion` 对 target 和 evidence 的要求不同,当前配置只知道名称。 | 「保持可配」会退化成 validator 无法理解的任意字符串;可配置性直接破坏核心不变量。 | 配置增加 `schemaVersion`;将 disposition 改成结构化规则,至少描述 `targetKind`、`targetType`、`targetRequired`、`reasonRequired`、`evidenceRequired`。开发期可以增加/修改规则,但底层元模型必须稳定。配置格式建议裁为 JSONC + JSON Schema,不选可执行的 mjs。回写 §7.4/§13 Q4。 | ✅ 采纳,已回写(2026-07-13,§18) |
| R2-C6 | 🔴 | **`main` 合并后 CI 只能发现 semantic merge conflict,不能「拦下」已经进入主干的破态。** | PR-A/PR-B 各自为绿、合并结果为红时,main push 门只能事后告警;第三支柱承诺的主干完整性不成立。 | 多人模式把 required status check、up-to-date-before-merge 和 merge queue/`merge_group` 上的拟合并结果检查列为必要条件;main push 保留为事后探测和恢复兜底,正文不得称其为「拦下」。回写 §4.2/§9/P3。 | ✅ 采纳,已回写(2026-07-13,§18) |
| R2-C7 | 🔴 | **多人同任务仍是共享文件竞态。** `status/<line>.md` 只消除不同线之间的冲突;给共享 `progress.md` 增加作者标题只解决归属,不解决并发追加、closeout 与新候选竞态。 | 当前「multi-person-ready」实际只覆盖多人做不同任务,没有覆盖多人协作同一任务。 | 二选一:① `progress/events/<timestamp>-<author>-<id>.md` 每事件一文件再生成视图;②明确 task owner 是唯一写者,其他人写独立 contribution 文件。另规定仅 owner 发起 closeout,以及 closeout 期间如何冻结/拒绝新候选。回写 §4.1/§4.3/P3。 | ✅ 用户裁**①+单人保常规**的混合(2026-07-13);可行性评估成立,细则四条见 §18;已回写 §4.3/P3/Q11 |

### §16.2 重要项

| ID | 严重 | 发现 | 影响 | 建议裁决 / 回写位置 | 状态 |
|---|---|---|---|---|---|
| R2-M1 | 🟠 | **采纳默认值冲突。** §7.5 先说核心不变量默认 `enforce`,下一条又说新库从 `warn` 起步。 | `init` 无法确定实际配置;测试无法判断违规应返回非零还是只打印警告。 | 定义显式 profile:`greenfield`=核心 enforce/风格 warn;`brownfield`=旧债 baseline、仅新增违规 enforce;`advisory`=全 warn;`strict`=全 enforce。不要再让「新仓/存量仓」与规则级别隐式覆盖。回写 L9/§7.5/§10。 | ✅ 采纳,已回写(2026-07-13,§18) |
| R2-M2 | 🟠 | **「不依赖 AI」的消费路径没有独立完成 start→closeout 生命周期。** 三件套创建阈值、维护与收口主要寄托在 skill;门禁又只对已经存在三件套的任务强制 closeout。 | 无 AI 用户若不手工建三件套,蒸馏缝永远不会触发;两层平级实际退化为 skill 驱动。 | 治理层提供一等 CLI:`task start`、`task status`、`task close`、`task archive`、`doctor`;skill 只指导或调用同一生命周期。回写 §2/§4.3/§6/P1/P4。 | ✅ 采纳,已回写(2026-07-13,§18) |
| R2-M3 | 🟠 | **只有 `init`,没有升级、迁移与存量仓安全落地模型。** 方案未说明引擎是 package 运行还是复制进用户仓,也未定义 `init` 重跑、文件冲突、schema major 与回滚。 | 产品会退化成一次性 template generator;semver 无法转化为用户仓的可升级契约。 | 增加工具版本/schemaVersion manifest、`init --dry-run`、`doctor`、`upgrade --dry-run`、migration registry、原子写入与备份/回滚规则;明确 thin runner vs vendored scripts 的唯一安装拓扑。回写 §6/§11/§12。 | ✅ 采纳,已回写(2026-07-13,§18) |
| R2-M4 | 🟠 | **CI 无条件运行 `install-skills --check` 违背 skill 可选原则。** CI 的临时 HOME 也通常没有个人 skill 副本。 | governance-only 用户会被无关门禁阻断;个人 onboarding 状态被错误提升为仓库一致性。 | 按 profile 生成 CI:governance-only 不检查个人 skill;project-skill 只校验仓内 canonical skill;个人安装由本机 `doctor` 检查,不进入仓库 CI。回写 §4.3/§7.3/§9。 | ✅ 采纳,已回写(2026-07-13,§18) |
| R2-M5 | 🟠 | **P1.5 只 dogfood 本仓,不能验证「多人协作」第三支柱。** | 无法验证存量仓、无 AI 使用、并发 PR、warn→enforce 迁移及贡献者绕过行为。 | P1.5 至少加入一个存量仓 pilot、两人并发 PR + merge queue 场景、一次无 skill 的完整 start→closeout;记录误报率、配置耗时、合并冲突数、closeout 完成率、绕过次数,并设进入 P2 的 go/no-go 阈值。实体模型、owner、并发写模型和 merge-group 门应早于生成式索引落地。回写 §12。 | ✅ 采纳,已回写(2026-07-13,§18) |

### §16.3 随文清理项

| ID | 严重 | 发现 | 建议回写 | 状态 |
|---|---|---|---|---|
| R2-m1 | 🟡 | append-only 单文件不能天然避免冲突;两分支同时向文件尾追加仍可能冲突。 | 将「冲突大减」降级为诚实描述,或改成每条登记一文件。回写 §4.1 三档表。 | ✅ 采纳,已回写(2026-07-13,§18) |
| R2-m2 | 🟡 | MVP 只有中文 catalog,§10 却测试缺 key fallback 到英文。 | P1 fallback 到中文;英文 catalog 上线后再定义 `requested → en → zh` 或其他明确 fallback chain。回写 §5/§10/P5。 | ✅ 用户裁纯中文(2026-07-13),已回写 §5/§10 |
| R2-m3 | 🟡 | §12 写「无 i18n」,P1 又要求字符串外置并走 zh catalog。 | 改成「无多语言 catalog」,区分 i18n 基础设施与翻译内容。 | ✅ 采纳,已回写(2026-07-13,§18) |
| R2-m4 | 🟡 | P2 仍写「C-3 commit/gitignore 定案」,但 C-3 已裁;Q8 仍写 `--check` 兜底忘重生成;文末 Review 说明仍把 C-3 列入待收敛。 | 删除过期动作并与 R2-C1 一次性统一。回写 §12/§13/文末 Review 说明。 | ✅ 采纳,已回写(2026-07-13,§18) |

### §16.4 建议收敛顺序(不扩产品面)

1. 裁定工作线、任务、文档、候选、closeout 的实体关系与稳定 ID。
2. 裁定权威文档与生命周期不变量。
3. 统一 gitignore 生成物下的 `build`/`validate`/CI 语义。
4. 定义 closeout 正式 schema 与结构化 disposition。
5. 裁定多人同任务写入模型及 merge queue 要求。
6. 定义 greenfield/brownfield/profile 默认值。
7. 定义 `init`、`doctor`、`upgrade`、migration 生命周期。
8. 用存量仓、无 AI 流程和并发 pilot 重写 P1.5 验收。

> **第二轮结论**:完成上述阻塞项裁决并回写正文后,本文方可从「方向成立」进入「可施工」;在此之前保持 `status: 施工中`,不据当前 P0–P3 直接开工。

## §17 第三轮深审 · 事实核验 + 对第二轮复核 + 增量发现(2026-07-13)

本轮为独立第三轮:①对方案正文的**事实断言**逐条对照现役实现核验(`tools/check_docs.mjs`、`tools/check_docs_index.mjs`、`scripts/install-skills.mjs`、`.claude/skills/planning/SKILL.md`、`docs/README.md`、`docs/worklogs/README.md`、`.github/workflows/ci.yml`);②对 §16 第二轮 16 项逐项独立复核(第二轮发现于工作树未提交状态、非本轮产物,本轮验后随本次落盘一并入库留痕);③增量发现 R3-*。遵既定范围:不回炉 L1–L9,不探测仓名/npm/GitHub 可用性等边缘事项。(✅ 全部发现已于 2026-07-13 获用户裁决——除 R3-12 改判「建仓迁移」外全按建议采纳,并已按红线回写正文,见 §18;本节其余文字为裁决前时点快照。)

### §17.1 事实核验:方案对现役实现的断言基本全部属实

| 方案断言 | 核验结果 |
|---|---|
| §7.1 `walk()` 无存在性守卫 | ✅ `check_docs.mjs` 的 `walk()` 直接 `readdirSync`,无 `existsSync` 守卫 |
| §7.1 写死 `src-tauri/`、`Cargo.toml`、`copy.bara.sky` | ✅ 1b 扫描清单硬编码三者 |
| §7.1 `plan-docs` 旧前缀兼容为 Scrollery 专属历史 | ✅ 正则 `(?:plan-)?` 兼收旧前缀,注释言明更名史 |
| §7.2/§8.1 `check_docs_index` 中文小节标题写死 | ✅ `/^##\s*目录职责/`、`/^##\s*工作线字母登记表/`、`/^##\s*已归档任务/` 三处硬编码 |
| §4.1 对 Phase 3 拒绝生成式理由的引用 | ✅ `check_docs_index.mjs` 头注原文「不做『从 frontmatter 重生成索引』——字母登记表的 立项/权威文档 列(人工判断)…」,引用忠实 |
| §1/§3 closeout 门行为(全覆盖/枚举/验存/冻结引用/verified=yes) | ✅ 与 `check_docs.mjs` 收口节及 `worklogs/README.md` 契约一致 |
| §7.3 install-skills 三模 / exit 2 / `--force` 备份+原子替换 / 不镜像 `~/.claude` | ✅ 头注与实现一致;「Claude Code 刻意不在目标表内」有注释明证 |

唯一不符:**§9 把 `install-skills --check` 列为 CI 门禁步并称「与本仓自用 CI 同构」**——Scrollery 实际 CI 只跑 `--selftest`(fixture HOME),脚本头注明言「不读写 runner 真实 user-home;真实漂移仅由本地 --check 报告」。此即 R2-M4,证据升级见下表。

### §17.2 对第二轮 16 项逐项复核:全数成立,0 项推翻

| ID | 复核 | 补强 / 调整 |
|---|---|---|
| R2-C1 | ✅ 确认 🔴 | 罅隙实测共 **12 处**:§4.1 item4 内「生成物≠源即红」与紧随其后的 C-3 附注**相邻互斥**;§7.2 首句「`--check` 验漂移」与次条自相矛盾;另及 §4.1 冲突面对照表、诚实代价段、三档表「取决于 C-3」、§6 布局注 drift 门、§9 门禁步命名、§10 负例「索引不新鲜」、P2 交付「C-3 定案」、Q8、glossary drift gate 词条、文末 Review 说明。pre-commit hook 建议在 (b) 下同样失义(产物不入库,hook 无物可拦) |
| R2-C2 | ✅ 确认 🔴 | 补强:若裁 `lines/<slug>.md` 线实体,撞名自动成为 git **add/add 冲突**,合并时即被原生暴露(早于任何事后门)——唯一性需求实质上已把 Q7 推向「线实体文件」一侧 |
| R2-C3 | ✅ 确认 🔴 | 机理补充:中心表「一线一行」的唯一性是**结构免费**的;元数据下沉分布式 frontmatter 后,同一不变量必须显式成门——去中心化的守恒代价,方案只搬了数据、没搬不变量 |
| R2-C4 | ✅ 确认 🔴 | 两点补强:①契约有完整可抽取源(`worklogs/README.md` + skill 模板 + `check_docs` 收口节),但含四个方案正文**未提**的要素——去重证据列、N/A 理由、冻结边界(语义列冻结/仅 docs 侧 target 随目标迁移同 commit 可改)、同 commit 修链义务——「原样保留」若不成文,这些会在移植中静默丢失;②本文迁入新仓后全部 `../../` 相对引用断链、前身「融合评估方案」居私仓,公开受众**永不可达**——自包含是硬需求而非文风偏好。修复成本低(成文即可),但必须发生 |
| R2-C5 | ✅ 确认 🔴 | 代码实证:`check_docs.mjs` 有 `FROZEN_REF` 集(code/test 走冻结引用 grammar)、todo 固定 target `repo:docs/todo.md`、no-promotion 强制 N/A 理由——per-disposition 语义**已在现役代码里分叉**,裸字符串数组连现状都表达不了,遑论可配 |
| R2-C6 | ⚖️ 确认,建议 🔴→🟠 | 架构本身健全(事后探测 + required-check/merge-queue 建议均已在 §4.2 正文);须改的是「拦下」措辞过承诺 + 多人档把 merge-queue 从「建议」升「要件」。P1 不受阻,P3/P4 CI 模板受阻 |
| R2-C7 | ✅ 确认 🔴(对 P3) | 补充:两人并发**追加同一 `progress.md` 文末**在 git 文本层必冲突;事件文件化(每事件一文件)或 owner 单写者须二择一。P1 单人不受阻,但 L6 称 multi-person-ready 是「最大 delta」,founding 文档不解此题则第三支柱名不副实 |
| R2-M1 | ✅ 确认 🟠 | L9/§7.5「新库从 warn 起步」与同节「核心不变量默认 enforce」直接冲突,`init` 无法同时满足 |
| R2-M2 | ⚖️ 确认缺口,收窄修法 | 缺口实在(无 AI 路径无人建三件套 → 蒸馏缝永不触发);但 task CLI 全家桶入 P1 与 L8 冲突。建议 MVP 最小解 = trio 模板入 `templates/`(现清单缺失,见 R3-4)+ 手册化收口流程;`task start/close` 等 CLI 降 P2+ |
| R2-M3 | ✅ 确认 🟠 | §6「`init`/`index`/`check` 皆为子命令」已暗示 thin-runner(引擎驻包)拓扑,但从未言明;stamped 进用户仓的 ci.yml/templates 的版本钉、`init` 重跑语义、schema major 迁移全部缺位 |
| R2-M4 | ✅ 确认 🟠(证据升级) | `install-skills.mjs` 头注:「(CI 用 fixture HOME),不读写 runner 真实 user-home;真实漂移仅由本地 --check 报告」;`ci.yml` 实跑 `--selftest` 而非 `--check`。§9 与被抽取系统的**自身设计意图**相悖,非笔误级 |
| R2-M5 | ✅ 确认 🟠 | P1.5 单人单仓 dogfood 结构性产不出第三支柱证据;建议保留 go/no-go 关卡但指标从简(合并冲突数 / closeout 完成率 / 绕过次数三项足矣) |
| R2-m1 | ✅ 确认 🟡 | 两分支同时向文末追加 = git 相邻行冲突,「冲突大减」仅对低并发成立;或改每登记一文件 |
| R2-m2 | ✅ 确认 🟡 | MVP 唯一 catalog 是 zh,fallback 链应为 zh;`requested → en → …` 待英文上线再定义 |
| R2-m3 | ✅ 确认 🟡 | 「无 i18n」应为「无多语 catalog」;i18n 基础设施(字符串外置)恰是 P1 交付物 |
| R2-m4 | ✅ 确认 🟡 | 与 R2-C1 同源,一次清剿 |

### §17.3 第三轮增量发现(R3-*,均 pending)

| ID | 严重 | 发现 | 影响 / 建议 | 回写位置 |
|---|---|---|---|---|
| R3-1 | 🟠 | **机器面 / 展示面未切分。**§5 只切了「工具 UI ⟂ 使用者内容」一刀,没切第二刀:机器可判定面(frontmatter 字段名、枚举值、文件名约定、closeout 表头)是否锁 ASCII。现状自相矛盾:Scrollery 现役枚举是中文(`status: 施工中`、`type: 工作记忆`),而本方案 §4.1/§7.4 示例是英文(`active`/`design`),L5 又要求「模板骨架中文首发」 | 若中文模板配中文枚举,未来国际化 = schema breaking change(major);若英文枚举配中文展示,须 catalog 映射层。建议:机器面锁 ASCII canonical 值 + 展示走 catalog;或明示「枚举值属用户配置域,工具只做字面比较」并写进兼容性契约 | §5 / §7.4 |
| R3-2 | 🟠 | **(b) 的可见性补偿「CI 构建后发布(如 docs site)」是一块未设计的基建。**发布到哪(Pages / artifact / 分支)、谁维护、是否进 MVP,全部未定;对采纳团队是新增基建负担 | MVP(不变量门档)不受影响;P2 上生成式前须裁:最低承诺 = Actions artifact,docs site 属 opt-in;README 如实陈述「本地预览是默认路径」 | §4.1 / §9 |
| R3-3 | 🟠 | **三档索引不是配置开关,是数据布局迁移**:不变量门档(中心表 + todo 单文件)↔ 生成式档(元数据下沉 frontmatter + status/ 分片)之间切换要迁数据;且 `SKILL.md` 收口步骤写死「worklogs/README 登记一行」「回写 todo.md」——收口靶点随档位变,skill 文本须按档参数化,§8.1 提取清单未含此耦合 | 档间升级须一次性迁移命令(或至少文档化手工步骤);skill 与治理档位解耦(读配置定收口靶点) | §4.1 / §8.1 / §12 |
| R3-4 | 🟡 | `templates/` 清单缺 trio 模板(task_plan/findings/progress 只活在 skill 文内)——governance-only(无 AI)用户无低摩擦起点,加剧 R2-M2 | trio 空模板入 `templates/` | §6 |
| R3-5 | 🟡 | skill 触发护栏须重键:现文案认「存在 `docs/planning/` 或 docs README 登记的契约」,产品应认 `.<tool>rc`/配置的 planning 目录;§8.1 只说清路径示例,漏了护栏判据本身 | 护栏改键到工具配置;补进 §8.1 清单 | §7.3 / §8.1 |
| R3-6 | 🟡 | check-docs 与 generate-index 双引擎各自解析 frontmatter/config,产品级会漂移(Scrollery N=2 尚可容忍) | `src/` 增 shared 解析模块(frontmatter/config 单一实现) | §6 |
| R3-7 | 🟡 | skill-only 入口(L1 路径2)无门禁,closeout 纪律回退为自觉——方案未言明该模式的价值边界 | 文档明示:skill-only = 工作流指导;机械强制须装治理层 | §2 / §6 |
| R3-8 | 🟡 | §7.4 `types` 枚举含 `index`/`rolling-status`,但 C-3 后 INDEX/STATUS 不入库,是否仍属「受治理文档」未清 | 并入 R2-C1 的语义统一一并裁 | §7.4 |
| R3-9 | 🟡 | 烂尾三件套无时效信号:closeout 门只咬**归档目录**,弃置在 planning/ 的任务永不触发任何门;多人仓会积尸 | `doctor`/status 报 stale trio(>N 天无 progress 更新)为 warn 级 | §4.3 / §7 |
| R3-10 | 🟡 | 抽取代码再许可未着一字:源码自 Scrollery(公开镜像 Apache-2.0)抽出改发 MIT——单一版权人合法,但外部贡献者会问 | CONTRIBUTING/NOTICE 留一句来源与再许可声明 | §11 |
| R3-11 | 🟡 | 采纳梯度骨架不必进 P1:梯度语义未定(R2-M1),而 P1.5 dogfood 自用仅需 enforce;置于 P1 是 MVP 膨胀 | 梯度移 P2/P4(对外分发前);P1 单 profile | §12 |
| R3-12 | 🟡 | (宿主仓流程,非本方案正文)本工作线未入 Scrollery `todo.md` / 字母登记表,方案态与两轮 review 状态只存于本文——与「新立项先取号」「todo 唯一滚动现状源」相悖;或沿「文档治理」meta 线不取号的既有惯例,二者择一明示 | 用户裁: 不入 Scrollery 库,而是新建目录(D:\photoapp\ 下,与 D:\photoapp\picasa-next 同级)及仓库(✅ 已执行 2026-07-13:`D:\photoapp\worklog-kit` 建本地私有仓,本文迁入其 `docs/designs/`;Scrollery 侧副本移 `docs/archive/` 留横幅;远程仓/GitHub 未建,属对外动作待批) | Scrollery `docs/todo.md` / `docs/README.md` |

### §17.4 裁决队列(两轮归并,按解锁相位排序)

- **解锁 P0/P1(MVP 最短路径,五项,全部纯成文、低成本)**:①R2-C1(+R2-m4/R3-8)语义统一清剿——build/validate 命名、12 处残留、glossary;②R2-C4 closeout schema 正文化(从 `worklogs/README.md` + skill 模板 + `check_docs` 收口节成文,含去重证据/N-A/冻结边界);③R2-M1 profile 默认值;④R2-M2 最小解 + R3-4(trio 模板入 templates/,CLI 后置);⑤R3-1 机器面 ASCII 契约。附带瘦身:R3-11(梯度出 P1)、R3-5、R2-m2/m3。
- **解锁 P2(生成式档)**:Q7/R2-C2(线实体,复核倾向 `lines/<slug>.md`)、R2-C3(权威/生命周期不变量)、R2-C5(结构化 disposition + schemaVersion;配置格式建议裁 JSONC + JSON Schema)、R3-2(发布管线)、R3-3(档间迁移 + skill 参数化)。
- **解锁 P3/P4(多人/分发)**:R2-C7(同任务写模型)、R2-C6(merge-queue 要件化 + 「拦下」措辞)、R2-M3(升级/迁移模型)、R2-M4(CI 按 profile 生成)、R2-M5(试点重设计)。
- **随文**:R2-m1、R3-6/7/9/10;宿主仓侧 R3-12。

### §17.5 第三轮结论

1. **事实面全过**:方案对被抽取系统的全部关键断言核验属实(唯 §9 install-skills 一处与现役 CI 设计相悖,已由 R2-M4 覆盖);§15 第一轮 12 项的回写抽查一致,唯 C-3 贯穿不足(= R2-C1)。「再验证后重述」纪律合格,文档事实可信度高。
2. **第二轮 16 项全数成立,0 项推翻**(1 项建议降级、2 项收窄修法)。两轮独立收敛于同一判断:**v0.2 = 方向已定、契约未闭的中间态,不宜直接作为施工基线**——维持 §16 结论。
3. 第三轮增量 12 项(3🟠 + 9🟡),集中在:机器面语言契约、C-3 的基建后果、档位迁移成本、skill↔治理档耦合。
4. **与第二轮的关键差别**:P1 的解锁不必等多人模型闭环——按 §17.4,五项低成本裁决即可让 P0/P1 开工,P2+ 契约后置收敛,与 L8 MVP-first 自洽。
5. 建议下一步:用户按 §17.4 队列裁决 → 按「规范文本回写红线」改写正文(含 12 处残留清剿)→ 出 **v0.3 施工基线** → P0/P1 待批开工。(✅ 已发生:2026-07-13 全批裁决 + 回写完成,本文即 v0.3;P0 建仓部分已按 R3-12 执行,见 §18。)

> 本轮独立完成。§16(第二轮)以工作树未提交状态发现于本轮开工时,经 §17.2 逐项复核后随本次落盘一并入库留痕。

## §18 裁决记录与回写台账(2026-07-13,v0.3)

**裁决**(用户,2026-07-13):两轮共 28 项 pending **全批**——除以下三条修正外,全按各项「建议裁决」原样采纳:

1. **R2-C7 裁「①+单人常规」混合**:多人同任务用 `progress/events/<timestamp>-<author>-<id>.md` 事件文件模式,单人保持常规三件套。**可行性评估:成立**——events 化只需要解 `progress` 这一个高频追加热点;`task_plan`/`findings` 低频共享可容忍人工合并;且该混合与三档索引梯度**同构**(按团队规模选形态、单人档零成本、升档=显式迁移),不引入第二种配置哲学。成立系于四条细则(已回写 §4.3):①模式在**任务创建时**声明(`mode: solo|team`),中途切换 = 一次性迁移,不隐式混用;②team 模式候选 ID 带作者命名空间 `F-<author>-NNN`——否则 events 解了 progress 文本冲突,**候选 ID 分配竞态仍在**;③时间线视图由 `index build` 按文件名 UTC 时间戳字典序聚合,同秒撞时靠 `<seq>` 去歧义;④仅 owner 发起 closeout,开启后候选账冻结。
2. **R2-m2 裁纯中文**:MVP fallback 链即 zh(唯一 catalog);已回写 §5/§10。
3. **R3-12 改判「建仓迁移」**:本线不入 Scrollery 治理(不取字母号、不进 todo),新建 `D:\photoapp\worklog-kit`(与 picasa-next 同级)本地私有仓为正式家;本文迁入其 `docs/designs/`,Scrollery 侧副本移 `docs/archive/` 留横幅。远程仓/GitHub 属对外动作仍待批(L3 先私有不变)。

**回写台账**(红线执行:正文重写,非脚注;本表仅索引去向):

| 裁决项 | 回写位置 |
|---|---|
| R2-C1(+R2-m4/R3-8)C-3 语义统一清剿,CLI 定名 `index build` + `check` | 头注、§4.1 item4/对照表/代价段/三档表、§6 布局、§7.2、§7.4 types 注、§9、§10、§12 P2、§13 Q8、§14 glossary、Review 说明(12 处残留全清) |
| R2-C2 / Q7 线实体 `lines/<slug>.md` | §3、§4.1 item1/item2、§7.2、§13 Q7 |
| R2-C3 稳定 id + supersedes + 权威/生命周期不变量 | §4.1 item1、§7.2 |
| R2-C4 closeout schema 正文化 + 自包含 | §7.1(唯一真源)、§1/§14 引用改出处标注、§10 |
| R2-C5 结构化 disposition + schemaVersion + JSONC 定案 | §7.4、§0 待定、§13 Q4 |
| R2-C6 「拦下」措辞修正 + merge-queue 要件化 | §4.2、§9、§12 P3 |
| R2-C7 写模型①混合(用户修正) | §4.3、§12 P3、§13 Q11 |
| R2-M1 显式 profile 模型 | §0 L9、§7.5、§12 P2 |
| R2-M2 最小解(trio 模板+runbook,CLI 后置)+ R3-4 | §4.3、§6 templates、§12 P1 |
| R2-M3 thin-runner 拓扑 + doctor/upgrade/migration | §6、§12 P4 |
| R2-M4 CI 按 profile 生成、撤 `install-skills --check` | §7.3、§9 |
| R2-M5 P1.5 收窄(三指标 go/no-go,多人证据移 P3 试点) | §12 P1.5/P3 |
| R2-m1 append-only 冲突描述诚实化 | §4.1 三档表 |
| R2-m2 纯中文 fallback(用户确认) | §5、§10 |
| R2-m3 「无 i18n」→「无多语 catalog」 | §12 核心原则段 |
| R3-1 机器面 ASCII ⟂ 展示面 catalog | §5、§4.1 item1、§7.4、§13 Q6 |
| R3-2 可见性/发布分层(artifact 最低承诺) | §4.1 item4、§9 |
| R3-3 档间迁移 + skill 收口靶点参数化 | §4.1、§8.1、§12 P2 |
| R3-5 skill 触发护栏重键 | §7.3、§8.1 |
| R3-6 frontmatter/config 共享解析 | §6 |
| R3-7 skill-only 价值边界 | §2 |
| R3-9 stale trio 时效信号 | §4.3、§6(doctor) |
| R3-10 来源与再许可声明 | §11 |
| R3-11 采纳梯度实现出 P1 | §7.5、§12 P1 |
| R3-12 建仓迁移(用户改判) | 头注、§0 Q1、§12 P0、§13 Q1、§17.3 执行注记 |

> 本文自此为 **v0.3 施工基线**。P1 写码、远程仓、发布仍在批准门内,待用户显式批准后开工。
