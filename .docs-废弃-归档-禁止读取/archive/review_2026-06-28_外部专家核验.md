# 第 8 轮 · 外部 AI 专家审查结论核验（2026-06-28）

> 📦 **已归档(2026-07-10 文档治理补标)**:第 8 轮外部专家审查核验快照,使命已完成;订正项已回写 Part0 §10.5 / external_references。

> 对一份外部 AI 专家提交的《refactor_2026 重构 Plan 全面审查结论》（判定「不具备按现有文档直接全面开工条件」，列 10×P0 + 11×P1 + 外部事实核验）做**独立逐条核验**。
> 方法：38-agent workflow 双阶段（P0「核验→对抗复核」、P1/外部「单轮深核」）+ 人工亲核 3 条（workflow 因输出过长失败）。基线 HEAD=`066c8ed`（与专家同一 commit，行号精确对位）。

## 一、总判定

🟡 **专家审查可信、有价值，但「P0 全修完才能开工」的总判定偏重。**

- **证据层几乎全准**：21 条 P0/P1 + 9 条外部事实，引用文件/行号**逐条命中，无一凭空捏造**。
- **主要系统性偏差 = P0 定级虚高**：10 条 P0 中约 **5 条实为 P1/P2**（真实但非全盘阻断，多属渠道 cfg 门控后置 / mac 纸面缺口 / 文档措辞）。
- **真正硬阻断 ≈ 4 条**（P0-2 / P0-3 / P0-5 / P0-7），且各自只卡**对应工作线**，非卡整份 plan——与专家「允许先行的工作」清单（Part1 框架 / CI / 主题 / ScanChannel TS / canonical / SBOM 均可动工）本身自洽，故「不具备开工条件」措辞过强。
- **几处事实/归因瑕疵**：P0-1「可能复活用户删除项」方向写反；E8 冲突归因错（Part8 内部，非 Part0↔external）；E9「NOTICE 已列全」误读为既成事实。

## 二、P0 逐条裁决（10 条）

| # | 论断 | 引用属实 | 裁决 | 定级订正 | 核心依据 |
|---|---|---|---|---|---|
| P0-1 | 删除/缺失共用 `is_deleted` | ✅ | **PARTIAL** | P0 保留 | soft_delete 与计划的 mark_missing 共用 `is_deleted+deleted_at`、无判别字段、同回收站混选；缺独立 `missing` 态。**专家「upsert 可能复活用户删除项」方向写反**——upsert UPDATE 不触碰 is_deleted，唯一清零是 restore_items 显式恢复；真缺陷相反：缺失项重现因共用列**无法安全自动恢复**，被埋回收站 |
| P0-2 | 百万级全选契约悬空 | ✅ | **CONFIRMED** | **真 P0** | Part2 全文 grep `SelectionDescriptor/GalleryFilter/ViewDescriptor`=**零**；Part5:173 却把类型+filter→ids SQL 解析 owner 指派给 Part2（T14.5 仅 `get_view_ids()->Vec<i64>` 只读整包）。冻结红线手势 Ctrl+A 后端落点零落地 |
| P0-3 | AI worker 协议承载不足 | ✅ | **CONFIRMED**（亲核） | **真 P0**（卡 Part4 worker 化） | Part6:155 权威：单 `model_handle`（face YuNet+SFace 双权重不够）；`EmbedBatch{...cache_keys, fingerprint}` 的 `ai_cache_dir` **不在任何协议字段**；`fingerprint` **单值**、回程 `Success{embeddings[]}` 无逐项 fingerprint/error/长度校验/陈旧防护。部分缓解（profile 带 image_file+text_file、数组化 item_ids），但逐项一致性+多句柄+缓存根三处属实欠规约 |
| P0-4 | CLIP 224 预处理错 | ✅ | **PARTIAL** | **降 P2** | **源码是对的**（pipeline.rs:520 `ShortEdge(image_size)` 解码 + clip.rs `preprocess_decoded` CenterCrop = 正确 Resize→CenterCrop）；仅 Part4:269 / Part3:450 **文档措辞**漏写 Resize。非开工阻断 → 文档修订（**已落地**） |
| P0-5 | crate 循环依赖 | ✅ | **CONFIRMED**（亲核） | **真 P0**（卡 pro 拆分） | Part6:215「trait 在开源核心」(=src-tauri) + Part6:262 pro 实现 + Part6:273 src-tauri 经 overlay `--features pro` 链 pro = **cargo crate 依赖环**（商业 build 编译失败）。members 清单无独立 api crate；free-stub「trait re-export」含糊且与「trait 在开源核心」打架。需显式无环 trait 归属（独立 `plugin-api` 叶 crate 或定义在叶 crate） |
| P0-6 | Store Provider 同步 trait 不兼容 | ✅ | **PARTIAL** | **降 P1** | `EntitlementProvider` 确同步（Part6:217-228）、Part8 把异步 WinRT 塞同步 fn 体属实。但 `#[cfg(channel-msstore)]` 门控、compile_error 互斥 → 非 day-0 阻断；v1.0 走直销+Store-EXE 不触 IAP；evaluate 可 `IAsyncOperation.get()` 阻塞实现，仅 RequestPurchaseAsync(购买,须UI线程+owner窗口+elevated禁IAP) 硬阻 |
| P0-7 | cargo feature 无法物理排渠道 | ✅ | **CONFIRMED** | **真 P0** | feature/`#[cfg]` 仅作用 Rust 编译链，管不到前端 `plugin-updater` import / `capabilities/default.json` 的 updater:default / tauri.conf.json 单文件内 updater endpoint；§6 验收仅扫二进制（cargo tree+strings），缺 JS/capability/tauri.conf 残留维度。msstore .msix 残留 updater 被 Store 静态扫描命中 → Policy 10.2.2 上架硬阻断 |
| P0-8 | 商业密钥生命周期未闭合 | ✅ | **PARTIAL** | **降 P1（一簇）** | 四子论断均属实（model_id 句柄粒度欠规约 / `--enc-seed` CLI arg 暴露但限气隙工具 / 轮换后旧token验签过解不开新密文系**文档自陈设计代价** / expires_at=None 永久买断·模型更新1年为业务侧非token字段·CDN下载无entitlement校验）。但密钥主干已设计（DerivedSecret 双门/per-plugin 主种子/轮换重加密），缺边界精化，与第7轮终审「0 新 P0」一致 |
| P0-9 | 执行提示词重引安全洞 | ✅ | **PARTIAL** | **降 P1（已落地）** | 519 行 D4 提示词确仍 `{license_id, subject_hash}`，与正文 §3.3.1（240/250 RK14）的 base_token+PoP+nonce 矛盾。但正文 4 处权威已含修正，读正文即规避，仅照抄提示词才回归 → 回填提示词（**已落地**） |
| P0-10 | mac `.ppx` 交付未闭合 | ✅ | **PARTIAL** | **降 P1** | Part7:290 承认裸 Mach-O 不能直接 staple，产线（步②④/T14/T15/§7提示）仍把 worker 放 `.ppx` 后声称「stapler 覆盖 sidecar」=自相矛盾，且 §6 缺 quarantine/离线 Gatekeeper/更新后重验。但 mac 链本机无法编译、6/7 轮已记 P1 纸面缺口，非当前开工阻断 |

## 三、P1 逐条裁决（11 条）

| # | 论断 | 裁决 | 依据 |
|---|---|---|---|
| P1-1 | V10 定义/验收自相矛盾 | **CONFIRMED**（已落地） | 更严重：`capture_time` 是**不存在的列**（真名 sort_datetime）；验收漏算 face_rejections 表（24+1→24+2）；157 行 `ON DELETE SET NULL` vs 393 行「无级联」 |
| P1-2 | content_hash 首次无基线 | **CONFIRMED** | INSERT 列清单不含 content_hash → 新行恒 NULL；plan 设计了 hash 比较却遗漏基线建立/回填/NULL 兜底三环（待 Part2 设计补充） |
| P1-3 | TIFF 超时漏线程 | **PARTIAL** | 技术真实，但 §3.9.2 L386 **已记录该泄漏 + 硬上限/header magic 预校验缓解**，专家未承认 → 收窄为「硬上限改强制+补 quarantine」 |
| P1-4 | Coordinator 未通用化 | **CONFIRMED**（源码待办） | coordinator.rs:312 硬编码 `CAPABILITY_STR="thumbnail"` 而非 `capability.as_str()`；Part6:167 却称 evaluate_run「不改/已通用」。当前仅 PSD 在用=潜伏缺陷，落地多能力插件即误查 |
| P1-5 | ScanChannel 修复方向反 | **CONFIRMED**（文档已落地/源码待办） | Rust 扁平 internally-tagged；Part5 第一选项 `msg.progress.scanned` 走反，应改 TS 为扁平联合 + 删 scanStore 强制 cast |
| P1-6 | 人脸审批状态机不完整 | **PARTIAL** | ①②③④成立（reassign/unassign 后未规定 persons 质心/封面/数量重算，merge_persons 范本反证此为必需）；**⑤「事务边界不统一」REFUTED**——Part4:219 明写「全部单事务、AppError」 |
| P1-7 | 具名共享内存缺安全设计 | **CONFIRMED**（亲核） | §3.7.3 具名 shm 传**解密明文权重**。plan 有随机 name+基础 shm_unlink 清理，但**缺** Windows DACL / POSIX 0600 / open 后立即 unlink / 崩溃清理 / name 抢占防护。收窄：plan 已自承公开权重弱护城河，实际严重度有界，但私有模型路径硬化必要 |
| P1-8 | Release 私钥策略冲突 | **CONFIRMED** | Part7:432 CI 步骤签 .ppx (Ed25519) vs Part7:448 凭据表「私钥不入任何 CI」直接互斥，无远程签名/digest 调和机制（且交叉引用 §5.1 悬空不存在） |
| P1-9 | 版本单一事实源未闭环 | **CONFIRMED** | tauri.conf.json 维度真闭环（删字段取 CARGO_PKG_VERSION），但 package.json 仍独立值；纯相等断言路径下单改锚点必 CI fail 而非自动同步；无 dry-run/dirty-tree/tag 校验 |
| P1-10 | 商业服务缺生产必需项 | **PARTIAL** | 多数属实（webhook 验签/幂等键/订单状态机/重复通知/投递重试/审计/限流/灾备全缺 + bundle 购买未定义 + LicensePayload.plugin_id 单值）；两处收窄（enc_seed 主种子轮换已覆盖、退款 CRL 部分覆盖，专家精确指向的是「退款 vs 签发竞态」） |
| P1-11 | 全仓禁 picasa 不可执行 | **CONFIRMED**（已落地） | 实测 `grep -i picasa`=**172 命中/48 文件**，含 D10/D14 文档自身+_deprecated 历史+README/SEO 文案+lock 文件 → 门禁恒红。需限定代码标识符+allowlist |

## 四、外部事实裁决（9 条）

| # | 事实 | 裁决 | 要点 |
|---|---|---|---|
| E1 | MS Store 10.2.2 非绝对禁 DRM | **CONFIRMED** | v7.19 原文禁「以不符已声明功能方式动态包含代码」，无「DRM」字样；10.8.1 非游戏 PC 应用可用第三方支付。文档（Part0/7 + external L2）已对齐 |
| E2 | Tauri #14935 状态 | **CONFIRMED** | 双源核实 state=open、无关联 PR、WACK/S-Mode 特定（CreateProcessW/ShellExecuteW Blocked Executables）。文档已对齐 |
| E3 | sqlite-vec #25 状态 | **CONFIRMED** | Open、无分支/PR、ANN 未落地；两阶段 ANN（BruteForce+simsimd → ANN 后置）策略合理 |
| E4 | InsightFace 许可 | **CONFIRMED** | README 逐字：代码 MIT、权重(含 SCRFD/ArcFace)非商用研究。文档（Part4 §1.3/§2.2 + external L5）已对齐隔离要求 |
| E5 | YuNet/SFace 许可 | **CONFIRMED** | YuNet MIT(子目录 LICENSE)、SFace Apache-2.0；默认轨商用合规。文档已对齐 |
| E6 | Chinese-CLIP 权重许可 | **CONFIRMED** | 代码仓 MIT(含 sell)、HF 模型页无 license 字段；商用依据=仓库 MIT 非 HF 卡。文档（Part4 §3.10.3 + external L6）已对齐 |
| E7 | 中国人脸法规名称/日期 | **CONFIRMED**（已落地） | 正名《人脸识别技术应用安全管理办法》、2025-06-01 施行。external L4 已正确，**Part0:465 漏回填旧名** → 已订正 |
| E8 | Steam 定价无绝对成文规则 | **PARTIAL**（已落地） | 核心事实对（无成文「官网不得更便宜」，系 Valve 施压/Wolfire 诉讼中）；但**归因错**：冲突在 Part8 内部（§2.3:115/§4:467 绝对句残留 vs §3.6.2/铁律7 收窄 + external L9），非 Part0↔external（Part0 §7.3 无绝对句）→ 已订正 Part8 两处绝对句 |
| E9 | 依赖审计数字/NOTICE | **PARTIAL**（已落地） | Cargo.lock **669≠文档 653**（653 系 cargo metadata 口径）；package-lock 199 ✅；**NOTICE/SBOM 仓库确不存在**（属待落地配置非既成事实）。许可宽松性实质结论未被推翻 → 已订正 Part0 §10.5 + external L8 |

## 五、本轮已落地的文档速修（13 处）

| 文件 | 修正 | 对应 |
|---|---|---|
| Part0 §10.5 标题 | 653 → Cargo.lock 669 / cargo metadata 653 + NOTICE/SBOM 待生成注 | E9 |
| Part0:465 | 法规名 →《人脸识别技术应用安全管理办法》+ 2025-06-01 | E7 |
| Part1:19 | `capture_time` → `sort_datetime DESC, id DESC` | P1-1 |
| Part1:393 | scan_roots.volume_id「无级联」→「ON DELETE SET NULL」 | P1-1 |
| Part1:401 | 验收 24+1 → 24+2(volumes+face_rejections) | P1-1 |
| Part3:450 | center_crop 措辞 → Resize(短边224)→CenterCrop(224) | P0-4 |
| Part4:269 | 同上 + 注明源码已正确（ShortEdge 解码） | P0-4 |
| Part5:220 | ScanChannel 方向 → TS 改扁平联合、勿用 msg.progress.scanned | P1-5 |
| Part8:115 | Steam 绝对句 → key 平价成文/官网平价非成文待法务 | E8 |
| Part8:467 | 同上（验收清单） | E8 |
| Part8:509 | D10 全仓 grep → 限定代码标识符+allowlist | P1-11 |
| Part8:519 | D4 提示词回填 base_token+PoP+nonce（删旧口径） | P0-9 |
| external L8 | 669 数字 + NOTICE/SBOM 非既成事实 | E9 |

## 六、待办落地状态（✅ 2026-06-28 全部落地）

> ✅ **本节待办已于第 8 轮当天全部完成**，下列内容转为「落地内容索引」：
> - **源码 P1-4 / P1-5**（commit `0b66f72`）：`cargo check --offline` + `npm run typecheck`(vue-tsc) 均通过。
> - **4 真 P0 设计 P0-2/3/5/7**（commit `c4519ba`）：Part2 §3.10+T18 / Part6 §3.2.1a / Part6 §3.9.1a / Part7 §3.6.5。
> - **7 P1 设计 P0-1/P1-2/6/7/8/9/10**（commit `9ace332`）：Part2 §3.2.4·§3.4 / Part4 §3.5.1a·§3.7.3a / Part7 §3.7.3a·§3.2 / Part8 §3.3a。
> - canonical 7 不变量始终通过。
> - 🔵 **仍待实现阶段**：源码原型未建——`plugin-api` crate 实际创建、协议结构落 `message.rs`、`sync-version.mjs`/签发服务/激活服务等脚本与服务实现；mac 真机签名/公证；跨进程 AES 端到端对拍。设计已就位，编码与真机验证留待实施。

### 真 P0 设计补充（各卡对应工作线，✅ 已落地为设计）
1. **P0-2**（卡 Part2 / Part5 全选）：定义不可变 `ViewDescriptor`（视图类型/scope/查询条件/排序/filter/layout 版本），后端 `SelectAll{filter}` 在 SQL 层解析全集；推荐服务端 snapshot token；所有批量命令逐一迁 `SelectionDescriptor`。
2. **P0-3**（卡 Part4 worker 化）：协议改 `model_handles: role→handle`（或单一共享内存容器+明确 offsets）；SessionInit 传受限 `ai_cache_dir` 句柄/根；`EmbedBatch`/响应用对齐**逐项**结构 + 强制 `item_ids.len()==embeddings.len()` 校验 + 每项 fingerprint/error + 拒陈旧结果。
3. **P0-5**（卡 Part6 pro 拆分）：新建独立 `plugin-api` 叶 crate（持 `EntitlementProvider`/`ManifestVerifier`/`LicenseStatus`/`ActivationInfo`/错误+DTO），src-tauri / free-stub / pro **单向**依赖，去环。
4. **P0-7**（卡 Part7 / Store 上架）：channel 专用 tauri config overlay + 前端 channel env/条件编译排除 updater import + channel 专用 capability 清单 + pro crate feature 转发 + 最终 **bundle/JS/配置/符号联合扫描**验收。

### P1 设计/源码待办
- **P0-1**：Part2 加独立 `missing` 状态或 `deletion_reason`，分离 `online↔missing` 与 `active↔trashed↔restored` 状态机。
- **P1-2**：content_hash insert/backfill 建基线，或旧 hash NULL 时保守判 SourceChanged；明确大文件抽样算法+漏检边界。
- **P1-4**（源码）：coordinator.rs:312 `CAPABILITY_STR` → `capability.as_str()`；Part6:167「evaluate_run 不改」论断订正。须 `--offline` 编译验证。
- **P1-5**（源码）：src/types/ipc.ts 改扁平联合 + scanStore.ts 删 `as unknown as` cast。
- **P1-6**：Part4 §3.5.1 补 reassign/unassign 后 persons 质心/封面/数量重算（仿 merge_persons）+ unassign 是否清 is_confirmed + reassign 同 model_name 前置守卫。
- **P1-7**：§3.7.3 具名 shm 补 DACL/0600 + 高熵名 + open 后 unlink + 崩溃清理 + 抢占防护。
- **P1-8**：Part7 明确远程 HSM signing API / 离线签名 ceremony / CI 只交 digest 取回签名。
- **P1-9**：发布脚本从 workspace version 生成 package.json（或 package.json 不承担版本事实）+ dry-run/dirty-tree/tag 校验。
- **P1-10**：Part8 补 webhook 验签 + 幂等键 + 订单状态机 + 退款/签发竞态 + 投递重试 + 审计/限流/灾备 + bundle→token 映射（一单签三 token 或 bundle entitlement）。
- **P0-6 / P0-8 / P0-10**：降 P1，排入 channel-msstore / 密钥治理 / mac 交付 各实施期，本轮不阻断。

## 七、开工建议

- **可立即先行**（专家与本核验一致）：Part1 迁移事务化框架、CI 加 `cargo test` + 覆盖 dev、前端主题/toast/幽灵常量、ScanChannel TS 扁平化、canonical checker 扩规则、生成 NOTICE/SBOM。
- **四条真 P0 须先定稿**（各卡单线，非卡全盘）：P0-2 ViewDescriptor / P0-3 worker 协议 / P0-5 crate 去环 / P0-7 渠道物理排除。
- **底线**：采纳专家全部**实质发现**，但**逐条回调定级**——真 P0 约 4 条而非 10 条。专家把关质量高、证据可信；其总判定「不具备开工条件」应理解为「四条真 P0 + 若干文档一致性须先收口」，而非整份 plan 冻结。

## 八、三原型验证结论（2026-06-28，本机实证）

review 开工标准之一「协议 / crate / channel 三个最小原型通过」**已达成**（scratchpad 实证，本机 offline）：

| 原型 | 验证内容 | 结果 | commit |
|---|---|---|---|
| **P0-5 crate 去环** | cargo 依赖拓扑 | A 版 trait 在叶 crate → OSS + 商业 `cargo check` 均过；B 版 trait 在 host → cargo 报 `cyclic package dependency` | `da33603` |
| **P0-3 worker 协议** | serde wire + host 逐项核对 | 6 单测全过（round-trip / 多句柄寻址 / 长度不符整批判错 / 陈旧 fingerprint 丢弃 / embed_dim 校验 / 逐项 Err 不连坐） | `82159c8` |
| **P0-7 渠道排除** | overlay 合并 + 联合扫描 + tree-shake | 四维实证（node + esbuild）；暴露「联合扫描须基于 minify 后 bundle、扫模块说明符而非函数名」工程细节 | `0899bdb` |

- 四条真 P0 中三条（P0-3/5/7）核心机制已从纸面升级为**机器实证**；**P0-2**（ViewDescriptor）是纯 SQL/类型设计、无需独立原型（实施期随 Part2 落地即验）。
- 原型的增量价值：P0-7 跑出「未 minify bundle 的 `if(false)` 死块函数名残留会令 grep 误命中」——纸面 review 看不出，已回写验收。
- **原型 ≠ 落地**，仍待实现阶段：真实 workspace / `plugin-api` crate 建立、协议入 `message.rs`、tauri overlay/Vite 配置入真实工程、各服务脚本；mac 真机签名公证；跨进程 AES 端到端对拍。

---
〔核验：38-agent workflow（10×P0 双阶段对抗 + 11×P1 + 9×外部事实，130 万 token）+ 人工亲核 P0-3/P0-5/P1-7。3 条 workflow agent 因结构化输出超长失败，已人工补全。三原型本机实证（cargo/serde/node/esbuild）。本文档为第 8 轮留档。〕
