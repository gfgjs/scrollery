# 重构 Plan 第 6 轮独立代码核验 + 外部专家审查交叉对账（2026-06-27）

> 📦 **已归档(2026-07-10 文档治理补标)**:重构 plan 开工前第 6 轮核验快照,使命已完成;CANON 类不变量已固化进 tools/check_plan_canonical.mjs。

> 方法：本轮**不信任记忆与既往 review 对"现状"的任何断言**，一切以打开真实源码/文档核实为准。
> 三条独立证据链交叉对账：① 本机 15 单元对抗审查工作流（9 Part + 6 横切维度，69 agents）；② 逐条核验**外部 AI 专家**的 11 P0 + 12 P1 审查（24 agents，refute-by-default）；③ 5 条外部事实类断言**联网核实官方来源**。
> 本文件同时充当**修订 changelog**：本轮已落地的代码 + plan 修订逐条登记。
> 结论：方案**整体成熟、无方向性错误**，但开工前存在**4 条内核硬阻断**（既往 5 轮 + 外部专家**均漏掉**），及一批真实接缝缺口。本轮已全部修订。

---

## 一、本轮独立确认的 4 条内核问题（既往审查盲区，已全部修订）

| # | 问题 | 证据（亲验） | 修订 |
|---|---|---|---|
| **CANON-01** | 权威元规则**自相矛盾** | Part0:39「§3 与 §8 冲突以 **§8 为准**」↔ Part6:469 + 全 Part 横幅「正文即权威、**不再以 §8 为准**」，两者均自称对齐 §0.3 | ✅ Part0 §0.3 定为「**正文即唯一权威、§8 仅留痕**」，废止「以 §8 为准」；今后 §8 裁决须同步回写正文 |
| **CANON-02** | AES `salt` 位置**跨 Part 撕裂** | Part6:201 §3.7.2「salt 随**密文前缀**存」↔ Part6:450 §8.5 + Part4:248/477「salt 存 `ModelBlob.enc_salt`」；落地即「取 nonce 当 salt → HKDF 派错 key → **付费模型解不开**」 | ✅ Part6 §3.7.2 + Part4 §8.4 回写为 `ModelBlob.enc_salt`（随 Registry 签名），删「密文前缀」旧值；密文仅 `nonce(12B)‖ct‖tag(16B)` |
| **CANON-03** | Part6 §1.1 概述残留旧值 | §1.1「G4 限批 ≤128」+「G6 错误码 `OrtError/GpuOom/ModelNotLoaded`」与 §8.1/§8.6 canonical 冲突 | ✅ §1.1 回写：G4 改 `cache_keys`/blob≈0/无限批；G6 改 `GpuUnavailable/SessionExpired/ModelLoadFailed/EmbedDimMismatch` |
| **SEC-02** | worker 路径**旁路验签**（当前代码真漏洞） | `installer.rs:271` `EXOTIC_PSD_WORKER_PATH` 在 `verify_installed_integrity` 前直返、**无 `debug_assertions` 门控**（release 编入）；`worker.rs:34` 同样 → 任意未签名 exe 当 worker = **信任链击穿** | ✅ **代码已修**：`installer.rs` 旁路加 `#[cfg(debug_assertions)]`（release 整条剔除）；`worker.rs::resolve_psd_worker_path` 改 `#[cfg(test)]`；`cargo check --tests --offline` 通过 |
| **P3-F1** | mac 门控落点严重低估 | `get_gpu_engine`/`windows::Win32` 实测散布 **≥12 文件 66 处**（`ai/engine.rs`~25、`ai/provider.rs`~19、`system_commands.rs`~5…），Part3 T8 仅列 3 处 | ✅ Part3 §3.7.1 + T8 按实测 12 文件重列；验收须遍历全部落点 |

> **元教训**：第 5 轮复审为消歧引入的 Part0:39「以 §8 为准」，本身与全 Part 横幅对立 → **修订动作制造了新矛盾**。CANON-02/03 证明 §8→正文回写**仍未收敛**（推翻记忆「canonical 已收敛」结论）。根因 = 缺「正文↔§8 一致性」自动校验，回写靠人工纪律。

---

## 二、外部 AI 专家审查（11 P0 + 12 P1）逐条核验

> 总评：**证据扎实、事实层多数属实**（仅 P0-2 事实层误读）；**但严重度系统性高估**（把 plan 待建设计当当前缺陷），且**完整漏掉上述 4 条内核**。

### 属实（已修订）

| 专家项 | 裁定 | 处置 |
|---|---|---|
| P0-3 协议 v2 破坏已装 v1 worker | 属实 P1 | ✅ Part6:153 补「已装远程 worker 版本错配防护」ADR：`PackageManifest` 加 `protocol_version`，启动前比对、不符则拒拉起 + `needs_reinstall` |
| P0-5 workspace glob 断裂 | 属实 P1 | ✅ Part6 §3.9.1 members **显式逐一列出**（禁 `crates/*`）；Part7:141 同步 |
| P0-6 pro feature 不可执行 | 属实 P1 | ✅ Part6 §3.9.2 定稿「私有 CI overlay」（非 `[patch]`/非 OSS 声明 optional dep）+ 漂移检测 |
| P0-9 全选无后端契约 | 属实 P1（专家判 P0） | ✅ Part5 §3.1.1 补 `SelectionDescriptor` 契约 + owner（Part2 解析 / 各命令改造） |
| P0-10 mac 签名/updater | 属实 P1（**联网证实**） | ✅ Part7：`--deep` 改 inside-out 逐级签；`latest.json` 须自建（非 `createUpdaterArtifacts` 产物）；裸二进制须封容器再 staple |
| P0-11 权威冲突 | 属实（= CANON-01，专家判 P2 偏低） | ✅ 见 CANON-01 |
| P1-1 HEIC/ContentIdentifier | 属实 P2 | ✅ `content_identifier` 列补入 Part1 V10 DDL |
| P1-4 SourceChanged 自相矛盾 | 属实 P2 | ✅ Part2 §3.4 + 风险表对齐 §3.3.2 hash 确认路径（删「仅 touch」旧值） |
| P1-5 scan_roots 无级联 | 属实 P2 | ✅ Part1:151 `scan_roots.volume_id` 加 `ON DELETE SET NULL` |
| P1-8 前端测试断层 | 属实 P2 | ✅ Part7:247 标注 Part5 须落具体 vitest/交互回归任务 |
| P1-9 MS Store 10.2.2 过度解释 | 属实（**联网证实**） | ✅ external_references L2 口径收窄：10.2.2 行为口径、10.8.1 允许第三方支付 |
| P1-10 Steam 同价红线缺依据 | 属实（**联网证实**） | ✅ external_references L9 新增 + Part8 §3.6.2：key 平价成文、官网平价非成文/诉讼中 |
| P1-11 中国人脸合规缺产品任务 | 属实（**联网证实**） | ✅ Part4 §3.10.4 补「形态×义务」矩阵；法名更正为《人脸识别技术应用安全管理办法》 |
| P1-12 模型许可分层 | 属实（**联网证实**） | ✅ external_references L5/L6：InsightFace 权重非商用、Chinese-CLIP 商用依据=仓库 MIT-LICENSE 非 HF 卡 |

### 证伪 / 夸大 / 待建当缺陷（不进开工 gate）

| 专家项 | 裁定 | 依据（亲验） |
|---|---|---|
| **P0-2 GpuLimiter 生命周期** | **部分属实 P2**（专家事实层误读、但我亦推翻本机工作流的「none」） | 额度=1 物理互斥（Part0:225），但 Part4:232「推理前取/完成释放」与「会话级」名实张力真实——若 SessionInit/VRAM 分配不被令牌门控则双 session 仍可驻留 OOM。✅ 已 Part4 §3.6 + Part6 §1.1 G2 回写「令牌覆盖会话生命周期、SessionInit 前获取」 |
| P0-1 worker 协议未闭环 | 夸大 P2（专家「禁实施」过激） | EmbedBatch/Capability/fingerprint/双模型(model_profile)均已设计；真残留仅文本查询编码 op + 人脸 bbox 响应字段待补 |
| **P0-4 删除检测裸写** | **失实**（专家错） | Part2 §3.2.2 已有🔴完整扫描门闩（`WalkReport.complete`，walker 错误绝不写 is_deleted）+ 三重守门 + TOCTOU 复查。**plan 删除安全实际设计完善**（亦纠正本机早先「门闩缺失」的误判——门闩在 plan 里，只是尚未落码） |
| P0-7 开源边界防绕过 | 部分证伪 P3 | 「公钥=秘密」是简写=「生产公钥不进 OSS 仓」；Part0:429 已承认 fork 只得 free-stub。属措辞清晰度 |
| P0-8 激活缺授权证明 | needs-human | 须结合 Part8 §3.3 全文 + activation-* 方案判 PoP/nonce 是否足；倾向部分属实（挑战机制确未见） |
| P1-2 persons.model_name 反规范化 | 夸大 P2 | 多模型切换今天是 gated 死代码（单轨商业 build）；明确推迟 TODO2/F4 |
| P1-3 百万级性能 | 部分证伪 P2 | 文档核算不全属实；但「与百万无卡顿冲突」被驳（Part1:265 已给百万 simsimd 暴力 50-100ms） |
| P1-6 TIFF 超时 | 夸大 P3 | 有界并发+尺寸上限 §3.9.2 已设计；scope.join 天然背压 |
| P1-7 CSS Grid 虚拟滚动 | 夸大 P3 | Part5:295 已设为待建 G7/P2、预判前端纯 CSS 路线 |

---

## 三、联网核实的 5 条外部事实（官方来源，2026-06-27）

| # | 结论 | 关键官方原文 |
|---|---|---|
| P0-10 Apple/Tauri | **三点全对** | Apple TN2206「`--deep`...for emergency repairs only / not recommended」；Tauri `tauri build` 不产 `latest.json`（仅 tauri-action）；裸 Mach-O 无法 staple（须封 pkg/dmg/app） |
| P1-9 MS Store | **属实** | 10.2.2 v7.19：禁「以违反政策/不符已声明功能方式动态包含代码改变行为」（非禁所有下载执行）；10.8.1 允许非游戏 PC 应用用安全第三方支付 |
| P1-10 Steam | **属实带 nuance** | 成文仅约束 Steam key 平价；更广「官网不得更便宜」= Valve 非成文施压（Wolfire v. Valve PMFN 指控、诉讼中、未定论） |
| P1-11 中国人脸法 | **7 项全确认** | 《人脸识别技术应用安全管理办法》2025-06-01 施行；单独同意/告知/撤回/最短保存/PIA/未成年人/≥10万备案；义务随产品形态触发 |
| P1-12 模型许可 | **确认** | InsightFace：代码 MIT 但「models trained with these data...non-commercial research only」；Chinese-CLIP：仓库 MIT-LICENSE.txt（含 sell），HF 模型卡缺 license 字段 |

---

## 四、本轮修订清单（changelog）

**代码**（src-tauri）：
- `exotic/installer.rs`：`resolve_worker_path` 的 `EXOTIC_PSD_WORKER_PATH` 旁路加 `#[cfg(debug_assertions)]`（SEC-02）。
- `exotic/worker.rs`：`resolve_psd_worker_path` 改 `#[cfg(test)]`（SEC-02）。
- 验证：`cargo check --tests --offline` exit 0。

**plan 文档**：
- Part0：§0.3 权威规则定稿（CANON-01）。
- Part1：V10 DDL 补 `content_identifier`(P1-1) + `face_rejections` 表(DBX-01) + `scan_roots.volume_id ON DELETE SET NULL`(P1-5)。
- Part2：§3.4 + 风险表 SourceChanged 对齐 hash 确认（P1-4）。
- Part3：§3.7.1 + T8 mac 门控落点重列（P3-F1）。
- Part4：§3.6 GpuLimiter 会话生命周期(P0-2)、§8.4 salt 回写(CANON-02)、§8.4 face_rejections 定稿(DBX-01)、§3.10.3 CLIP 许可来源(P1-12)、§3.10.4 人脸合规形态矩阵(P1-11)。
- Part5：§3.1.1 SelectionDescriptor(P0-9)、§3.2.2 set_color_label owner(IPC-2)。
- Part6：§1.1 G4/G6 回写(CANON-03) + G2 会话令牌(P0-2)、§3.2.1 协议版本错配 ADR(P0-3)、§3.7.2 salt(CANON-02)、§3.9.1 members 显式(P0-5)、§3.9.2 pro overlay(P0-6)。
- Part7：P0-5 成员引用、P0-10 mac 签名×3(--deep/latest.json/staple)、P1-8 前端测试、10.2.2 口径。
- Part8：§3.6.2 Steam 平价(P1-10)、人脸法全名 + 形态触发(P1-11)。
- external_references：L2/L4/L5/L6 更新为联网核实结论 + 新增 L9(Steam)。

---

## 五、开工 gate（合并两份审查 + 本轮修订后）

**已修硬阻断**：CANON-01/02/03、SEC-02、P3-F1、P0-2 —— 全部本轮落地。
**已修真接缝**：P0-3/5/6/9、P1-1/4/5/8 —— 全部本轮落地。
**待人工/法务终判**（非代码可裁）：P0-8 激活威胁模型（PoP/nonce 挑战）；L1 改名 FTO；L4 中国法律评估；Steam Distribution Agreement 实约束。
**系统性盲区（记录，未消除）**：① mac 全链纸面无实跑；② 跨进程 AES/HKDF/enc_seed/activation **0 代码 0 测试**，付费模型「解得开」从未对拍；③ canonical 传播无机械校验；④ 百万级性能数字未实测。

> 一句话：plan 方向成熟、本轮已关闭全部代码可核的开工阻断；剩余为法务/平台终判与「实测断层」，须在对应阶段以真实环境验证，不可纸面定论。
