---
id: 2026-07-02-审查修复与收敛计划
status: active
type: plan
line: 审查修复(F)
created: 2026-07-02
---

# 审查修复与收敛计划(R 计划)

> 版本:v1(2026-07-02)
> 输入:[2026-07-02-Fable5全面审查报告](../reviews/2026-07-02-Fable5全面审查报告.md)(证据与论证,本文不复述)+ [todo.md](../todo.md)(2026-07-01 进度快照)
> 定位:**refactor_2026 的卫星文档**,与 PartN 平级引用、不改写 PartN 正文。审查发现横切 Part1–7,若散回各 Part 会加剧「正文回写病」;故集中于此,任务状态以 todo.md「F. 审查修复」节为滚动源。
> 任务编号统一前缀 **R**(R0-x 速赢 / R1-x 结构 / R2-x 债务),避免与各 Part 的 T 编号冲突。

---

## §1 目标与范围

**目标**:把审查报告的 17 项行动清单转化为可执行、有依赖排序、与在途工作(③b/③c、v0.1 切割线)对齐的任务计划;同时把「文档修缮工程」(10 处正文回写)清单化。

**不解决**(明确出界):
- Part4 AI/人脸 worker 化全段、AES 层1(维持 Part0 §11.4.3 后置裁决)。
- T16 方案B 的**实施**(~2 周工程,另有 [T16 评估文档](T16_方案B_bucket分段_可行性与改造范围评估.md));本计划只含「给它一个正式里程碑决策」这一步。
- MediaGrid batch-ops/context-menu 抽取:todo.md D 节已有「有意不抽」的记忆背书决策(coupling-relocation 退化设计 + 无法运行时验证),**本计划尊重该决策、不重启争论**;仅保留不动结构即可做的 R2-3(`as any` 清零)。
- 产品命名拍板(Part0 §10.7,用户决策项,本计划仅提醒其前置死锁属性)。

**与在途工作的关系**:
- 当前在途 = ③c 收尾(oss-gate 修红→绿→重建干净 baseline→分支保护,todo.md B2)。R0 波与之**无文件面冲突,可并行**。
- **R1-1(swap 点)是 ③b 的硬前置**:todo.md B1 已有「激活 IPC 随 KeyringLicenseStore 迁移一并处理,否则开源构建激活路径断裂」一行——审查把它从「注意事项」升格为**结构缺陷**(evaluate 走 pro 生产钥 / activate 走开源占位钥的信任根分裂),且给出了修法。③b 动工前必须先落 R1-1。
- v0.1 切割线(Part0 §11.4)不变;R0/R1 多数项本就是 v0.1 范围内欠账(CI 门控、稳定多选 S4、坐标平移验证)。

---

## §2 现状基线(快照,详证据见审查报告)

- 验证:`cargo test --workspace` 326 全绿、vitest 103 全绿、check/clippy/vue-tsc 干净——**但全部仅本地,CI 零门控**(ci.yml 只有 main 触发的 `cargo check`)。
- 在途:③c Copybara 管线已实跑打通(baseline 已建,gate 尚红待修);③b 未动工;Part5 剩 S4/T4c 与若干「有意 defer」项。
- 四处「宣称即险」:百万级滚动(T16-A 已证结构性失败)、HEIC(WIC 未注册)、S4 半程、<10MB 无排期。
- 新增代码级缺陷:布局缓存版本号锁外递增竞争、缩略图非原子落盘、IPC 读命令普遍阻塞 executor、InstallError 无稳定错误码、破坏性删除无错误反馈。
- 纪律条目已于 2026-07-02 写入 CLAUDE.md(Done 的 CI 口径 / 测点由风险决定 / spawn_blocking 硬化 / 原子落盘 / 正文回写红线 / 快照时效 / todo.md 单源 / 宣称对齐)。

---

## §3 任务分解

### R0 波 — 速赢与止血(每项 ≤ 半天,可与 ③c 并行,建议一次会话内全清)

| # | 任务 | 修法要点 | 归属 | 工时 | 验收 |
|---|---|---|---|---|---|
| R0-1 | **CI test 门控** ✅ **已完成(2026-07-02,`d9c0436`)** | ci.yml:触发扩 `pull_request` + `dev`;加 `cargo test --workspace --locked`;新增前端 job(`npx vue-tsc --noEmit` + `npx vitest run`)。即 Part7 T1–T3 的执行;顺带修私有 CI 连红(Rust job 补 npm ci 材料化 DirectML 资源) | Part7 | 1h | ✅ CI 实跑已实证(`d1430dc` run 28579149102 全绿) |
| R0-2 | **WIC 引擎注册进 EngineArena** ✅ **已完成(2026-07-02,`038051d`)** | [engine/mod.rs:30](../../src-tauri/src/engine/mod.rs#L30) `phase1()` 的 engines vec 加 WicEngine(排 ImageRsEngine 之后,遵循 Part3 §3.2 注册顺序裁决);HEIC/HEIF 格式路由核对;顺带补 windows dep target 限定 + cfg(windows) 门控 | Part3 T5 | 2-4h | ✅ arena 分发顺序锁测试 + env 门控真实解码测试(Nokia HEIF 样张本机实跑通过);GUI 级手测待下次 tauri dev |
| R0-3 | **store_layout 版本号入锁** ✅ **已完成(2026-07-02,`3c2a4d1`)** | [layout/cache.rs:98](../../src-tauri/src/layout/cache.rs#L98) 的 `fetch_add` 挪进 `cache.write()` 临界区(先锁后取号),消除写序倒置窗口 | Part2 | 0.5h | ✅ +8 线程并发回归测试(仅本地) |
| R0-4 | **缩略图原子落盘** ✅ **已完成(2026-07-02,`c9c43de`)** | [generator.rs:356,443,527](../../src-tauri/src/thumbnail/generator.rs#L356) 三处 `fs::write(final)` → 写 `{path}.tmp` + 同卷 `fs::rename`;失败清理 tmp | Part3 | 1h | ✅ write_atomic 助手 + 3 单测(成功无残留/原子顶掉截断旧文件,仅本地) |
| R0-5 | **破坏性操作错误反馈** ✅ **已完成(2026-07-02,`94ab03a`)** | MediaGrid.vue:964,975 `SOFT_DELETE_ITEMS`/`RESTORE_ITEMS` 裸 invoke → `invokeIpc` + catch → `ui.addToast` 失败提示 | Part5 | 1h | ✅ 删除失败不进暂存集防前后端状态分叉;MediaGrid 28 个 lint 报错均存量(归 R1-7/R2-3) |
| R0-6 | (在途,引用不新开)oss-gate 修红至绿 + 重建干净 baseline | 见 todo.md B2 末行,不在本计划重复排期 | Part6 ③c | — | gate 三步(check/test/gitleaks)全绿一次 |

### R1 波 — 结构修复(与 v0.1 成色直接相关;R1-1 为 ③b 硬前置)

| # | 任务 | 修法要点 | 归属 | 工时 | 验收 |
|---|---|---|---|---|---|
| R1-1 | **entitlement 激活族收敛到 swap 点**(③b 前置)✅ **已完成(2026-07-02,`e2723cb`+pro `4997bf1`)** | 按推荐 (a) 落地:trait 增 `activate/deactivate`(fail-closed 默认实现,deactivate 幂等 Ok)+ `ActivationInfo`(Debug 脱敏 enc_seed,不携带 LicensePayload/subject_hash)+ `LicenseError::ActivationUnsupported`;free-stub/KeyringLicenseStore/pro DirectEntitlement 三实现同步;命令层三处直构改走 `state.entitlement_provider()`(统一入口→`default_entitlement_provider` swap 点) | Part6 ③b | 0.5-1 天 | ✅ 337 测试全绿(仅本地)+ pro 独立 test/clippy 绿;**验收修正**:grep `KeyringLicenseStore::new` 唯一命中=swap 点自身(mod.rs `default_entitlement_provider`)——字面零命中须待 ③b 把 KeyringLicenseStore 整体下沉 pro,原「零出现」表述过强 |
| R1-2 | **S4 收尾:批量命令后端迁 SelectionDescriptor** ✅ **已完成(2026-07-02,`718a1e8`)** | 5 命令迁描述符(共享 resolve_then_write,resolve+写全程 spawn_blocking);分块批量写入 db 层(SELECTION_BATCH_CHUNK 防超绑定上限);**wire 契约定形**:变体字段补 rename_all(serde enum 级只改变体名,此前形状错配无消费者未暴露)+GalleryFilter 补 recent_only(修 recent 视图 SelectAll 丢谓词);前端 toBackendDescriptor+useViewDescriptor(与 useJustifiedLayout 装配双向同步注释)。**范围修正**:materializeIds 保留给确需 id 的消费者(移动/复制/加收藏夹/删除暂存撤销/框选基线),原「删 materializeIds」表述过强;语义搜索视图回退物化;删除/撤销走 Explicit(暂存置灰 UX 需 id,SelectAll 深迁移留 T18) | Part5/T18 S4 | 1-2 天 | ✅ 后端解析规模仅见于日志(payload 无关性证据);+4 后端单测(wire 锁/select-all 批量写/排除集/跨块边界)+既有 companion 展开测试;340 测试全绿(仅本地) |
| R1-3 | **IPC 读命令 spawn_blocking 机械迁移** 🌓 **命名清单已完成(2026-07-02,`6ba13ff`)** | §6.3 名单全迁(media_commands 12 命令 + search_media;`read_blocking`/`write_blocking` 通用助手,闭包收 `&Connection` 池连接 Deref 复用既有 q::*;batch_* 已随 R1-2 的 resolve_then_write 落硬化)。**范围修正**:全量 grep 实测违规面 **~120 处 / 18 文件**(thumbnail/doc/ai/face/exotic/file_ops/scan/collection/system/volume/storage/proofread/derive),远大于本行原估计——剩余清扫另开会话按文件分批(助手模式可直接复制),完成后再落「grep ipc/ 零直查」断言脚本 | Part5 §3.4 | 1 天+剩余 1-2 天 | 命名清单部分:✅ clippy 0 + 14 suite 全绿(仅本地);全量断言脚本待剩余清扫完成 |
| R1-4 | **InstallError 补 `code()`** ✅ **已完成(2026-07-02,`1c98833`)** | 全变体稳定码映射(Manifest 委托 PackageError::code 透出 bad_signature 等 manifest 粒度);安装命令泛码改 `e.code()` 透传;**范围注**:实测三处泛码中 rollback_failed/uninstall_failed 本就是步骤级稳定码、可区分,保持不动——真正折叠粒度的只有 install 路径;锁测试落 install.rs(全集钉死) | Part6 | 2h | ✅ open_zip/bad_signature/install_io 可区分;341 测试全绿(仅本地) |
| R1-5 | **布局缓存失效契约显式化** ✅ **已完成·裁决变更(2026-07-02,`ca22c66`)** | 🔴 **原案「删除后 bump version」作废**:与 T18 暂存删除 UX 直接冲突(该 UX 要求前端退出选择前继续按旧布局滚动,bump 令暂存窗口所有取行失效→滚动破图)。改为**契约文档化 + 安全网锁测试**:cache.rs 落「失效契约」正文注(软删/恢复有意不失效;stale 窗口写路径由 SQL 双守卫兜底——SelectAll 解析恒带 is_deleted 谓词、批量写恒带 AND is_deleted=0;扫描插入/恢复刷新由前端既有重算触发器承担) | Part2 | 0.5 天 | ✅ +2 契约锁测试(select_all_excludes_soft_deleted / batch_write_skips_soft_deleted);原「旧 version 取行得 ViewStale」验收随方案作废 |
| R1-6 | **T16 方案B 里程碑决策**(决策任务,非实施) | 🔴 **已裁决(用户 2026-07-02):选 (a)——话术不降级,方案B 正式排期**(todo.md F 节已加实施行,归 Part2)。原备选 (b) 话术降级作废 | Part2 T16 / Part8 | 决策 0.5h | ✅ todo.md F 节已有归属行 |
| R1-7 | **i18n 清扫** ✅ **已完成(2026-07-02,`3d6f2d8`)** | **范围修正:实测 431 处/43 文件**(原估 ~217 处/27 组件为模板口径,加 script/store/router/constants 层近翻倍);locale +297 键(402→699);删 8 处 `t('x') \|\| '中文'` 回退并顺带修「5 键缺失致 UI 渲染键路径」存量 bug;`vue/no-bare-strings-in-template` 上规(allowlist 仅符号/单位/品牌/技术占位/语言自名);新增 localeIntegrity.spec 双断言(键树对齐+字面键存在性)防回潮;有意保留:语言自名/console 日志/未渲染 history label(见 todo.md F 节) | Part5 | 2-3 天 | ✅ 注释剥离残留扫描 16 行全可归因;vue-tsc+vitest 109 全绿(仅本地);**验收修正:「lint 规则上 CI」移交 R2-3**——MediaGrid 24+thumbhash 9 存量 lint 错(R0-5 已记录)清零前 lint job 只能开红门,违反「不许长期软门」 |
| R1-8 | **可访问性底线** ✅ **已完成(2026-07-02,`8215629`)** | 媒体卡 `tabindex`/`role="button"`/按类型 aria-label/aria-pressed + Enter/Space 复用点击语义(修饰键行为免费成立;`.self` 防内嵌按钮冒泡);Confirm+CloseConfirm 双弹框 `role="dialog"`+`aria-modal`+labelledby/describedby + 自研 useFocusTrap(入框/循环/归还,纯逻辑单测);**范围补注**:图标按钮清扫实测 25 文件 107 个(镜像既有 title 键,零新键);CloseConfirmDialog 顺带补 Esc 关闭。范围止于底线,FaceApprovalPanel 等其余模态的焦点陷阱与全面 WCAG 审计留 Part8 | Part5 | 1-2 天 | 代码级验收全绿(vue-tsc/vitest 109/bare-strings 0,仅本地);**键盘全流程手测步骤已列于 todo.md F 节,待下次 GUI 会话执行**(无 jsdom,DOM 接线不可自动化,如实标注) |

### R2 波 — 债务清理与文档修缮(fast-follow,不阻塞 v0.1)

| # | 任务 | 修法要点 | 归属 | 工时 |
|---|---|---|---|---|
| R2-1 | **文档修缮工程** ✅ **已完成（2026-07-02，`dc8da36`）** | 按 §4 清单逐处回写；完成度审查快照补「已过时」头。**范围修正：原「10 处」实测 12 项/8 文件/66 处替换**（回声复述与跨文档同病一并清理；W8 主体前期已回写仅补措辞）；canonical 守卫绿 | 各 Part | 0.5-1 天 |
| R2-2 | **死代码清理** ✅ **已完成（2026-07-02，`fb3bedd`）；裁决=标 dormant** | 前端：`rowCache`+`fetchRows`+`patchRowItem`(mediaStore)、`utils/justifiedLayout.ts`;后端:`VectorStore`/`BruteForceStore` 去留决断(接线给 Part4 或显式标 dormant 并注明启用条件,二选一,不许无主) | Part5/Part1 | 0.5 天 |
| R2-3 | **MediaGrid `as any` 清零（16 处）** ✅ **已完成（2026-07-02，`1d030a0`）** | 判别式收窄落地；**修法简化**：LayoutRow 本是判别式联合且 y/height 为公共字段，模板 as any 直接删即可，原案 `normalRows` computed 访问器不需要；**顺带修隐患**：v-memo 在嵌套 v-for 内从不生效且 deps 缺 item.id 有 vnode 串位风险，删除留注；全仓 lint 归零后 ci.yml 加 ESLint 硬门（含 bare-strings 防回潮）；**不做结构抽取**（尊重 todo.md D「有意不抽」） | Part5 | 0.5 天 |
| R2-4 | **uiStore 启动配置 N+1 合并** ✅ **已完成（2026-07-02，`f7e26f9`）** | 9 个模块初始化 `GET_APP_CONFIG` 并入 `GET_STARTUP_CONFIG` 批；**范围扩注**：后端批扩 4→14 键，App.vue 自身 4 键批与 first_launch 二次往返一并归入（共享 promise）——启动配置 IPC 11 次→1 次；各键解析守卫逐条原样保留 | Part5 | 0.5 天 |
| R2-5 | **高风险纯逻辑补测** ✅ **已完成（2026-07-02，`7695740`）** | **+63 测试**：useVirtualScroll 30（pre-T16 特性化锁定，含 SAFE_MAX 严格边界/映射数学/fetchId 守卫/wheel 补偿）、useRequestQueue 21（去重/批容量/看门狗/released 幂等/簿记归零）、supervisor 12（引 ChildHandle trait seam——Child 1:1 委托，库内 ThumbnailWorker 先例；kill 决策表/shutdown 四分支/Drop 兜底/64KiB 环截断）；生产改动仅 2 处零行为 seam；vitest 160+cargo 302+clippy+fmt 全绿（仅本地） | Part5/Part6 | 1 天 |
| R2-6 | **其余 Medium/Low 扫尾** ✅ **已完成（2026-07-02，`63f5511`）** | 六项全落:分批 UPDATE（**签名改收 &Mutex<Connection>**——竞争在 Rust Mutex 层，批间必须放锁；sync 加不一致谓词后常态零写）；目录树 tagged-CTE 去相关化（有意不做内存建树与维护列）；stats 单扫描 FILTER 合一（口径差异逐字保留）；exotic token 4 处毒锁统一 into_inner；sha256 五处收拢 utils::hash；裸 invoke 23 处清零；+7 characterization 测试；全套验证绿（仅本地） | Part1/2/6 | 1 天 |
| R2-7 | (提醒,非本计划执行)产品名拍板 + identifier 锁定 | 所有上架工作的前置死锁,拖延成本单调上升 | Part0/Part8 | 用户决策 |

**执行顺序建议**:R0 一次会话全清(≈1 天)→ R1-1(解锁 ③b)→ ③b 本体 → R1-2/R1-3(可并行会话)→ R1-4~R1-8 按会话余量 → R2 穿插。每项完成即改 todo.md「F」节 + 中文 commit(遵循分阶段提交)。

---

## §4 文档修缮清单(R2-1 的逐处工单)

> 修法统一:**改写正文**或加顶置横幅「🔴 本段已被 XX 推翻,现行方案见 …」,二选一;不新增 inline 批注层。完成一处勾一处。
>
> ✅ **2026-07-02 全部完成（`dc8da36`）**：W1–W12 逐项回写（8 文件 66 处替换）；W8 主体经第 5-8 轮复审已先期回写，本次仅补具名 shm 措辞指向。**范围修正**：超原「10 处」口径——含各主段回声复述（Part2 尤多）与跨文档同病（Part0 overlay/目录树、Part6 .gitignore 行）。

| # | 文件 · 位置 | 被推翻的正文 | 回写动作 |
|---|---|---|---|
| W1 ✅ | Part1 §3.1 | `migrate_one`+`&mut Connection`+IMMEDIATE 完整代码块 | 横幅指向实际 `unchecked_transaction` 方案([migration.rs:56](../../src-tauri/src/db/migration.rs#L56)) |
| W2 ✅ | Part1 §3.2/§4/§6/§7 | `CURRENT_VERSION=10` 多处 | 全部改 11,并加「版本号以 migration.rs:23 为准」脚注 |
| W3 ✅ | Part1 §3.4/T8 | 「Part1 实质清零」表述 | 补注 VectorStore 为未接线 dormant 抽象,启用归 Part4 |
| W4 ✅ | Part2 §3.6/§2.7 | 坐标平移「修复已落地待验证」 | 横幅:方案A 四轮实测失败已 revert(ba084b5),结构性不可修,现行=方案B 待决策(R1-6) |
| W5 ✅ | Part2 §3.9.1 | 时区 naive Local 转换设计 | 横幅:已证有害(双重偏移)停用 |
| W6 ✅ | Part2 §3.4 | (mtime,size) 双键 + content_hash 三环 | 横幅:T5 仅落 mtime 单键,content_hash 链未实现,残留缺陷如实列出 |
| W7 ✅ | Part3 §3.3.1 | 「绝不能无上限」 | 改写为 T6 实际裁决(有意排除 LRU)+ 遗留:GC 周期化仍未做 |
| W8 ✅ | Part4 §3.1/§3.7 | tensor-over-IPC / AES bytes-in-frame 旧文本 | 横幅指向 cache_keys / 具名 shm 现行方案 |
| W9 ✅ | Part5 §3.11.1 | 「优先前端 CSS grid」 | 横幅:T20 已定后端 uniform-packing 并落地 |
| W10 ✅ | Part6 §2.4/§3.9.1/§3.9.2 | 「无根 workspace/pro 不存在」/members=6/overlay 注入「唯一确定方案」 | 改写:7 members(补 exotic-trust);**正式废止 overlay 注入,定 Copybara INTERNAL 标记剥离为唯一 pro 注入机制**(与 Part6_3c 对齐) |
| W11 ✅ | 2026-06-30-Part1-4完成度审查 | 头条结论「T18 零代码是唯一硬阻塞」 | 文首加「快照已过时」头 + 已被推翻项清单(同日 S0–S3 已交付) |
| W12 ✅ | Part0 §1.3 矛盾#8 | 「TIFF 无超时保护」 | 补注:已修([metadata.rs:19-39](../../src-tauri/src/scanner/metadata.rs#L19)) |

---

## §5 风险与回滚

| 风险 | 应对 |
|---|---|
| R0-1 开门控后现存某测试在 CI 环境红(本地绿) | 先 `continue-on-error: true` 试跑一轮收集环境差,修完转硬门;**不许长期软门** |
| R1-1 trait 变更波及 pro 骨架(gitignored,CI 看不见) | 本地 `cargo build --features pro` 实证后再提交;pro 侧同步改动单独 commit 入私有仓 |
| R1-2 改批量命令签名破坏前端调用 | 开发期不冻结契约(既有约束),前后端同一会话内成对改;vue-tsc 兜底 |
| R1-3 spawn_blocking 迁移引入 `Send` 边界编译错 | 逐命令小步迁移、每步 check;模式严格抄 compute_layout |
| R0-2 WIC 注册后对既有格式产生分发抢占 | 注册顺序守住 ImageRsEngine 优先(Part3 §3.2 裁决);加 arena 分发单测锁行为 |
| W10 废止 overlay 与私有构建流程耦合 | 仅文档层废止;若未来私有 CI 需要 overlay 再重开,决策留痕即可 |

## §6 验收标准(计划级)

1. R0 全清后:私有仓 PR 出现红绿门;HEIC 手测出图;并发 store_layout 单测绿;缩略图 tmp+rename 单测绿。
2. R1-1 后:`grep -r "KeyringLicenseStore::new" src-tauri/src` **唯一命中 = swap 点自身**(`default_entitlement_provider`,③b 下沉 pro 时随 license.rs 整体删除而归零;原「零命中」表述过强,2026-07-02 完成时修正),③b 解除阻塞。✅ 已达成(`e2723cb`)。
3. R1-2 后:全选百万虚拟项批量操作的 IPC payload 与选区大小无关(日志证据)。
4. R2-1 后:按 §4 清单逐项勾销,任何执行会话按 PartN §3 正文施工不再会重建已否决设计。
5. 每项完成同步 todo.md「F」节 + 中文 commit(哈希回填该节)。

## §7 给执行会话的提示词

```
你是 Picasa Next 的资深全栈架构师。先读:
1. docs/refactor_2026/Part0_总纲与产品定稿.md(全局基准)
2. docs/refactor_2026/2026-07-02-审查修复与收敛计划.md(本计划,任务表 §3)
3. docs/2026-07-02-Fable5全面审查报告.md(证据,按需查阅对应节)
4. docs/todo.md「F. 审查修复」节(当前状态)

执行本次指派的 R 任务。铁律:遵守 CLAUDE.md(含 2026-07-02 新增纪律:async 内
rusqlite 一律 spawn_blocking / 派生产物原子落盘 / 测试全绿须注明验证环境 /
正文回写红线);修法要点与验收标准以计划 §3 表为准;完成后同步 todo.md F 节
并中文 commit;凡「完成度/是否存在」用 Grep/Read 实测,不采信文档断言。
```

<!-- 哨兵: R 计划 v1 全文完(§1–§7);任务状态滚动源 = todo.md F 节 -->
