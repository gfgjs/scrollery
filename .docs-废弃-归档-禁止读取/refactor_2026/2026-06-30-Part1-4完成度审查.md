---
id: 2026-06-30-Part1-4完成度审查
status: archived
type: review
line: refactor_2026
created: 2026-06-30
---

# Part 1–4 完成度审查报告(2026-06-30)

> 审查方式:5 路并行 agent 逐条对代码 file:line 取证(非 plan 自述)+ orchestrator 独立验证基线。
> 验证基线(亲自跑过):`cargo test --lib` **265 passed / 0 failed**(3.68s)、schema **V11**(`migration.rs:23` `CURRENT_VERSION=11`)、工作树干净。
> 适用范围限定:Rust 后端 + 前端 TS/Vue 的"离线可 build"范围;iOS/Android 与联网预取(simsimd/ANN crate)不在本次可验证面内。

> 🔴 **快照已过时（2026-07-02 标注）**：本文为 2026-06-30 时点快照，头条结论已被后续工作推翻；规划一律以 `docs/todo.md` 为滚动现状源。**已被推翻/状态已变项**：
> 1. 「T18/T14.5 选择契约零代码、唯一硬阻塞」（§一/§二表/§三/§七）——**同日**拍板 D1–D6 并交付 S0–S3 后端（commit 4b12286→c13d5e3，见 Part2 §4 回写、T18 设计文档施工状态）；8 批量命令迁 SelectionDescriptor 由 R1-2 S4 完成（2026-07-02，`718a1e8`）。
> 2. §四「set_color_label 未实现」——已实现并注册（media_commands.rs:344/361，Part5 T16）。
> 3. §四「AppError::VolumeOffline 未定义」——已定义并接线（error.rs:63，Part5-T13，`6583331`）。
> 4. §四「WicEngine 未注册 arena、HEIC 缺失」——R0-2 已修（`038051d`，2026-07-02，HEIF 样张实跑通过）。
> 5. §五「T16 backlog」——R1-6 用户拍板方案B 正式排期（2026-07-02；实施仍待动工）。
> 6. §六③「CURRENT_VERSION 残留 10 / Part1 §6 复选框未勾」——均已回写(Part1_数据层.md 全部注解 + 复选框据实勾选,2026-07-02)。
> 7. §五「T11 HDR」——**误标已订正**:与 Part3 §4 任务表 T11(ai_thumb 默认值联动)矛盾,全仓无 HDR 设计内容,系早期草稿错误标签(2026-07-02)。
> 仍有效示例：thumbhash 二选一悬置（归 R2 波）、§六 face_cluster.rs 陈旧注释仍在（2026-07-02 实测）。

---

## 一、一句话结论

**真正"Part 5 前必须完成"的只有一件事:T18 / T14.5 选择契约**(`ViewDescriptor` / `SelectionDescriptor` + `get_view_ids` IPC + `AppError::ViewStale`)。它是 Part 2 的产出,当前**零代码、纯设计稿**,卡在 6 个决策点(D1–D6)待拍板。除此之外,Part 1–4 的未完成项要么可安全延后,要么本就属于 Part 5 自身的前端工作。〔🔴 本段已被同日进展推翻，见文首「快照已过时」清单〕

---

## 二、四个 Part 完成度速览

| Part | P0 红线 | 整体 | 未完成项性质 | 是否卡 Part 5 |
|---|---|---|---|---|
| **Part 1 数据层** | ✅ 全绿 | 离线范围**实质清零** | 仅 T9 simsimd / T11 ANN(联网预取阻塞) | ❌ 不卡 |
| **Part 2 扫描画廊** | ✅ T1–T5 全绿带单测 | 主体扎实 | **T18+T14.5 选择契约(零代码)**、T8 配对精度、T9 时区、T16 滚动、T6 真机验证 | ⚠️ **T18/T14.5 强阻塞**→已解除（同日 S0–S3 交付，见文首） |
| **Part 3 缩略图/GPU** | ✅ T1–T4 全绿带单测 | 完成度高 | T5 WIC 注册、T6 缓存 GC、T8 mac 门控、T10 pdf 页数、thumbhash 接线 | ❌ 不硬卡(有 2 处可见体验缺口) |
| **Part 4 AI/人脸** | ✅ T3/T4 已交付带单测 | 核心就绪 | T1/T2 合规、T5 设计取舍、T6/T7 增强、T10–T19 全段(待 Part 6 平台) | ❌ 不卡(人脸审批后端已齐备) |

---

## 三、Part 5 前**必须完成**(硬阻塞,唯一一类)〔🔴 本节整体过时：当日 S0–S3 交付，硬阻塞已解除，见文首清单〕

### 🔴 T18 + T14.5 选择契约 —— Part 2 owner,当前零代码

- **是什么**:`SelectionDescriptor`(`Explicit{ids}` / `SelectAll{view, excluded_ids}`)+ `view_to_sql` / `resolve_selection` / `get_view_ids` IPC + `AppError::ViewStale`。
- **取证**:全仓 grep `ViewDescriptor` / `SelectionDescriptor` / `resolve_selection` / `view_to_sql` / `get_view_ids` **全部 0 命中**;8 个批量命令(thumbnail / system / collection×2 / media×4)仍清一色 `Vec<i64>`,完全未迁移。
- **为何硬阻塞**:Part 5 最核心的 **T4 稳定多选**(交互契约一旦发布即冻结 🔴)+ **Ctrl+A 百万级全选**直接依赖它。没有它,全选只能停在 UI 展示层、批量操作拿不到"视图全集 id"。
- **当前卡点**:T18 设计文档明确"本文不改代码,文末 **D1–D6 决策点需拍板后方可开工**",后端工作量 S0–S3 约 7 天(S4 计入 Part 5 T4)。

> ⚠️ **plan 阅读陷阱(必须点明)**:T18 / T14.5 在 Part 2 的 §4 任务表里挂在 **P3 演进行**,但条目内有红字标注"实为 P0:Part 5 全选/批量硬前置"。**极易被表格优先级误导为可延后,实际是 Part 5 的最高阻塞项。** 两个独立 agent(Part 2 视角 + Part 5 入口视角)交叉印证了这一点。

---

## 四、建议 Part 5 启动时**一并拍板/处理**(非外部阻塞,但需明确决策)

| 项 | 性质 | 建议 |
|---|---|---|
| **thumbhash 接线 / 删除** | Part 3 后端已落库 thumbhash BLOB,但 IPC DTO 未透出字段;前端 `thumbhash.ts` 的 `thumbHashToRGBA` 解码链是**残缺 stub**(产出近似纯色而非真模糊图,9 处 unused 即在此),且全仓零消费。线上占位实际走的是**正确的** `thumbhashToAverageColor`(O(1) 纯色,header DC 取色) | 二选一:① 删掉坏死链(`thumbHashToRGBA`+`getThumbhashBgAsync`+`useThumbnail`,最省);② 换官方 thumbhash 算法正确移植,接进 Part 5 渐进占位。**这本就是 Part 5 体验项** |
| **`set_color_label` 命令** | Part 1 已加 `color_label` 列,但写命令未实现 | Part 5 自身 owner,随 T16 星级颜色标签落 |
| **`AppError::VolumeOffline`** | error.rs 未定义该 code(结构化 `{code,message}` 基建已就绪) | Part 5 离线 UX 自己补 |
| **存量 ~72 命令 String→AppError 化** | Part 5 T9 自己分批做 | 非前置 |
| **T5 HEIC 出图(可选前移)** | Part 3 WicEngine 未注册 arena,非 GPU 策略下 HEIC/HEIF 缩略图可能缺失 → 现代 iPhone 图在画廊显示占位 | 评估是否前移,否则有可见体验缺口 |
| **T10 pdf/svg 页数** | 后端留了 `None` 占位,需 Part 5 前端传 pdf.js `numPages` 回填 | Part 5 配合 |

---

## 五、可安全延后(不卡 Part 5)

- **Part 1**:T9 simsimd、T11 ANN —— 联网预取阻塞,`BruteForceStore` 功能正确仅未提速。
- **Part 2**:T8 ContentIdentifier 配对精度、T9 本地时区(暂缓有据)、**T16 bucket 分段滚动**(backlog,方案 A 已兜底,>100 万项顺滑度是结构性遗留)。
- **Part 3**:T6 LRU 容量驱逐(磁盘无限增长隐患,非阻塞)、T8 mac 平台门控(卡 mac 出码不卡前端)、T11 ai_thumb 默认值联动(← Part6 install/Part4,🔴 本行原写「T11 HDR」与 Part3 §4 任务表矛盾,全仓无 HDR 相关设计内容,2026-07-02 订正为真实标签)、T13 metrics。
- **Part 4**:T1/T2(发行合规红线,需 cargo feature + 闭源 crate 决策)、T5(sha256 设计取舍)、T6/T7(模型切换 UI / ANN 性能,非核心)、**T10–T19 worker 化全段**(硬依赖 Part 6 平台框架先行)。

---

## 六、审查中发现的需修正项(均不阻塞)

1. **陈旧注释与 schema 矛盾**:`ai/face_cluster.rs:21` 和 `db/queries.rs:2379` 注释称"persons 表没有 model_name 列",但 `db/schema.rs:577` 已 `ADD COLUMN model_name`(Part 1 加列后注释没同步)。建议清理,避免误导后续 T6 实现。
2. **Part 4 命令命名层**:plan §4 表写 DAO 名(`confirm_face_assignment`),实际 IPC 名是 `confirm_faces` / `reassign_faces` / `unassign_faces` / `reject_faces` / `create_person` / `list_likely_face_matches`(避 E0255 同名冲突)。前端对接以 IPC 名为准。
3. **多处 plan 文档版本号/勾选未回写**:`CURRENT_VERSION` 残留 10(实际 11)、Part 1 §6 验收复选框未勾(对应单测实已通过)。属文档滞后,非功能缺失。

---

## 七、建议下一步

1. **解锁 Part 5 的唯一关键动作**:过一遍 T18 设计文档的 D1–D6 决策点并拍板 → 即可实施 S0–S3 后端契约。〔已完成：当日拍板并交付〕
2. **Part 5 启动同批要定的**:thumbhash 修/删、是否前移 T5 HEIC。
3. 其余延后项维持现状。

---

> 本报告对应的 Part 文档回写见各 Part 文档"本轮审查回写(2026-06-30)"小节。
