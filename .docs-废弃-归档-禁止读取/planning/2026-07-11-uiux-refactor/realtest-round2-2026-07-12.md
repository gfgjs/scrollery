---
id: 2026-07-12-realtest-round2-2026-07-12
title: 第二轮真机验收清单(真机反馈 6 项修复/特性)
status: snapshot
type: acceptance
created: 2026-07-12
acceptance: 已通过(用户 2026-07-12 确认)
snapshot_date: 2026-07-12
scope: 用户首轮真机反馈 #1-#5 + 同类 SettingsView:497 的修复与两特性
commits: ab88960 / 43b3aab / a4d4fda / 5776ae4
---

# 第二轮真机验收清单

> ✅ **2026-07-12 用户确认验收通过** —— 6 项(4 bug 修复 + 2 特性)真机复测全部通过,本清单转历史留档。以下勾选项均已实测确认。

> 用户首轮真机测出 5 项问题(#3 数据丢失级、#4 重大回归)。本轮全部处理:4 项 bug 修复 + 2 项产品特性(用户已裁决方向)。以下逐项列复测点。起真机 `npm run tauri dev`。

## 🔴 A. Bug 修复(4 项 + 1 同类)

### #3 危险区确认(数据丢失级)〔ab88960〕
- [x] 设置 › 高级 › 危险操作,点**清除数据库**:应弹出确认框,**且须在输入框原样键入「清除数据库」**(英文界面 "DELETE")才能点亮「确认」按钮——不输入或输错则确认键禁用。**绝不能像之前那样无弹框直接清库。**
- [x] 点**清除设置 / 清除缩略图 / 清除日志**:各弹确认框,确认按钮为**红色(danger)**;取消则不执行。
- [x] 确认框初始焦点:清库落在输入框;其余落在「取消」。Esc / 点遮罩可取消。

### #1 新建子文件夹「创建」键键盘可达〔43b3aab〕
- [x] 侧栏新建子文件夹,弹框打开后按 **Tab**:应能循环聚焦到「创建」按钮(此前 disabled 导致 Tab 跳过)。
- [x] 文件夹名为空时点「创建」或按 Enter:应弹行级错误「请输入文件夹名称」,而非静默无反应。
- [x] 输入名字后创建正常。

### #2 危险区提示缩进〔43b3aab〕
- [x] 危险操作卡顶部的提示文字应与下方各设置行**左对齐**(此前顶到卡片左边框)。

### SettingsView 高级元数据警告(#3 同类)〔43b3aab〕
- [x] 设置 › 缩略图信息元素,勾选**地理/相机/参数**任一(高级项):应弹确认框(此前原生 confirm 真机失效会静默放行)。取消则该项不勾上。

## 🟠 B. 新特性(用户已裁决方向)

### #4 标题栏 ↔ 工具栏「合并/分离」一键切换〔5776ae4,默认合并〕
- [x] **默认合并**:Gallery 工具栏(筛选/搜索/视图)应在**自绘标题栏同一行**,与窗口三键共享一条 bar(恢复你原设计,不再是独立第二条)。
- [x] 设置 › 界面 › **合并标题栏与工具栏** 开关:关掉 → 工具栏落到标题栏**下方独立第二条 bar**;开回 → 并回标题栏。**切换即时生效,无需刷新**。
- [x] 合并模式下:工具栏空白区可**拖动窗口**;窗口变窄时筛选/视图控件**溢出折叠**正常;不遮挡窗口三键。
- [x] 进入图片查看器(viewer 路由):两种模式下工具栏都不显示,标题栏显示当前项标题(不受开关影响)。

### #5 DOM/Canvas 渲染引擎设置开关〔a4d4fda,实验性〕
- [x] 设置 › 高级 › **开发者工具**:应出现**画廊渲染引擎(实验)** 与 **时间轴渲染引擎(实验)** 两个下拉(DOM / Canvas)。
- [x] 切**画廊**为 Canvas:画廊网格应即时切到 canvas 渲染(无需刷新);切回 DOM 亦即时。
- [x] 切**时间轴**为 Canvas:右侧时间轴切到 canvas 变体。
- [x] 超大图库 / 若在 iOS:选 Canvas 仍应自动回退 DOM(canvasCapable 门控),不崩。

## 已知边界(非本轮待测)
- 合并模式下工具栏在标题栏行的**精确视觉观感**(高度对齐/间距/极窄窗折叠临界)属真机像素级,门禁只保证结构与逻辑——这是本轮最需你反馈观感的点。
- canvas 渲染引擎本身是**原型**(标注「实验」),其渲染质量/性能是既有原型状态,非本轮改动。
- SettingsView / MediaGrid / App.vue 三个大 .vue 文件是既存非-standalone-prettier-clean(仓库历史状态,经 ESLint 门禁,非本轮引入)。
