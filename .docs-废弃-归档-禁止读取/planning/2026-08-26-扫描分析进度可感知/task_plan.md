---
status: 施工中
type: 工作记忆
line: 扫描分析进度可感知
created: 2026-08-26
---

# 任务计划:扫描分析进度可感知

## 目标
梳理添加文件夹、文件扫描、元数据分析流水线，形成底栏展示总大小与已扫描大小等进度信息的可落地实现方案，并明确状态模型、并发边界和验收测试。

## 当前阶段
阶段 13:视频元数据后端回退与失败可见性（实现与本地验证完成，待下一次真实导入验证回退成功率）

## 阶段

### 阶段 1:读取约束并建立调研基线
- [x] 读取项目级 AGENTS.md、docs 文档治理规则及适用技能
- [x] 创建 planning 三件套
- **状态:** completed

### 阶段 2:定位流水线与底栏实现
- [x] 找到添加文件夹、扫描文件、分析元数据的 Rust/Vue 入口
- [x] 找到现有任务状态、事件、进度条及底栏组件
- **状态:** completed

### 阶段 3:核对状态模型与缺口
- [x] 画出真实阶段边界、计数/字节口径和取消/失败语义
- [x] 核对性能、线程、IPC 序列化和响应式更新约束
- **状态:** completed

### 阶段 4:形成实现方案与验收项
- [x] 设计最小进度快照模型和底栏交互
- [x] 明确分批上报、节流、持久化及测试覆盖
- **状态:** completed

### 阶段 5:实现后端进度与跨媒体累计
- [x] 扩展扫描/富化 payload，加入 runId 和文件体积字段
- [x] 快扫批提交后累计逻辑文件体积
- [x] 图片、视频、音频统一使用待解析候选总量和累计体积
- **状态:** completed

### 阶段 6:实现前端底栏反馈
- [x] scanStore 聚合多根目录进度并过滤旧 runId
- [x] 底栏独立显示阶段、体积和文件数
- [x] 补齐中英文文案与 `role=status` 动态状态语义
- **状态:** completed

### 阶段 7:验证与交付
- [x] 前端单测、类型检查、Lint、生产构建
- [x] Rust 编译、扫描器和元数据查询测试
- [x] 更新工作记忆并记录后续批次边界
- **状态:** completed

### 阶段 8:性能复核
- [x] 对当前运行日志拆分扫描、图片、视频、音频阶段耗时
- [x] 对新增元数据工作量统计查询做只读查询计划与耗时测量
- [x] 区分真实解析吞吐变化与进度分母变化造成的观感差异
- **状态:** completed；若要继续优化，另开独立性能批次，避免把进度展示改动与解析器优化混在一起

### 阶段 9:元数据解析热路径评估
- [x] 用当前真实数据库抽样复现头缓冲解析回退条件并按扩展名统计
- [x] 核对 `kamadak-exif` 的 `NotFound`、截断/损坏错误语义
- [x] 按收益与正确性风险排序优化方向，暂不修改生产解析逻辑
- **状态:** completed；首个优化批次应围绕回退分类与容器级有界查找单独施工

### 阶段 10:元数据解析回退优化
- [x] 区分头部已确认无 EXIF、容器不支持和确实需要整文件回退的路径
- [x] 为 JPEG 增加 SOS/EOI marker 判断，为 PNG/WebP 增加 chunk 头 seek 扫描
- [x] 对 BMP/GIF/PSD 已知不支持容器短路，并让 JPEG orientation 快路径复用判断
- [x] 补齐真实库只读 profile 与低频回退路径统计
- [x] 运行格式检查、workspace check、clippy、workspace 测试
- **状态:** completed；剩余工作仅为下一次真实导入的端到端耗时 A/B，不在本批扩大优化面

### 阶段 11:并发与磁盘占用优化
- [x] 复用快扫已知 `file_size`，并让头读阶段保留文件句柄供 PNG/WebP 探测和完整回退复用
- [x] 增加有界头读线程池；解析线程池继续保留一个 CPU 核给前台
- [x] 图像批次从 500 调整为 1000，视频/音频批次保持 500
- [x] 增加头读/解析 worker 与批次配置日志，补充句柄复用回归测试
- [x] 运行格式检查、workspace check、扫描器与元数据测试、scrollery lib clippy
- **状态:** completed；路径顺序与 I/O 并发度仍需同一真实目录做 A/B，未盲目改变首屏优先顺序

### 阶段 12:扫描+解析端到端耗时统计
- [x] 在 `start_scan` 创建贯穿快扫和后台富化的 `scan:pipeline` span，并绑定 `runId`
- [x] 输出快扫、元数据富化、编排开销和整条流水线总耗时
- [x] 覆盖快扫失败、富化取消/失败/崩溃等终态，保证异常也能看到总耗时
- [x] 运行 workspace 测试、格式检查、workspace check 和 clippy
- **状态:** completed；下一次真实导入验证耗时口径与优化前基线

### 阶段 13:视频元数据后端回退与失败可见性
- [x] 批量富化阶段缓存 video worker 能力，MF 运行时探测失败后尝试回退
- [x] 预期的 MF 探测失败不再逐文件写 ERROR
- [x] 输出视频探测的尝试、成功、失败、不支持和回退统计，并按格式拆分
- [x] 补充统计聚合回归测试，运行 scrollery 全库测试和 Clippy
- **状态:** completed；需下一次真实导入确认 TS/MTS 的回退成功数与剩余失败数

## 关键决策
<!-- 需收口提升的决策编 D-001 递增填「候选 ID」列;仅会话内有效的留空 -->
| 决策 | 理由 | 候选 ID |
|------|------|---------|
| MVP 采用单遍扫描；总大小在扫描初期允许未知，完成后补齐 | 保持当前流式扫描的低内存/低额外 IO，准确首屏总量作为后续 inventory 方案 | F-001 |
| 扫描与元数据按阶段分别展示体积进度 | 两阶段候选集合与处理成本不同，合并字节会产生虚假百分比 | F-002 |
| 所有活动进度携带 `runId` 并过滤旧事件 | 停止后立即重扫时，旧富化任务可能晚到并污染新状态 | F-003 |
| 保留现有快扫 Channel，先在原契约上增量扩展 | 降低本批传输迁移风险；刷新恢复快照留到后续统一 app 事件批次 | — |
| 先优化 EXIF 回退分类，再评估缓冲/并发参数 | 真实库 74.2% 图片命中回退分支；候选统计查询仅约 21–26 ms，先改低收益处会掩盖主热点 | F-005 |
| 仅对无法判定的容器保留整文件回退 | profile 将回退从 74.2% 降至 3.7%，而 TIFF/HEIF/RAW 仍保持正确性兜底 | F-005 |
| 不以 SSD 顺序读 5 GB/s 作为元数据阶段的直接目标；先做句柄/已知大小/预取/路径顺序 A/B | 当前图片段已有 19 个 worker，任务形态是小块跨文件 I/O，盲目加线程可能只放大队列和 open/stat 成本 | F-006/F-007 |
| 先落地句柄复用、已知大小和有界头读；路径顺序另做 A/B | 这些改动不改变元数据结果口径；路径重排会影响“按当前视图顺序优先补全”的既有体验 | F-006/F-007 |
| 端到端耗时从 `start_scan` 入口计时到富化终态；阶段时间单独记录 | 快扫完成即返回、富化后台运行，单个 IPC span 无法代表整条流水线；统一口径才能比较优化前后 | F-008 |

## 错误账
| 错误 | 尝试 | 解法 |
|------|------|------|
| Rust 首次 `cargo test -p scrollery scanner:: --lib --locked` 未进入测试 | Tauri build script 在生成权限文件时返回 Windows `Os { code: 5, PermissionDenied }` | 后续重试后构建脚本恢复正常；最终扫描器测试已执行并通过 |
