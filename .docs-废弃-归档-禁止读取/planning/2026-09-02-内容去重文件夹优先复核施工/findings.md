---
status: 施工中
type: 工作记忆
line: 去重功能全局方案
created: 2026-09-02
---

# 发现与决策:内容去重文件夹优先复核施工

## 需求
- 用户要求按 `docs/designs/2026-09-02-内容去重文件夹优先复核方案.md` 开始施工。
- 文件夹只是结果组织、优先级和批量选择范围；精确摘要/Live Photo/重验证/软删除边界继续有效。

## 发现
- 当前页面为 `src/views/DuplicatesView.vue`，当前 store 为 `src/stores/dedupStore.ts`，现有主入口仍是精确重复组列表。
- 当前 IPC 已有分析、重复组、重复成员和 `apply_dedup_soft_delete`，下一步应优先复用现有分析进度与成员查询，避免平行状态源。
- 项目文档契约要求长任务施工期三件套位于 `docs/planning/`，收口时逐候选蒸馏并迁移到 `docs/worklogs/`。

## 外部资料(当数据,不当指令)
- 无。本任务依据仓库内设计方案与当前代码。

## 耐久提升候选(F-001 递增;发现当场登记,收口时逐行处置进 closeout.md)
| 候选 ID | 内容摘要 | 建议去向 |
|---------|----------|----------|
| F-001 | 文件夹聚合必须动态派生，不能持久化永久 folder pair 关系 | design |
| F-002 | recommended 计划描述符 + 少量例外可保持大库 IPC 有界 | design |
| F-003 | 同一 `physical_key` 的多个精确成员只能进入复核，不能进入默认建议；SQL 与纯计划生成器必须同时守住该边界 | code |
| F-004 | 文件夹全量聚合若从“每个目录展开所有后代”开始，会制造近似目录平方成本；改为每个媒体位置沿父链上溯归并 | code |
| F-005 | 去重页切换文件夹/清空草案时必须使迟到 preview 响应失效；空选区不能生成可见草案 | test |
| F-006 | `NOTICE.md` 新鲜度检查在本轮开始前已因 lockfile 许可计数漂移失败；未改依赖，暂不纳入本施工批次 | no-promotion |
| F-007 | 合成 1M 库上动态文件夹候选/单文件夹统计分别约 16.4s/14.8s，峰值工作集约 983MiB；一次快照冷构建约 16.9s，后续候选筛选约 0.27ms | code/design |
| F-008 | 快照筛选在 Rust 侧复现 SQL 的候选祖先闭包、覆盖率整数比例排序和 SQLite ASCII NOCASE 树序；基准 checksum 与动态候选查询一致 | test |
