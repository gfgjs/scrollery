---
status: 施工中
type: 工作记忆
line: 去重功能全局方案
created: 2026-09-02
---

# 任务计划:内容去重文件夹优先复核施工

## 目标
按文件夹优先复核方案，把精确重复证据聚合为可解释的文件夹候选、范围画廊和单目标文件夹清理草案，并以源状态重验证保证整批原子软删除。

## 当前阶段
阶段 1–3:首批实现、性能基准和树交互已完成，等待真机补验后进入收拢

## 阶段

### 阶段 1:基线盘点与查询契约
 - [x] 梳理现有去重表、查询、IPC、状态和测试边界
 - [x] 增加文件夹统计/候选/范围画廊/计划 DTO 与稳定分页契约
 - [x] 用父子目录、覆盖、内部重复、保护/未知项冻结统计口径
 - [x] 建立 100K/1M 聚合基准并记录阈值；动态 SQL 在 1M 超过 14s，改用按视图代次失效的进程内快照
 - **状态:** complete

### 阶段 2:文件夹优先只读工作区
 - [x] 新增文件夹候选树、摘要、范围画廊和比较抽屉投影
 - [x] 隔离去重页选择状态、迟到请求与大数组响应式
 - [x] 接入画廊虚拟化、缩略图、键盘语义和响应式布局
 - [x] 完成候选树懒展开与树超过 50 项的虚拟化
 - **状态:** complete

### 阶段 3:草案与原子软删除
 - [x] 实现纯计划生成器、preview/apply、稳定错误码与事务重验证
 - [x] 接入采用建议、逐项覆盖、确认对话框、成功刷新与回收站入口
 - [ ] 补齐 Live Photo、保护、未知、重叠扫描根和源漂移的端到端/真机验证
 - **状态:** in_progress

### 阶段 4:收拢与验证
- [ ] 删除无消费者的旧首页组状态/IPC，保留比较抽屉数据来源
- [ ] 更新 i18n、权威设计/状态文档与操作说明
 - [x] 运行 Rust 去重聚焦、Vitest、vue-tsc、eslint、生产构建和本地 CI 相关门禁
- **状态:** pending

## 关键决策
<!-- 需收口提升的决策编 D-001 递增填「候选 ID」列;仅会话内有效的留空 -->
| 决策 | 理由 | 候选 ID |
|------|------|---------|
| 先在现有 dedup_index 与当前媒体目录关系上动态派生文件夹聚合，不新增持久化文件夹关系表 | 方案明确要求避免易漂移关系与 folder² 复杂度 | D-001 |
| 清理计划只允许一个目标文件夹，采用 recommended 描述符加少量 include/exclude 覆盖 | 大库下不向前端传全库 ID，同时保持 preview/apply 一致 | D-002 |
| 候选聚合改为每个媒体位置沿可见目录父链上溯，而不是为每个目录递归展开全部后代 | 保持父子范围统计口径，同时避免深目录树生成近似目录平方的中间关系 | D-003 |
| 1M 基准证明动态聚合首请求过慢，加入按分析代次/数据代次/哈希版本/筛选器失效的只读快照缓存；缓存只驻留目录统计，不新增 schema | 保留动态派生的正确性边界，同时让树分页、摘要存在性和清理预检复用同一份统计 | D-004 |

## 错误账
| 错误 | 尝试 | 解法 |
|------|------|------|
