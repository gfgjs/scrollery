---
status: 施工中
type: 工作记忆
line: 画廊大图首次打开慢与闪烁修复
created: 2026-08-19
---

# 进度日志:大图加载后重复闪烁排查

## 会话:2026-08-19

- 做了:建立独立排查记录，冻结上一轮“返回画廊闪烁”已验收结论。
- 验证:确认当前源码工作树除用户既有未跟踪文件和本任务三件套外干净；尚未修改产品代码。
- 遗留:检查查看器加载链、历史修复与当前工作树。

## 会话:2026-08-19（历史修复与加载链对照）

- 做了:核对 `10df855`、`7a51632`、`2940600` 三个已提交查看器修复，并审阅 2026-08-18 备份中未提交的双图层交接实现。
- 验证:当前代码确有“离屏 `new Image` decode → 仅提交 URL → Vue 新建可见 `<img>`”的两次图层绑定；备份实现则让候选始终存在于前后双可见 DOM 层，完成 `load/decode/两帧 rAF` 后再交叉交接。当前单元测试不含真实 WebView2 首帧/GPU 合成断言。
- 遗留:需要按实际现象区分两类路径：普通 sRGB 的 placeholder→完整图二次提交，或非 sRGB 的原图→ICC 派生图异步换源；在确认后才提出最小、独立的实现方案。

## 会话:2026-08-19（sRGB 排除与方案收敛）

- 做了:用户切换为 sRGB 直显复测；额外核对 `onImgLoad`、`useContentViewerZoomRotation` 与查看器 `ResizeObserver` 的首次显示后写入。
- 验证:sRGB 下仍闪，排除 ICC 派生源异步换源；普通图片不会被首次 load 后的缩放逻辑通用改写。剩余共同链是离屏 `Image` 被丢弃、Vue 再创建可见 `<img>` 的图层重新绑定。
- 遗留:等待用户决定是否授权独立提取双层候选交接；不得恢复旧的关闭等待、gallery-ready 或路由状态机。

## 会话:2026-08-19（授权实施）

- 做了:用户已授权实施独立的查看器双层候选交接。
- 范围:仅替换离屏 Image 到新可见 img 的重复绑定链；保持跨条目立即撤旧帧、同条目换源保留当前帧。
- 约束:不引入加载延迟；交接计时只可在候选真实 load/decode/两帧 paint-ready 后作为 transitionend 丢失的释放兜底。

## 会话:2026-08-19（实现与自动验证）

- 做了:以 front/back 两个真实 DOM 图层替换离屏 Image 后再新建可见 img 的重复绑定；同条目换源只在候选 ready 后交接，跨条目仍立即撤旧图。
- 测试:新增 9 项状态机 characterization；本地 npm test 全绿（135 文件、1544 项），npm run typecheck、npm run lint、npm run build 全绿。
- CI 对照:版本同步、路径卫生、exotic 协议同步、主题对比度和生产 bundle 渠道扫描均通过。改名门禁仍因既有 scripts/bench/canvas-realapp-bench.mjs:123 的旧词根失败；本次改动未涉及该文件。
- 遗留:等待用户在真实 Windows WebView2 验收首次打开大图、连续翻图、缩放/旋转与返回画廊的观感；验收前不提交。

## 会话:2026-08-19（真机反证）

- 验证:用户在 sRGB 直显下复测，双层候选实现仍有“已显示后再闪”。
- 结论:此前离屏 Image 到新可见 img 的重复绑定只是可能放大器，不是唯一或充分根因；禁止把该实现误报为已修复。
- 下一步:先说明当前显示链并取得用户对 sRGB 单一原图 img 短路 A/B 的授权；该实验会暂时去掉应用级缩略图加载层和候选 opacity 交接，以定位余下闪烁是否来自应用呈现或 WebView/布局重绘。

## 会话:2026-08-19（sRGB 单图直显 A/B）

- 做了:用户确认后，sRGB 查看器改为单一 img 直接绑定原始 asset URL；查看器候选状态机在该分支停用，缩略图加载层、透明候选层和 decode/rAF 交接均不参与。
- 验证:npm run typecheck、npm run lint、npm run build 与 npm test 全绿（135 文件、1544 项）。
- 遗留:等待用户真机复测；若仍闪，下一轮优先排查查看器尺寸变化、transform 写入和 WebView2 合成，而不再假设图片来源或前端图层切换。

## 会话:2026-08-19（真机通过与最小收敛）

- 验证:用户复测 sRGB 单图原文件直显后反馈“不闪了”。
- 做了:撤回未通过真机的双图层候选实验，仅保留 ContentViewer 的 sRGB 单图分支；非 sRGB 恢复原有预解码状态机。
- 测试:收敛后再次通过 npm run typecheck、npm run lint、npm test（135 文件、1544 项）和 npm run build。
- 遗留:技术目标已达成；代码和三件套尚未提交，等待用户决定保存或收口。

## 会话:2026-08-19（提交后续项）

- 做了:按用户要求提交 sRGB 原图直显修复，提交号为 0f4ed1c；任务不收口。
- 新反馈:重复闪烁已消失，但原图首帧前有极短黑屏。
- 下一步:用稳定缩略图底层补位，避免重新引入会与原图做显隐交接的状态机。

## 会话:2026-08-19（首帧占位修正）

- 新反馈:真机截图显示全屏放大的模糊缩略图长期露在自然尺寸原图周围，确认不是原图内容错误，而是占位层尺寸与原图不一致。
- 做了:缩略图改为仅在 sRGB 原图 `load` 前显示，并按媒体原始宽高居中；原图本身仍是同一个直接 asset URL 的可见 img，load 后只撤下底层占位。完成 key 避免切图时旧 load 事件误撤新图占位。
- 测试:新增 4 项完成 key 回归（含快速切图的迟到 load）；`npm run typecheck`、`npm run lint`、`npm run build` 均通过；`npm test` 通过（136 文件、1548 项）。生产构建只有既有动态导入分包提示，无失败。
- 遗留:等待 Windows WebView2 手动确认小图、超大图、连续翻图的首帧观感，以及重复闪烁未回归；未提交本续项。

## 会话:2026-08-19（补位层首帧冻结）

- 新反馈:占位缩略图在原图 `load` 后被移除时，真机重新出现“图片已显示后再闪”。
- 做了:撤销原图首帧后的占位删除和新增 helper；恢复原图为查看器直接子节点。缩略图按原图尺寸静态置于其下，但只允许在该原图 `load` 前锁定；首帧后迟到的缩略图被忽略，避免新增任何可见层变更。
- 测试:`npm run typecheck`、`npm run lint`、`npm run build` 均通过；`npm test` 通过（135 文件、1544 项）。生产构建只有既有动态导入分包提示，无失败。
- 遗留:等待 Windows WebView2 验收。若仍闪，直接撤销黑屏补位，保留已提交的 0f4ed1c 单图直显；不再以应用级图层变化换取首帧黑底改善。

## 回顾(收口时填)

- 亮点:待填。
- 教训:待填。
- 意外:待填。
