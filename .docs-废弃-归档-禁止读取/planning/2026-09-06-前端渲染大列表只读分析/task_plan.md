---
status: 施工中
type: 工作记忆
line: 前端渲染大列表只读分析
created: 2026-09-06
---

# 任务计划:前端渲染大列表只读分析

## 目标
按 `docs/reviews/2026-09-05-性能流水线梳理与分线优化提示词.md` 的提示词6（前端渲染/大列表/事件消费流水线），在只读约束下对 B1–B6 六个静态可疑瓶颈逐项核实（以文件+函数名重定位、确认代码证据是否仍成立、记录漂移与新发现），产出分析报告落盘到本任务目录 `report.md`。**不改任何代码、不跑构建、不做优化实施**——实施是后续独立会话的事。

## 范围界定
- 本会话 = 只读分析 + 报告落盘。提示词6的「画廊三往返基准量化」「分批优化实施」不在本会话执行（需要运行环境，且属改动性工作）；报告中只盘点既有基准工具并给出实施会话的量化方案。
- 交叉边界：B3 的后端侧（thumbnail_commands.rs regenerate_missing_thumb 失效语义）属缩略图流水线（提示词2线，已有独立任务 `2026-09-06-缩略图派生流水线只读分析/`），本线只核实前端侧合并空间。
- 报告定位：给后续实施会话的「瓶颈点现状核实表 + 既有基准工具盘点 + 风险与前置条件清单」，不是优化方案终稿。

## 当前阶段
已完成（只读分析 + report.md 落盘；优化实施留待后续独立会话）

## 阶段

### 阶段 1:建立三件套
- [x] 读取提示词文档，确认提示词6范围
- [x] 确认 HEAD=7fea98e3 与文档快照一致；确认工作区有未提交的主题系改动（涉及 MediaGrid.vue/MediaGridCanvas.vue 等，行号会漂移，分析以当前工作区为准）
- [x] 建立三件套
- **状态:** done

### 阶段 2:并行只读探索与汇总
- [x] 路A：双引擎虚拟滚动——B1 响应式部分（visibleRows 深 ref 属实但规模有界）、B5（显式 {passive:false} + 补偿语义核实）、useJustifiedLayout watch 源扩至 15 项
- [x] 路B：Canvas 分支——draw 每帧全窗扫描+selAnim.sync 每帧新建 Map 核实；新发现 6 项每帧/每格开销；主题未提交改动仅 cellRenderer 每格一次 mirrorCellId 比较
- [x] 路C：DOM 分支——每格一组件+深代理 props 属实、DOM 侧无深 watch（快照措辞需改写）、fetchRowsByY 纯透传（快照机制位置错置）、B6 viewportMeta 行号漂移至 mediaStore.ts:231-244
- [x] 路D：事件消费+B3 前端侧+性能工具盘点——B2 链路属实+4 路扇出新发现；B3 regenerate 无跨实例去重；__scrolleryBench API 面与全部 spec 基线确认
- **状态:** done

### 阶段 3:报告撰写与落盘
- [x] 汇总核实结果，逐项给出「成立/部分成立/已漂移」判定与当前行号证据（B1 部分成立、B2/B4/B5/B6 成立、B3 前端侧部分成立）
- [x] 写入 report.md（含基准工具盘点、测试基线、实施会话前置条件与风险清单）
- [x] 回写三件套状态
- **状态:** done

## 关键决策
<!-- 需收口提升的决策编 D-001 递增填「候选 ID」列;仅会话内有效的留空 -->
| 决策 | 理由 | 候选 ID |
|------|------|---------|
| 只做静态核实，不做实测基线与优化实施 | 用户指令限定「只读分析+报告落盘」；三往返基准需运行环境，实施属后续会话 | |
| 报告落盘在本任务目录 report.md，不进 docs/reviews/ | 与姊妹任务 `2026-09-06-缩略图派生流水线只读分析` 保持同一模式；本报告是核实过程产物，随三件套归档；后续实施会话产出的优化清单表才是正式交付 | |
| 4 路并行探索按「代码邻近性」分组而非按瓶颈编号 | 同文件多处瓶颈一次读完，减少子代理重复读文件 | |
| 分析以当前工作区为准，同时记录与 7fea98e3 快照的漂移 | 提示词文档行号是 2026-09-05 快照；工作区有主题系未提交改动直接触及 MediaGrid.vue/MediaGridCanvas.vue 等，实施会话面对的是工作区 | |
| 三件套与报告不单独 commit，留待用户批量提交 | 工作区有多条并行会话的在途改动（姊妹线 planning 目录同样未提交），单线提交会打断用户的批次交付节奏；显式 pathspec 提交由用户批次收口时统一做 | |

## 错误账
| 错误 | 尝试 | 解法 |
|------|------|------|
