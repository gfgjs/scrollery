---
status: active
type: working-memory
line: 文档缩略图叠加标题与章节
created: 2026-07-17
---

# 进度日志:文档缩略图叠加标题与章节

<!-- 验证行怎么填(F-005):门禁末行须在**全部内容落盘后**才跑得出来——顺序是
     先写占位 → 跑门 → 回填真实末行(改动了再复跑)。别倒过来抄一行旧输出充数。 -->

## 会话:2026-07-17(建线)
- 做了:
  - 从「批量15项问题清单」线(`docs/planning/2026-07-17-批量15项问题清单/`)拆出 #13,建本线三件套。
    用户当日裁决:#7 走方案 A(已落地 a66d01a)/ #9 暂不做(no-go 结案)/ **#13 另起独立线,新会话单独接续**。
  - 一路 Explore 对旧设计稿 6 条说法逐条核实:**5 条完全属实(含 `helpers.ts:128` 行号至今未漂)**,
    1 条(pdf/svg「可见性门控」)语义偏松——是窗口级 `visibilityState` 非视口级。
  - **揪出旧稿 3 处过度承诺**(全部写进 findings「设计稿更正」,是本线最值钱的产出):
    ① 「零期零代码」成立但 `thumbInfoElements` 默认空数组 → 零代码非零操作,开箱看不到东西,
       且不覆盖 compact 模式,且只给文件名(需求的 1/3);
    ② epub 取 title 有位置陷阱:`doc.rs:101` 的 `?` 会让**无封面 epub** 在读到 title 前就 bail,
       而那恰是降级成文本卡、最需要标题的那批 → 必须上提到 `?` 之前;
    ③ `text_index` 走 `fs::read` 整读全文(100MB 级),开卷可接受、烘 50 万格不可接受 → 不能据
       「已有」推「章节近乎免费」,三期需另设计浅层分章。
  - 补摸底(旧稿未覆盖):设置项确切点法(设置→缩略图→「缩略图信息悬浮窗」→勾文件名)、
    Canvas 文字绘制现成能力(`truncateToWidth` / `palette.fontMono` / DPR 已统一在 :543-557 做掉)、
    二期加字段的 7+3 个改动点(最大头 = `get_media_meta_batch` 需加 LEFT JOIN document_meta)。
  - 候选登记 F-001~F-003。
- 验证:本会话**未改任何代码**,无需跑门。摸底结论全部带 file:line 出处(见 findings),
  由独立 Explore 读实码得出,非沿用旧稿断言。
- 遗留(等用户):
  - **D-C(阻塞阶段 1/2)**:一期(文本卡画文件名)做不做?视觉样式?是否把 `filename` 预置进
    `thumbInfoElements` 默认值(影响开箱观感)?
  - **D-D(阻塞阶段 3/4)**:二期/三期优先级;后台提取性能预算(50 万库 epub title 上限策略;
    txt 章节是否只做浅层)。
  - 接续从这里起:读本线三件套 → 按 D-C 裁决进阶段 1 或 2。**开工前先复核 findings 里的行号**
    (摸底于 2026-07-17,代码会漂)。

## 回顾(收口时填)
- 亮点:
- 教训:
- 意外:
