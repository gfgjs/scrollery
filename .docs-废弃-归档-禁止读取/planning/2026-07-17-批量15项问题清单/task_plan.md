---
status: active
type: working-memory
line: 批量15项问题清单
created: 2026-07-17
---

# 任务计划:批量15项问题清单

## 目标
用户攒的 15 项问题/需求,按从易到难排序,无人值守尽可能多地施工落地;涉及产品决策的项写清方案留档跳过,等用户回来裁决。

## 当前阶段
阶段 7:三项难项裁决已落(#7 做方案A 已施工 / #9 no-go / #13 拆独立线),15 项**全部有归宿**。
等用户回来:①真机 GUI 验收 13 项(⏸清单见 progress.md)②ESC 全局兜底是否要做(D-001)
③#1 无缝下是否要保时间轴(D-003;已消解——2026-07-17 无缝 minimap 轴线落地即其答案,见 todo.md 同名增补段)。D-B 仍未拍板但**不再阻塞本线**(#9 已 no-go 结案,#7 方案A 已覆盖迁移主场景)。

## 阶段

### 阶段 1:全量摸底
- [x] 15 项五路并行侦察完毕,证据全落 findings.md
- **状态:** done

### 阶段 2:难度排序与分批
- [x] 易→难序列见下「施工序列」
- **状态:** done

### 阶段 3:易批施工(小改动、根因明确)
- [x] #3 退出大图键盘失灵:onActivated nextTick 后焦点无主才 focus 收回(9380b96)
- [x] #14 「文件夹」截断:钮压 24px+标题 3.5em 保底+容器查询窄态藏低频钮(56b5e72)
- [x] #4 查看器侧栏钮低调化:去边框投影/静息半透明/hover 显形,token 保留;immersive-exit 属 S5 豁免未动(56b5e72)
- [x] #8 状态栏设置入口:侧栏隐藏时显 22px 钮(56b5e72)
- [x] #11 扫描不消失:enrichmentDone 终态账本+10min 富化怠速看门狗(2efac68)
- [x] #2 增量生成缩略图:run_thumbnail_generation(reset_all) 共享流水线+新 IPC+双钮(2efac68)
- **状态:** done

### 阶段 4:中批施工
- [x] #6 扫描期自动切宫格:effectiveLayoutMode 接线,watch 看 effective(1866ea0)
- [x] #5 拖拽手柄开关:show_drag_handle 21键+DOM/Canvas(props 注入)双 guard+设置 toggle(1866ea0)
- [x] #12 Ctrl+W:uiStore.requestAppClose 单源分派 closeBehavior;ESC 留决策(1866ea0)
- [x] #1 无缝分组:LayoutParams.seamless 打包旁路+gen_key+工具栏 Ungroup toggle+2 特征化测试(1866ea0)
- [x] #10 EXIF 提速:HeaderBuf 单次头读(open 3→1)+批选直取路径删逐项持锁查询+3 测试(aa59b7f)
- [x] #15 Canvas 边界卡顿:rows 深 watch 换 浅 watch+canvasPatchTick 信号(6 patch 点)(e63c1d1)
- **状态:** done

### 阶段 5:难项设计方案(留档不施工)
- [x] #7/#9/#13 三项方案落 designs-难项方案.md(含 #13 零期发现:filename 元素已现成)
- **状态:** done

### 阶段 6:收尾
- [x] 三件套回写+todo.md「进行中未登记」指针行+候选 F-001~F-006 登记
- **状态:** done

### 阶段 7:难项裁决落地(2026-07-17 用户回来后)
- [x] **#7 = 方案 A,已施工落地**(a66d01a):relink_scan_root 命令 + 根节点右键「文件夹已迁移…」+
      抽样 100/阈值 95% 安全闸 + 6 个新测试。施工期推翻设计稿三处旧信息(见 designs 稿「地面事实更正」表)
- [x] **#9 = 暂不做,no-go 结案**:评估正文保留作未来重启的现成账;重启前置 = D-B 拍板做 content_hash 地基
- [x] **#13 = 拆独立线**:`docs/planning/2026-07-17-文档缩略图叠加标题与章节/`(三件套已建,含摸底核实
      与旧稿 3 处过度承诺的更正);D-C/D-D 随该线裁,不在本线
- [x] 顺带(非 15 项):`cargo test --lib` 在 HEAD 基线即 3 红,已修(76ad9ea)——见「错误账」
- **状态:** done

## 施工序列(易→难,摸底证据见 findings.md)
易:#3(一行级) → #14(CSS) → #4(CSS) → #8(小) → #11(小-中,竞态修) → #2(小-中,新 IPC)
中:#6(小-中,接线) → #5(中,多点小改) → #12(中,Ctrl+W 部分) → #1(中,前后端) → #10(中,Rust 性能) → #15(中,正确性风险)
难(仅设计):#7、#9、#13

## 15 项清单(用户原话摘要)
| # | 内容 | 类型初判 |
|---|------|----------|
| 1 | 画廊分组无缝模式(视觉不分组,实际按组汇聚排序) | 功能 |
| 2 | 「全量生成缩略图」旁加「增量生成」 | 功能 |
| 3 | 大图退出后画廊丢键盘响应 | bug |
| 4 | 沉浸模式开/关侧栏按钮太显眼,需主题适配+低调 | UI |
| 5 | 选择态缩略图拖拽手柄可选显隐 | 功能 |
| 6 | 扫描期宫格布局/扫完等高;或根治等高扫描重排 | 功能/性能 |
| 7 | 文件夹迁移(D→C盘)智能识别,免重扫重生成 | 大功能 |
| 8 | 隐藏左边栏时顶栏/底栏加设置入口 | UI |
| 9 | 重复图只生成 1 张缩略图,评估性能 | 功能/评估 |
| 10 | EXIF 解析慢(500张 1-2s)提速 | 性能 |
| 11 | 长期bug:添加文件夹后「正在扫描...」不消失 | bug |
| 12 | Ctrl+W/ESC 快速退出/隐藏程序 | 功能 |
| 13 | 文档缩略图显示文件名/内嵌名/章节,后台静默生成 | 功能 |
| 14 | 左侧栏「文件夹」被挤剩「文」字 | UI bug |
| 15 | Canvas 模式按住方向键滚动周期性卡顿 | 性能 bug |

## 关键决策
<!-- 需收口提升的决策编 D-001 递增填「候选 ID」列;仅会话内有效的留空 -->
| 决策 | 理由 | 候选 ID |
|------|------|---------|
| #12 只施工 Ctrl+W(走 closeBehavior);ESC 全局隐藏留用户决策 | ESC 已被 5 处强消费(查看器/全屏守卫/对话框/选区/backBar),全局兜底语义激进,误触隐藏窗口代价高 | D-001 |
| #6 走方案(a) 扫描期自动切宫格;弃(b) 增量等高 | 排序 sort_datetime DESC 新文件散插非尾部追加,增量假设不成立;宫格几何与宽高比无关,天然免疫双 reflow 源 | D-002 |
| ~~#7/#9/#13 只出设计稿不施工~~ → **已被 2026-07-17 用户裁决取代**:#7 做方案A(已落地 a66d01a)/ #9 no-go 结案 / #13 拆独立线 | 原理由(#7 自动识别与 #9 均压 §5.2 内容身份结构债、T13 defer 红线)**对方案 B 仍成立**——方案 A 不碰该地基故可做;#9 无独立立项价值随 D-B 走 | |
| #7 relink 新增 `AppError::Relink` 专用变体,不复用 `InvalidMove` | 摸底实证 InvalidMove 非预留、已被 file_ops 占 8 处(语义「文件夹移动非法」);挤进同一 IPC code 前端无法分流三种 relink 失败 | D-005 |
| #7 抽样校验用 size+mtime 双比,**不复用** upsert 的 Unchanged 判据 | 职责不同:upsert 问「文件变了吗」(mtime 够,实测判据在 scan.rs:793 且 size 不参与),relink 问「是同一棵树吗」(要更强指纹);size 在同一次 `fs::metadata` 里白送,双比零额外 IO | D-006 |
| #7 兜底重扫由**前端**补发而非后端自触发 | `start_scan` 签名要 `Channel<ScanChannelPayload>`,进度通道只有前端造得出;后端自调需把扫描编排从命令层抽出,代价远大于收益 | |
| exotic 陈旧档位测试修法取「绑 THUMB_TIERS 事实源」而非「480 换成 512」 | 硬编码正是本次失配的成因(b554aa5 改梯漏改测试),换个数字只是把同一颗雷埋到下次改梯;THUMB_TIERS 注释本就自称「全后端唯一档位事实源…避免多处硬编码档位漂移」 | D-007 |
| #1 无缝模式下 date 时间轴 scrubber 先禁用(自动隐藏) | monthBuckets 依赖分隔符行,seamless 不产,canShowScrubber 判据自然退化;单独产月桶属增量,待用户确认需要再做 | D-003(已消解:落地为无缝 minimap 轴,见 todo.md 增补段) |
| #15 修法取「浅 watch+patchTick 信号」而非段 rows markRaw | markRaw 会同时切断 DOM bucket 模式的就地 patch 响应,改动面失控;窄修只动 canvas 消费侧,DOM 路径零改动 | D-004 |
| #11 看门狗阈值取 10 分钟 | 视频批 500 项 MF 逐项探测,单批静默期可达数分钟;阈值过低会误杀正常富化,竞态主修靠终态账本,看门狗仅兜底 | |
| #2 「全量」语义改为显式「全量重建」,增量成为常规动词 | 原描述文案本就是增量语义而实现整表 reset,文实不符;双钮(Play 增量/RotateCcw 重建)镜像 AI 卡片动词分工 | |

## 错误账
| 错误 | 尝试 | 解法 |
|------|------|------|
| `cargo test --lib` 全量 3 红(exotic::fingerprint / sink / pipeline),`thumb_path` 对档位 480 硬断言 panic | 先 `git stash push -- src-tauri src/` 验基线:HEAD 同样 3 红 → 非本线引入。再 `git log -L 30,30:generator.rs` 定责:b554aa5「档位重定」把 THUMB_TIERS 从 [120,240,480,960] 改成 [64,128,256,512,1024],**漏改三处测试硬编码的 480**;生产侧自洽(是有意的 feature 提交)→ 判**测试陈旧,非生产回归** | 三处改绑 `THUMB_TIERS[3]` 事实源(76ad9ea)。628 全过(原 625+3) |
| **为何这红一直没露头**:前几轮每次只跑过滤子集(`cargo test --lib layout::` / `scanner::`),输出全绿很踏实,但被过滤掉的 480 个测试里正藏着 3 红;CI 本该兜住(`.github/workflows/ci.yml:70` 跑 `cargo test --workspace`),偏偏因主机硬件不稳被冻结 —— **两道防线的失效窗口重叠** | — | 教训见 findings F-007 |
