---
id: 2026-07-06-R2-7-改名施工计划-Scrollery
status: archived
type: plan
line: 产品命名 R2-7
created: 2026-07-06
---

# R2-7 改名施工计划(草案):picasa-next → Scrollery

> 📦 **已执行(2026-07-10 文档治理补标)**:全仓改名施工已于 2026-07-06 完成(rename-gate active=true 生效、三仓改名、公开 main 提升,todo/completed 有账);文首「草案/等 FTO 绿灯」为历史状态。FTO 委托仍在途,若红按 [FTO brief](2026-07-06-R2-7-Scrollery-FTO-brief.md) 预案回退。

> 状态:**草案就绪,施工本体等 FTO 绿灯**(拍板记录与 FTO 委托见 [Scrollery FTO brief](2026-07-06-R2-7-Scrollery-FTO-brief.md))。
> 目标:英文 **Scrollery** / 中文 **画卷**;tauri identifier 锁定 **`com.scrollery.app`**。
> 本计划所有清单均为 2026-07-06 全仓 grep 实证(非转抄初筛文档 §4;两处口径差已按代码现状订正)。
> 配套:D10 改名门禁已随本计划落地为**休眠模式**(`scripts/check-rename-gate.mjs` + `rename-gate.json`,施工第 7 步翻 active)。

## 0. 总览

- 工作量:约一个工作日,分 7 步,每步一 commit + 独立验证,可中断可回滚。
- 三条纪律:①**状态坐标**(identifier/keyring)按既有裁决「内测期重装重激活」处理,不写迁移代码;②🔴 **同步管线联动面**(copy.bara.sky/FORBIDDEN/oss-gate)与 crate 改名必须同一步落地,否则四层防线失效;③ 历史文档(plan-docs/**)不改名,由 D10 allowlist 豁免。

## 1. 状态坐标(运行期,改错会丢用户状态;共 2+1 处)

| 坐标 | 现值(实证位置) | 改法与裁决 |
|---|---|---|
| tauri `identifier` | `com.picasanext.app`(`src-tauri/tauri.conf.json:4`) | → `com.scrollery.app`。决定 appDataDir(DB/缩略图/已装插件全在内)。**裁决(初筛 §4.1,维持)**:内测期不写迁移代码,发公告让内测用户卸载重装+重激活;旧目录 `%APPDATA%/com.picasanext.app` 附手动删除指引 |
| `KEYRING_SERVICE = "picasa-next"` ×4 | `src-tauri/src/ipc/storage_commands.rs:24`(存储后端密码)/ `src-tauri/src/exotic/license.rs:36`(激活凭据)/ `src-tauri/src/ipc/proofread_commands.rs:17`(校对 API key)/ `crates/picasa-next-pro/src/lib.rs:24`(DirectEntitlement) | → `"scrollery"`。**Boy Scout:4 处硬编码收敛为 `picasa-next-plugin-api`(改名后 scrollery-plugin-api)单一 pub const**,主包与 pro 同源引用,杜绝再分叉。旧凭据不迁移(重激活/重录 API key/重存后端密码,内测公告列明) |
| 内测 registry base(编译期注入) | `PICASA_REGISTRY_BASE_DEFAULT` → 指向 `picasa-next-registry` 仓 raw(`src-tauri/src/ipc/exotic_commands.rs:437`) | GitHub 仓改名自动 301 redirect,**已发内测包不断供**;新包注入值直接换新仓名。风险低,列此防漏 |

另:DB 文件名 `picasa_next.db` 在 appDataDir 内,identifier 一换整目录即新,顺手改 `scrollery.db`(mock_data/worker_e2e 两个 bin 的默认路径同步)。

## 2. 机械替换面(编译期/物料,无状态风险;全部实证)

### A. Tauri 配置与前端物料
- `src-tauri/tauri.conf.json`:`productName "Picasa Next"` → `"Scrollery"`(决定安装目录/开始菜单/窗口组名)、双窗口 `title`(行 17/29)。
- `src-tauri/tauri.direct-release.conf.json:7`:updater 端点占位 → **`updates.scrollery.invalid`**(施工时偏离本计划原写的 `.app`:`.invalid` 是 RFC 2606 保证不可解析域,占位期指向真实存在的 scrollery.app 反而制造「像活着的死端点」;Part7-T8 转正时一并定死真实域)。
- `src-tauri/capabilities/default.json` ×1、`public/splashscreen.html` ×2、`src/router/index.ts` ×1(文档标题)。
- `package.json` `"name": "picasa-next"` → `"scrollery"`;未来 npm 包一律走 `@scrollery/*` scope(裸名被 2022 JS 库占)。

### B. Cargo crate 名族(7 个改,4 个不动)
改:`picasa-next`(主 bin,src-tauri)+ lib 名 `picasa_next_lib` / `picasa-next-plugin-api` / `picasa-next-free-stub` / `picasa-next-exotic-trust` / `picasa-next-ai-core` / `picasa-next-pro` → `scrollery` / `scrollery_lib` / `scrollery-plugin-api` / `scrollery-free-stub` / `scrollery-exotic-trust` / `scrollery-ai-core` / `scrollery-pro`;`crates/` 目录名与 workspace members / 各处 path 依赖同步;`Cargo.lock` 重生。
不动:`exotic-protocol` / `psd-worker` / `psd-probe` / `ai-worker`(无品牌词根)。
另:`remote_registry.rs:251` User-Agent `"picasa-next"` → `"scrollery"`。

### C. 🔴 同步管线联动面(与 B 同一步落地,漏改 = 四层防线失效)
- `copy.bara.sky`(10 处 picasa 引用):glob `crates/picasa-next-pro/**` → `crates/scrollery-pro/**`;Cargo.lock 剥离正则两处 `"picasa-next-pro"` 字面 → `"scrollery-pro"`;**FORBIDDEN 词表 `picasa_next_pro` → `scrollery_pro`**(过渡期双词并列,待公开树确认零 picasa 残留后再摘旧词);两仓 URL 随 F 组仓库改名同步。
- workflows:`oss-gate.yml` ×13、`ci.yml` ×3、`sync-oss.yml` / `release.yml` / `drift-alarm.yml` 各 ×1(`-p picasa-next` 构建参数、路径断言等)。
- `scripts/generate-notice.mjs`(发货物闭包按 crate 名圈定)→ 改后重生 `NOTICE.md` 并过 `--check` 门。
- 剥离模拟器(scratchpad `strip-internal.mjs`)正则同步,改后重跑字节级对拍 + sync-oss dispatch dry_run 实证。

### D. 文案与文档(活文档改,历史快照豁免)
- `README.md` / `CHANGELOG.md`([Unreleased] 加改名条)/ `CLAUDE.md` + `CLAUDE_中文对照.md`(项目名与角色描述)/ `NOTICE.md`(随 C 组再生成)。
- ~~i18n locale 文件实证零品牌硬编码~~ **🔴 订正(2026-07-06 施工时发现)**:原「实证」系 Grep 40 文件分页截断的误判——zh-CN/en-US 各含 4 处「Picasa Next」品牌串(welcome/onboarding/插件升级提示/退出确认),已随映射更换。教训:count 模式命中文件数超过分页上限时结论不可信,须 `head_limit 0` 复核。
- `plan-docs/**` 全部**不改**(历史快照 + 私有仓不投影),D10 allowlist 豁免。

### E. env 前缀 `PICASA_*`(~12 变量,**维持缓改裁决**)
`PICASA_EXOTIC_KEYSET_FILE / PICASA_EXOTIC_DEV_KEYSET / PICASA_EXOTIC_DEV_FILE_URLS / PICASA_REGISTRY_BASE(+_DEFAULT) / PICASA_INTERNAL_REGISTRY_BASE / PICASA_SMOKE_TEST / PICASA_HEIC_SAMPLE / PICASA_AI_WORKER_PATH` 等分布于 build.rs/CI/scripts/肌肉记忆,首期**不改**(D10 以 token 前缀 `PICASA_` 豁免);第二阶段可选迁 `SCROLLERY_*` + 旧名兼容读(独立小任务,非本施工范围)。

### F. GitHub 仓与 org(用户侧点选,GitHub 自动 301 兜底旧链)
`picasa-next-private` → org `scrollery-app` 下 `scrollery-private`;公开镜像 `picasa-next` → `scrollery`;`picasa-next-registry` → `scrollery-registry`。转移后复核:公开仓 deploy key / 私有 secrets(`OSS_SYNC_DEPLOY_KEY` 等)/ rulesets 分支保护随仓存活;`copy.bara.sky` 两 URL + drift-alarm 指向同步(C 组)。

## 3. 施工顺序(7 步,每步 commit + 验证)

| 步 | 内容 | 验证信号 |
|---|---|---|
| 1 | 用户侧:建 org `scrollery-app`,三仓转移/改名(F 组) | 旧 URL 301 生效;secrets/rulesets 复核清单过 |
| 2 | crate 名族 + 目录 + workspace + path 依赖 + lock 重生(B 组) | `cargo check/test --workspace` + `clippy -D warnings` 全绿 |
| 3 | 同一 commit 内:同步管线联动全量(C 组) | 剥离模拟字节级对拍绿;FORBIDDEN 双词 verify 过 |
| 4 | tauri.conf(identifier/productName/title)+ A 组前端物料 + DB 文件名 | `tauri dev` 冒烟:新 appDataDir 自建、UI 标题正确 |
| 5 | KEYRING_SERVICE 收敛至 plugin-api 单 const + 改值;registry 注入值换新仓 | 激活/存储后端/校对 key 三路重录手测;swap 冒烟测试绿 |
| 6 | D 组文案 + NOTICE 再生成 + i18n 复扫 | `generate-notice.mjs --check` 绿;全仓 grep 复扫仅剩 allowlist 面 |
| 7 | **D10 门禁翻 active** + 全链收官 | 本地全绿 + CI 三 job 绿 + sync-oss 实推 + oss-gate 绿 + 内测包重打可激活 |

## 4. D10 改名门禁(已随本计划落地为休眠模式)

- `scripts/check-rename-gate.mjs`:零依赖 + 启动即 selftest(仓例同 verify-channel-bundle);`git grep -I -i` 扫 tracked 文件中的品牌旧词根 `picasa`,按 `scripts/rename-gate.json` 的路径豁免(plan-docs/** 等)与 token 前缀豁免(`PICASA_` env 族,大小写敏感)过滤,余量非零即红;`--force-active` 支持本地彩排(无视休眠配置)。
- `scripts/rename-gate.json`:`active: false`(休眠——改名前全仓合法满是旧名,门禁只在施工第 7 步翻 true 后生效);豁免清单即本计划 §2-D/E 裁决的机器可读形式。
- **彩排实测(2026-07-06,`--force-active`)**:休眠态 selftest+skip 绿(exit 0);激活态全仓扫描拦截 **433 处**豁免后残留(exit 1)——此数即本施工机械面的真实底数,且已抓到人工盘点漏网面(`.cargo/config.toml` 注释引用 crate 名),证明第 7 步翻门后旧名回流必被咬住。
- CI 接线:ci.yml 轻量 job,休眠期打印 skip 一行,不耗时。
- 分工边界:D10 管「新名时代旧名残留回流」,Copybara FORBIDDEN 管「公开树商业符号泄漏」,两表独立演进互不替代。

## 5. 内测用户迁移公告要点(改名日随包发布)

产品更名 Scrollery(画卷)→ 卸载旧版 → 安装新版 → **重新激活 license**(凭原激活码)→ 重录校对 API key / 存储后端密码(如曾配置)→ 旧数据目录 `%APPDATA%/com.picasanext.app` 可手动删除(图库文件本体不受影响,重扫描即回);缩略图/AI 缓存自动重建,首开稍慢属预期。

## 6. Definition of Done

施工 7 步全过 + D10 active 后 CI 绿 + 公开树 oss-gate/drift-alarm 绿 + 新内测包真机激活链通 + todo.md/G3 行与 Part7-T18、Part8 D10 状态同步回写。

## 7. 施工实录(2026-07-06,用户拍板「先做改名」提前于 FTO;FTO 红灯风险已知情)

**代码面已完成(步骤 2-6)**:git mv 五 crate 目录 + 脚本原子替换(哨兵保护 repo URL → 有序 token 映射 → 残留审计;84 文件 247 处 + 二轮测试临时目录前缀 10 文件)+ KEYRING_SERVICE 收敛至 `scrollery-plugin-api::KEYRING_SERVICE` 单一常量(原四处硬编码)+ CHANGELOG 迁移条目 + TRADEMARK 填名 + NOTICE/SBOM 再生成(产物名 scrollery.cdx.json)。

**剥离模拟三真雷(全部当场修复)**:
1. **新禁词子串碰撞**:`scrollery_pro` 是 `scrollery_probe_*`(volume_probe 测试临时目录名)的子串,裸词正则必假阳性 → FORBIDDEN 全链加 `\b` 词边界(copy.bara.sky RE2 + oss-gate GNU grep BRE 语义一致;⚠ oss-gate env 必须 YAML 单引号,双引号把 `\b` 吃成退格符)。
2. **门禁配置字面随同步**:`rename-gate.json` 的过渡豁免词字面会进公开树,触发 verify_match/oss-gate → 两层扫描各加 `rename-gate.json` 排除(oss-gate.yml 自指排除同一先例)。
3. **模拟器 glob 仿真缺口**:strip-internal.mjs 只模拟标记剥离与 lock 剥离、不模拟 origin_files glob 排除 → 补删 pro/plan-docs/同步配置后再断言。
另修 D10 门禁自身一雷:CJK 文件名被 git 引号转义致 `plan-docs/` 路径豁免失配 → `core.quotepath=false`。

**验证(⚠ 仅本地——CI 因 Actions 配额欠费未跑,恢复后补实证)**:cargo fmt/clippy -D warnings/test --workspace --locked **474 全绿**;前端 eslint/vue-tsc/vitest 221/build/渠道扫描全绿;NOTICE `--check` 绿;剥离模拟(write-tree 字节级导出+glob 仿真)INTERNAL 恰 3 文件+lock 恰 1 命中+四禁词 `\b` 零命中+投影树 `cargo check --workspace --locked` 绿(剥 lock ≡ 重生)。残留审计=11 处全豁免面内(9× repo URL 待仓库改名批次+2× FORBIDDEN 过渡词自身)。

**✅ URL+guard 批次已完成(2026-07-06 当日,用户在 GitHub 原地改名三仓后)**:实际拓扑=**留在 gfgjs 账号下改名**(未建 org,偏离 §5 设想有记):`gfgjs/scrollery-private`(私有 canonical)/`gfgjs/scrollery`(公开镜像)/`gfgjs/scrollery-registry`(内测发行源),gh api 跟随 301 实证三仓新名。九处同批落地:copy.bara.sky 两 URL、oss-gate `github.repository` guard ×2+注释、release.yml/drift-alarm.yml guard 各一、exotic-internal-registry.mjs + build-internal-installer.ps1 两处 registry raw base;本地 `origin` remote 同步 set-url。**步骤 7 D10 已翻 active**:selftest(6 词例+路径豁免)+ 实扫全绿——tracked 文件零 picasa 残留(豁免面外)。剥离模拟按新树复跑:INTERNAL 恰 3+lock 恰 1+四禁词 `\b` 零命中+投影树 `cargo check --workspace --locked` 绿(1.74s,复用上轮 target 缓存)。⚠ 验证仍仅本地(CI 欠费未解)。

**✅ CI 实证补上(2026-07-06 同日,自托管 runner 绕开配额)**:dev-box-win(D:\actions-runner)上线,ci.yml rust/frontend/smoke 切自托管(fffa238;私有仓 windows 2× 倍率是额度大头,self-hosted 零计费且配额耗尽态照跑)。run 28786569249 实跑:**rust job 2m50s 绿**(check/clippy/test/渠道依赖树断言/NOTICE 门)+ **frontend 2m06s 绿**(含改名门禁/渠道合规扫描),均 runner=dev-box-win。真雷一枚:WindowsApps 的 WSL bash stub 在 PATH 压过 Git Bash,`shell: bash` 与 bash 系组合 action(dtolnay/rust-toolchain)55ms 退 1——`C:\Program Files\Git\bin` 前置 PATH 修复(持久化随服务化步骤归用户)。

**剩余缺口(收窄)**:`tauri dev` 冒烟(用户侧);rust-linux job + sync-oss 实推 + 公开仓 oss-gate/drift-alarm 绿——仍需 Billing 修复**或** phase-2(WSL2+Docker+Linux 自托管 runner,彻底零费用);runner 服务化(用户侧管理员命令块已交付,当前进程态重启即停);内测包重打可激活。
