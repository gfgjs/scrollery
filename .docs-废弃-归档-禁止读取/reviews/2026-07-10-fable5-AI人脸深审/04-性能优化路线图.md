---
id: 2026-07-10-04-性能优化路线图
status: snapshot
type: review
line: AI 人脸流水线深审
created: 2026-07-10
---

# 分册 04:性能优化路线图(全域合并)

> 快照 2026-07-10,dev `68e23e8`。原则:**测点由风险决定**——当前库量级(千级向量/脸)下全部条目无感;本册按「触发量级」分梯队,建议真机大库实测取数后再排期,不盲改。ANN/simsimd 已有 dormant 裁决(vector_store.rs 头注,owner=Part4,触发=50 万向量或 p95>300ms),本册不与其重复立项,只在相关条目引用。
> ⚠️ **时效**:P7(=A14,正确性级)未随批 1/2 交付仍待办;P20(=A13)已由并行会话 a064c34 修复;P16 的 batch_update_* 已随 X1 条件写重构(逐行 UPDATE 循环形态仍在)。其余条目维持待实测排期。

## 梯队 1|首跑吞吐(全库首次分析,一次性成本,但用户第一印象)

| ID | 条目 | 位置 | 预估收益 | 前置 |
|---|---|---|---|---|
| P1 | pending 取件每批全量排序:`WHERE status=0 ORDER BY created_at DESC LIMIT 512`,部分索引仅 status 一列 → 每批对全部剩余 pending 行排序,整程 O(N²/批·logN);**AI/face 两条流水线同病** | queries.rs:3002-3014(AI)、:3188-3216(face);schema.rs:240/443 | 百万库 ≈2000 批 × 均 50 万行扫描 → 每批几十 ms 级取数税(被推理掩盖与否待实测) | 新版本 DDL 升级为 `(status, created_at DESC) WHERE status<3` 复合部分索引;或改 `ORDER BY id DESC` 直接吃 PK |
| P6 | host 派生与 worker 推理批间无重叠(W11):首跑普遍走「缺 ai_cache 现场派生」,批耗时=派生段+推理段之和 | worker_pipeline.rs:166-278 | 两段取大(首跑吞吐可观) | 批级双缓冲;CPU permit「1批=1槽」记账拆分,需小设计 |
| P7 | EMBED_BATCH 超时固定 120s(A14):CPU EP 大批误杀 → 整轮 fatal,**正确性级别的性能项** | coordinator.rs:52 | 消除 CPU 机型整轮失败 | 仿 face 按项数缩放 |
| P8 | worker 端 SessionInit 全量重算 GB 级 sha256(W5) | ai-worker session.rs:175 | 每轮省数秒~十数秒冷启动 | 进程内 (path,len,mtime) 备忘;D1 附注回写 |
| P9 | resize 换 fast_image_resize(SIMD;K-💡):解码线程主要 CPU 消耗之一 | ai-core clip.rs 预处理 + derive/image.rs | 4-8×(resize 段) | 成熟广采 crate 合依赖纪律;须重跑金标对拍 |
| P10 | face letterbox 克隆+ndarray 逐元素写(K4)、动态 batch 冗余整批拷贝(K5)、YuNet 后处理 12 次 to_vec(K-🔵) | ai-core face.rs/clip.rs | 每图毫秒级 × 全库 | 仿 T18.5 平坦写,机械改 |
| P11 | CPU 线程超订(K3):4 池 × 全核 intra_threads | ai-core engine.rs:240-284 | CPU 机型吞吐(超订消除) | 容量×线程≈核数;文本塔池 2→1 |

## 梯队 2|检索与常驻内存(达到十万级向量前清偿)

| ID | 条目 | 位置 | 说明 |
|---|---|---|---|
| P2 | 嵌入缓存装载峰值 ≈3× 稳态(A9):先整表 collect f32 blob(1M×512d≈2GB)再打包 f16(1GB),两者共存到函数尾 | search.rs:66-86 + queries.rs get_all_embeddings | 改流式:行回调内直接转 f16 push,峰值降到稳态+单行。ANN 阈值前这是最大单点内存尖峰 |
| P3 | 边分析边搜反复全量重载(A10):每 512 条 flush 整体失效缓存 → 分析中每次搜索=全表重读+重打包 | pipeline.rs:353-354 + search.rs | 正解是 vector_store 增量 upsert(dormant 既定消费点,不另起炉灶);短期低风险缓解=flush 时增量 append 新行,仅切模型/reset 才整体失效 |
| P4 | 打分为标量循环 f16→f32 逐元素(无 SIMD) | search.rs:147-153 | **已有裁决**:Part1-T9 simsimd 提速与 T11 ANN 同 dormant(todo.md:198,现库 1554 向量毫秒级,YAGNI)。引用不重复立项 |
| P12 | 嵌入常驻内存量级:f16 下 1M×512d≈1GB(H/14 1024d 翻倍) | search.rs / vector_store.rs | 与「百万级」叙事对照:ANN 阶段附带评估 int8 标量量化(余弦损失<0.001 减半再减半)或 mmap;profile 预留 storage_dtype(K-💡) |

## 梯队 3|人脸功能面查询(人脸真机大库实测后按数据排期)

| ID | 条目 | 位置 | 说明 |
|---|---|---|---|
| P13 | list_likely_matches 全表扫描+质心 BLOB 逐脸复制+limit 事后截断(F16 🟠) | queries.rs:3938-4039 | 大库刚分析完(全部未确认)=数百 MB 搬运/次面板打开。修法:person_id 真 WHERE 走索引 / 质心先按 person 载 HashMap 不随行 JOIN / 组内外 limit 下推 |
| P14 | recluster_all 全量嵌入常驻(百万脸≈1GB,ArcFace ×4)+最坏 O(F²),零护栏(F17 🟠) | face_cluster.rs:518-594 | 近期:规模预检+前端预估时长警示;中期:单块 Vec<f32> 连续存储+偏移索引削堆开销;远期:ANN/分块(与模块头 F5 取舍一致) |
| P15 | 增量聚类每 flush 重读全量 persons 名册(F18) | face_cluster.rs:208-237 | writer 生命周期内 roster 常驻,apply 后就地更新占位→真 id 映射;与 F2 并发裁决一并设计 |
| P16 | batch_update_*_status 逐行 UPDATE 循环(F20)、sync 每批全表重扫(F21) | queries.rs:3231/3353 | 顺手项/切轨罕见可容忍 |

## 梯队 4|控制面与 UI 常驻成本

| ID | 条目 | 位置 | 说明 |
|---|---|---|---|
| P17 | 每 2s 轮询 = 多条无覆盖索引全表 COUNT(get_ai_status/get_face_status 各 3-5 条,含 `face_status IN (2,3)` 无法用 `<3` 部分索引) | ai_commands.rs:196-286、face_commands.rs:92-141 | 百万级库轮询期常驻 CPU/IO。修法候选:批粒度 emit 进度事件(下载 Channel 先例可仿,一并解 U9/U11 与「批内进度」缺口)/ 流水线内存计数器 / 补覆盖索引 |
| P18 | ai_worker 粗锁跨整批(W4):状态命令/搜索排队分钟级 | worker_pipeline.rs:243 等 | 快照拆 RwLock + 状态命令 try_lock 回退,低风险先做 |
| P19 | PersonsView 封面全量即时解码无 lazy(U12) | PersonsView.vue | 与 U5 修法合一(`<img loading="lazy">`) |
| P20 | fetch_tree 无超时(A13):黑洞路由挂分钟级 | remote_registry.rs:244 | 复用 secure_client(SmallFile),兼安全加固 |

## 不做/缓做(有据)

- ANN 实装、simsimd:dormant 有主,触发条件未达(P4/P12 引用)。
- PersonsView 虚拟化:数千簇前不必(P19 的 lazy 已够)。
- 搜索批间插队(W4 ③):收益有限,先做快照拆锁。
- K9 亚像素对齐:不改码,只补对拍基线证据。
