---
id: 2026-08-13-review-全仓未完成工作梳理与代码核证
status: active
type: review
line: 全局
created: 2026-08-13
---

# 全仓未完成工作梳理与代码核证报告

> - **日期**:2026-08-13(核对基线 = dev 尖端 `2f13d1a`,工作树仅 `.gitignore` 一处改动)
> - **性质**:全仓未完成工作盘点。**文档结论一律视为不可信,每条断言独立对代码核证**;文档引文逐字摘录、代码证据一律 `文件:行号` 定位。
> - **来源**:Workflow 多代理审计(11 路发现 → 去重 → 44 批对抗式对码核证 → 完整性批评),57 agent / 2025 次工具调用 / 约 25 分钟;另有 14 条批次漏判条目由主会话补核。核对全量数据:`3d197d77-f8f8-4729-8f25-7e8b08c8e1d3/subagents/workflows/wf_31f8410d-128/journal.jsonl`
> - **结论计数**:373 条原始声明 → 去重 372 → 主核证 359 条 + 完整性批评 6 条 + 批次补核 14 条。判决分布见 §一。
> - 本报告为只读盘点,**未改动任何状态源**(todo.md / 记忆 / git)。处置建议见 §九。

---

## 一、总览

### 判决分布(对码核证后)

| 判决 | 条数 | 含义 |
|---|---|---|
| CONFIRMED_UNFINISHED | 180(高 24 / 中 69 / 低 156*含信息) | 文档与代码双证:确实未完成 |
| DONE_IN_CODE | 50 | **文档称未完成/未提交/未推,代码实已完成 → 文档过期** |
| INTENTIONAL | 46 | 已裁/刻意保留,非遗漏,勿重开 |
| CANNOT_VERIFY_CODE | 50 | 仅真机/GUI/外部动作可验(⏸GUI 积压) |
| DUPLICATE_OF | 33 | 与其他条目同一事实 |
| NOT_FOUND | **0** | 无幽灵条目(所有文档声明的对象都能在代码中定位到) |

完整性批评补漏 6 条全部核证为 CONFIRMED_UNFINISHED;批次补核 14 条中 8 条确认未完成、1 条文档过期(fmt 红已转绿)、5 条为事实/刻意。

### 总体结论

1. **文档体系整体可信度比预期高**:零 NOT_FOUND,180 条未完成声明中绝大多数与代码吻合。但 **50 条 DONE_IN_CODE 证明「未提交/未推/待合并」类状态声明大面积过期**——todo.md 头部 frontmatter「更新日期 2026-07-02」亦本身过时。
2. **发布链仍有三处硬阻断**:① enhance/video worker 无生产 bundle(F-01 残余);② 生产公钥仍是占位(用户 ceremony 未跑);③ 签名/公证/mac 矩阵/商业发布流水线零落地。另有增强模型托管仓名未定(PENDING_USER_REPO)。
3. **最集中的未完成类别不是代码债,而是待裁决簇(D-001..D-008、J-1..J-8、D-C/D-D 等)与 ⏸GUI 真机验收积压(50 条)**——多数代码已就绪,卡在决策或人手。
4. **07-25 深审 13 项 finding 的「均未修」已过期**:ai-worker 闭包、F-02/F-07/F-09/F-11/F-12 已修(504f209/2564dc1),F-03/F-04/F-05/F-06/F-10/F-13 半修或未修(见 §二/§三)。

### 最应优先处理(按风险/阻塞)

1. **enhance/video worker 发货闭包**(`tauri.conf.json:54` externalBin 仅 raw+ai-worker;`exotic/installer.rs:396` video-worker prod 显式拒绝)——增强与 rmvb/vob 在正式包不可用。
2. **生产公钥 ceremony**(`crates/scrollery-pro/resources/exotic-keyset-prod.json:3` PLACEHOLDER)——所有插件授权链无真实信任根。
3. **增强模型托管仓名**(`src-tauri/src/enhance/registry.rs:20` PENDING_USER_REPO)——用户定 HF/GitHub 仓名即解除。
4. **配置层 P1**(F-03 跨进程写锁 / F-04 schema 无区间 / F-05 空串路径,`config/` + `ipc/config_commands.rs:421`)——`derive_batch_size=0` 可停流水线级缺陷仍未修。
5. **RAW F-06 交叉构建修复在 codex 分支未合入 dev**(9 commit 悬置,见 §六)。

---

## 二、确认未完成(按域)

> 判决 = CONFIRMED_UNFINISHED。证据为核证员的 `文件:行号` 定位。已合并同事实条目(DUPLICATE_OF)。

### 2.1 发布/CI/基建(ci-infra)

| 项 | 严重度 | 要点与证据 |
|---|---|---|
| F-01 残余:enhance/video worker 无生产 bundle | **高** | ai-worker 已收口(2564dc1);`tauri.conf.json:54` externalBin 仅 `raw-worker`+`ai-worker`,package.json 无 build:enhance/build:video;`enhance/service.rs:1113` 主程序同目录查找 enhance-worker;`exotic/installer.rs:396` video-worker prod(H2b-prod)显式拒绝拉起。归 F-003 跟踪。 |
| D-001 未采纳:统一 target-aware build-sidecars | **高** | 实际走 `scripts/build-ai-worker.mjs:43-49` win32-only 补丁(非 Windows 直接 exit 0),mac/Linux/ARM worker 打包悬置。 |
| 平台签名/公证零落地 | **高** | release.yml 无签名/公证步骤;tauri.conf.json 无 signCommand;updater 端点 `updates.scrollery.invalid`(.invalid 保留 TLD)占位;pubkey 为 updater-dev.key 非生产。 |
| Part7 mac 矩阵 + 证书链 T13-T16 | 中 | 全仓 workflows grep macos 零命中;release.yml 仅 windows-latest。 |
| 私有商业发布流水线未落地 | **高** | release.yml 即 oss-release(公开免费版);无私有 commercial-release workflow;smoke 仅为开机冒烟。 |
| CI 无 macOS/iOS/Android | 低 | ci.yml 仅 rust/rust-linux/frontend/smoke 四 job(全部 `if: github.repository == 'gfgjs/scrollery-private'` 守门,自托管)。移动端交叉编译/授权门/触屏交互全未验证。 |
| F-06(P1):RAW externalBin 交叉构建缺文件 | **高** | 修复(933a488/a3ad8ac)在 `codex/fix-ci-raw-worker` 分支,**未合入 dev**(git cherry 全 '+',见 §六)。 |
| 发行前置基建/法务 | 中 | 签名证书/签发机/updater 密钥入 CI secrets/支付通道/FTO 意见书/PIPL 评估——代码侧可核部分全为未就绪,外部法务资源仓内无痕迹。 |
| D-006:dev 依赖 npm audit 10 high | 低 | adm-zip 0.5.18(经 onnxruntime-node)/brace-expansion 1.1.18 锁文件仍钉脆弱版,留待裁决;不随产物分发。 |
| F-013 残余:commit-range whitespace check 未接 CI | 低 | 文件尾空行缺陷已消(762d1e2),但该门从未接线。 |
| 耐久候选 F-002~F-004 待排期 | 信息 | F-001(安装包内容断言)08-11 已落地并接 release.yml:99-100 硬门;F-002(fresh-install 四链冒烟+mac/Linux 真 build)/F-003(配置层确定性测试)/F-004(增强状态机测试)仍无。 |
| 私有仓 CI 无安装包构建 | 事实 | 私有 ci.yml smoke 仅 `tauri build --no-bundle`;MSI+NSIS 拆包硬验只在公开仓 release.yml(tag/dispatch)。 |
| worker 全链 e2e 未接 CI | 低 | worker_e2e 协议 v3 全链仅本地验证,workflows 内零引用。 |
| 自托管 runner 未服务化 | 低 | `C:\workspace\actions-runner` 存在,`sc query` 两个服务名均 1060 不存在——进程级,重启即掉,服务化待用户(需密码)。 |
| codex/fix-ci-raw-worker 9 commit 未合入 | 中 | 见 §六,含 F-06 修复与 CI 冷工作区材料化。 |

### 2.2 核心后端(backend-core)

| 项 | 严重度 | 要点与证据 |
|---|---|---|
| F-03(P1):配置写锁仅单进程 | **高** | `config/mod.rs:50` `write_lock: Mutex<()>` 进程内;`config/file.rs:381` 固定 `toml.tmp` 双实例碰撞;`config/watcher.rs:211-213` 取旧快照后 `replace_values` 不持锁、不校验磁盘版本。 |
| F-04(P1):schema 无区间约束 | **高** | `config/schema.rs:41-55` SettingKind 无 min/max;`file.rs:130` UInt 仅拒负数;`derive_batch_size=0` → LIMIT 0 停流水线,巨值 → `Vec::with_capacity` 异常分配(`derive/pipeline.rs:681`);`thumb_webp_quality=200` 持久/运行两源分裂(`ipc/config_commands.rs:375` clamp)。 |
| F-05(P1):thumb_cache_dir 空串当相对路径 | **高** | `ipc/config_commands.rs:421-432` `PathBuf::from("")` + async 内持写锁 create_dir_all(失败被 unwrap_or_default 吞)+ allow_directory 空路径放行;前端 personStore.ts 模块级 ref 只取一次不随热切换刷新。 |
| D-002/D-003/D-004/D-007 待裁簇 | 中/低 | D-002=上述配置事务/值域/路径契约;D-003=layout 30s watchdog 复位后旧 IPC 未取消、无 latest-wins 仲裁;D-004=错误消息型变体非 fail-closed(泄漏面已清);D-007=冷配置重启提示未按启动快照基准计算。 |
| 增强队列无 FIFO(F-10,重框 D-005) | 中 | `enhance/service.rs:327` 取锁前先标 Running → 多 job 同显 Running、执行序可重排;`ipc/enhance_commands.rs:268-272` 每次入队独立 spawn_blocking。07-31 审查重框为 D-005 未修。 |
| Part5 SelectAll 深迁移未完成 | 中 | `ipc/file_ops_commands.rs:186-187` copy_media_items/relocate_media_items 仍收 `Vec<i64>` 物化 id;`useGallerySelectionOps.ts:384` materializeIds;useSelection 模块级单例无多面板 scope 隔离。 |
| F-007 搜索谓词 localtime 待裁 | 中 | `db/queries/layout/query_builder.rs:251/284` 谓词仍 localtime(双重时区偏移),排序侧 :337 已改 UTC;D-013 红线「须一次显式 go」。 |
| G4 schema 债 4 项 | 低 | early.rs:399 cover_face_id 无 FK、:484 exotic_tasks 无 AUTOINCREMENT 等——有意 defer 但 decisions/ 无正式裁决文件,依据仅在 08-10 报告。 |
| Part6 ④T11 AES/DerivedSecret 后置 | 低 | enc_seed 字段存在但恒 None,变现后启用(首发 exotic-formats 无 ONNX 权重)。 |
| 备份:不可信暂存库直接 run_migrations | 中 | `backup/restore.rs:334` TODO 仍在;恶意包可携触发器在迁移 DML 期间执行攻击者 SQL;须跑全迁移套件验证后方可加固。 |
| 备份测试硬化专项未开工 | 低 | 2026-07-21 立项(U-14/U-15)仅建档;deferred 红线 #5 指纹守卫/#10 迁移加固须点名才开工。 |
| 备份 Stage 6 跨层硬化 | 低 | 阶段 1-5 已落地,Stage 6 未做;真实系统文件选择器/重启交换须 Tauri 真机。 |
| 导出 3 项语义裁决无记录 | 信息 | 整文件级取消粒度/崩溃遗留 staging 无启动清扫/磁盘满明细保留——代码默认行为均在,无用户裁决。 |
| 恢复失败「已回滚」前端提示 | 低 | 回滚逻辑已落(防 boot-loop),但启动事件→前端提示链路未做,现仅 error 日志。 |
| U-4/U-8/U-9/U-10 挂起 | 低 | 随画廊线/版本升级波/需专项/随 DB 波,维持未变(U-13 已裁决落地 08045d8,文档标注过期)。 |
| P2 余池 + 手势层测试盲区 | 低 | R4/R6/R12-R22/F 系/C 系维持池内;`useSelection.ts:44-204` 手势采集逻辑全仓无 spec(useSelection.spec.ts 不存在)。 |
| 三序统一 natural_cmp | 中 | 目录 BINARY/文件 NOCASE/画廊 NATURAL_CMP 三序并存既有债,明示独立立项未做。 |
| WebDAV 扫描器远程遍历(8B) | 低 | WebDavBackend 已抽象(netfs 门控)但 scanner 远程遍历 D3 后置未接;自定义 URI 流式代理未施工。 |
| 卷插拔 mac 即时唤醒 + DAVolumeUUID | 低 | `scanner/volume_watch.rs:13/28` 纯 15s 轮询;`volume_probe.rs` mac DAVolumeUUID 未实现(Windows 卷 GUID 已落地)。 |
| 文件系统 watcher 阶段 3 | 低 | `scanner/mod.rs:9` `// pub mod watcher; // 阶段 3` 被注释,扫描靠 fast_scan 轮询。 |
| 真 GPU 解码引擎未实现 | 低 | `engine/gpu/wic_engine.rs:5` 明示「OS 原生 CPU 解码…真 GPU 引擎 nvjpeg/dxva 尚未实现」;README「WIC GPU decode」措辞与代码不符(见 §五)。 |
| 非 Windows HEIC 无解码引擎 | 中 | `engine/mod.rs:77` 非 Windows heic 返 None(=UnsupportedFormat),待 Part3 T9 mac Image I/O。 |
| Linux/Android 线程 QoS no-op | 低 | `thumbnail/qos.rs:67/122` 暂 no-op,后续 setpriority/SCHED_BATCH;Windows(SetThreadPriority+EcoQoS)/Apple(pthread_set_qos_class)已实现。 |
| refactor_2026 Part2 残留 | 中 | ContentIdentifier 双重验证(纯 stem 配对,`scanner/live_photo.rs:114` 不守门,列已建但配对从不读它);TIFF 落单线程并发上限+header magic 预校验仍文档级「可后续加固」。 |
| 前后端分离待拍板 | 低 | design 2026-07-11:276「未立施工项,待用户拍板」;§7 八项开放问题(含 axum spike)全仓无 server 依赖痕迹。 |
| dominant_hue 等列无写入路径 | 信息 | `db/schema/early.rs:139-141` 注释明示预留(供未来颜色检索),恒 None,刻意。 |

### 2.3 前端(frontend-core)

| 项 | 严重度 | 要点与证据 |
|---|---|---|
| D-008 bundle 余量治理 | 中 | 入口 675.12kB 距 708kB 门槛 4.6%;aiStore/scanStore/faceStore 静态+动态双导入致不拆包;待统一三 store 导入边界。 |
| derivations.rs 1083 行未拆(D-454) | 低 | 按 D-454 明示跳过,真 user WIP 红线未解除,待用户裁归属;MediaGrid 3 件/ContentViewer 4 件子组件抽取留账待单独授权(scoped 后代选择器约束)。 |
| UIUX:UiField 余 5 消费者 | 低 | DocumentViewer/ReaderSettings/HGalleryLab/ReplacementPanel/SettingsView 因非同构真实视觉变更 defer;UiCheckbox 5 站点 out-of-scope;UiPopover 定位方案子项已过期(progress 已更新)。 |
| 前端动画遗留阶段 3 | 低 | query 模式/预留集/D-403..405 待裁;查看器关闭无淡出 defer 待升 Vue 版本(vuejs/core#12465);§P5 5 条 GUI 手测。 |
| J3-J6 部分未落地 | 中 | J5/J6 明确未应用、J3 audio/doc 死角仍在;J4 及 J3 视频部分已随 af6bf97 落地(文档未区分)。 |
| 顶栏 Phase3/5 | 中 | ContextualToolbar 自身溢出接线未接、数据命令(需 id)与默认呈现集未实现;Phase 6(自定义工具栏+OS shell 集成)为明示预留。 |
| [profile.lite] 未接线 | 低 | 定义存在但零构建路径调用,免费版 release 走 release profile,<10MB 裁剪决策悬置。 |

### 2.4 视频(video)

| 项 | 严重度 | 要点与证据 |
|---|---|---|
| F-029 video-worker 进程隔离二期 | 信息 | 未开工(seek 不设护栏代码注释明示 defer),启动须点名立项。 |
| 视频格式扩展未解项 | 中 | faststart 产物级深检、worker 侧 fingerprint 核对、H2b-prod release 验证(即 video-worker 闭包)、error.rs tracing 竞态 flake(未复现维持已知)。V1-V8 已合 dev(821c290)。 |
| 播放器二期 remux/libmpv/MF 原生解码 | 信息 | 全仓 grep FfmpegBackend/AVFoundation 仅注释命中,零实现;立项留待用户裁决(D-407 已登记)。 |
| ffmpeg/AVFoundation 后端 | 低 | `Cargo.toml:267` `ffmpeg = []` 空 feature 仅边界;实际落地后端=MediaFoundation(Windows)。 |
| 悬停重视频判据未并 codec 轴 | 低 | 判据仅像素/体积,phase-2 TODO 待 Rust 铺 video_codec 列。 |
| MF 诊断测试 #[ignore] | 低 | diag_pipeline_env 需 VIDEO_DIAG 环境变量手跑,不进 CI。 |
| 音频查看器 Space 键位 | 信息 | AudioPlayer.vue 无 keydown 处理,与查看器统一为后续项。 |
| video_meta 专用轻量查询 | 信息 | load_video_subtitle 复用偏重查询,专用查询超出修复范围留后续。 |

### 2.5 增强(enhance)

| 项 | 严重度 | 要点与证据 |
|---|---|---|
| 模型托管仓名未定(P0 批 6) | **高** | `enhance/registry.rs:19-20` `TODO(批6): 用户 HuggingFace/GitHub 仓名定案后回填` + `PENDING_USER_REPO` 占位;:35-48 manifest_ready 恒 fail-closed(sha256=None/size=0),模型无法真实下载。批 1-5.5 已全落地。 |
| 队列 FIFO(F-10) | 中 | 见 §2.2。 |
| 会话关闭单测被注释 | 低 | enhance-worker 两个错 id 分支单测整块注释,缺真 EnhanceSessionState helper(EnhanceSessionClose 幂等单测仍在)。 |
| 前端「体量待定」占位 | 低 | 后端未下发模型体积,前端真实占位,待批 6 回填。 |

### 2.6 RAW

| 项 | 严重度 | 要点与证据 |
|---|---|---|
| 阶段 D/E 二期未做 | 中 | D(大图预览,Capability::Preview 不存在,`fe-raw-preview-undecoded`:仅提示不可预览)/E(移动端 in-process)零施工。阶段 A/B/C 已闭合。 |
| tail:CI gnu job 缺位 | 中 | 样张厂商实测(仓内零 CR2/NEF 样张)/mac-Linux 交叉编译/GUI 真机验收均未做。 |
| F-06 交叉构建 | **高** | 见 §2.1(codex 分支)。 |

### 2.7 缩略图/缓存(thumbnail-cache)

| 项 | 严重度 | 要点与证据 |
|---|---|---|
| 60px 极密多档缩略图源未做 | 中 | `mediaGridCanvas.helpers.ts:276` 高度桶为解码期预缩放,`useCanvasThumbPipeline.ts:103` 仅单一 thumbPath 源;无按 max(w,h)×DPR 选 120/240/480 档回退。实施前须 A/B。 |
| C3/C4 池未做 | 中 | C4:领取查询 `ORDER BY created_at DESC`(`thumbnail.rs:45/57`)而 idx_media_thumb/idx_exotic_tasks_ready 均不含 created_at;C3:驱逐复位 thumb_path IN 全表扫无索引(`thumbnail.rs:158-172`)。 |
| 缓存 GC 全量重建即时触发(T6) | 低 | 仅启动 5 分钟首跑+24h 周期,无全量重建完成事件接线。 |
| EXIF 命中率日志 + P1-14 坐标恢复 | 低 | 无埋点;引擎切换 scrollCache 坐标恢复无代码,尚需定位修复。 |
| mac Image I/O 引擎(Part3 T9) | 中 | 非 Windows HEIC 无解码引擎,跨平台缺口。 |
| 文档缩略图叠加二期 | 中 | D-C/D-D 待裁;findings 列 7 处后端+3 处 UI 改动点全未实施(`document_meta` 无标题列、get_media_meta_batch 无 JOIN、buildThumbInfoLines 无标题元素);零期(文件名文本卡)已可用。 |

### 2.8 画廊(gallery)

| 项 | 严重度 | 要点与证据 |
|---|---|---|
| 极密 canvas 去留定案 | 中 | G-T3(decoding=async)DOM 路径未做(仅 loading=lazy);真机探针(canvas vs DOM FPS+selectEnterMs)未跑。 |
| 「拖滑块一闪一闪」根因悬置 | 信息 | d532140 回退 + 3a51302 重证,media_enriched 事件链未撤;用户 fork 分离变量实验未回结论。 |
| 密度带 P4 paint timing 实测 | 低 | P1-P3 已落地,P4=真机并排实测未做。 |

### 2.9 OCR/AI

| 项 | 严重度 | 要点与证据 |
|---|---|---|
| OCR swap_rb 未定案 | **高** | `ocr_profile.rs:73/107/129` swap_rb:true 仍在;ocr_golden(#[ignore],需 OCR_MODELS_DIR+fixture,fixtures/ocr/ 仅 README「待落位」)/ocr_bench 未跑;PINNED_ASSETS 过渡源(全 ModelScope)待换自有仓;定案前管线不宣称可用。 |
| 首次语义搜索结果不显示:仅诊断 | 中 | LayoutNotReady 仍粘成 error、同窗不重试;修复与受控重试回归测试均未做。 |
| AI 批 3 性能/批 4 功能 | 中 | 批 3 待真机大库取数(P7=A14 超时缩放);批 4 缺失面=阈值 UI(仅后端 face_same_threshold 键)、以图搜图、逐项相似度(人物墙/人脸框审批已存在,文档措辞过时)。 |
| SCRFD+ArcFace 非商用轨对拍 | 低 | tools/scrfd_crosscheck.py 脚本头无 ✓ 记录(对比 face.rs:10-19 F8 已记录);scrfd onnx 未下载到本地。 |
| CLIP F8 GPU 对拍 | 低 | 文本塔已强制 CPU 缓解(engine.rs:393-400);CNN 在 GPU 上的正确性留待 F8 参考实现验证。 |
| h-14 架构导出 | 低 | 本地仅 export_clip_onnx.py,远端导出状态待定。 |
| 人物墙封面 bbox 裁剪 | 低 | 仅脸中心映射 background-position,v1 近似。 |
| OCR quad 高亮二期 | 低 | 后端 OcrLineDto 带 quad 但前端无组件消费。 |
| OCR 二期划走 | 低 | 结果持久化/批量任务化/搜索接入/DirectML/大图 tile 零实现;storeUrl 占位随域名统一替换(仓既有)。 |
| F-002 SigLIP2 双轨候补 / F-003 DirectML 冻结观察 | 低 | 引擎可插拔成立,SigLIP2 未落地;DirectML 入 sustained engineering 维护冻结未处置。 |

### 2.10 阅读器(reader)

| 项 | 严重度 | 要点与证据 |
|---|---|---|
| R5 字体包下载位 | 中 | BookReader 无字体下载(重基建,复用模型库下载);08-10 报告「竖排 logical 联动未实现」**证伪**(readerStyles.ts:100/128 logical 属性已落,R5-b bb15cd8)。 |
| R6 后置池 | 中 | 高亮笔记/信息条/统计/TTS/双页/插件 text sink(mobi/azw3)/规则编辑器/AI 问书零施工;但「仿真翻页未起」证伪(R2 已交付,BookReader.vue:95/258-263)。 |
| txt blob URL 竞态 | 低 | syntheticBook.ts:112-118(行号已漂移,08-10 引 :79)check-then-act 无锁;触发需并发双请求同章,未证实为实际路径,defer。 |
| loc1 未接入实时阅读 | 中 | 编解码+三级恢复已落地并单测,实时路径仍 cfi: 前缀,列已预留。 |
| 校对本地实现(D5) | 低 | 仅远程 OpenAI 兼容端点,本地 Ollama/进程内模型按决策 D5 待调研。 |

### 2.11 色彩/主题/沉浸(icc/theme/immersive)

| 项 | 严重度 | 要点与证据 |
|---|---|---|
| ICC A2:WIC 出 ICC 未做 | 低 | 非 WIC 路径 image_rs.rs:42 已出 ICC;heic/avif 经 WIC 仍 icc=None 按 sRGB,色偏留 A2。 |
| ICC B2:16-bit/f32 位深分派(D-420) | 低 | color.rs:38-39 docstring 声称原位深 CMS 仅对有 ICC 源成立,无 ICC 分支仍先量化后 CMS,精度损失。 |
| 主题 badge 白字压彩底 | 低 | badge-document 白字对 #ff9f0a 对比约 2.04,非纯 token 可解(组件级排版方案)。 |
| 主题来源 'external' 预留 | 低 | 六主题全 source:'builtin'(registry.ts:27-70),主题商店/本地导入为文档化预留。 |

### 2.12 插件/商业化(plugin-exotic)

| 项 | 严重度 | 要点与证据 |
|---|---|---|
| 生产公钥 ceremony 未跑 | **高** | `scrollery-pro/resources/exotic-keyset-prod.json:3` 🔴 PLACEHOLDER(key_id=release-prod-placeholder);注入通道已就绪(build.rs:28-62 PICASA_EXOTIC_KEYSET_FILE fail-fast)。唯一剩余动作=用户离线机跑 ceremony。 |
| Part8 D1-D15 全零 | **高** | 无 License 签发服务端/支付/上架码/层1 加密权重/层1b 设备绑定/层2 在线激活;D13 官网已建成且已推(scrollery-site 仓与 origin/main 同步,「待 push」过期)。 |
| MS Store/Steam 渠道为 fail-closed 桩 | 中 | channel_stubs.rs:4-6 恒 Unlicensed/ActivationUnsupported,真实 StoreContext/Steam DLC 归 Part8 D5-D8。 |
| PSD 进度硬编码/RAW 计数未单列 | 中 | exotic_commands.rs 由字面 "psd" 换成 PSD_PLUGIN_ID 常量,功能上同样硬编码;RAW 任务计数未单列。 |
| 插件下载进度回调/FFmpeg registry 签发/non-Win worker 优先级 | 低 | 各归 Part8/Part4;引擎 Range/镜像回退已具备,仅缺接线。 |
| CDN 生产域名占位 | 中 | 默认基址 .invalid;「安装/购买链路端到端未实测」子句过期(内测发行链已 E2E)。 |

### 2.13 工具/产品决策/文档治理(other/docs-process/tools)

| 项 | 严重度 | 要点与证据 |
|---|---|---|
| R2-7 剩余:main 提升/FTO/域名 | 中 | 产品名+identifier 代码侧已锁定;占位域名与 FTO 意见书为外部动作,main 提升 git 可证未做。 |
| free 社区版最短路径 | 中 | 硬阻断=CHANGELOG 无 [0.1.0] 切分+公开仓打 tag 未做;F-002 自动更新/F-003 阅读器划线两决策待表态(划线零代码)。 |
| 07-13 综合 Review 9 裁决 | 低 | Windows-first/核心画像/XMP 写回/Free-Pro 边界/AI 默认 opt-in 等无裁决文件落盘(部分 de facto 已定)。 |
| F-008 双综合报告归属待裁 | 中 | 本线终版 vs ChatGPT 镜像版并存,单源 status/新立项视角全面Review.md,无拍板。 |
| 第 18 章关键问题拍板+另行立项 | 中 | 门控待决。 |
| runbooks 四文件带日期违规 | 低 | 违反 README「不带日期、名即主题」,改名待裁决。 |
| 16 篇 as-built 待主线提交 | 低 | 四轮复核零 P0/P1,正文已提交 c20af3f,status 文档未回写。 |
| Spec15 计数错误 | 低 | Spec00:248「16 篇」→应为 17、asbuilt-spec.md:11 计数——commit 落了但计数原样保留。 |
| docs 门 frontmatter 挂账 9 文件 | 低 | 日志线 5 文件+图片编辑线 3 文件 4 处,转各线收口自办,未代修。 |
| check_plan_canonical.mjs 孤立脚本 | 低 | 仍存 tools/ 但无 workflow 引用(canonical 门 B5 已删)。 |
| todo.md P4 行门禁列表含已退役 check_docs | 低 | 455 行仍把退役脚本列入八门,文档自身诚实标注「本地非 CI」但列表过时。 |
| 拆分方案线收口归档未做 | 低 | 阶段 6 终审+commit/归档待用户裁。 |
| 施工规格集留置 5 项 | 低 | #4 CSP 缺 tauri: scheme(见 §2.13 下)、restore.rs #3 已核销,#5/#6/#7/#10 他线/Copybara 风险无裁决记录。 |
| CSP 平台分档未落 | 中 | `tauri.conf.json:44` 单串全平台统一,仅 http://*.localhost,无 tauri: scheme、无平台 conf 变体——与 CLAUDE.md 硬约束(macOS/iOS/Linux 必须含 tauri:)存在差距;mac 侧是否真炸须平台验证。 |
| quick 增量扫描前端未接线 | 中 | 后端 `scan_commands.rs:534` quick 选项就绪默认 false;`scanStore.ts:259-265` START_SCAN 不传 quick,无 UI。 |
| designs/ 目录 12 份开放设计未全覆盖 | 低 | 顶栏重构/阅读器完善/DOM 膨胀复核/极密网格/plan-docs/UIUX 审计/queries 拆分/后端拆分/格式筛选/图片编辑升级/日志重构/前后端分离——部分与 planning 线同源,未逐一合并对码。 |
| 移动端代码整体缺失 | 中 | CLAUDE.md 声称四平台,但仓内无 src-tauri/mobile、无 gen/android|ios、tauri.conf 零 mobile 配置、无移动 runner;仅剩 UnsupportedPlatform 拒绝契约(exotic/mod.rs:62、reveal.rs:54)。移动壳在前端分离设计 §6 阶段 3 列为未来路线。 |
| ESC 全局兜底(D-001) | 中 | 自 2026-07-17 无裁决记录;ESC 已被五处强消费,方案存批量 15 项清单。 |
| pwf 插件本体待用户禁用 | 低 | 全局插件禁用是用户操作;.planning/ 已删故 hook 注入已停。 |
| 官网真机走查 | 低 | 展卷手感/暗色滚轴/读屏/Lighthouse 须真机浏览器(代码侧无法判)。 |
| 旧 Claude 项目数据副本清理 | 低 | 路径已不在原处,可能迁往别处,留用户确认。 |

---

## 三、文档与代码不符(文档过期,代码已订正)

> 判决 = DONE_IN_CODE。**这些条目文档仍标「未完成/未提交/未推」,代码实已完成**——是本次核证对文档体系的主要修正面。

| 条目 | 文档说法 | 代码实况(证据) |
|---|---|---|
| 07-31 第三轮深审直修「未提交,保留施工态」(todo.md:17) | 未提交 | 已入 dev `504f209`;仅 planning 三件套未获收口授权(文档状态非代码)。 |
| 视频格式扩展「待合并 dev」(todo.md:37) | 待合并 | merge `821c290` 已合入 dev(且 08-10 报告:176 已核 821c290 在 origin/dev);仅 GUI 手测未做。 |
| 缩略图档位重定「本地改毕待提交」(todo.md:43) | 待提交 | commit `b554aa5` 已存在且全链落地。 |
| 日志 S1-S6「唯一遗留=批准后 push」(todo.md:59) | 未推 | 六 commit 均已是 origin/dev 祖先,早已推送。 |
| AI/face 根治「commit 未 push」(todo.md:67) | 未推 | 五 commit 已推 origin/dev。 |
| 图片编辑 v2「全部改动未 push」(todo.md:63) | 未推 | 六 commit 均在 origin/dev。 |
| 07-25 深审「13 项全部仍成立均未修」 | 均未修 | F-01 半修(ai-worker 闭包 08-11 收口)/F-02/F-07/F-09/F-11/F-12 已由 504f209 修;F-03/F-04/F-05/F-06/F-10/F-13 半修或未修(§二)。 |
| S 线审查修复「三批待用户 go + R-07 二选一」(rv-16-sline) | 待 go | 三批全落地(2026-07-16),R-07 已采修法 a(parentDirectoryId+spec)。 |
| Windows 卷稳定 ID「尚待落地」(Spec02:119) | 待落地 | volume_probe.rs PlatformVolumeResolver 已实现并接线 add_scan_root(仅 mac DAVolumeUUID 未做)。 |
| D-C 混池驱逐「未裁」(memory) | 未裁 | cache.rs:303-307 内嵌 2026-07-19 裁决已实现(维持 mtime+10GB 预算)。 |
| 目录排序 B-file-iii「待显式 go」 | 待 go | 三阶段已落地(全局 rank+UTC 桶);真实遗留仅 B-file-ii(D-013)与 F-007 谓词。 |
| 文件树格式筛选 P1-b「未动」 | 未动 | 9166d3d/549861c 已落地(FS 适配器+三态菜单+持久化+reveal);P2/P3 也已落地。 |
| OCR「下载源尚未上线」(fe-ocr-manifest-unready) | 未上线 | PINNED_ASSETS 2026-07-23 已钉定主源/镜像 URL+实测 sha256,两档 manifestReady=true。 |
| 超长拆分「纯方案未施工」(memory) | 未施工 | 当日施工线即立项并收官(ba3abde..fb7fe57)。 |
| uiStore「无 spec 基建」(memory logging 线) | 无 | 2026-07-24 已建 uiStore.spec.ts。 |
| CDP 基准脚本「存 scratchpad 未入仓」 | 未入仓 | scripts/bench/ 已在仓。 |
| fmt 5 文件「持续红」(2026-07-21 文档) | 红 | `cargo fmt --all --check` 实测 exit 0,已转绿(762d1e2)。 |
| 官网/播放器/深审首二轮「待批 push」 | 未推 | 全部已推 origin/dev(或 origin/main);当前真正未推的只有 dev 领先的 17 commit(§六)。 |
| 施工规格集 16 篇「待主线批 commit」 | 待提交 | c20af3f 已提交,仅 progress/status 未回写。 |
| 快照处置「未决」 | 未决 | 已由后续提交跟踪入库(而非 ignore/删除)。 |
| 注释清理「五 commit 未推」 | 未推 | 已全部进 origin/dev。 |
| 图片编辑 frontmatter「3 文件 4 处红」 | 红 | designs/lines 已改 status:现行;reviews 用 status:快照属合法枚举。 |
| Q9 USE_PIPELINE「待真机实测」 | 待测 | 2026-07-10 已真机 A/B 定案,运行时开关全链退役。 |
| 「隐藏根排除」静态谓词 V21 后续 | 后续 | roots.rs:74 注释过期——谓词已全接入缩略图/AI/人脸排空流水线。 |
| 增强后端「存在未实现路径」(fe-ipc 旧注释) | 未实现 | enhance_preview 已完整实现(service.rs:646-837);前端 mapping 已映射。 |
| check_docs/check_docs_index「现役」 | 现役 | 均已从 tools/ 物理移除;索引门迁 worklog-kit(docs-governance.yml)。 |
| 降噪线「J-1..J-8 待裁、未施工」(lines) | 待裁 | design.md J 节 2026-07-24 已全裁,P0 批 1-5.5 已施工;唯一真未决=J-2 仓名。 |

**规律**:「未 push/待提交」类声明随 2026-08 前各线 push 批量过期;07-25 报告作为「当前 HEAD 快照」被后续 504f209/2564dc1 等部分推翻而未回写。todo.md frontmatter「更新日期 2026-07-02」与实际增补(至 2026-08-11)严重不符,头部块非严格时间序。

---

## 四、已裁/刻意保留(非未完成,勿重开)

> 判决 = INTENTIONAL。这些是已裁决的终态或明示 defer,重开须新决策。按红线纪律仅列名。

- **红线裁决类**:C-1 沉浸态轴钮不可达、R-5 指针停线性滚动位(2026-07-25 用户裁定,注释钉定);J1/J10 残留窗已裁接受;G4 有意 defer 汇总(AES/设备绑定/mac 实功能/MediaGrid batch-ops/Part2 T9·T14/Part1 schema 债);Part4 T19 mac defer;Part1 T9/T11 dormant(触发条件=50 万向量);注释清理二轮「已裁暂不做仅留账」(515 行/84 文件+99 自指路径头,数已测);渠道四项有据 defer(Steam/MSIX/直销/DRM,重评触发条件已写死);WAL 慢源测量暂不施工;U-P1-b 可选重测。
- **刻意设计边界**:VideoPlayable 派生 kind 路由 not_implemented(is_implemented=false 门控,永不入队);heic/raw 引擎模块 Phase 2 注释保留(系统层已有替代路径:RAW 经 exotic raw-worker、HEIC/AVIF 经 WIC);ass/ssa 内嵌字幕固定提示文案;canvas 画廊/时间轴为实验原型(capable 门控自动回退 DOM);SemanticSearch 纯 SQL 解析显式拒绝;SelectAll 深迁移 T18 后续;OS shell 直达预留(路由以 :id 为主);文件移动合并 v1 不支持(稳定行为);OCR 资产过渡源(用户明示后续换自有仓);RAW 不支持增强(enhance_input_unsupported,design J-8 自注 P0 RAW 链未通);动画 WebP/音频封面 ICC 假定 sRGB;编辑不支持格式显式拒绝;编辑峰值内存常量为工程判断待设备预算;校对面板仅「全部接受」;loc1 精密重锚后续增强;pager 扩展点预留;文本索引 dead_code 字段留日志/规则编辑器;主题 external 源预留;exotic 'ms_store'/'steam' 渠道预留;metadata/text capability 首发只交付 thumbnail;harness 未覆盖 IPC 兜底 console.warn(dev 专用);fe-selection roving 需新基建;ESC 兜底隐藏留档(D-001 裁决);thumbhash 金标/OCR golden/bench/dev-registry/internal-registry #[ignore] 门控、ORT 依赖测试 J16 裁决不进 CI;Linux CI 范围(仅 check+test)为刻意决策;未知 kind 字符串保持处理中;fs 卷 15s 轮询为 v1 既定主机制。
- **审计方法论提醒**:本节条目**不得**当作待办报回用户(违反红线会污染后续工作)。

---

## 五、仅真机/GUI/外部可验(⏸ 积压)

> 判决 = CANNOT_VERIFY_CODE(50 条)。代码侧经核证**已就绪**,唯一剩余=真机验收或外部动作。按线分组列名,明细在各线 progress。

- **画廊/时间轴**:画廊重排滚动恢复、无缝 minimap 轴、minimap 渲染模式、轴解耦 R-5 三查、目录排序 40 万库、文件树 40 万库复测、极密 canvas 探针、主页 canvas perfProbe 对比、时间轴 T0、密度带 P4、iOS canvas(未出包不可证)。
- **视频**:视频格式扩展 GUI 10 项(HEVC 商店 productid 核实)、自研播放器 12 项(crossorigin 首验)、封面关键帧手感 3 项、AI/face 30s 超时路径真机未 exercise。
- **OCR/AI**:GUI 六项、AI/face GUI 终证(U1/U2/U3·G1/F10/K1+加固 5 项)、OCR swap_rb 真机识别质量定案(golden 对拍前置)、批 3 性能大库取数。
- **编辑/导出/备份**:编辑 v2 三感(Cropper 触摸/广色域目视/50MP 帧率)、导出 8 步验收、备份真机(系统选择器/重启交换)、备份/恢复端到端。
- **阅读器/文档**:R5 竖排真机矩阵(WebView2+CJK 字形)、md 内存爆炸真机、阅读器 GUI。
- **窗口/沉浸**:窗口化开关手感、F11 让位观感、顶栏 Win10/11 三键/mac 红绿灯/ACL 拒绝态、Phase G 折叠观感。
- **设置/日志**:配置重构 GUI、缩略图进度 4 步、缓存统计行、日志窗口 TopN 出行、uiStore hydration 人工走查。
- **前端 UI**:UI 加载/关闭链路、S 线三态菜单、文件行拖拽手感、超长拆分 GUI 清单、J7/J13/J14/J11、UIUX per-view 逐视图、动画 §P5、前端动画 5 条。
- **官网/外部**:官网真机走查 4 项、FTO 三辖区委托、旧数据副本清理、Lap 竞品跟踪(需联网)、性能面板 Release 静默基准。
- **质量池**:R6/F5/F9/C41/C43/C45/C46/C47 仍成立但需拍板或实测。

---

## 六、git 状态(实测)

- **分支拓扑**:main(`b1cba5de`,2026-07-06)是 dev 的**严格祖先**(dev..main=0);dev 领先 main **1001** commit。工作树干净:仅 `.gitignore` 改动(删 3 行),无 untracked/无 stash/单 worktree。
- **dev 领先 origin/dev 17 commit 未推**,三主题:① docs 收口批(2f13d1a/d3d1c2d/475cb5a/897324d/8cf2d21/e534110/a00e213/d9a0451);② 前端修复批(a9bb5fb/cb459c4/2940600/0c9ac89/ac07f9f);③ 代码/构建单点(080aa9e/2564dc1/fb18a53/504f209)——**含 504f209(第三轮深审直修)与 2564dc1(ai-worker 发货修复),这两笔属未推的高价值修复**。
- **codex/fix-ci-raw-worker**:与 dev 双向分叉(领先 dev 9 / 落后 dev 17),git cherry dev 9 条全 '+' **未合入**。内容:raw-worker CI 冷工作区材料化(933a488/ca3cf43)、非 Windows 打包守卫 F-7(a3ad8ac,即 F-06 的修复)、runbook 实证 3 条、NOTICE 重生成、alignment-grid 测试对齐、ai-worker 构建修复 d7f84cf(与 dev 2564dc1 同主题独立实现,patch 不同)。**合入时有双实现冲突需调**。
- WIP 检索:13 条命中全部已在 dev,无未合入 WIP 分支。

---

## 七、审计覆盖与盲区

**扫描范围**:todo.md 全文 510 行、reviews/ 14 份、planning/ 55 目录、worklogs/ 18 目录、lines+status 135 份、decisions 7 份、spec 17 篇、src-tauri+crates 全部 .rs、src 全部 477 个 .ts/.vue、6 个 workflow、36 条记忆线、git 全量考古。

**完整性批评补漏 6 条(均已核证)**:
1. docs/refactor_2026/(README 称「architecture of record」)此前未被任何路扫描——内含 Part2 残留(ContentIdentifier 双重验证/TIFF 加固)。
2. 卷插拔 mac DAVolumeUUID + WM_DEVICECHANGE 即时唤醒(Windows GUID 已落,两笔在途)。
3. docs/designs/ 顶层 18 份中 12 份带开放标记,未逐一与 planning 线合并对码。
4. 前后端分离设计的开放决策面(§6 阶段 0-2+§7 八项开放问题)。
5. **移动端代码整体缺失**(产品范围声称四平台 vs 仓内零移动目标)。
6. README/CHANGELOG 声称能力未反向对码——实测发现 1 处活漂移:README「WIC GPU decode」vs `wic_engine.rs:5`「OS 原生 CPU 解码,真 GPU 引擎 nvjpeg/dxva 尚未实现」;CHANGELOG「色标」例证被证伪(已端到端实现)。

**盲区声明(不声称已覆盖)**:真机/GUI 行为(§五 50 条)、外部法务/注册类动作(FTO/域名/证书购买/HF 仓名)、CI 远端实跑状态(仅静态读 workflow 文件)、07-06/07-10 审查 P2 池逐条重对(仅抽样复核)、vendor 目录、归档记忆(archive/)、公开仓 sync-oss 同步机制的实际运行。

---

## 八、与 2026-08-10 盘点报告的关系

本报告是 `2026-08-10-中断工作盘点与核验.md` 的**代码核证后继版**:08-10 报告 165 候选(confirmed 154/stale 10)中绝大多数 confirmed 项在本轮复核下仍然成立(180 条 CONFIRMED 中有大量与其重叠),但:
- 其 ⑥「声明与实际不符」16 条 + 本轮的 50 条 DONE_IN_CODE,证明**盘点类文档自身也在快速过期**(尤其在 push 状态、07-25 finding 修复状态两面上)。
- 本轮新增其未覆盖面:designs/、refactor_2026/、移动端缺失、README 声称漂移、CI workflow 全量静态审计、14 条批次补核(fmt 现绿等)。
- 08-10 报告 :176/:177 对「ahead 17 未推」的 stale 标注已被本轮 git 实测证实(ahead 17 是 08-04 之后的真实状态,但其中仅 504f209 等 4 笔含代码,其余为 docs)。

---

## 九、建议处置顺序(供决策,未施工)

1. **用户侧外部动作**(非代码,卡链最久):① 生产公钥 ceremony(半小时);② 增强模型 HF/GitHub 仓名;③ FTO 委托+域名;④ runner 服务化(密码);⑤ codex 分支 9 commit 合入裁决。
2. **待裁决簇集中清**:D-001..D-008(F-01 残余/D-002 配置/D-003 layout/D-004 错误/D-005 FIFO/D-006 audit/D-007 重启提示/D-008 bundle)、D-C/D-D(文档缩略图)、D-013(B-file-ii)、D-403..405(动画)、D-454(derivations 归属)、ESC D-001、J-2 外其余 enhance J 项、导出 3 语义项、U-4/8/9/10。
3. **代码修复**(裁决后即可开工):F-03/F-04/F-05(配置 P1,`config/` 域)、F-10(D-005)、F-006(合 codex 修复)、CSP 平台分档(CLAUDE.md 硬约束差距)、enhance/video worker externalBin(F-01 残余,等批 6)、C3/C4 索引。
4. **文档回写**:todo.md frontmatter 更新日期、§三 50 条过期状态订正、Spec00:248/asbuilt 计数、P4 行退役门禁、08-10 报告 stale 标记回传。**建议由用户确认后另行批量执行**。