---
id: 2026-07-05-完成度审查快照
status: snapshot
type: review
line: 完成度审查(H)
created: 2026-07-05
---

# Picasa Next · 完成度审查快照(2026-07-05)

> **快照日期**:2026-07-05 · **审查基线**:dev 分支工作树,HEAD `9170dc7`(工作树干净、与上游同步)。
> **方法**:源码 / 配置 / 脚本静态核实 + 本地实跑取证(`cargo check/test`、`vue-tsc`、`vitest`、`cargo tree`),**不采信文档 / 注释 / commit message**;5 路只读 agent 按子系统互斥分区对账,orchestrator 独立复核关键结论。
> **时效声明**:本文为某一时刻的完成度快照。其结论被后续工作推翻时,须回头在文首补「已过时 / 被推翻项」标注(承接 CLAUDE.md「快照类文档须带时效」)。滚动现状以 [todo.md](../todo.md) 为唯一权威源;本文是取证附录。

---

## 0. 结论先行

项目工程质量与文档诚实度都**很高**:实跑测试数与 todo.md 声称逐字吻合,声称 ✅ 的项几乎全部证实为真实落地、无空壳 / 桩冒充。**「未完成」绝大多数是文档已如实标记的商业化 / 发布 / 闭源下沉几大块**(受 R2-7 产品名死锁与外部密钥 / 证书基建阻塞),而非隐藏的施工欠债。真正需要注意的暗坑只有 §3 的 **7 处文档 / 注释偏差**——其中 thumbhash 误标(#1)有误删风险、pro 注释红线描述过时(#3)有安全语义。

---

## 1. 独立取证结果(客观信号,非文档转述)

| 取证项 | 命令 / 方法 | 结果 |
|---|---|---|
| workspace 编译 | `cargo check --workspace` | ✅ **exit 0**,9 crate 全过 |
| 后端测试套件 | `cargo test --workspace` | ✅ **exit 0**,~464 测试 0 失败(主包 387 passed + 4 ignored) |
| 前端类型 + 单测 | `vue-tsc --noEmit` + `vitest run` | ✅ **exit 0**,207 测试全过(15 文件) |
| 去 ort 彻底性 | `cargo tree -p picasa-next -e normal` | ✅ 主包零 ort/tokenizers/ndarray,仅 ai-worker 载 ort |
| 后端未完成标记 | grep `TODO/FIXME/unimplemented!/todo!` | ✅ **零命中** |
| 项目活跃度 | `git rev-list --count HEAD` | 670 commit(近 30 天 599) |
| in-flight 工作 | `git status` | 干净,无半成品未提交 |

`#[ignore]` 测试仅 5 个:2 个性能基准(`bench_layout_1m`、`bench_canonical_fat_table_1m`)+ 3 个依赖外部 fixture 的 registry harness,均有文档说明,非遗漏。

---

## 2. 真正未完成的工作(按阻塞优先级)

### 🔴 第一梯队 · 全局死锁(你侧决策,代码侧做不了)
- **R2-7 产品名拍板 + 三辖区商标 FTO(US/EU/CN)+ tauri identifier 锁定**。前置 Part7-T18 / Part8 D10-D13 / 官网 / 全部上架。拖延成本单调上升。

### 🟠 第二梯队 · ③b 闭源物理下沉(技术阻塞已解除,待变现节奏 + 密钥基建)
源码证实**全部 5 项确未做**:
1. `KeyringLicenseStore` 仍在 `src-tauri/src/exotic/license.rs:50`,未迁 pro、未删源;pro 侧 `DirectEntitlement` 仅为**对等骨架**(`pro/src/lib.rs:10` 自述「非最终形态」)。
2. 生产公钥仍为**占位**(`crates/picasa-next-pro/resources/exotic-keyset-prod.json:3` 明标 PLACEHOLDER)——需离线 / HSM 签发机。替换机制已就位(exotic-trust `build.rs` 编译期注入,已被内测键实跑验证)。
3. 开源默认是否由 KeyringLicenseStore 切 `FreeStub`——待拍板。
4. `exotic_commands` 激活 IPC 随 KeyringLicenseStore 一并迁移。
5. pro 入 workspace members 并用 `BEGIN/END-INTERNAL` 包裹 member 行(标记块已存在于 `Cargo.toml:12-20` 但仅含注释,pro 尚不在 members)。

### 🟠 第三梯队 · Part8 商业化与分发(代码侧真全零)
源码证实 Cargo.lock 无 stripe/paddle/steamworks/msix,channel_stubs 确为 fail-closed 桩、Steam 自检仅 `tracing::info!` 占位。D1–D15 全部待做:签发端(D1)/ 加密打包 picasa-pack(D3 ← T11 AES)/ 激活微服务(D4,可选)/ MsStore+Steam Provider 实装(D5-D8)/ 支付 + webhook 生产硬化(D9)/ 改名门禁 CI(D10)/ MSIX+Steam 上架(D11-D12)/ 官网落地页(D13)/ 获客(D14)。**强前置** = Part6-T13 + Part7 T10-T12(均已 ✅)+ R2-7 产品名。

### 🟡 第四梯队 · Part7 证书链 + mac 矩阵(需外部资源)
源码证实**全仓零签名 / 公证配置**(grep `signCommand/notarytool/codesign/TAURI_SIGNING_PRIVATE_KEY/Developer ID` 零命中,ci.yml 无 `macos-latest`):
- T13 Win Authenticode / T14 mac codesign+notarize(需证书)
- T15 worker 签名产线 · T16 commercial release 流水线(需 T13-15 证书链)
- mac 矩阵(← Part3-T8 mac 门控)

### 🟢 第五梯队 · 后置 / dormant(有据 defer,**勿误重启**)
- **Part6-T11 AES 解密 / DerivedSecret**:crypto.rs 仅 Ed25519 验签,`enc_seed` 恒 None,ai-worker Named 共享内存通道协议级拒绝直到 AES 启用(`ai-worker/src/session.rs:143`)。变现后随首个加密权重插件。
- **Part1-T9 simsimd / T11 ANN 实装**:VectorStore dormant(暴力 cosine 有主,`ANN_THRESHOLD=500_000`),现库 1554 向量、暴力打分毫秒级,实装属 YAGNI。simsimd/usearch/sqlite-vec 确未入依赖树。
- **mac 实功能**(Part3-T8 门控 / T9 objc2 后端 / Part4-T19 mac worker):非 Windows 无 HEIC 引擎(测试锁定),fast-follow。

### 🔵 第六梯队 · 小残留 + 实验(低风险)
- **Part5 SelectAll 深迁移**:收藏 / 评分 / 色签 5 写命令已迁 SelectionDescriptor,但**删除 / 移动 / 复制 / 加收藏夹仍物化 explicit ids**(暂存置灰 / 撤销闭包需具体 id),后端 `relocate_media_items/copy_media_items` 仍收 `Vec<i64>`(`file_ops_commands.rs:143`)。
- 语义面板接入后的多面板 scope 隔离(依赖语义面板落地)。
- **H-Lab 横向画廊毕业收编**:源码证实**零生产渲染耦合**(仅一个工具栏烧瓶入口 + 独立缓存字段);滚动器合并 / 坐标压缩移植 / 选择接回**均未做**,阻塞于真人调研结论。

---

## 3. 发现的文档 / 代码偏差(「不可轻信」的实锤,均已独立复核)

按风险排序——多为**注释 / 文档滞后于已推翻的旧设计**(违 CLAUDE.md「正文回写红线」):

| # | 偏差 | 位置 | 风险 |
|---|---|---|---|
| 1 | **thumbhash 被误标为「残缺 stub 全仓零消费」**,实为完整 + 被消费特性(后端 `generate_thumbhash` 接入 generator/exif_thumb/exotic sink 4 派生路径 + 前端 10 文件消费);真实残留仅**前端 JS 解码器**是仅亮度近似(跳过 P/Q/alpha AC 系数,色彩保真降级但功能可用) | `todo.md:798`(已回写) vs `src-tauri/src/thumbnail/thumbhash.rs:21` | **高**:据「修 vs 删」可能误删在用特性 |
| 2 | **Part4 注释债 5 处**仍描述 T16 已推翻的「双活 / 按 ai_backend 分流」;尤其引用**已删除的 `state.ai_engine` 字段**(实为 `ai_worker`) | `worker_client.rs:8` / `worker_pipeline.rs:2,47` / `face_pipeline.rs:514,538,122` | 中:按注释理解会误判架构仍有 inproc 分支 |
| 3 | **pro/Cargo.toml 注释「永不 git add,被 .gitignore 排除」** 与现行 `.gitignore:52`(pro 作为一等 member 被跟踪)+ `git ls-files`(pro 4 文件确被跟踪)矛盾 | `crates/picasa-next-pro/Cargo.toml:2` | 中:描述的红线执行方式已被 Copybara 剥离机制取代 |
| 4 | **release.yml 注释称「`[profile.lite]` 无对应 cargo profile(未落地项)」**,实际 `Cargo.toml:102` 已定义(opt-z+fat LTO+strip);且该体积优化 profile **从未被任何构建路径调用**——发布走 `release` profile + `lite` **feature** | `.github/workflows/release.yml:11` | 中:关系 <10MB 目标,「定义了却没接线」的死配置 |
| 5 | **CHANGELOG.md 只有 `[Unreleased]` 无 `[0.1.0]`** → 现打 `v0.1.0` tag 会被 `extract-changelog` 硬门挡下 → **oss-release 全链从未用真 tag 端到端跑通** | `CHANGELOG.md:11` | 中:首发前必补,否则发布失败 |
| 6 | **README 陈旧**:「known scroll bug」指向已废弃的 `_deprecated/perf_hardening_plan_v2.md`(该坐标平移路径已被 T16 bucket 引擎替换);`ort` 仍列为主后端(已迁 ai-worker 子进程) | `README.md:20`、`README.md:11` | 低:对外文案漂移 |
| 7 | `2026-07-02-Fable5全面审查报告` 称批量命令「仍收 Vec<i64>」已过时(实际 5 写命令已迁 SelectionDescriptor);installer T13 渠道字段实际在 `installer.rs`+DB 而非文档所述 catalog.rs | — | 低:审查文档间转述滞后 |

> **修复状态(2026-07-05,当日全部清理)**:#1 按「正文回写红线」在 todo.md 回写;**#2–#7 亦于当日按用户指令全部清理**——Part4 五处注释改写为「T16 后 worker-only」+ 死字段 `state.ai_engine`→`ai-worker 子进程`(#2);pro 红线注释改「永不入公开仓 + Copybara 剥离」(#3);release.yml lite profile 注释订正(#4);README ort/坐标平移/废弃指针三处(#6);Fable5 报告整表加过时横幅(#7);CHANGELOG 补 `[Unreleased]` 真实首版特性(#5,`[0.1.0]` 切分留发布动作)。改后 `cargo check` + `clippy -D warnings` 复验绿。逐项对照见 [todo.md](../todo.md) 「H2 节」。

---

## 4. 架构性小观察(proactive feedback)

- ~~**USE_PIPELINE 死代码**:`thumbnail_commands.rs:24` `const USE_PIPELINE=true` + 两处 `if !USE_PIPELINE` 恒不执行的旧路径仍留源码(todo G4 有意 defer,可按 Boy Scout Rule 顺手清)~~ **(🔴 2026-07-10 已过时:开关已运行时化为 A/B 待实测分支而非死代码,勿按本条清理;见 todo N 节 + Part3 §3.1.4 修订)**。
- **`[profile.lite]` 未接线**:若真要 <10MB 极致裁剪,免费版 release 当前走 `release` profile 而非体积优化的 `lite` profile——值得一个明确决策(接线 or 删 profile + 修注释)。
- **schema 债 4 项**确认仍在(cover_face_id 无 FK / exotic_tasks 无 AUTOINCREMENT / doc_replacements.`replace` 同名 / ai_search_results 多窗口互覆写),均属有意 defer,记录在案。

---

## 5. 本次审查的边界(未覆盖面,需相应环境)

诚实标注:以下无法从源码 / 本地实跑核实——
- **GUI 手测**:多项标「待 GUI」(键盘可访问性流程、bucket 滚动手感、商店安装 E2E、离线卷插拔)。
- **CI 真实运行**:多个 workflow 为「配置就位、未 CI 实证」(smoke job、updater 联网编译、`.sig` 产出、bundle 内容物无 ONNX DLL 的 `ls` 核对)。⚠ todo G1-4 声称 smoke job 已于 2026-07-05 dispatch 实跑全绿——外部 CI 事件,源码不可见,本审查无法证实亦无法证伪。
- **真机性能**:百万库重排 ~500ms、face 缓存 45× 提速等为性能声明,机理与代码路径一致但本次未跑基准复核倍数。
- **mac / 移动端 / 联网**(updater/CDN/签发机)全未覆盖。
