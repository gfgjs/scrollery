---
id: 2026-07-06-全库代码质量审查报告
status: snapshot
type: review
line: 质量审查待修池(J)
created: 2026-07-06
---

# 2026-07-06 全库代码质量审查报告

> 🟢 **实施进度(2026-07-06 无人值守修复,承接本报告 §5 路线图)**:批 1-4 + P2 池已落地并逐批过验证,提交 `59de6e0`(批1)/`a753e9b`(批2)/`ef1987c`+`021da68`(批3)/`1422a53`(批4)/`665ff81`(P2池)。各条修复状态见下方对应项的 **[✅ 已修 / ⏸ GUI-defer / 📋 defer]** 行内标注。全程仅本地验证(clippy/cargo test/vue-tsc/eslint/vitest/vite build 全绿,CI 欠费)。**订正**:P1-25(ci.yml 守门)经复核系 agent 过时发现——四 job 早已带 `github.repository` 守门(2026-07-06 已修),非缺陷。
>
> **快照日期: 2026-07-06**。本报告结论反映当日 dev 分支(b1cba5d)状态,被后续工作推翻的项须回头在此补「已过时」标注。
>
> **方法**: 6 路只读 agent 按子系统互斥分域深审(DB/IPC 边界、缩略图/扫描/布局管线、exotic 插件平台+AI、前端性能核心、前端状态/视图层、配置/安全/构建横切面),orchestrator 对全部 P0/P1 逐条独立复核(16 条亲核属实,标注「✅亲核」;其余标注「agent 取证」或「存疑」)。
>
> **定位**: 本报告是**质量审查**(找缺陷/找债),与 2026-07-05 完成度审查快照(宣称与实现对账)互补,不重复其结论。todo.md G4/H3 已录的有意 defer 项(USE_PIPELINE 死代码、profile.lite 未接线、schema 债 4 项、ANN dormant 等)不重复计入。

## 0. 客观取证基线(全绿,仅本地验证)

| 取证项 | 结果 |
|---|---|
| `cargo clippy --workspace --all-targets` | ✅ 0 警告 |
| `cargo test --workspace` | ✅ 0 失败(主 crate 396 过 + 5 ignored,全 workspace 约 475 过) |
| `vue-tsc --noEmit` | ✅ 0 错误 |
| `eslint .` | ✅ 0 违规 |
| `vitest run` | ✅ 227 过(17 文件) |

工具能抓的层面已经很干净——本报告全部发现均为工具链盲区: 异常路径上的契约破洞、跨模块不变量、生命周期竞态、权限面。

## 1. 总体评价

**整体健康度: 良好偏上,纪律执行是真实的而非纸面的。** 六路审查一致的结论: 主路径质量显著高于同规模项目平均水平——

- **exotic 插件平台**是全库最扎实的子系统: 协议先验长度上限再分配、验签先于解包、清单即白名单、create_new 防链接逃逸、Drop 兜底 kill→wait→join、stderr 独立排空防管道死锁,均有测试锁行为。
- **布局缓存契约**(四契约)全部自洽: store_layout 锁内递增、HIT 四判据与 is_hit_valid 逐字一致、gen_key 覆盖全部影响输出的参数、写路径 bump 点位对账无漏(常规路径)。
- **scanner 缺失检测四道闸 + TOCTOU 复查**是全仓最扎实的数据安全设计。
- **前端**: `as any` 全仓为零、监听/observer/rAF 配对纪律整体到位、bucket 引擎三重代际守卫、请求队列 spec 是高风险区的模范测试。
- 2026-07-02 硬化条款(rusqlite 下沉/原子落盘/参数绑定)在 ipc/ 主干已系统性落地,且有 tripwire 回归测试自我看守;SQL 全库**未发现注入面**。

**债集中在四条主线**(本报告发现的共性归因):

1. **纪律的边界与例外**——tripwire 只扫 `src/ipc` 漏掉 state.rs;原子落盘与 panic 拦截伞在 generator 做对了却没推平到 derive/ 与 ipc 写盘点;String 错误契约「有意保留」与 CLAUDE.md 冲突。
2. **异常路径上的契约跳过**——「部分成功 + 中途 early-return」这一族会跳过 bump/失效,产生用户可见的鬼影/裂图。
3. **组件生命周期 × 异步在途**——前端多处 fire-and-forget IPC 缺 token/存活守卫,模式重复出现。
4. **权限面偏宽**——fs 全量授权未用、生产 CSP 含 unsafe-eval、assetProtocol 整盘 scope: 「webview 被攻破后的爆炸半径」偏大,而非「当前已泄漏」。

## 2. P0(正确性,建议立即修)

### P0-1 Motion Photo 视频缓存: `exists()` 判命中 + 直写最终路径 ✅亲核 [两路独立发现] **[✅ 已修 59de6e0]**
- 位置: `src-tauri/src/ipc/media_commands.rs:184-195`
- 证据: `if cache_path.exists() { return Ok(...) }` → `extract_embedded_mp4` → `std::fs::write(&cache_path, &video_bytes)`。内嵌 MP4 常为数十 MB,写入窗口大。
- 影响: 崩溃/断电留半截 `.mp4` → 命中判定只查存在性 → 该动态照片**永久播放坏文件**(直到源文件 cache_key 变化)。正中 CLAUDE.md「派生产物一律原子落盘」红线描述的原型故障。
- 修法: 改用现成的 `thumbnail/generator.rs::write_atomic`(已 `pub(crate)`)。顺带: 同函数 162-170 行用 db_writer 写连接跑纯读查询,应改读池。

### P0-2 useViewIds 拉取无 in-flight token——迟到应答可清空新版本 flat_ids(选区正确性竞态) ✅亲核 **[✅ 已修 59de6e0,+首个 spec]**
- 位置: `src/composables/useViewIds.ts:29-45`;消费方 `src/components/media/MediaGrid.vue:1392`(fire-and-forget)
- 证据: `refresh()` 是「最后落地者赢」;catch 分支无条件 `viewIds=[]; loadedVersion=null`。布局版本快速连跳(缩略图滑块/enrichment 每 2s 重算)时,迟到的旧版本拒绝会清掉刚落地的新版本数据,且无人重取。
- 影响: 下一次版本变更前,Shift 区间选择静默退化为单项 toggle,框选退化 DOM fallback——Part5 G1 症状的无声回归。该文件**零测试**,属「零测试的命门路径」(选区全集)。
- 修法: 模块级 `refreshToken` 自增,落地(含 catch)前判 token 相符,不符即丢弃;并补该文件首个竞态 spec。

## 3. P1(纪律违反/潜在 bug/安全面,应尽快修)

### 3.1 Rust 后端

**P1-1 派生流水线 `kind::run` 全部裸跑,不在 catch_unwind 拦截伞下** ✅亲核
- 位置: `src-tauri/src/derive/pipeline.rs:429-476`。全仓非测试 catch_unwind 仅 2 处(generator.rs:52 panic_guard、scan_commands.rs:393 enrichment),派生分发的 epub 封面/lofty 音频/MF 视频/WIC 图像解码全无隔离。
- 影响: 一个畸形文件 panic → rayon scope 传播 → 整条派生流水线中止;在途任务留 status=1,重启复位后**同一毒文件再次触发**——派生子系统被单文件永久卡死。
- 修法: 任务闭包内用 generator 同款 `panic_guard` 包裹,panic → status=3 仅废该项。

**P1-2 EXIF 内嵌缩略图偏移基准错误,快路径疑似恒 miss** ✅亲核(静态);最终确认建议加命中率日志实测
- 位置: `src-tauri/src/engine/image_rs.rs:125-150`
- 证据: `JPEGInterchangeFormat` 按 EXIF 规范是**相对 TIFF 头**的偏移,代码却 `f.seek(SeekFrom::Start(offset))` 按文件绝对偏移读——通常提前约 12 字节,`FFD8` 魔数校验必然失败 → 恒返回 None 静默回退全解码。
- 影响: JPEG 缩略图快路径(核心性能卖点之一)沦为死代码,因回退正确而无任何日志暴露。失败模式安全(不会出错图)。
- 修法: 从 `exif.buf()[offset..offset+len]` 切片,不再二次开文件 seek。

**P1-3 关键帧雪碧图直写最终路径**(agent 取证)
- 位置: `src-tauri/src/derive/video.rs:100`(`std::fs::write(&disk_path, &webp)`);同文件 run_cover 与 derive/image.rs:115 均已走原子写,此处是遗漏。修法: 改 `write_atomic`。

**P1-4 缓存清理/LRU 淘汰与 DB 状态机脱钩——删除后永不重建/永久置错**(agent 取证,部分存疑)
- 位置: `thumbnail/cache.rs:330-351`、`ipc/thumbnail_commands.rs:81-84`(fast path 只按 DB status 回发不查文件)、`db/queries.rs:4308-4318`(sprite 只查 status=2)、`db/queries.rs:4281`(backfill INSERT OR IGNORE 不重入队)
- 影响链: 清理 sprites/motion 或 LRU 淘汰后,derivation 行仍 status=2 → 永不重建、返回死路径;视频项走回主 generator 会被 UNSUPPORTED_TYPE 写成 thumb_status=2 永久置错。存疑点: 前端 img onerror 兜底与启动全量兜底未核实。
- 修法: 清理时按 kind 同步复位 media_derivations 与 thumb_status;或读取侧加存在性校验降级重建。

**P1-5 state.rs quiesce/resume 在 async 上下文直跑 rusqlite + `let _` 吞关键标志写失败** ✅亲核
- 位置: `src-tauri/src/state.rs:535-570`。`quiesce_exotic`(async fn)与 `resume_after_quiesce` 直锁 db_writer 跑 get/set_config;`exotic_paused` 写失败静默——quiesce 防护形同虚设,resume 侧失败则 exotic 流水线永久停摆无日志。tripwire 测试只扫 `src/ipc`,此处是法外之地。
- 修法: DB 段下沉 spawn_blocking;set_config 失败至少 error 日志,resume 失败上传;**tripwire 扫描范围扩到 src/ 全域**。

**P1-6 file_ops 四命令「部分成功 + 中途报错」跳过 bump/失效,UI 呈现已删项** ✅亲核
- 位置: `src-tauri/src/ipc/file_ops_commands.rs`——move_media_items(71-139)、relocate_media_items(262→296-302)、copy_media_items_db(361→386-392)、remove_media_items_hard(420/442→457-462)
- 证据: 循环内逐条 DELETE/UPDATE 已提交(无事务),任何 `?` early-return 跳过尾部 `bump_data_version()` → items 缓存按旧版本命中,画廊持续显示已删项,点击报 MediaNotFound,直到下次任意写 bump 才恢复。
- 修法: bump/失效挪进不可跳过位置(guard struct 或 Err 分支补 bump,只要部分成功即 bump)。

**P1-7 clear_all_thumbnails 不失效 items 缓存、不 bump——全屏裂图不自愈** ✅亲核
- 位置: `src-tauri/src/ipc/thumbnail_commands.rs:1123-1160`。对照 clear_database(scan_commands.rs:517-519)三件套齐全(layout_cache/items_cache/bump),此处只清了 layout_cache。
- 影响: compute_layout HIT 路径按未变的 filter_key+data_version 直接从 items 快照派生 `thumb_status=1 + thumb_path`(指向已被 remove_dir_all 删除的文件)→ 全屏裂图,且 status=1 前端不会重新请求生成。
- 修法: 补一行 `items_cache::invalidate` + bump(或按「纯展示写」路线 patch 回 status=0)。

**P1-8 约 55 个 command 返回 `Result<_, String>`,系统性违反 IPC 错误契约** ✅亲核(75 处签名证实)
- 位置: face_commands(20 个)、doc_commands(15)、storage(4)、proofread(5)、derive(4)、system(2)等;96 处 `map_err(|e| e.to_string())` 把 rusqlite/锁中毒原文甩给前端。
- 影响: 前端只能文案匹配分流(error.rs 注释自认脆弱反模式);泄漏内部错误原文;与 AppError 双轨并存,新命令随时抄错模板。face_commands.rs:433 注释显示是**有意保留**——与 CLAUDE.md 命令边界契约直接冲突,应裁决收敛。
- 修法: 分批迁回 `crate::error::Result`;`write_blocking_str` 并入 blocking.rs 助手顺手完成大半。

**P1-9 AI 模型下载: 落盘 dest 无路径净化 + 完整性元数据未签名** ✅亲核
- 位置: `src-tauri/src/ai/remote_registry.rs:278-297`(classify: `split_once('/')` 后 base 可含 `../`)、`ipc/ai_commands.rs:985/1007/1089`(`models.join(&asset.dest)` 无越界断言,rename 落盘)
- 影响: 恶意/被劫持镜像(用户可切 hf-mirror.com)可借 `dest` 获得**目录外任意写**原语;且 size/sha256 与文件同源于同一份未签名 tree JSON,校验自证无效。HTTPS 挡 MITM、真实 HF git 树无 `..`,故 P1 非 P0——但 exotic 侧对已签名内容尚且逐一 `is_safe_relative_path`,此处对未签名第三方内容零校验,纵深防御缺口明显。
- 修法: dest 套单路径分量白名单(复用 `registry::is_safe_blob_file_name` 同级校验)+ join 后断言 `starts_with(models_dir)`;长期为模型分发引入 release-key 签名。

### 3.2 前端

**P1-10 MediaDetailOverlay 人脸框加载无 token + 切图不清空——旧图人脸框叠新图**(agent 取证)
- 位置: `src/components/media/MediaDetailOverlay.vue:691-699`(loadFacesFor),watch 触发 :632-641。方向键快速翻图时后落地者赢;image→image 切换 faces 不先清空,旧框投影到新图坐标系位置全错。修法: watch 先清空 + token 守卫。

**P1-11 `await listen` 卸载竞态——unlisten 永不执行的系统性泄漏(5 处)**(agent 取证)
- 位置: `MediaGrid.vue:1327-1355`、`MediaDetailOverlay.vue:840-846`、`DocThumbRenderer.vue:146-156`、`useDerivationAutoStart.ts:64-76`、`PluginStoreView.vue:412`
- 影响: 组件在 await 解析前卸载(画廊↔设置路由切换每次重建 MediaGrid)→ 监听器永久泄漏,每次 MEDIA_ENRICHED 在死组件闭包上跑 compute+updateVisible。窗口窄但泄漏一次终身存在。
- 修法: 封装 `useTauriListen(event, handler)` composable(内部处理 disposed 竞态),5 处一次性收敛。

**P1-12 useVirtualScroll(方案 A 回退引擎)pendingUpdate 饿死** ✅亲核
- 位置: `src/composables/useVirtualScroll.ts:250-278`——pendingUpdate 唯一消费点在 scheduleUpdate 自己的 rAF 回调尾部;宿主直调 updateVisible(MediaGrid 5+ 处)在途时置起的标志无人消费,滚一格停手 → 视口停在旧窗口直到下次滚动。
- 修法: updateVisible 的 finally 里消费 pendingUpdate,直调与调度路径共用收尾。

**P1-13 MediaGrid 嵌套 watch 脱离组件 scope——卸载后永久存活**(agent 取证)
- 位置: `src/components/media/MediaGrid.vue:1486-1507`。异步回调里再建 watch 无 active effect scope,布局计算中途离开画廊即泄漏,未来 compute 完成时对死组件跑 scrollToDir 并写 ui 状态干扰活组件。修法: 顶层单 watcher + 挂起变量。

**P1-14 scrollCache 物理/逻辑坐标混存同一键——引擎切换后恢复错位**(agent 取证)
- 位置: 写 `MediaGrid.vue:750-755`/:671-678,读 :1400-1405。缓存值坐标语义由写入时引擎决定,键不带语义;方案 A 下物理≠逻辑(ratio 可达 4×),切 bucket 后其它视图旧缓存落点偏差最高 4 倍。修法: 引擎开关翻转时 `scrollCache.clear()`(最简)。

**P1-15 useSelection 框选未监听 pointercancel——监听残留 + 幽灵拖选** ✅亲核
- 位置: `src/composables/useSelection.ts:195-196`(仅 pointermove/pointerup)。触屏被 OS 手势接管时清理不执行,之后移动鼠标继续改写选区。同仓 `usePointerDrag.ts` 是正确范本。修法: 补 pointercancel 成对注册/移除。

**P1-16 用户动作 IPC 裸奔簇: 失败零反馈 + unhandled rejection**(agent 取证,ProofreadPanel 定性已核实修正为「静默失败」)
- 位置: `CloseConfirmDialog.vue:71-85`(先关弹窗再 await,退出/托盘失败零提示)、`DocumentViewer.vue:339-368`(保存失败无反馈)、`ProofreadPanel.vue:150-163/189-203`、`VersionPanel.vue:143,149`、`ReplacementPanel.vue:117,145`(toggleEnabled 先改 UI 后 await,失败即 UI/DB 分叉)
- 修法: 统一 `try { await; toast } catch { toast error }` 定式;CloseConfirmDialog 改为 IPC 成功后再关弹窗。

**P1-17 双 IPC 调用风格并存,invokeIpc 错误码契约被系统性绕过(28 文件)**(agent 取证)
- 位置: `utils/ipc.ts:72` 提供 invokeIpc(常量强制 + IpcError.code 分流),但 raw invoke 遍布 scanStore/historyStore/collectionStore/DocumentViewer/AudioPlayer/三个 doc 面板/useFolderTree 等;错误展示退化为 `(e as Error).message`。正例: KnownVolumesSection/ExoticActivateDialog 按 code 分流。
- 修法: 全库替换 → invokeIpc;ESLint `no-restricted-imports` 挡回潮。

**P1-18 UI 字号 CSS 变量「同一事实两处算」,公式已漂移** ✅亲核
- 位置: `src/App.vue:114-124`(基准 12,13,15,16,19,23,28 + size-15) vs `src/stores/configStore.ts:112-123`(12,13,16,17,20,24,30 + size-16)——xs/sm/2xl 对任意 size 恒差 1px,启动后一开设置页全局字号跳 1px。修法: 抽 `applyUiFontSize(px)` 单一函数两处共用。

**P1-19 语义搜索把临时态持久化进 group_by,用户分组偏好被永久覆盖** ✅亲核
- 位置: `src/stores/aiStore.ts:122-124,177-184`——切语义模式 `ui.setGroupBy('none')`(该 setter 持久化 SET_APP_CONFIG),恢复所需 previousGroupBy 只在内存;语义模式中关闭应用 → 重启后偏好永久变 'none'。修法: 模式驱动的覆盖走非持久化通道(运行时 override 层或 `persist=false` 参数)。

**P1-20 S6 注册式设置页类型未锁死**(agent 取证)
- 位置: `src/constants/settingsMap.ts:62`(`Record<string, SettingSpec>` 宽键,未导出 SettingKey 联合)、`DynamicSettingControl.vue:147/169/197`(三张绑定表同为宽 Record,漏登记编译期无感、运行时静默落兜底)、:172-193(6 处 as-cast 硬塞联合类型)。`SettingRow.vue:13` 对拼错 key 运行时 `undefined.label` 崩溃(无守卫)。
- 修法: `as const satisfies` + `export type SettingKey = keyof typeof SETTINGS_MAP`;绑定表键用映射类型对账。

**P1-21 uiStore 已是上帝 store(579 行,约 18 个互不相关状态域)**(agent 取证,架构项)
- 修法: 渐进拆 viewStore(四互斥过滤+滚动目标+拖拽悬停)与 toastStore;不必一次到位,但应止增。

### 3.3 配置/安全/构建

**P1-22 fs 插件全量读写授权,前端零使用——纯攻击面 + RCE 升级链** ✅亲核
- 位置: `src-tauri/capabilities/default.json:10-13`(fs:default/read-all/write-all/allow-read-dir)、`lib.rs:96`。全 src/ grep `@tauri-apps/plugin-fs` 零命中。
- 影响: webview 一旦被注入,可读写 `$APPDATA` 下 scrollery.db 与 **exotic/ 已装 worker exe**——改写二进制等下次 spawn 即本地执行。
- 修法: 删 capability 四行 + 摘除 `tauri_plugin_fs::init()` + package.json 删依赖,零功能损失。

**P1-23 生产 CSP 含 `'unsafe-eval'`(dev/prod 共用同一份)** ✅亲核
- 位置: `src-tauri/tauri.conf.json:41`;perf/direct overlay 不覆盖 CSP → 生产在编。本应用渲染**不可信文档**(epub/pdf),注入面真实。最可能消费者是 pdf.js 字型编译(禁 eval 只回退慢路径不失功能)。
- 修法: 删 `'unsafe-eval'`(保留 wasm-unsafe-eval),pdf.js 传 `isEvalSupported: false`,实测 PDF 回归。顺带: style-src/font-src 仍白名单 fonts.googleapis.com/gstatic.com——字体已本地化(@fontsource),两条远程白名单可一并删除。

**P1-24 copy.bara.sky 剥离与禁词复验存在共同的文件类型漏网面**(agent 取证;当前无活泄漏)
- 位置: `copy.bara.sky:77-97`(仅剥 `//` 与 `#` 注释形态)、:137(verify_match 仅 **.rs/.toml/.json/.yml);`oss-gate.yml:72` grep --include 同四种。`.md/.vue/.html/.css` 的 INTERNAL 标记既剥不掉也验不到——未来一处此类标记会静默进公开镜像,四层防线全旁路。当前实测标记全在 .rs/.toml,纯靠惯例维持。
- 修法: verify_match 扩为 `glob(["**"], exclude=[自身])`(BEGIN-INTERNAL 零合法出现,扩面无误报);oss-gate 对两标记词单独跑全扩展名 grep。

**P1-25 ci.yml 无 repository 守门且随投影进公开仓——自托管 runner 的潜在 fork-PR RCE 面** ✅亲核
- 位置: `.github/workflows/ci.yml`(唯一没有 `if: github.repository ==` 的 workflow,runs-on self-hosted ×4);drift-alarm/oss-gate/release 均有守门。公开仓 fork PR 触发即排队等 runner(红叉噪声);若未来公开仓注册同标签 runner,fork PR 可在 dev-box-win 执行任意代码——「绝不挂公开仓」目前靠人不靠机制。
- 修法: ci.yml 加 `if: github.repository != 'gfgjs/scrollery'`,或加入 copy.bara.sky exclude 使公开仓根本没有这份 workflow。

## 4. P2(质量/健壮性,排期修)

### 4.1 Rust 后端

| # | 位置 | 问题(一句话) |
|---|---|---|
| R1 | `ipc/config_commands.rs:140-144` + `generator.rs:208-218` | thumb_size 档位变更不触发任何失效,存量图永远返回旧档缓存;exotic 项走指纹校验会重做——同库两类项行为分裂 |
| R2 | `engine/image_rs.rs:73-87`、`wic_engine.rs:91-101`、`derive/image.rs:123-146` | ResizeHint::ShortEdge 会上采样,违反 AI/face 缓存「绝不上采样」契约(generator 顺带产出有守卫,派生路径没有);小图被放大插值喂给 CLIP/YuNet |
| R3 | `scanner/metadata.rs:309-318` | parse_exif_datetime 字节切片 `s[0..4]` 遇多字节字符 panic → enrichment 整根中止且重扫复发;fast_scan eager-dims par_iter 同类无隔离 |
| R4 | `scanner/enricher.rs:200-213,284-318` | enrichment 无前进性保证: 拿不到路径/写失败的项被同一查询永久重选 → 忙循环 + 事件风暴(触发前提罕见,防御缺口) |
| R5 | `ipc/audio_commands.rs:82-99` + `thumbnail/cache.rs` 治理清单 | audio_covers/ 目录直写非原子,且完全游离于 GC/统计/清理之外(只增不减孤儿目录) |
| R6 | `storage/local.rs:24-30` | LocalFs::abs 无 canonicalize/越界检查,rel_path 含 `..` 直接逃逸 base(存疑: 当前 IPC 面是否透传 rel_path 未核实) |
| R7 | `scanner/volume_watch.rs:103-111` | 卷对账在 async runtime 线程同步跑整卷 UPDATE(可达百万行),违反 rusqlite 下沉硬化条款 |
| R8 | `storage/webdav.rs:224-249` | read_range 不校验状态码/206,错误页或全文件被当数据返回 |
| R9 | `ipc/doc_commands.rs:252-262` | save_version(overwrite) 非原子覆盖**用户源文件**(有先行备份可救,但用户文档比缓存更值得原子写) |
| R10 | `scanner/metadata.rs:250-253` | detect_motion_photo_xmp 单次 read() 短读即漏检(网络盘合法短读) |
| R11 | blocking.rs:38 vs face_commands.rs:152 vs thumbnail_commands.rs:352 等 | db_writer 锁中毒三套策略并存(报 System 永久失败/into_inner 恢复/静默跳过整批),一次 panic 后行为按命令随机分叉 |
| R12 | `thumbnail_commands.rs` 批量/全量路径 | 逐 id 双查询 N+1(10 万库=20 万次往返)+ `let _ = tx.commit()` 吞提交失败(7 处) |
| R13 | `thumbnail_commands.rs:606-1102` | 全量生成 spawn_blocking 的 JoinHandle 与闭包 Result 双双丢弃 → 任务无声死亡,UI 永久 running,幽灵 token 令 AI/派生流水线永久让步 |
| R14 | `db/queries.rs:3347` + faces 五消费点 | in_clause 无分块,face_ids 超 SQLite 绑定上限(32766)整批报错(媒体路径已有 5000 分块+回归测试,faces 漏了) |
| R15 | `ipc/scan_commands.rs:64-99` | add_scan_root 四步写无事务,中途失败留残根且「已存在」early-return 使重试不自愈 |
| R16 | `ipc/doc_commands.rs:516-542` | store_doc_thumbnail 三段 DB 写不同一事务,崩中间 → 文档封面永久缺失不自愈 |
| R17 | ai/face/derive 各 3 处 | 「期望运行」持久化标志(ai_analysis_active 等)写失败 `let _` 静默(9 处)——暂停重启后复活/开始不续传,无日志可查 |
| R18 | `ipc/exotic_commands.rs:366-391` | activate/deactivate 的 keyring 同步 RPC 直跑 async 正文(同文件 uninstall 已下沉,双标准) |
| R19 | `db/queries.rs:1239` | 搜索 LIKE 未转义 %/_(参数绑定无注入,行为性错配) |
| R20 | `error.rs:174-177` | System/Os/Internal/PathResolution 变体 message 原文透传前端(code 稳定✓,message 泄漏✗) |
| R21 | `db/queries.rs`(8318 行) | 巨石文件: 211 fn + 13 个 cfg(test) 块混排、25 列选择列表多处整段重复、占位符构造三种写法并存——建议按域拆 queries/{media,faces,exotic,docs,config}.rs |
| R22 | `crates/exotic-workers/ai-worker/src/batch.rs:384` | face 直派原图解码无像素上限(psd-worker 有 MAX_SOURCE_PIXELS=1e8),解压炸弹式图片可反复 OOM worker |
| R23 | ai/face 侧 6+ 处 | 锁中毒处理与 exotic 侧不一致: 裸 `.unwrap()` 会把 panic 级联到无关读路径(应统一 into_inner) |

### 4.2 前端

| # | 位置 | 问题(一句话) |
|---|---|---|
| F1 | `useVirtualScroll.ts:353-394` | 方案 A fetch 失败毒化跳过框,同窗滚动永不重试(已特性化锁定,但回退引擎带病上路;catch 中 `lastFetchedTop=-1` 即修) |
| F2 | `useVirtualScroll.ts:124,264,288,424` | rafId 死账: rAF id 从未存入,卸载 cancelAnimationFrame 恒 no-op |
| F3 | `MediaDetailOverlay.vue` | 卸载未清 deltaTimer/zoomHighlightTimer/faceRaf 3 个句柄;`media.detailItem!` 非空断言与同文件防御姿态矛盾 |
| F4 | `src/composables/useThumbnail.ts` | 死代码且是缺竞态守卫的旧版实现,全仓零消费者——留着必被误用,建议删除 |
| F5 | `stores/mediaStore.ts:46-65` | viewportMeta 深 ref Map(逐条深代理,可累积上万条)+ 每 120ms O(n) 全量拷贝——违 shallowRef 纪律精神 |
| F6 | `useHoverPreview.ts:22-25` | 模块级缓存无界且不随文件移动失效(移动后 hover 黑屏直到刷新) |
| F7 | 多处(合并项) | 卸载后仍跑的后台泵/定时器群: useBucketVirtualScroll 泵不 bump generation、DocThumbRenderer 泵只看 visibilityState、useViewportDimPriority/useRequestQueue/useDerivationAutoStart timer 无 dispose——建议统一 onScopeDispose 惯例 |
| F8 | `stores/configStore.ts` | 卫生包: thumbSize 三处默认值(480/240/240)漂移、15 次串行 IPC(uiStore 已批量化此处 N+1 原样)、:102 console.log 遗留、setter 无 catch 设置静默丢失 |
| F9 | `types/ui.ts:32` vs `types/media.ts:144` | MediaFilter 双定义双双过期(均无 colorLabel),热路径 filters 是 `Record<string, unknown>` 无类型保护;ui.ts 版是死类型 |
| F10 | `utils/pdfjs.ts:12-20` | 懒加载把 rejected promise 永久缓存,一次瞬时失败 = 整个进程生命周期 PDF 不可用 |
| F11 | `uiStore.ts:435-458` | 启动配置 groupBy/sortWithinGroup as-cast 无白名单(同函数 layoutMode 有)、JSON.parse 结果未 Array.isArray 校验,损坏值运行时崩 |
| F12 | `App.vue:8,86-89,159` + `uiStore.ts:292` | 幽灵事件与死代码: @sort-change 永不触发、semantic-search emit 无人听、isLayoutLoading 零消费、window-close-requested 裸字符串未入 EVENTS 常量 |
| F13 | `ManagementSection.vue:14` | `split('/').pop()` 在 Windows 反斜杠路径返回整条路径(主力平台可见 bug);库内已有正确 basename 写法 |
| F14 | 五个对话框 | overlay/动画/关闭骨架逐字复制 ×5,且仅 2 个有 focus-trap + aria——抽 BaseDialog.vue 一并抹平无障碍 |
| F15 | `FoldersSection.vue`(764 行)/`ToolsSection.vue`(600 行) | 树拖拽/扫描根导入/计时器应抽 composable;跨组件靠 window 自定义事件(folder-stats-changed/request-add-folder)无类型难追踪;addRoot 外层 catch 兜底文案误报 |
| F16 | i18n | S6 后设置页文案键(注册表数据 $t(spec.label))+ PluginStoreView 拼键整体脱离 localeIntegrity 锁测试覆盖(当前无缺键,风险在漂移);跨域键复用 3 处;AppStatusBar v0.1.0 写死 |
| F17 | personStore/collectionStore/aiStore.initEngine/ToastContainer | 「catch 只 console」簇: 用户直接触发的重命名/合并/测试加载失败零反馈 |
| F18 | `SettingsView.vue:345`、`DynamicSettingControl.vue:306-336` | 原生 window.confirm 与自研 useConfirm 并存(Tauri 下脱离主题且阻塞 webview) |
| F19 | `utils/markdown.ts:23` | v-html 的 XSS 面经核实安全(escapeHtml 先行+协议白名单),唯 href 未转义引号——当前不可利用,建议顺手补 |

### 4.3 配置/构建

| # | 位置 | 问题(一句话) |
|---|---|---|
| C1 | `tauri.conf.json:44-52` vs `lib.rs:277-298` | assetProtocol 静态 scope 整盘级通配($PICTURE/$DESKTOP/$DOCUMENT/...)与运行时精确授权是 OR 关系——运行时收窄形同虚设,应收敛到 $APPDATA/** + 运行时动态授权 |
| C2 | Cargo.lock | 依赖多版本(base64×3/rand×3/getrandom×4/thiserror 1+2/zip 2+4/windows×3)——多为传递依赖,发布前 SBOM 复核;本仓可动的是 src-tauri 直接依赖 thiserror="1" 可统一升 2 |
| C3 | `exotic/mod.rs` dev 旁路 | PICASA_EXOTIC_DEV_KEYSET/DEV_FILE_URLS 运行时旁路声称 debug 双门控,本轮未逐行独立取证 release 物理不可达——建议单独核对(信任根唯一旁路,值得一条独立取证) |
| C4 | `sync-oss.yml:70-73` | deploy key 持久化写 runner 家目录 ~/.ssh,非 job 结束即擦(key 仅公开仓写权,风险已限;job 末尾 rm 或改 RUNNER_TEMP) |

### 4.4 测试盲区(按风险排序)

1. **useViewIds 零测试**——P0-2 竞态所在,选区全集命门路径,优先补 token 竞态 + ViewStale 迟到清空锁测。
2. **useSelection 手势层零测试**——classicMode.spec 只锁策略纯函数;applyDragRange DOM fallback/lastValidRangeIds 续接/pointercancel 全无覆盖。
3. **tripwire 扫描范围**——`ipc_commands_keep_rusqlite_off_async_workers` 只扫 src/ipc,state.rs/volume_watch.rs 是法外之地(P1-5/R7 均由此漏出)。
4. S6 注册表键与 i18n 键的对账 spec(F16)。
5. useHoverPreview resolveToken 竞态(好测的纯逻辑)。

## 5. 修复路线图(建议四批,每批独立可验收)

**批 1「用户可直接观测的正确性」(约 1 天)**
P0-1(write_atomic 一行)+ P0-2(token 约 5 行 + 首个 spec)+ P1-6/P1-7(bump/失效挪位)+ P1-10/P1-12/P1-15(前端渲染竞态三小修)+ P1-3(雪碧图一行)。
验收: cargo test + vitest 全绿;手测: 清缩略图后画廊自愈、语义搜索退出后分组恢复。

**批 2「两条红线推平到全域」(约 1 天)**
P1-1(panic_guard 包 kind::run)+ R5/R9(剩余直写点改 write_atomic)+ P1-4(缓存治理与状态机对齐)+ P1-2(EXIF 先加命中率日志实测,证实后改 exif.buf 切片)+ R1(档位失效)。
验收: 畸形文件注入测试(毒文件只废单项);EXIF 命中率日志 > 0。

**批 3「契约收口」(1-2 天)**
P1-8(String→AppError 七文件)+ R11/R23(锁中毒统一 into_inner)+ P1-5/R7(state.rs/volume_watch 下沉 + tripwire 扩域到 src/)+ P1-9(dest 净化)+ P1-17/P1-16(invokeIpc 统一 + 裸奔簇 + ESLint 挡回潮)+ R17(标志写失败留痕)。
验收: tripwire 新范围全绿;grep 无 raw invoke(白名单外)。

**批 4「安全加固 + 防漂移」(约 1 天)**
P1-22(删 fs 授权)+ P1-23(CSP 去 unsafe-eval + 删 Google Fonts 白名单)+ P1-25(ci.yml 守门)+ P1-24(verify_match 扩面)+ C1(assetProtocol 收敛)+ P1-18/P1-19(双源漂移)+ P1-20(S6 类型锁)+ F16(i18n 对账 spec)。
验收: 剥离模拟四层防线复跑全绿;PDF/EPUB 阅读回归;六主题门禁全过。

P2 余项(R/F/C 表)不入批次,按 Boy Scout 规则触碰时顺手清,或在对应子系统下次动工时并入。

## 5.5 修复状态总表(2026-07-06 无人值守实施;2026-07-07 增量续做独立 PR 类 defer)

图例:✅ 已修并过验证 · ⏸ GUI-defer(运行时/GUI 才能验的失效面) · 📋 defer(宽爆炸半径/设计决策,留后续) · ✅订正(agent 过时发现,实为已修/非缺陷)

> **2026-07-07 增量续做**:承接 §5.5 首轮的 📋「独立 PR 类」defer,本次收敛 6 项(P1-11/13/16/17/8、R11/R23)+ 1 个 fmt chore,提交链 cf9b947→c135a66→5ee79c5→5ae4fd1(+c1b8a5c fmt)→90f7e1c→544959f→1891d8f→b0092b4→d62a0f5→7ec918a→c7b40ac,均逐项过 vue-tsc/eslint/vitest 或 cargo check/clippy/test。下表对应行已更新为 ✅。剩余 📋 仅:P1-21(架构级 uiStore 拆分,渐进)、⏸ GUI 类(P1-14/23/C1)、P2 余池(Boy Scout)。

| ID | 状态 | 提交 / 说明 |
|---|---|---|
| P0-1 motion 缓存原子写 | ✅ | 59de6e0(write_atomic + 读查询迁读池) |
| P0-2 useViewIds token | ✅ | 59de6e0(+ 5 用例 spec) |
| P1-1 派生 panic 伞 | ✅ | a753e9b(kind::run 套 panic_guard) |
| P1-2 EXIF 偏移基准 | ✅ | a753e9b(exif.buf 切片 + 2 测试;命中率日志已加,GUI 实测终证待真机) |
| P1-3 雪碧图原子写 | ✅ | a753e9b |
| P1-4 缓存治理↔状态机 | ✅ | a753e9b(clear_cache 复位 derivation+thumb_status;sprite 冷路径自愈)。**2026-07-07 GUI 实测复现并修复本条最严重侧面「两流水线竞写」(此前『部分存疑』实为真 bug):659bff0** —— 图像调度器(FullThumbGen)领取 thumb_status=0 时把 pdf/svg 卷入、命中 generator UNSUPPORTED_TYPE 分支回写 thumb_status=2/NULL,冲掉 DocThumbRenderer 刚写好的封面(derivation 仍成功→DB 自相矛盾;也是清缓存后仍占位之因)。修法=防冲(EXCLUDE_FRONTEND_DOC_THUMB 三查询排除 pdf/svg)+ 自愈(启动 reconcile_cover_thumbs 从权威派生产物回填,幂等非破坏,覆盖 video/audio/doc 三封面类)。生产库副本实证:id=11 healed 指向实存 webp(24460 字节)。**✅ 真机 GUI 确认(用户 2026-07-07:重 build 重启后 PDF 缩略图恢复正常显示)**。video/audio/epub 同源竞写留独立跟进(后端失败路径不写 thumb_status,完整排除需配套改派生失败态语义+对应媒体实测)。热路径 LRU 重建=📋(热路径 stat vs 淘汰点 DB 耦合的性能权衡) |
| P1-5 state.rs 下沉+标志留痕 | ✅ | ef1987c(self:&Arc 下沉 spawn_blocking + tripwire 扩域) |
| P1-6 file_ops 部分成功跳 bump | ✅ | 59de6e0(Drop 守卫) |
| P1-7 clear_all_thumbnails 漏失效 | ✅ | 59de6e0(补三件套) |
| P1-8 55 命令 String 错误契约 | ✅ | 2026-07-07 分文件增量:system 544959f / derive 1891d8f / proofread b0092b4 / storage d62a0f5 / doc 7ec918a / face c7b40ac(删 write_blocking_str)。全库 #[tauri::command] 已无 Result<_,String>,命令契约清零;错误码按语义映射(模型未启用→AiModelNotLoaded 等)。download_assets(pub(crate) 助手、非 IPC、与 CLIP 共享)有意保留 String |
| P1-9 AI 下载 dest 净化 | ✅ | ef1987c(单分量白名单 + starts_with + 2 测试) |
| P1-10 人脸框 token+清空 | ✅ | 59de6e0 |
| P1-11 await listen 卸载竞态(5处) | ✅ | cf9b947(抽 useTauriListen,disposed 标志仲裁;onScopeDispose 锚点;5 处收敛 + AppEvent 类型强制 + 3 测 + 顺带补 kick 定时器清理) |
| P1-12 pendingUpdate 饿死 | ✅ | 59de6e0 |
| P1-13 MediaGrid 嵌套 watch 泄漏 | ✅ | 5ee79c5(顶层静态 watcher + pendingScrollWhileComputing 挂起变量;补 deferredScrollTimer 卸载清理;副产品:连点只滚最后一个) |
| P1-14 scrollCache 坐标混存 | 📋 | 引擎开关翻转 clear;需 GUI 验证切引擎后各视图恢复,⏸/📋 |
| P1-15 useSelection pointercancel | ✅ | 59de6e0 |
| P1-16 用户动作 IPC 裸奔簇 | ✅ | c135a66(CloseConfirmDialog 反转「先关后 await」;DocumentViewer/三 doc 面板补 try/catch+toast;ReplacementPanel toggle 失败回滚 UI;+7 i18n 双语键) |
| P1-17 invokeIpc 统一(28 文件) | ✅ | 90f7e1c(18 文件 raw invoke→invokeIpc;convertFileSrc/Channel 保留;去重 3 处已部分迁移的重复 import;ESLint no-restricted-imports 挡回潮,实测触发+豁免 utils/ipc.ts) |
| P1-18 字号双源漂移 | ✅ | 021da68(抽 utils/uiScale.ts) |
| P1-19 groupBy 持久化污染 | ✅ | 021da68(persist 参数) |
| P1-20 S6 注册表类型锁 | ✅ | 1422a53(satisfies + SettingKey + getSettingSpec 安全检索,三消费方判空) |
| P1-21 uiStore 上帝 store | 两具名目标✅ | 架构项渐进拆分,报告点名的 **toastStore + viewStore 两刀均落地**(2026-07-07)。**478c1dc toastStore**:toast 域(状态零耦合)独立,27 消费文件迁移+清 18 处闲置 ui,副产品消 uiStore↔scanStore 循环 import warning 一条。**aedca27 viewStore**:四互斥视图筛选域(activeSmartAlbum/Directory/Collection/Person + 四 setter,含 clearSelection 耦合)独立,8 消费文件迁移+清 3 处闲置 ui,uiStore 净减 63 行。迁移安全网=先删源域让 vue-tsc 报全残留点+eslint 揪闲置 ui;store 实例名用全名避命名遮蔽。两刀均 vue-tsc/eslint/vitest 235/build 全绿(仅本地)。uiStore 由 ~19 域降至 ~17;进一步拆分超出报告具名范围,列 Boy Scout 可选 |
| P1-22 fs 全量授权未用 | ✅ | 1422a53(删权限+插件+双依赖) |
| P1-23 CSP unsafe-eval + Google Fonts | 代码就位 / ⏸ GUI 复验 | 1422a53 删 Google Fonts 远程白名单(字体本地化实证);**2026-07-07 422a806** 删 `'unsafe-eval'`(留 `'wasm-unsafe-eval'`):vite 开 `__INTLIFY_JIT_COMPILATION__` 使 vue-i18n 消息编译从 `new Function` 转 AST 解释(CSP 安全)+ 两处 pdf.js getDocument 传 `isEvalSupported:false`。**GUI 实测(build)暴露连锁两缺口,均已修**:①**c2e00e1 补 connect-src**(放行 asset.localhost):pdf.js/epub.js 用 Fetch 抓 asset,CSP 无 connect-src 时回退 default-src(不含 asset)被拦——非 unsafe-eval 所致(本刀未动 connect-src),属生产 CSP 早存缺口,报告处方「实测 PDF 回归」预期收尾。②**da80c46 清缓存复位覆盖 status=3**(健壮性改进,保留):真失败文档(派生 status=3)应可经清缓存重试,原 reset 只复位 status=2 → 扩至 status IN (2,3)+characterization test。🔴 **订正(2026-07-07,生产库地面真相)**:此前把「status=3 卡死」当作 PDF 缩略图占位之因**属误判**——查生产库全库**无 status=3 行**,PDF(id=11)派生实为 **status=2 成功、webp 已落盘**(thumbnails/480/88/*.webp 24460 字节)。真因是 **P1-4 两流水线竞写**冲掉 media_items.thumb_path(见 §5.5 P1-4 行,659bff0 已修)。da80c46 仍是有效改进,仅其「即用户占位之因」的叙事更正。静态过验(前端 build/vitest 235、cargo fmt/clippy 0/test 404 ✓);⏸ 安全对(P1-23/C1)本身仍待 GUI 复验(i18n 渲染 + Console 无 CSP 报错 + 图像/文档加载);**PDF 缩略图恢复=重 build 重启后由启动 reconcile 自动收敛(无需清缓存)** |
| P1-24 verify_match 扩面 | ✅ | 1422a53(扩至 .md/.vue/.html/.css/.ts/.mjs/.js,实测零命中) |
| P1-25 ci.yml 仓库守门 | ✅订正 | 四 job 早已带 `github.repository=='gfgjs/scrollery-private'` 守门(2026-07-06 已修),agent 发现过时,非缺陷 |
| R1 档位变更不失效 | ✅ | a753e9b |
| R2 ShortEdge 上采样 | ✅ | a753e9b(short>target 只下采样) |
| R3 parse_exif_datetime panic | ✅ | a753e9b(str::get 按边界) |
| R5 audio_covers 原子写+治理 | ✅ | a753e9b(write_atomic + 纳入 CacheStats/GC/清理单一事实源) |
| R7 volume_watch 下沉 | ✅ | ef1987c |
| R8 webdav 状态码 | ✅ | 665ff81(非 2xx 报错 + 带 Range 须 206) |
| R9 save_version 原子写 | ✅ | a753e9b |
| R10 detect_motion 短读 | ✅ | a753e9b(take+read_to_end) |
| R11/R23 锁中毒策略统一 | ✅ | 5ae4fd1(收敛原则=同一 mutex 全点位统一才生效:write_blocking System 臂/thumbnail 静默跳过臂/裸 unwrap 全归 into_inner;抽 flush_thumb_results 助手消 8 处重复;lib.rs PRAGMA 补块作用域防 guard 跨 await;范围=AppState 共享锁生产点,排除测试/子系统局部锁) |
| R18 keyring 同步 RPC 下沉 | ✅ | ef1987c |
| R19 LIKE 通配转义 | ✅ | 665ff81(六 scope 18 处 + search_media + 回归测试) |
| F1 方案A 失败毒化跳过框 | ✅ | 59de6e0(失败回滚,特性化锁测同步改) |
| F2 rafId 死账 | ✅ | 59de6e0 |
| F4 useThumbnail 死代码 | ✅ | 665ff81(删除) |
| F8 configStore console.log | ✅ | 021da68 |
| F10 pdfjs rejected promise 缓存 | ✅ | 665ff81 |
| F12 幽灵事件+死状态 | ✅ | 665ff81(@sort-change/isLayoutLoading) |
| F13 Windows basename | ✅ | 665ff81 |
| C1 assetProtocol 整盘 scope | 代码就位 / ⏸ GUI 终证 | **2026-07-07 e42963f** 静态 scope 7 整盘 glob → 仅 `$APPDATA/**`;运行时授权早已完整(lib.rs 启动授 cache_dir+各扫描根、scan_commands.add_scan_root 授新根、config_commands 授新缓存目录),此前静态整盘 glob 与之 OR,收窄形同虚设。零功能损失论证:12 处 convertFileSrc 落点仅 cache_dir/扫描根两类,均被运行时授权覆盖。cargo check ✓(build.rs 校验 config);⏸ 运行时终证待 GUI:缩略图/原图/文档/视频加载 |
| 其余 P2(R4/R6/R12-R17/R20-R22/F3/F5-F7/F9/F11/F14-F19/C2-C4) | 📋 | 按 Boy Scout 触碰时顺手清,或随子系统下次动工并入;详见 §4 各表 |

**首轮(2026-07-06)已修合计**: P0×2 + P1×12(+P1-25 订正) + P2×13 = 27 项落地并过验证;defer/GUI-defer 约 20 项(均有明确后续路径与原因)。defer 决策原则:①宽爆炸半径机械替换(P1-8/17、R11/23)留独立 PR 避免与本轮混提;②GUI-only 失效面(unsafe-eval、assetProtocol、部分前端渲染)待真机验证;③架构级(P1-21)渐进推进。

**次轮(2026-07-07)增量收敛**: 承接首轮①「独立 PR 类」defer,再修 6 项(P1-11/13/16/17/8、R11/R23)+ 1 fmt chore,均逐项过验证。至此 P1 全部落地或明确 GUI-defer(仅 P1-14/23 待真机、P1-21 架构渐进);Result<_,String> 命令契约、raw invoke 双轨、锁中毒三套策略三大系统性问题清零。

**次轮续:安全加固对(2026-07-07,用户在环)**: 承接 top-4 系统性发现「权限面偏宽」最后未清的一块,收敛 2 项(**422a806** P1-23 CSP 删 unsafe-eval + **e42963f** C1 assetProtocol scope)。二者均属 ⏸ GUI 失效面:代码就位 + 静态过验(前端 vue-tsc/eslint/vitest 235/build ✓;后端 cargo check ✓),但**运行时行为为唯一可能失效面且仅真机可证**,故不计入「已修」,状态标「代码就位 / ⏸ GUI 终证」。GUI 验收清单见下方交办。

**次轮续:P1-21 架构渐进(2026-07-07,用户在环)**: uiStore 上帝 store 渐进拆分,报告点名的**两个具名目标均落地**——**478c1dc toastStore**(toast 域独立,27 消费文件迁移 + 清 18 处闲置 ui + 消一条循环 import warning)、**aedca27 viewStore**(四互斥视图筛选域独立,8 消费文件迁移 + 清 3 处闲置 ui,uiStore 净减 63 行)。两刀均 vue-tsc/eslint/vitest 235/build 全绿(仅本地)。uiStore 由 ~19 域降至 ~17;进一步拆分超出报告具名范围,按「不一次到位」列 Boy Scout 可选。

**当前剩余**:⏸ GUI 真机验证(P1-14 引擎切换;P1-23/C1 代码已就位待跑验)、P2 余池(R4/R6/R12-R17/R20-R22/F3/F5-F7/F9/F11/F14-F19/C2-C4,Boy Scout;P1-21 进一步拆分并入此列)。

**GUI 验收清单(P1-23 + C1,须真机跑一遍确认无回归)**:
1. **i18n 渲染**(P1-23 JIT):启动应用,界面所有文案正常显示(非 key 原文如 `settings.title`);切换中/英语言,含 `{name}`/`{count}` 插值的文案(如「已选 N 项」)正确渲染。若 JIT 失效会整界面显示 key 或空白。
2. **PDF 打开**(P1-23 isEvalSupported):打开一个 PDF 文档,首页缩略图正常出图 + 阅读器逐页渲染无乱码/缺字。
3. **EPUB 打开**(P1-23 潜在 eval 面):打开一个 EPUB,阅读器正常分页显示。
4. **控制台无 CSP 报错**:开发者工具 Console 无 `Refused to evaluate ... unsafe-eval` 类 CSP 违规。
5. **图片加载**(C1 scope 收窄):缩略图网格出图、点开原图大图、文档/视频加载均正常;**重点测**启动后**新增一个扫描根**(触发 add_scan_root 运行时授权),其图片能加载;若配置了**自定义缓存目录**,缩略图仍出图。
6. **(权威)** 上述最好在 `npm run tauri:build:lite` 产物上跑一遍(生产 CSP/scope 才完全生效),dev 跑通为必要非充分。

## 6. 审查边界与存疑项

- 未覆盖: GUI 手测、CI 真实运行、真机性能、mac/iOS/Android、联网面(与 07-05 快照 H4 相同边界)。
- 存疑待取证: P1-2 需命中率日志实测终证;~~P1-4 的前端 onerror 兜底未核实~~ → **2026-07-07 已取证并修复**:P1-4「部分存疑」的核心即两流水线竞写冲掉 pdf/svg 封面,经生产库地面真相实证复现并修(659bff0,见 §5.5 P1-4 行);残留 video/audio/epub 同源竞写留独立跟进。R6 的 storage IPC 透传面未核实;C3 的 dev 旁路 release cfg 门控未逐行取证。
- 本报告全部「✅亲核」项均由 orchestrator 直接读源码复核,未采信 agent 转述;「agent 取证」项均带 file:line 且 agent 声明经系统性 grep 而非抽样。
